---
phase: 01-live-viewer-bridge
plan: 10
type: execute
wave: 3
depends_on: [01-08, 01-09]
gap_closure: true
autonomous: true
requirements: [AC-3, AC-4, AC-5, O-4]
files_modified:
  - osim-ui/portal/tests/live-stream.spec.ts
must_haves:
  truths:
    - "Der E2E-Test treibt einen realen Lauf über die GAP-2/3-Endpoints (kein test.fixme mehr)"
    - "Neu angehängte Frames erscheinen in /live innerhalb < 1s (AC-3)"
    - "Tab-Wechsel isoliert genau einen Stream (AC-4)"
    - "Reload setzt vom Byte-Offset fort, ohne Frames zu doppeln (AC-5)"
  artifacts:
    - path: "osim-ui/portal/tests/live-stream.spec.ts"
      provides: "Playwright-E2E gegen realen Run-Flow, prepareDemoRun an echte Endpoints angeschlossen"
      contains: "live-start-run"
  key_links:
    - from: "osim-ui/portal/tests/live-stream.spec.ts"
      to: "POST /api/v1/models/{id}/runs + GET /runs/{id}/stream"
      via: "realer Run-Start über die /live-UI + Frame-Append über den Stream"
      pattern: "live-start-run"
---

<objective>
E2E entpinnen (GAP-5). Die Spec `live-stream.spec.ts` ist heute `test.fixme`, weil
ihr der Backend-Stream-Read-Endpoint fehlte. Mit 01-08 (Endpoints) + 01-09 (Wiring)
existiert er — die Spec wird zum echten Lauf gegen die GAP-2/3-Endpoints am Dev-Stack
verdrahtet und entpinnt.

Purpose: AC-3 (Tail-Pickup < 1s), AC-4 (Stream-Filter) und AC-5 (Offset-Restart)
werden automatisiert verifiziert statt manuell. O-4 wird maschinell beweisbar.

Output: Eine laufende (nicht mehr test.fixme) Playwright-Spec, die einen realen
Run startet und Live-Pickup/Filter/Offset-Restart prüft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-live-viewer-bridge/01-SPEC.md
@.planning/phases/01-live-viewer-bridge/01-07-SUMMARY.md
@.planning/phases/01-live-viewer-bridge/01-08-SUMMARY.md
@.planning/phases/01-live-viewer-bridge/01-09-SUMMARY.md
@.planning/phases/01-live-viewer-bridge/UAT.md
@osim-ui/CLAUDE.md

<read_first>
- osim-ui/portal/tests/live-stream.spec.ts — die bestehende Spec. `test.fixme()` (Z.109) +
  `prepareDemoRun()` (wirft Platzhalter, Z.67) sind die zwei Stellen, die GESCHLOSSEN werden.
  Die drei Test-Bodies (AC-3/AC-4/AC-5) + `ganttFrame()`-Helper + `loginAs(ADMIN)`-Fixture sind
  bereits geschrieben — sie erwarten eine `append(lines)`-Funktion und data-testids
  `live-tab-gantt_durchlauf`, `gantt-row-<auftrag>`, `kpi-grid`, `gantt-panel`,
  `stream-router-<tag>`.
- 01-08-SUMMARY.md — endgültige Endpoint-Pfade + Run-Start-Response-Form (run_id).
- 01-09-SUMMARY.md — die data-testids des Run-Setups (`live-model-select`/`live-model-option-<id>`/
  `live-start-run`, `nav-link-live`). MASSGEBLICH für die UI-Driving-Schritte.
- osim-ui/portal/playwright.config.ts — testMatch deckt `tests/**/*.spec.ts` ab (01-07-Fix).
- osim-ui/portal/tests/../e2e/fixtures/auth.ts + test-users.ts — `loginAs(page, email, pwd)`, `ADMIN`.
  Default-Test-User (osim-ui/CLAUDE.md): jwfischer69@gmail.com / 123456 — falls ADMIN-Fixture
  davon abweicht, prüfen welcher User im Firebase-Seed existiert.
- scripts/dev-up.sh — startet den Dev-Stack (Container + Firebase-Seed), localhost:3002.
- engine/scripts/demo_stream_run.py — Referenz, falls ein deterministisches Modell als E2E-
  Fixture hochgeladen werden muss.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prepareDemoRun an echte Endpoints anschließen + test.fixme entfernen</name>
  <files>osim-ui/portal/tests/live-stream.spec.ts</files>
  <action>
    Entferne `test.fixme()` (Z.109) und ersetze den Platzhalter-Throw in `prepareDemoRun()`
    durch den realen Flow. prepareDemoRun muss zwei Dinge liefern: einen aktiven `runId`,
    dessen stream.jsonl von /live tail-gelesen wird, UND eine `append(lines)`-Funktion, die
    neue JSONL-Frames an genau diesen Stream anhängt (simuliert die weiterschreibende Engine).
    Strategie (M1, minimaler HTTP-Polling-Transport):
    - Stelle ein deterministisches E2E-Modell sicher: lade per UI (oder per API über apiFetch-
      Äquivalent im Test) eine kleine OTX hoch mit Namen-Prefix `E2E-live-<ts>`, ODER nutze ein
      vom dev-up-Seed bereitgestelltes Modell. Wähle den Weg, der mit dem vorhandenen E2E-
      Upload-Flow (siehe bestehende e2e-Specs für Upload-Muster) am wenigsten neuen Code braucht;
      begründe im SUMMARY.
    - Run-Start NICHT serverseitig faken, sondern über die echte /live-UI fahren: navigiere zu
      `/live`, wähle das E2E-Modell (`live-model-select`/`live-model-option-<id>` aus 01-09),
      klicke `live-start-run`. Lies den run_id, den 01-09 nach dem Start im UI/Store hält
      (data-testid für die aktive run_id ergänzen, falls 01-09 keinen liefert — dann in 01-09-
      SUMMARY nachschlagen; primär das, was 01-09 dokumentiert hat).
    - `append(lines)`: hänge zusätzliche Frames an die stream.jsonl DES LAUFS an. Da der Browser
      nicht ins Container-Volume schreiben kann, geht das über einen schmalen Test-Pfad: entweder
      (a) ein bereits vom run_otx geschriebener, fortlaufender Stream (der Lauf produziert von sich
      aus genug Frames für AC-3/AC-5 — dann assertet der Test auf ein bekanntes Auftrags-Testid
      aus dem deterministischen Modell statt auf frei gewählte FA-IDs), ODER (b) ein test-only
      Append-Hook. Bevorzuge (a): nutze die deterministischen Auftrags-IDs des E2E-Modells; passe
      die Test-Bodies an, sodass `gantt-row-<bekannter-auftrag>` statt `FA-LIVE-001` erwartet wird,
      und entferne/ersetze `ganttFrame()` falls dann ungenutzt. (a) ist robuster + braucht keinen
      Test-Schreib-Endpoint im Prod-Backend. Begründe die Wahl im SUMMARY.
    - AC-3: die zum Lauf gehörende GanttRow erscheint < 1s nach Stream-Fortschritt (timeout 1000ms).
    - AC-4: Tab-Wechsel zwischen gantt_durchlauf und kpi_auswertung isoliert den Stream
      (gantt-panel weg, kpi-grid da und umgekehrt) — Bodies bestehen bereits, nur lauffähig machen.
    - AC-5: nach `page.reload()` + Tab-Reselect setzt der Tail-Reader vom gespeicherten Byte-Offset
      fort; genau EINE GanttRow pro Auftrag (kein Doppel-Ingest).
    Halte den Stil der bestehenden Spec (Header-Doku auf „jetzt aktiv" aktualisieren, ehrlich:
    setzt laufenden Dev-Stack voraus). Keine fenced-code-Blöcke in Plan-Prosa.
  </action>
  <verify>
    <automated>cd osim-ui/portal && npx playwright test tests/live-stream.spec.ts --list | grep -c "fixme" | grep -qx 0 && echo "NO-FIXME"</automated>
  </verify>
  <verify>
    <automated>cd osim-ui/portal && npx eslint tests/live-stream.spec.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "live-stream.spec" | grep -qx 0 && echo TSC-OK</automated>
  </verify>
  <verify>
    <human-check>Mit laufendem Dev-Stack (`bash scripts/dev-up.sh`, localhost:3002): `cd osim-ui/portal && npx playwright test tests/live-stream.spec.ts` → 3 passed (AC-3/AC-4/AC-5). Grafik-Viewer nutzen, Link http://localhost:3002/live mitgeben.</human-check>
  </verify>
  <done>live-stream.spec.ts ist NICHT mehr test.fixme; prepareDemoRun fährt einen realen Lauf über
  die 01-08/01-09-Endpoints; AC-3/AC-4/AC-5 sind als laufende Tests formuliert; tsc + eslint clean;
  am Dev-Stack 3 passed (Human-Check).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E-Test → Dev-Stack-API | Test treibt echten Run + tail-liest über die runs-Endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-E2E-01 | Tampering | E2E-Test-Modelle verschmutzen die Bibliothek | mitigate | Namen-Prefix `E2E-`; models/index.tsx filtert sie bereits aus der Prod-Liste |
| T-E2E-02 | DoS | E2E startet viele Läufe | accept | deterministischer kurzer Lauf (wenige Perioden); periods-Cap im RunService (T-RUN-04) greift |
| T-E2E-SC | Tampering | keine neuen npm-Installs | accept | nutzt vorhandenes @playwright/test; kein Package-Gate |
</threat_model>

<verification>
- `npx playwright test tests/live-stream.spec.ts --list` enthält KEIN fixme mehr (NO-FIXME).
- eslint + tsc clean für die Spec.
- Mit Dev-Stack: 3 Tests passed (Human-Check, AC-3/AC-4/AC-5).
</verification>

<success_criteria>
- Die E2E-Spec ist entpinnt und fährt einen realen Lauf über die GAP-2/3-Endpoints.
- AC-3 (< 1s Tail-Pickup), AC-4 (Stream-Filter), AC-5 (Offset-Restart) sind automatisiert geprüft.
- Touched file lint- + tsc-clean.
</success_criteria>

<output>
Create `.planning/phases/01-live-viewer-bridge/01-10-SUMMARY.md` when done.
Dokumentiere: gewählte append-Strategie (a deterministisches Modell / b Test-Hook) + Begründung,
und den Human-Check-Status (Dev-Stack-Lauf 3 passed oder pending).
</output>
