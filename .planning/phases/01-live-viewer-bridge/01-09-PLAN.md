---
phase: 01-live-viewer-bridge
plan: 09
type: execute
wave: 2
depends_on: [01-08]
gap_closure: true
autonomous: true
requirements: [O-3, O-4, AC-3, AC-4]
files_modified:
  - osim-ui/portal/src/components/AuthenticatedLayout.tsx
  - osim-ui/portal/src/api/runs.ts
  - osim-ui/portal/src/routes/_authenticated/live.tsx
  - osim-ui/portal/src/features/live-stream/__tests__/runs-api.spec.ts
must_haves:
  truths:
    - "Von jeder authentifizierten Seite gibt es einen sichtbaren, auswählbaren Navigations-Punkt 'Live' zu /live"
    - "Auf /live kann ein gespeichertes Modell ausgewählt werden"
    - "Ein Klick auf 'Lauf starten' startet einen Run und beginnt das Live-Tail des Streams"
    - "Live Gantt + KPI erscheinen im StreamRouter aus dem realen Stream (kein noopRead mehr)"
  artifacts:
    - path: "osim-ui/portal/src/api/runs.ts"
      provides: "startRun + buildStreamReadFn + fetchRunMeta gegen die 01-08-Endpoints"
      contains: "buildStreamReadFn"
    - path: "osim-ui/portal/src/routes/_authenticated/live.tsx"
      provides: "Modell-Picker + Lauf-Start + HTTP-ReadFn statt noopRead"
      contains: "buildStreamReadFn"
    - path: "osim-ui/portal/src/components/AuthenticatedLayout.tsx"
      provides: "Topbar-Navigation mit Links Bibliothek + Live"
      contains: "to=\"/live\""
  key_links:
    - from: "osim-ui/portal/src/routes/_authenticated/live.tsx"
      to: "GET /api/v1/runs/{run_id}/stream"
      via: "buildStreamReadFn als ReadFn in createTailReader"
      pattern: "runs/.*stream"
    - from: "osim-ui/portal/src/api/runs.ts"
      to: "POST /api/v1/models/{id}/runs"
      via: "apiFetch"
      pattern: "models/.*runs"
    - from: "osim-ui/portal/src/components/AuthenticatedLayout.tsx"
      to: "/live"
      via: "TanStack <Link to=/live>"
      pattern: "to=\"/live\""
---

<objective>
Frontend-Wiring + Navigation (GAP-1 + GAP-4). Schließt: `/live` ist verwaist
(kein Menü-Eintrag) und nutzt `noopRead` (kein Run-Auswahl/-Start, leere Steps).

Purpose: O-3 (selektierbare Navigation) und O-4 (End-to-End) real machen — der
User kann ein Modell wählen, einen Lauf starten und live zusehen. Speist die in
01-02 vorbereitete injizierbare `ReadFn` mit dem 01-08-Stream-Endpoint.

Output: Topbar-Navigation mit „Live"-Eintrag, ein runs-API-Client, und eine
`/live`-Route mit Modell-Picker + „Lauf starten" + HTTP-Polling-ReadFn. vitest
grün; berührte Dateien lint-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-live-viewer-bridge/01-SPEC.md
@.planning/phases/01-live-viewer-bridge/01-CONTEXT.md
@.planning/phases/01-live-viewer-bridge/01-05-SUMMARY.md
@.planning/phases/01-live-viewer-bridge/01-08-SUMMARY.md
@osim-ui/CLAUDE.md
@osim-ui/docs/3FLS-EAM-STYLE-GUIDE.md

<read_first>
Diese Dateien VOR Implementierung lesen — alle Verträge stehen hier, keine
Codebase-Exploration nötig:

- 01-08-SUMMARY.md — der finale Endpoint-Pfad-Satz + die ReadFn-Vertrags-Signatur
  `{text, next_offset}` aus dem Backend. MASSGEBLICH; falls Abweichung von diesem
  Plan, der SUMMARY-Satz gewinnt.
- osim-ui/portal/src/routes/_authenticated/live.tsx — die bestehende Route. Sie akzeptiert
  bereits eine injizierbare `read?: ReadFn` (Default `noopRead`) und optionale `meta?`.
  Tail-Tick (200ms) + 30Hz-Coalescing + StreamRouter pro Tab sind GEBAUT. Aufgabe: noopRead
  durch eine echte HTTP-ReadFn ersetzen, NACHDEM ein Run gewählt + gestartet wurde.
- osim-ui/portal/src/features/live-stream/tail-reader.ts — `type ReadFn = (offset: number) =>
  Promise<{ text: string; nextOffset: number }>`. createTailReader hält den Offset intern
  (getOffset/setOffset, Restart-fest = AC-5-Basis). DIE READFN MUSS DIESE SIGNATUR ERFÜLLEN.
- osim-ui/portal/src/api/models.ts — Muster für TanStack-Query-Hooks + Wire-Type-Mirrors
  (`useModels()` liefert `ModelMeta[]`). Reuse für den Modell-Picker.
- osim-ui/portal/src/api/fetch.ts — `apiFetch<T>(path, init?)` hängt automatisch den
  Firebase-Bearer-Token an, BASE_URL aus VITE_API_BASE_URL. Für den Stream-Read NICHT
  apiFetch<T> (das JSON-parsed) — siehe action: der Endpoint liefert JSON `{text,next_offset}`,
  also IST apiFetch<StreamChunk> korrekt. (Kein Raw-Body-Bedarf.)
- osim-ui/portal/src/routes/_authenticated/models/index.tsx — Picker-/Two-Pane-Muster +
  data-testids (`model-card-<id>`, `model-grid`). Tokens-/3FLS-Stil-Referenz.
- osim-ui/portal/src/components/AuthenticatedLayout.tsx — die Topbar. Hat HEUTE KEINE Nav-Links.
  Hier die Navigation ergänzen (Links Bibliothek + Live), 3FLS-Topbar-Stil (weiße Schrift auf
  Gradient), Tokens-only, :focus-visible.
- osim-ui/portal/src/features/live-stream/store.ts + stream-router.tsx — Store + StreamRouter
  (data-testids kpi-grid/gantt-panel/record-table/stream-router-<tag>). Konsumieren die Frames,
  die der Tail-Reader liefert. NICHT ändern, nur speisen.
</read_first>

<interfaces>
# tail-reader (Vertrag, gegen den die ReadFn baut)
type ReadFn = (offset: number) => Promise<{ text: string; nextOffset: number }>;

# runs-Endpoints (aus 01-08; SUMMARY ist maßgeblich)
POST /api/v1/models/{model_id}/runs -> { run_id, model_id, coverage_ratio, status }
GET  /api/v1/runs/{run_id}/stream?offset=<n> -> { text, next_offset }
GET  /api/v1/runs/{run_id}/meta -> MetaJson

# bestehend
useModels(): UseQueryResult<ModelMeta[]>
apiFetch<T>(path, init?): Promise<T>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Topbar-Navigation (GAP-1) + runs-API-Client</name>
  <files>osim-ui/portal/src/components/AuthenticatedLayout.tsx, osim-ui/portal/src/api/runs.ts, osim-ui/portal/src/features/live-stream/__tests__/runs-api.spec.ts</files>
  <action>
    AuthenticatedLayout.tsx: ergänze in der Topbar (links neben/unter der Wordmark) eine
    `<nav>` mit TanStack-`<Link>`-Einträgen: „Bibliothek" → `/models` und „Live" → `/live`.
    3FLS-Topbar-Stil: weiße Schrift auf dem Gradient, aktiver Link via TanStack
    `activeProps`/`[&.active]`-Token-Klasse hervorgehoben (NICHT nur über Farbe — Unterstrich
    oder font-weight ergänzen, A11y/3FLS). `:focus-visible` mit Token-Focus-Ring. Tokens-only,
    keine ad-hoc Hex. data-testid `nav-link-live` + `nav-link-models` für E2E (01-10).
    runs.ts: drei Funktionen gegen die 01-08-Endpoints (Pfade aus 01-08-SUMMARY):
    - `startRun(modelId: string): Promise<StartRunResponse>` via `apiFetch(..., {method:"POST"})`
      auf `/api/v1/models/${modelId}/runs`. Type `StartRunResponse { run_id, model_id,
      coverage_ratio, status }` (Mirror der Backend-Pydantic-Schemas).
    - `buildStreamReadFn(runId: string): ReadFn` — liefert eine Funktion `(offset) =>
      apiFetch<{text:string; next_offset:number}>(\`/api/v1/runs/${runId}/stream?offset=${offset}\`)
      .then(r => ({ text: r.text, nextOffset: r.next_offset }))`. (Snake→camel-Mapping auf
      die tail-reader-ReadFn-Signatur.)
    - `fetchRunMeta(runId: string): Promise<MetaJson>` via apiFetch auf `/api/v1/runs/${runId}/meta`.
    Tests (runs-api.spec.ts, vitest): mocke apiFetch/fetch, prüfe dass buildStreamReadFn das
    `next_offset`→`nextOffset`-Mapping korrekt macht und die richtige URL mit offset baut.
    Keine fenced-code-Blöcke; Identifier verbindlich.
  </action>
  <verify>
    <automated>cd osim-ui/portal && npm run test:run -- src/features/live-stream/__tests__/runs-api.spec.ts</automated>
  </verify>
  <verify>
    <automated>cd osim-ui/portal && npx eslint src/components/AuthenticatedLayout.tsx src/api/runs.ts</automated>
  </verify>
  <done>„Live"- und „Bibliothek"-Nav-Links in der Topbar (auswählbar, aktiv-markiert, A11y);
  runs-API-Client mit korrektem snake→camel-Mapping der ReadFn; vitest grün; eigene Dateien lint-clean.</done>
</task>

<task type="auto">
  <name>Task 2: /live wiring — Modell-Picker + Lauf-Start + HTTP-ReadFn (GAP-4)</name>
  <files>osim-ui/portal/src/routes/_authenticated/live.tsx</files>
  <action>
    Erweitere die bestehende `/live`-Route (NICHT neu schreiben — sie hat schon Tabs +
    StreamRouter + Tail-Tick). Füge einen Run-Setup-Bereich über den Tabs ein:
    - State: `runId: string | null`, `meta: MetaJson | undefined`, ausgewählte `modelId`.
    - Modell-Picker: `useModels()` (reuse api/models.ts), rendere eine kompakte Auswahl
      (Select oder schmale Liste, 3FLS-Tokens) mit data-testid `live-model-select` und
      Option-testids `live-model-option-<id>`. E2E-Modelle (Prefix `E2E-`) NICHT herausfiltern
      hier (der E2E-Test in 01-10 braucht sein Modell) — anders als models/index.tsx.
    - Button „Lauf starten" (data-testid `live-start-run`): ruft `startRun(modelId)`; bei Erfolg
      `setRunId(resp.run_id)`, danach `fetchRunMeta(run_id)` → `setMeta(...)` (speist den
      partial-/Schema-Mismatch-Banner-Pfad, D-2.2/AC-7). Lade-/Disabled-State während des Starts,
      Fehler via toast (apiErrorMessage-Muster). Zeige `coverage_ratio` als Hinweis, wenn < 1.0
      (partielles Modell — surface, nicht verstecken).
    - read-Prop-Ableitung: solange `runId === null` → `noopRead` (bestehender Default-Pfad bleibt
      sauber). Sobald `runId` gesetzt: `read = buildStreamReadFn(runId)`. WICHTIG: bei Run-Wechsel
      MUSS der Store zurückgesetzt werden (`useLiveStreamStore.getState().reset()` falls vorhanden,
      sonst clear), damit Frames des alten Laufs nicht mit dem neuen vermischt werden, und der
      Tail-Tick-useEffect re-initialisiert (read in der Dependency-Liste — ist bereits so).
    - Reproduzierbarkeit: KEINE UI-seitige Reorder/Aggregation der Frames (osim-ui/CLAUDE.md) —
      der Store ingestet in Stream-Reihenfolge; nichts daran ändern.
    Behalte die bestehenden data-testids (live-tab-*, gantt-panel, kpi-grid, stream-router-*).
    Tokens-only, A11y (Status nie nur über Farbe), :focus-visible. Keine fenced-code-Blöcke.
  </action>
  <verify>
    <automated>cd osim-ui/portal && npm run test:run -- src/features/live-stream</automated>
  </verify>
  <verify>
    <automated>cd osim-ui/portal && npx eslint src/routes/_authenticated/live.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | grep -c "live.tsx" | grep -qx 0 && echo TSC-OK</automated>
  </verify>
  <done>/live hat einen Modell-Picker + „Lauf starten"; nach Start liefert die ReadFn echte Frames
  aus `GET /runs/{id}/stream`, StreamRouter zeigt live Gantt+KPI, meta speist den Banner; noopRead
  bleibt nur der run-lose Default. vitest + tsc grün, Datei lint-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /live (Browser) → /api/v1 runs-Endpoints | model_id/run_id/offset werden client-seitig gebildet, serverseitig (01-08) validiert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-LIVE-FE-01 | Info-Disclosure | ReadFn pollt run_id | mitigate | Authz liegt serverseitig (01-08, run_id tenant-confined); FE schickt nur den von startRun erhaltenen run_id; apiFetch hängt Firebase-Token an |
| T-LIVE-FE-02 | DoS (Client) | unbegrenztes Polling großer Streams | accept | bestehender 200ms-Tail + 30Hz-Coalescing + RecordTable-Windowing (T-01-12) cappen die UI-Last; inkrementeller Byte-Offset hält Payload klein |
| T-LIVE-FE-03 | Tampering | Frame-Vermischung bei Run-Wechsel | mitigate | Store-Reset + Offset-Reset bei runId-Wechsel; keine UI-seitige Reorder (Reproduzierbarkeitsvertrag) |
| T-LIVE-FE-SC | Tampering | keine neuen npm-Installs | accept | nutzt vorhandene Deps (tanstack-query/-router, sonner); kein Package-Gate nötig |
</threat_model>

<verification>
- `cd osim-ui/portal && npm run test:run -- src/features/live-stream` grün (bestehende 29 +
  neue runs-api-Tests).
- `npx eslint` der drei berührten Dateien → 0 errors (repo-weiter Lint-Stand ist auf main
  bereits rot durch ~77 Fremd-Fehler — NICHT anfassen, nur eigene Dateien clean halten).
- `npx tsc --noEmit` ohne Fehler in den berührten Dateien.
- Manuell/01-10: Nav-Link „Live" sichtbar + klickbar; nach „Lauf starten" erscheinen Gantt-Balken.
</verification>

<success_criteria>
- Von jeder authentifizierten Seite ist „Live" ein sichtbarer, auswählbarer Nav-Punkt zu /live (O-3).
- Auf /live: Modell wählen → „Lauf starten" → live Gantt+KPI aus dem realen Stream (O-4).
- noopRead ist nur noch der run-lose Default; mit Run läuft die echte HTTP-ReadFn.
- Berührte Dateien tokens-only, A11y-konform, lint- + tsc-clean.
</success_criteria>

<output>
Create `.planning/phases/01-live-viewer-bridge/01-09-SUMMARY.md` when done.
Dokumentiere die finalen data-testids des Run-Setups (live-model-select, live-model-option-<id>,
live-start-run) + nav-link-live für den E2E-Test (01-10).
</output>
