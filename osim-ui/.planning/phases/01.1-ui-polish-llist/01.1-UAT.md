---
status: testing
phase: 01.1-ui-polish-llist
source: [ROADMAP.md §Phase 1.1 (11 Success Criteria), 01.1-OFFENE-PUNKTE.md]
started: 2026-05-22T13:30:00Z
updated: 2026-05-22T13:30:00Z
---

## Current Test

number: 1
name: GraphObject-Foundation 1:1-Port (Vitest)
expected: |
  Foundation-Tests laufen grün und decken die Verhaltensgarantien aus
  GRAPHOBJ-NOTES.md §17 ab. `cd portal && npx vitest run` zeigt
  183/183 grün, davon ~80+ Tests in `src/graph/foundation/__tests__/`.
  Keine .skip/.todo-Marker, keine pending Tests.
awaiting: user response

## Tests

### 1. GraphObject-Foundation 1:1-Port (Vitest)
expected: |
  Foundation-Tests laufen grün und decken die Verhaltensgarantien aus
  GRAPHOBJ-NOTES.md §17 ab. `cd portal && npx vitest run` zeigt
  183/183 grün, davon ~80+ Tests in `src/graph/foundation/__tests__/`.
  Keine .skip/.todo-Marker, keine pending Tests.
result: [pending]

### 2. Nested Knoten / GObjSub mit Sub-Grids
expected: |
  Im Workspace einen Durchlaufplan öffnen, der einen GObjSub-Knoten
  enthält (Phase 1 ist Dummy.otx ohne Sub-Knoten — Fertigungsstruktur1
  oder Bosch2_wechseln hochladen, falls verfügbar). Doppelklick auf
  einen Sub-Knoten öffnet die innere Sub-Hierarchie inline; nochmal
  Doppelklick schließt sie wieder. State-Maschine D_CLOSED ↔ D_OPEN
  funktioniert rekursiv. Falls kein Modell mit GObjSub vorliegt:
  als "n/a" markieren — der Foundation-Code ist via Vitest-Tests
  abgesichert (GObjSub.spec.ts).
result: [pending]

### 3. Original-Symbole als SVG
expected: |
  Sidebar-Tree und ModelLibrary nutzen OSim-SVG-Symbole statt
  lucide-Defaults. Brand-Logo links oben zeigt "osim STUDIO" mit
  eigenem Symbol. Tree-Gruppen ("Auslöser", "Durchlaufpläne",
  "Belegungsressourcen", ...) zeigen jeweils ein gruppen-spezifisches
  Outline-Symbol (brand-600 Stroke). 12+ Symbole sichtbar integriert.
result: [pending]

### 4. PDurchlaufplanViewer-Design auf GraphObject-Foundation
expected: |
  Im Workspace einen Durchlaufplan öffnen. Der graphische Viewer rendert
  Knoten + Kanten via React-Flow, die Position kommt aus dem
  OGraphGrid (m_pntRaster → m_GOrg). Drag eines Knotens persistiert
  die neue Position (Status-Bar "Gespeichert" wechselt zu "Ungespeichert"
  → nach 30s zurück oder via "Speichern"-Button sofort). Hinzufügen
  eines Knotens via Toolbar funktioniert.
result: [pending]

### 5. GraphView-Reachability
expected: |
  Login als admin@osim-dev → /models → Dummy.otx hochladen → Workspace
  öffnet sich → im Tree die "Durchlaufpläne"-Gruppe expandieren → ersten
  Plan anklicken → Graph-Editor öffnet sich automatisch (viewerHint=design)
  mit gerenderten Knoten und Kanten. Verifikation auch durch
  `npx playwright test e2e/demo-flow.spec.ts` (1/1 grün in 4.2s).
result: [pending]

### 6. LList-Resolution im Backend
expected: |
  Beim Modell-Upload löst `app/services/otx_json_tree.py` die Engine-
  LListPtr-Ketten auf. Im Wire-JSON erscheinen sub_refs als echte OID-
  Arrays statt LList-Pointer-Integer. Verifizierbar via curl:
    `curl -H "Authorization: Bearer $TOKEN" /api/v1/models/$id/tree`
  → PDurchlaufplan.sub_refs[0] ist ein int-Array. Backend-Tests
  (`uv run pytest tests/backend/`) grün.
result: [pending]

### 7. Sidebar-Tree navigierbar
expected: |
  Tree zeigt initial nur den Simulator-Root expanded, Sub-Gruppen
  collapsed. Click auf eine Gruppe expandiert sie. Suchfeld oben filtert
  case-insensitiv. Performance bei großem Modell (Bosch ~30k Objekte)
  bleibt responsiv (Tree-Render via react-arborist mit Virtualisierung).
  Knoten zeigen sinnvolle Labels (m_sName mit Fallback auf Klass-Kurzform).
result: [pending]

### 8. Modell-Bibliothek kompakt
expected: |
  Route /models zeigt die Modell-Liste in einer kompakten Darstellung
  (Tabelle / Listen-View, NICHT 3-Spalten-Karten-Grid). E2E-Test-Modelle
  werden automatisch gefiltert (z.B. "Demo-Flow-Dummy" verschwindet
  nach Test-Cleanup wieder).
result: [pending]

### 9. ViewerFrame-Toolbar verdrahtet
expected: |
  Rechts oben im Workspace-Header sind 7 Buttons sichtbar: « (first)
  ‹ (prev) › (next) » (last) + (create) ↻ (reset) × (delete). Klick auf
  ‹/› navigiert zum vorigen/nächsten Geschwister derselben Klasse.
  Click auf + öffnet einen Create-Dialog (oder erzeugt direkt einen
  Default-Knoten je nach Viewer). Tooltips bei Hover sichtbar.
  PSimulatorViewer-Sim-Lauf-Button ist disabled mit "Phase 2"-Hinweis.
result: [pending]

### 10. Visuelles Design
expected: |
  Brand-Farbschema (OSim-spezifisch, nicht shadcn-Default): Akzent-
  Farbe brand-500/600, Surface-Töne abgestimmt. Workspace-Header zeigt
  Breadcrumb (Modelle → ModellName → AktuellesObjekt). Status-Bar unten
  zeigt Lock-Status ("Sperre aktiv") und Save-Status ("Gespeichert" /
  "Ungespeichert" / "Speichert..."). SVG-Symbole integriert, keine
  Mischung aus lucide + osim-Symbolen in den primären Tree/Toolbar-Bereichen.
result: [pending]

### 11. Demo-Smoke-Test (Playwright)
expected: |
  `cd portal && npx playwright test e2e/demo-flow.spec.ts` läuft grün
  (1/1 passed in ~4-5s). Verifiziert die Sequenz Login → Bibliothek →
  Upload Dummy.otx → Workspace → Tree-Expand → Plan-Click → GraphFlowCanvas
  rendert mit mindestens 1 React-Flow-Node. Stack-Voraussetzung:
  `docker compose ps` zeigt alle 5 Services healthy + Firebase-Emulator-Seed.
result: [pending]

## Summary

total: 11
passed: 0
issues: 0
pending: 11
skipped: 0
blocked: 0

## Gaps

[none yet]
