# Phase 1.1: Discussion Log

**Datum:** 2026-05-22
**Modus:** Direkt-Capture ohne iterative Discuss-Runde

## Kontext

Phase 1.1 wurde am Vortag (2026-05-21) in die ROADMAP eingefügt nach User-Feedback zur Phase-1-Abnahme: Workspace war zwar funktional, aber demo-untauglich (Bibliothek zu groß, Tree default-expanded, GraphView durch LList-Pointer-Bug nicht erreichbar, Toolbar ohne Funktion).

Am 2026-05-22 hatte der User morgens bereits zwei Code-Commits + eine 32-KB-Algorithmen-Notes-Datei (`01.1-GRAPHOBJ-NOTES.md`) zur GraphObject-Portierung erstellt — ohne formellen Phase-1.1-Plan.

## Initial-Fehler des Assistenten (korrigiert)

Beim Resume-Workflow habe ich die `01.1-GRAPHOBJ-NOTES.md` als "Phase-4-Scope-Creep" eingestuft und nach `.planning/phases/04-live-viz/` verschoben (Commit 0a9a2fb). Begründung war, dass die ROADMAP-Phase-4-Texte den GraphObject-"Vollausbau" dort verorten.

**User-Korrektur (scharf, 2026-05-22):** "du solltest den grapobject viewer nachentwickeln ich will das erst fertig haben vor eine[r] langwierigen diskussion hast du dir das nicht gemerkt!!!!!"

Konsequenz:
- Move-Commit per `git revert` rückgängig gemacht (449162a)
- Memory-Eintrag `graphobject-is-viewer-foundation` geschärft mit Datum und Vorrang-Klausel ("NIE als Scope-Creep behandeln, GraphObject-Foundation hat Vorrang vor Discussion in Phase 1.1")
- Discuss-Workflow ohne iterative AskUserQuestion-Runden — CONTEXT.md direkt aus Memory + User-Anweisungen + existierender Vorarbeit (NOTES, ROADMAP, Phase-1-Decisions) destilliert

## User-Anweisungen, die CONTEXT.md formen

1. **Initial (2026-05-22):** GraphObject-Schicht aus OSim2004 1:1 nachentwickeln. Visualisierung darf modern sein, Funktionalität exakt gleich. Quelle: `.planning/phases/01.1-ui-polish-llist/01.1-GRAPHOBJ-NOTES.md` (selbst erstellt, 32 KB).

2. **Nachschuss (2026-05-22):** "schau dir nochmal genau den code an. besonderes das ui also die funktionalität, die hierarchische graph anoprdnungslogik im raster sind absulut entscheidend!!!! bitte exakt nachentwicklen. insbesondere für nestend knoten!!!! auch die alten synbole in svgs wandeln und nutzen. optisch auffrischen ist okay!"

   Daraus drei zusätzliche Pflichtbereiche im CONTEXT:
   - **Hierarchische Raster-Anordnung** als Pflicht-Welle (nicht nur die Top-Level-`OGraphGrid`-Mechanik, sondern auch das Nested-Sub-Grid-System)
   - **Nested Knoten (`GObjSub`)** als Pflichtbestandteil der Foundation in Welle B (GraphObj.h Z. 2047-2150: Mehrfacherbung von `OGraphView` + `GObjLink`, D_CLOSED/D_OPEN-State, SetChildsVisible-Rekursion, IsParentFrom-Walk, CreateGrid für interne Layer, 4-Layer-Drawing mit Durchreichung)
   - **SVG-Migration der OSim2004-Symbole** (199 BMPs + 6 ICOs, Pflicht-Set ~25 für Phase 1.1)

3. **Zielgruppen-Framing-Korrektur (2026-05-22):** "lass hka oder diskussionen darüber weg das ist ein indsutrietool!!!!"

   Konsequenz: Memory `feedback-target-audience-framing` geschärft. CONTEXT-Texte und Discuss-Fragen framen osim-ui als Industrie-Werkzeug, NIE als Lehr-/HKA-Werkzeug, auch wenn ROADMAP-Originaltexte solche Formulierungen enthalten.

## Carrying forward (nicht erneut gefragt)

Aus `01-CONTEXT.md` (Phase 1):
- D-08: 12 konkrete Viewer-Klassen bleiben Registry-Bestand
- D-14: Save-Strategie A (Pass-Through Original-OTX) — wird in Phase 1.1 NICHT gebrochen, LList-Resolution betrifft nur Wire-Frontend-Darstellung
- D-09: Sidebar-Tree-Navigation existiert und ist seit `27ce085` redesigned

Aus Plan 1-10:
- Position-Persistierung via `wire.attrs.m_iPosX/m_iPosY` (Engine-OTX-Writer ist Pass-Through für unbekannte Felder)
- `@xyflow/react` als React-Flow-Backend (bereits installiert)
- Plan-10-Pitfall-Mitigations bleiben gültig (nodeTypes outside, React.memo, useCallback, onlyRenderVisibleElements)

## Deferred (in CONTEXT `<deferred>` dokumentiert)

- Live-Status-Animation (Phase 4)
- Volle 18-er GObjType-Knoten-Klassen (Phase 4)
- Matrix-Viewer-Migration auf GObject (Phase 7+)
- DnD-Palette-Sidebar (Phase 4)
- Dagre-Auto-Layout (Phase 4)
- Wire→OTX-Mutations-Apply (Backlog nach 1.1)
- Mobile-Responsive (out of scope)

## Nächster Schritt

`/gsd-plan-phase 01.1` — Planner soll die Welle-Reihenfolge aus CONTEXT D-1.1-03 + GRAPHOBJ-NOTES §19 als Tasks materialisieren, mit Welle 0a (GObjSub-Lese-Schritt + OGObject/OGObjLink/OGCollec-Lese-Schritt) vor Welle B.
