---
phase: 01-vertical-slice
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "pyproject.toml"
  - "app/core/config.py"
  - "app/core/database.py"
  - "app/core/logging.py"
  - "app/core/errors.py"
  - "app/auth/firebase.py"
  - "app/auth/middleware.py"
  - "app/auth/dependencies.py"
  - "app/api/v1/router.py"
  - "app/api/v1/auth.py"
  - "app/api/v1/health.py"
  - "app/services/tenant_service.py"
  - "app/models/tenant.py"
  - "app/models/user.py"
  - "app/main.py"
  - "db/alembic.ini"
  - "db/migrations/env.py"
  - "db/migrations/script.py.mako"
  - "db/migrations/versions/001_initial_schema.py"
  - "docker-compose.yml"
  - ".env.example"
  - "tests/conftest.py"
  - "tests/test_health.py"
  - "tests/test_auth_me.py"
autonomous: true
requirements:
  - D-15
  - D-16
  - D-17
  - D-18
implements_decisions:
  - D-15  # Firebase Auth ab Tag 1
  - D-16  # Schema-per-Tenant in Postgres ab Tag 1
  - D-17  # Lazy Tenant-Bootstrap beim ersten /auth/me-Call
  - D-18  # Volle FastAPI-Foundation in Phase 1

must_haves:
  truths:
    - "`docker compose up postgres firebase-emulator` startet ohne Fehler"
    - "`uv run uvicorn app.main:app` startet ohne Fehler, /health antwortet 200"
    - "/readiness antwortet 200 mit DB-Connectivity-Check"
    - "Anfrage ohne Authorization-Header an /api/v1/* wird mit 401 abgelehnt (RFC-7807 ProblemDetail)"
    - "Anfrage mit gültigem Firebase-Emulator-Token an POST /api/v1/auth/me legt beim ersten Call automatisch tenant_{uid}-Schema, tenants-Row und users-Row an"
    - "Zweiter Call mit demselben Token ist idempotent — keine zweite Tenant-/User-Row"
    - "Datenbankzugriffe nach /auth/me laufen mit gesetztem search_path={tenant_id},public"
    - "alembic upgrade head läuft idempotent auf leerer DB durch"
  artifacts:
    - path: "app/core/config.py"
      provides: "Settings (pydantic-settings) mit DATABASE_URL, FIREBASE_PROJECT_ID, FIREBASE_AUTH_EMULATOR_HOST, CORS_ORIGINS"
      contains: "class Settings"
    - path: "app/core/database.py"
      provides: "async SQLAlchemy-Engine + get_db-Dependency mit set search_path"
      contains: "SET search_path"
    - path: "app/auth/middleware.py"
      provides: "TenantAuthMiddleware (ASGI) mit Whitelist /health, /readiness, /docs, /openapi.json"
      contains: "class TenantAuthMiddleware"
    - path: "app/api/v1/auth.py"
      provides: "POST /api/v1/auth/me-Endpoint mit lazy Tenant-Bootstrap"
      exports: ["router"]
    - path: "app/services/tenant_service.py"
      provides: "ensure_tenant_bootstrap(uid, email, db) -> Tenant — idempotent"
      contains: "async def ensure_tenant_bootstrap"
    - path: "db/migrations/versions/001_initial_schema.py"
      provides: "Initiale Migration für tenants, users, models, runs, run_artifacts, model_versions, edit_locks"
      contains: "def upgrade"
    - path: "docker-compose.yml"
      provides: "postgres, firebase-emulator, optional minio"
      contains: "firebase-emulator"
  key_links:
    - from: "app/main.py"
      to: "TenantAuthMiddleware + api_router"
      via: "add_middleware + include_router"
      pattern: "include_router.*api_router"
    - from: "app/api/v1/auth.py"
      to: "tenant_service.ensure_tenant_bootstrap"
      via: "service-call mit request.state.user_uid"
      pattern: "ensure_tenant_bootstrap"
    - from: "app/core/database.py"
      to: "request.state.tenant_id"
      via: "SET search_path TO \"{tenant_id}\", public"
      pattern: "SET search_path"
---

<objective>
Welle 1: Volle FastAPI-Backend-Foundation gemäß D-18 mit Firebase Auth (D-15), Schema-per-Tenant (D-16) und lazy Tenant-Bootstrap (D-17).

Purpose: Alle späteren Plans (Storage, Modelle, Viewer-API, Save-Endpoint) brauchen einen funktionierenden Auth-Stack und eine konfigurierte DB. Hier wird der gesamte 3fls-Stack 1:1 angelegt — auch Endpoints, die in Phase 1 noch nicht produktiv konsumiert werden (z.B. /readiness mit DB-Check, RFC-7807-Error-Handler), damit Phase 2+ nicht im Backend nachflicken muss.

Output:
- Lauffähige FastAPI-App mit Firebase Auth Middleware
- Postgres-Schema-per-Tenant + Alembic-Migration für die initialen Tabellen
- Lazy Bootstrap des Tenants beim ersten /auth/me
- docker-compose mit postgres + firebase-emulator
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Test-Command: `uv run pytest tests/ -x -v`
Run-Command für Smoke: `docker compose up -d postgres firebase-emulator && uv run alembic -c db/alembic.ini upgrade head && uv run uvicorn app.main:app --reload`
</execution_context>

<context>
@.planning/phases/01-vertical-slice/01-CONTEXT.md
@.planning/research/copy-paste-guide.md
@.planning/research/3fls-patterns.md
@./pyproject.toml
@./app/main.py

# Vorlagen aus 3fls (1:1 übernehmen)
@C:\Users\JörgWFischer\PycharmProjects\tbx_stzrim\app\auth\middleware.py
@C:\Users\JörgWFischer\PycharmProjects\tbx_stzrim\app\auth\firebase.py
@C:\Users\JörgWFischer\PycharmProjects\tbx_stzrim\app\core\database.py
@C:\Users\JörgWFischer\PycharmProjects\tbx_stzrim\app\core\config.py

<interfaces>
<!-- Wesentliche Verträge, gegen die spätere Plans bauen -->

```python
# app/core/config.py — Phase-1-Settings
class Settings(BaseSettings):
    database_url: str
    firebase_project_id: str
    firebase_auth_emulator_host: str | None = None  # z.B. "localhost:9099"
    cors_origins: list[str]
    storage_backend: Literal["local", "minio", "gcs"] = "local"
    storage_local_root: Path = Path("./storage")
    environment: Literal["dev", "staging", "prod"] = "dev"

# app/auth/middleware.py — gesetzte request.state-Felder
# (Verträge für alle Downstream-Endpoints)
request.state.tenant_id: str     # z.B. "tenant_abc123" (= "tenant_" + firebase_uid kurz)
request.state.user_uid: str      # Firebase-UID
request.state.user_email: str
request.state.user_role: str     # "owner" | "editor" | "viewer", default "owner" für eigenen Tenant

# app/auth/dependencies.py
async def get_tenant_id(request: Request) -> str: ...
async def get_user_uid(request: Request) -> str: ...
async def get_user_email(request: Request) -> str: ...

# app/core/database.py
async def get_db(request: Request) -> AsyncGenerator[AsyncSession, None]: ...
# setzt SET search_path TO "{tenant_id}", public pro Request

# app/services/tenant_service.py
async def ensure_tenant_bootstrap(
    uid: str, email: str, db: AsyncSession
) -> Tenant: ...
# idempotent: legt schema + tenants-row + users-row beim ersten Call an
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyproject + Config + DB-Engine + Logging + docker-compose</name>
  <files>pyproject.toml, app/core/config.py, app/core/database.py, app/core/logging.py, app/core/errors.py, docker-compose.yml, .env.example, db/alembic.ini, db/migrations/env.py, db/migrations/script.py.mako</files>
  <action>
    1. **pyproject.toml** ergänzen (NICHT überschreiben — bestehende Dependencies bleiben). Sicherstellen, dass enthalten: `pydantic-settings>=2.6`, `firebase-admin>=7.2`, `structlog>=25.0`. Falls Engine als editable-install gewollt: optionaler dev-Extras-Block `osim-engine = { path = "../engine", editable = true }` — aber für Phase-1-Plan 02 ist die Engine NICHT direkt nötig (erst Plan 03), daher hier nur Vorbereitung. Workspace-Setup via `[tool.uv.sources]` (uv 0.5+): `osim-engine = { path = "../engine", editable = true }`.

    2. **app/core/config.py** — kopiere Pattern aus `tbx_stzrim/app/core/config.py` und passe an: Settings(BaseSettings) mit den oben in <interfaces> dokumentierten Feldern. Source: `.env`-File via `pydantic_settings.SettingsConfigDict(env_file=".env", extra="ignore")`. Modul-Singleton `settings = Settings()`.

    3. **app/core/database.py** — async SQLAlchemy-Engine basierend auf 3fls-Pattern, aber mit asyncpg statt psycopg (D-18 sagt async + asyncpg):
       - `create_async_engine(settings.database_url, pool_size=20, max_overflow=10, pool_pre_ping=True)`
       - `AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)`
       - `async def get_db(request: Request) -> AsyncGenerator[AsyncSession, None]`: holt `tenant_id` aus `request.state` (Whitelist-Endpoints haben keinen → KeyError sauber abfangen und 401 raisen), öffnet Session, executed `SET search_path TO "{tenant_id}", public` als raw-SQL, yieldet Session.
       - Base aus SQLAlchemy 2: `class Base(DeclarativeBase): pass`.

    4. **app/core/logging.py** — structlog-Setup nach 3fls-Pattern: JSON-Format in prod, console in dev. `configure_logging()` wird in lifespan aufgerufen.

    5. **app/core/errors.py** — RFC-7807-ProblemDetail-Schema + globale Exception-Handler:
       - `class ProblemDetail(BaseModel)` mit `type, title, status, detail, instance`.
       - `register_exception_handlers(app)`: HTTPException → ProblemDetail-JSON, RequestValidationError → ProblemDetail mit validation-errors-array, generic Exception → 500 ProblemDetail.

    6. **docker-compose.yml** — minimal:
       ```yaml
       services:
         postgres:
           image: postgres:17
           environment:
             POSTGRES_DB: osim_ui
             POSTGRES_USER: osim_dev
             POSTGRES_PASSWORD: osim_dev
           ports: ["5433:5432"]
           volumes: [pgdata:/var/lib/postgresql/data]
         firebase-emulator:
           # offizielles Image, kein NPM-Install bei jedem Start
           image: andreysenov/firebase-tools:latest
           command: firebase emulators:start --project osim-dev --only auth --host 0.0.0.0
           ports: ["9099:9099", "4000:4000"]
         minio:
           image: minio/minio:latest
           command: server /data --console-address ":9001"
           environment:
             MINIO_ROOT_USER: osim
             MINIO_ROOT_PASSWORD: osim_dev_password
           ports: ["9000:9000", "9001:9001"]
           volumes: [minio_data:/data]
       volumes:
         pgdata:
         minio_data:
       ```
       Per Discretion: Falls `andreysenov/firebase-tools` nicht verfügbar → fallback auf `node:20-slim` mit `npm install -g firebase-tools`-Approach aus copy-paste-guide.md.

    7. **.env.example** — alle Settings-Variablen als Template.

    8. **Alembic-Setup**: `db/alembic.ini` (3fls-1:1, sqlalchemy.url aus env), `db/migrations/env.py` (async-Variante — wichtig wegen SQLAlchemy 2 async), `db/migrations/script.py.mako` (Standard-Template). Beispiel-env.py für async:
       ```python
       # nutzt run_migrations_online() mit asyncio.run(do_run_migrations())
       # siehe https://alembic.sqlalchemy.org/en/latest/cookbook.html#using-asyncio-with-alembic
       ```
       Import-Strategie: `from app.core.database import Base` + `from app.models import *` damit autogenerate alle Modelle sieht (Modelle kommen in Task 2).

    9. **WICHTIG:** docker-compose-Service `firebase-emulator` ist initial nur Phase-1-Dev-Hilfsmittel. Phase 4+ wird auf echtes Firebase wechseln (siehe D-15 — "Firebase-Project in späteren Phasen").
  </action>
  <verify>
    <automated>uv sync && docker compose config --quiet && uv run python -c "from app.core.config import settings; from app.core.database import engine, get_db, Base; print('OK')"</automated>
  </verify>
  <done>uv sync clean, docker-compose-Config valide, app/core/* importierbar ohne Crash.</done>
</task>

<task type="auto">
  <name>Task 2: Models + Initial-Migration + Tenant-Service</name>
  <files>app/models/__init__.py, app/models/tenant.py, app/models/user.py, app/services/__init__.py, app/services/tenant_service.py, db/migrations/versions/001_initial_schema.py, tests/conftest.py, tests/test_alembic.py</files>
  <action>
    1. **app/models/tenant.py** — SQLAlchemy 2 typed Model im `public`-Schema:
       ```python
       class Tenant(Base):
           __tablename__ = "tenants"
           __table_args__ = {"schema": "public"}
           id: Mapped[str] = mapped_column(String(64), primary_key=True)  # = "tenant_" + firebase_uid_short
           owner_uid: Mapped[str] = mapped_column(String(128), unique=True, nullable=False, index=True)
           owner_email: Mapped[str] = mapped_column(String(256), nullable=False)
           schema_name: Mapped[str] = mapped_column(String(64), unique=True, nullable=False)
           created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
       ```

    2. **app/models/user.py** — User im `public`-Schema:
       ```python
       class User(Base):
           __tablename__ = "users"
           __table_args__ = {"schema": "public"}
           uid: Mapped[str] = mapped_column(String(128), primary_key=True)  # Firebase-UID
           email: Mapped[str] = mapped_column(String(256), nullable=False)
           tenant_id: Mapped[str] = mapped_column(ForeignKey("public.tenants.id"), nullable=False, index=True)
           role: Mapped[str] = mapped_column(String(32), nullable=False, default="owner")
           created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
       ```

    3. **app/models/__init__.py** — Re-Export: `from app.models.tenant import Tenant; from app.models.user import User; __all__ = ["Tenant", "User"]`. Für Plan 03 werden hier Model, ModelVersion, EditLock, Run ergänzt — JETZT NICHT vorgreifen, das ist Plan 03.

    4. **app/services/tenant_service.py** — `async def ensure_tenant_bootstrap(uid: str, email: str, db: AsyncSession) -> Tenant`:
       - Suche User per uid in `public.users`. Falls vorhanden → lade zugehörigen Tenant und return.
       - Falls NICHT vorhanden:
         a. Generiere `tenant_id = "tenant_" + uid[:16].lower()` (lowercase, alphanumerisch; falls Firebase-UID Sonderzeichen hat → hash-basiert).
         b. `db.execute(text(f'CREATE SCHEMA IF NOT EXISTS "{tenant_id}"'))` — **WICHTIG:** tenant_id muss whitelisted/escaped sein, niemals direkt aus User-Input. Hier kommt es aus uid, das Firebase validiert → ok, aber dennoch `[a-z0-9_]{1,63}`-Regex-Check vorab.
         c. Insert Tenant + User (atomar in einer Transaktion via `db.begin()`).
         d. db.commit().
       - Idempotenz: Bei Race-Condition (zwei parallele Requests) → eindeutiger Constraint auf `users.uid` führt zu IntegrityError; in except-Block → re-query und return.

    5. **db/migrations/versions/001_initial_schema.py** — `alembic revision --autogenerate -m "initial schema: tenants, users"` ODER manuell schreiben (wenn autogenerate für public-Schema-Tabellen schwierig). Das Script muss:
       - `CREATE SCHEMA IF NOT EXISTS public` (defensive, ist standard vorhanden).
       - Tabelle `public.tenants` + `public.users` analog zu den Models.
       - Indizes: `users.uid` (PK), `users.tenant_id`, `tenants.owner_uid`.
       - downgrade(): DROP TABLE in umgekehrter Reihenfolge.

    6. **tests/conftest.py** — pytest-Fixtures:
       - `event_loop` (asyncio scope=session).
       - `db_engine` (async, gegen Test-DB).
       - `db_session` (mit Rollback nach Test).
       - `app` (FastAPI-Instance mit Test-Settings).
       - `client` (httpx.AsyncClient).
       - `fake_firebase_token` (Helper zum Erzeugen eines Token, das verify_token() in Test-Mode akzeptiert — Strategie siehe Task 3).

    7. **tests/test_alembic.py** — Smoke: `from alembic.config import Config; from alembic import command; cfg = Config("db/alembic.ini"); command.upgrade(cfg, "head")` läuft auf leerer Test-DB durch, danach `command.downgrade(cfg, "base")` ebenfalls.
  </action>
  <verify>
    <automated>uv run alembic -c db/alembic.ini upgrade head && uv run pytest tests/test_alembic.py -x -v</automated>
  </verify>
  <done>Migration läuft idempotent durch, tenants- und users-Tabelle existieren in public-Schema, Models importierbar, tenant_service.ensure_tenant_bootstrap importierbar.</done>
</task>

<task type="auto">
  <name>Task 3: Firebase-Auth + Middleware + /auth/me-Endpoint + /health/readiness + main.py-Wiring</name>
  <files>app/auth/firebase.py, app/auth/middleware.py, app/auth/dependencies.py, app/api/v1/router.py, app/api/v1/auth.py, app/api/v1/health.py, app/main.py, tests/test_health.py, tests/test_auth_me.py</files>
  <action>
    1. **app/auth/firebase.py** — 1:1 aus `tbx_stzrim/app/auth/firebase.py`:
       - `initialize_firebase()` (idempotent, einmal in lifespan aufgerufen).
       - `verify_token(token: str) -> dict` — nutzt `firebase_admin.auth.verify_id_token`.
       - Emulator-Support: wenn `settings.firebase_auth_emulator_host` gesetzt → setze `os.environ["FIREBASE_AUTH_EMULATOR_HOST"]` VOR firebase_admin.initialize_app.

    2. **app/auth/middleware.py** — TenantAuthMiddleware (ASGI) 1:1 aus 3fls mit Anpassungen:
       - WHITELIST_PATHS: `/health`, `/readiness`, `/docs`, `/openapi.json`, `/redoc` (Stripe-Webhooks weglassen).
       - Extract `Authorization: Bearer <token>` → `verify_token(token)` → setze `scope["state"].user_uid = decoded["uid"]`, `user_email = decoded["email"]`, `tenant_id = decoded.get("tenant_id")` (Custom Claim, in Phase 1 noch nicht gesetzt — Fallback: aus User-DB-Lookup, oder leer = bootstrap-pending).
       - **WICHTIG für /auth/me:** Wenn `tenant_id`-Claim fehlt, MUSS die Middleware den Request trotzdem durchlassen und `tenant_id = None` setzen — der /auth/me-Handler bootstrapt erst. Für ALLE anderen `/api/v1/*`-Pfade ohne tenant_id → 401.
       - Bei Verification-Fehler → 401 mit ProblemDetail-Body.

    3. **app/auth/dependencies.py** — `get_tenant_id`, `get_user_uid`, `get_user_email` als FastAPI-Dependencies, lesen aus `request.state`.

    4. **app/api/v1/health.py**:
       - `GET /health` → `{"status": "ok", "service": "osim-ui", "version": "0.1.0"}`.
       - `GET /readiness` → testet DB-Connectivity via `await db.execute(text("SELECT 1"))`, returnt `{"status": "ok", "db": "up"}` oder 503 mit `{"status": "degraded", "db": "down"}`.

    5. **app/api/v1/auth.py**:
       - `POST /api/v1/auth/me` → Body leer, nutzt `Depends(get_user_uid)`, `Depends(get_user_email)`, `Depends(get_db_unscoped)` (eine NEUE Dependency, die KEINEN search_path-Switch macht, weil tenant noch nicht existiert; nur im public-Schema arbeitet).
         - Ruft `ensure_tenant_bootstrap(uid, email, db)`.
         - Response: `{"tenant_id": tenant.id, "user_uid": user.uid, "email": user.email, "role": user.role}`.
       - **WICHTIG:** Nach erfolgreichem Bootstrap sollte der Server idealerweise Firebase Custom Claims für den User setzen (`auth.set_custom_user_claims(uid, {"tenant_id": tenant.id, "role": "owner"})`), damit nachfolgende Tokens (nach Token-Refresh im Client) den tenant_id-Claim mitbringen. Das ist Phase-1-D-17-konform: "Self-Service, kein Admin-Eingriff".

    6. **app/api/v1/router.py** — Aggregator:
       ```python
       from fastapi import APIRouter
       from app.api.v1 import auth, health
       api_router = APIRouter()
       api_router.include_router(health.router, tags=["health"])
       api_router.include_router(auth.router, prefix="/auth", tags=["auth"])
       ```
       Plan 03 wird hier `models.router` ergänzen.

    7. **app/main.py** — bestehende Datei ergänzen:
       - lifespan: `initialize_firebase()`, `configure_logging()`.
       - `app.add_middleware(TenantAuthMiddleware)` NACH GZip+CORS.
       - `register_exception_handlers(app)`.
       - `app.include_router(api_router, prefix="/api/v1")`.
       - /health auf Top-Level entfernen (kommt jetzt aus api/v1/health.py — aber: Whitelist erwartet `/health` ohne Präfix; entweder Whitelist auf `/api/v1/health` setzen ODER Top-Level-Alias behalten). Empfehlung: BEIDE Pfade unterstützen — Top-Level `/health` als Liveness-Probe für Docker/k8s, `/api/v1/health` als versionierter API-Endpoint.

    8. **tests/test_health.py** — `GET /health` ohne Auth → 200; `GET /readiness` mit/ohne DB → entsprechend 200/503.

    9. **tests/test_auth_me.py** — Strategie für Auth-Tests:
       - Option A: gegen Firebase-Emulator (docker-compose service muss laufen) — `httpx`-Call gegen `http://localhost:9099/identitytoolkit.googleapis.com/v1/accounts:signUp?key=fake` → ID-Token → POST /auth/me.
       - Option B: monkeypatch `app.auth.firebase.verify_token` → fake-decoded-Payload. Einfacher, schneller, weniger Realismus.
       - Mische beide: Unit-Tests mit B (schnell), Integration-Test mit A (in test_auth_integration.py, marked `@pytest.mark.integration`).
       - Test-Cases:
         - test_auth_me_creates_tenant_on_first_call
         - test_auth_me_is_idempotent (zweimal call, gleicher tenant_id, kein zweiter User-Row)
         - test_auth_me_creates_schema_in_postgres (SELECT schema_name FROM information_schema.schemata)
         - test_unauthenticated_request_returns_401_problem_detail
  </action>
  <verify>
    <automated>uv run pytest tests/test_health.py tests/test_auth_me.py -x -v</automated>
  </verify>
  <done>Alle Tests grün. Smoke-Test: `docker compose up -d && uv run alembic upgrade head && uv run uvicorn app.main:app` + `curl http://localhost:8000/health` → 200; Login via Firebase-Emulator + `curl -X POST -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/v1/auth/me` → 200 mit tenant_id.</done>
</task>

</tasks>

<verification>
1. `uv sync` clean.
2. `docker compose config --quiet` valide.
3. `uv run alembic -c db/alembic.ini upgrade head` und `downgrade base` beides idempotent.
4. `uv run pytest tests/ -x` alle grün.
5. `uv run ruff check app/ tests/` clean.
6. `uv run mypy app/` clean (oder explizit-akzeptierte Ignores für firebase_admin-stubless).
7. Smoke: `docker compose up -d postgres firebase-emulator` + `uv run uvicorn app.main:app` + `curl /health` → 200.
</verification>

<success_criteria>
- Vollständige FastAPI-Foundation aus D-18 angelegt: versionierte APIs, Service-Layer, async SQLAlchemy, Alembic, Firebase-Auth-Middleware, structlog, RFC 7807, Health/Readiness, OpenAPI-Docs, Test-Infra.
- Schema-per-Tenant funktioniert: erster /auth/me-Call legt automatisch Schema + Tenant + User an (D-17).
- alle Phase-Plan-03+ können auf diesem Stack aufbauen ohne im Auth/DB-Bereich nachzuflicken.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/01-vertical-slice/01-02-SUMMARY.md` mit:
- tech_stack: fastapi 0.115, sqlalchemy 2 async, asyncpg 0.30, alembic 1.14, firebase-admin 7.2, structlog 25, pydantic-settings 2.6
- decisions: tenant_id-Format ("tenant_" + uid[:16]); Firebase Custom Claims werden nach Bootstrap gesetzt; /health auf Top-Level UND /api/v1/health; firebase-emulator Image-Wahl
- patterns: TenantAuthMiddleware aus 3fls 1:1; ProblemDetail-Pattern; get_db-Dependency mit search_path-Switch; ensure_tenant_bootstrap mit Race-Condition-Safety
- provides: Settings, Base, engine, get_db, get_db_unscoped, TenantAuthMiddleware, verify_token, ensure_tenant_bootstrap, api_router, Tenant, User, ProblemDetail, register_exception_handlers
- affects: app/, db/, docker-compose.yml, .env.example, pyproject.toml
</output>

<risks>
- **firebase-admin-Setup mit Emulator:** `FIREBASE_AUTH_EMULATOR_HOST` muss VOR `initialize_firebase()` gesetzt sein — sonst landet das SDK in prod-Modus. Mitigation: in `app/core/config.py` direkt nach Settings-Init `os.environ[...]` setzen, nicht erst in lifespan.
- **alembic + async + autogenerate:** Async Alembic ist fummelig. Falls autogenerate für SQLAlchemy 2 async Probleme macht → 001-Migration manuell schreiben (siehe Aktion 5). Verzicht auf autogenerate für Plan-Phase 1 ist akzeptabel; Plan 03 muss prüfen, ob sich autogenerate stabil betreiben lässt.
- **Schema-per-Tenant + Test-Isolation:** Tests, die Schema anlegen, müssen am Ende sauber droppen. conftest-Fixture mit `try/finally` und `DROP SCHEMA "{tid}" CASCADE`.
- **Whitelist /health vs. /api/v1/health:** Doppel-Mount kann zu Whitelist-Lücken führen. Mitigation: explizite Tests für beide Pfade ohne Auth → 200.
- **set_custom_user_claims** funktioniert mit dem Firebase-Emulator-Auth-Service eventuell anders/limited. Falls Emulator das nicht unterstützt → Phase-1-Compromise: tenant_id wird bei jedem Request aus DB nachgelesen (langsam, aber tolerierbar bis Phase 4 prod-Firebase).
</risks>
