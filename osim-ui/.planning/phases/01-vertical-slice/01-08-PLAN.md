---
phase: 01-vertical-slice
plan: 08
type: execute
wave: 4
depends_on: [04, 05, 06]
files_modified:
  - "portal/src/viewers/linking/PDlplBetriebsmittelViewer.tsx"
  - "portal/src/viewers/linking/PDlplPersonalViewer.tsx"
  - "portal/src/viewers/linking/index.ts"
  - "portal/src/viewers/arbeitszeit/AEinsatzWunschViewer.tsx"
  - "portal/src/viewers/arbeitszeit/AKapBedViewer.tsx"
  - "portal/src/viewers/arbeitszeit/schicht-helpers.ts"
  - "portal/src/viewers/arbeitszeit/index.ts"
  - "portal/src/viewers/property/type-maps.ts"
  - "portal/src/viewers/property/index.ts"
  - "portal/src/viewers/linking/__tests__/PDlplBetriebsmittelViewer.test.tsx"
  - "portal/src/viewers/arbeitszeit/__tests__/AEinsatzWunschViewer.test.tsx"
autonomous: true
requirements:
  - D-08
implements_decisions:
  - D-08  # Konkrete Viewer 8 (PDlplBetriebsmittel), 9 (PDlplPersonal), 10 (AEinsatzWunsch), 11 (AKapBed)

must_haves:
  truths:
    - "PDlplBetriebsmittelViewer zeigt Knoten ↔ Belegungsressourcen-Verknüpfung (Matrix oder Liste)"
    - "PDlplPersonalViewer zeigt Knoten ↔ Personal-Gruppen-Verknüpfung"
    - "AEinsatzWunschViewer ist ein Schicht-Editor (Wochentage × Stunden mit Einsatz-Wunsch-Eintragungen)"
    - "AKapBedViewer zeigt Kapazitätsbedarf-Sicht (read-only oder editable je nach Modell-Modus)"
    - "Alle 4 Viewer sind in der viewer-registry registriert"
    - "Damit sind alle 12 Viewer aus D-08 nach Plan 08 implementiert"
  artifacts:
    - path: "portal/src/viewers/linking/PDlplBetriebsmittelViewer.tsx"
      provides: "Verknüpfungs-Editor Knoten×Betriebsmittel"
      exports: ["PDlplBetriebsmittelViewer"]
    - path: "portal/src/viewers/linking/PDlplPersonalViewer.tsx"
      provides: "Verknüpfungs-Editor Knoten×Personal-Gruppe"
      exports: ["PDlplPersonalViewer"]
    - path: "portal/src/viewers/arbeitszeit/AEinsatzWunschViewer.tsx"
      provides: "Schicht-Wunsch-Editor (Wochen-Stunden-Raster)"
      exports: ["AEinsatzWunschViewer"]
    - path: "portal/src/viewers/arbeitszeit/AKapBedViewer.tsx"
      provides: "Kapazitätsbedarf-Übersicht"
      exports: ["AKapBedViewer"]
  key_links:
    - from: "portal/src/viewers/linking/*.tsx + portal/src/viewers/arbeitszeit/*.tsx"
      to: "portal/src/viewers/core/viewer-registry.ts"
      via: "Side-Effect-Import"
      pattern: "registerViewer"
    - from: "portal/src/viewers/arbeitszeit/AEinsatzWunschViewer.tsx"
      to: "portal/src/viewers/matrix/MatrixGrid.tsx"
      via: "Reuse für das Wochen-Stunden-Raster"
      pattern: "<MatrixGrid"
---

<objective>
Welle 4 (parallel zu Plan 06/07): die letzten 4 Viewer aus D-08.

Die Verknüpfungs-Viewer (PDlplBetriebsmittel/PDlplPersonal) sind Cross-Tree-Editoren: sie zeigen Beziehungen zwischen zwei Objekt-Gruppen. Die Arbeitszeit-Viewer (AEinsatzWunsch/AKapBed) sind Schicht-Modell-spezifisch.

Purpose: D-08 fordert genau 12 Viewer. Nach Plan 08 ist die Liste komplett:
1. PSimulator (Plan 05)
2. PDurchlaufplan-Std (Plan 05)
3. PDurchlaufplan-Design (Plan 07)
4. PGObjBase (Plan 05)
5. PRessBelegMatrix (Plan 06)
6. PRessMengeMatrix (Plan 06)
7. PRessVerknuepfung (Plan 06)
8. PDlplBetriebsmittel (Plan 08)  ← hier
9. PDlplPersonal (Plan 08)        ← hier
10. AEinsatzWunsch (Plan 08)      ← hier
11. AKapBed (Plan 08)             ← hier
12. AGruppe (Plan 05)

Output: 4 Viewer + TYPE_MAP-Erweiterungen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-vertical-slice/01-CONTEXT.md
@.planning/phases/01-vertical-slice/01-04-PLAN.md
@.planning/phases/01-vertical-slice/01-05-PLAN.md
@.planning/phases/01-vertical-slice/01-06-PLAN.md

# C++-Vorlagen
@C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\inc\PDlplBetriebsmittelViewer.h
@C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\inc\PDlplPersonalViewer.h
@C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\inc\AEinsatzWunschViewer.h
@C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\inc\AKapBedViewer.h

<interfaces>
Mapping Sidebar-Trigger → Viewer (siehe Plan 06 Synthetik-Folder-Pattern):
- "Knoten↔Betriebsmittel-Matrix" (Folder unter Workspace) → klass="DLPL_BETRIEBSMITTEL_GROUP" → PDlplBetriebsmittelViewer
- "Knoten↔Personal-Matrix" → klass="DLPL_PERSONAL_GROUP" → PDlplPersonalViewer
- AEinsatzWunsch direkt am AGruppe-Sub-Node oder als eigener Folder unter Personal-Group
- AKapBed analog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDlplBetriebsmittelViewer + PDlplPersonalViewer (Verknüpfungs-Matrizen)</name>
  <files>portal/src/viewers/linking/PDlplBetriebsmittelViewer.tsx, portal/src/viewers/linking/PDlplPersonalViewer.tsx, portal/src/viewers/linking/index.ts, portal/src/viewers/property/type-maps.ts, portal/src/viewers/property/index.ts, portal/src/viewers/linking/__tests__/PDlplBetriebsmittelViewer.test.tsx</files>
  <action>
    1. **TYPE_MAP ergänzen** für: "PDlplKnoten" (m_lRessBeleg link, m_lPersGruppe link), "AGruppe" (in Plan 05 schon basic, jetzt m_lZuordnungen).

    2. **PDlplBetriebsmittelViewer.tsx** — registriert für "DLPL_BETRIEBSMITTEL_GROUP" (synthetic-folder-klass, ergänzt im Sidebar-Tree-Adapter aus Plan 05/06):
       - rows = alle Knoten (alle PDpKn*-Nodes, klass-prefix-filter).
       - columns = alle Belegungsressourcen (PRessBeleg).
       - cell = Checkbox (Knoten×Ressource verknüpft ja/nein) ODER Number (Faktor — phase-1-discretion: Checkbox + optional Faktor wenn cell ≠ 0).
       - Reuse MatrixGrid aus Plan 06.
       - Edit: onCellChange → wenn von false→true: addChild zur PRessBeleg.m_lAssozBelegLinks (oder Knoten.m_lRessBeleg, je nach Modell-Schema); wenn true→false: removeNode des link-Objekts.

       **Wichtig zur Datenstruktur:** Lies otx_loader.py + Engine-Resource-Code, um zu verstehen wie Knoten×Ressource-Verknüpfung im OTX kodiert ist. Wahrscheinlich gibt es eine `PAssozBelegLink`-Klasse (im _SKIP-Set des Loaders gelistet). Phase-1-Tradeoff: wenn der Loader die Klasse skipt, müssen wir entweder den Loader erweitern (zusätzliche Engine-Arbeit) ODER Phase 1 macht den Viewer read-only mit Hinweis "Verknüpfungs-Edit kommt in Phase 2 (Engine-Schema-Erweiterung)".

       **Discretion-Entscheidung:** Phase 1 macht den Viewer **read-only** mit eindeutigem Hinweis-Banner "Edit-Funktion in Phase 2". Begründung: das Mit-Mod der osim-engine sprengt den Phase-1-Scope (CONTEXT.md sagt nur OTX-Writer als Welle 0). Plan 02-Roadmap-Update aus dem CONTEXT.md-deferred-Block wird das im Roadmap-Resync explizit tracken.

    3. **PDlplPersonalViewer.tsx** — analog für Knoten×Personal-Gruppe ("DLPL_PERSONAL_GROUP"). Gleiche read-only-Phase-1-Einschränkung.

    4. **Update Sidebar-Tree-Adapter** (in Plan 05/06): zwei neue synthetic-Folders unter Modell-Root: "Knoten↔Betriebsmittel", "Knoten↔Personal", mit den synthetic-klassen oben.

    5. **Tests** — Smoke-Render mit Mock-Tree (2 Knoten × 3 Ressourcen) → MatrixGrid mit 6 Zellen rendert; in read-only-Mode sind inputs disabled.
  </action>
  <verify>
    <automated>cd portal && npm run test -- --run PDlplBetriebsmittelViewer && npm run build</automated>
  </verify>
  <done>Beide Viewer rendern, sind in Sidebar-Folders erreichbar, read-only-Banner sichtbar.</done>
</task>

<task type="auto">
  <name>Task 2: AEinsatzWunschViewer + AKapBedViewer (Arbeitszeit)</name>
  <files>portal/src/viewers/arbeitszeit/AEinsatzWunschViewer.tsx, portal/src/viewers/arbeitszeit/AKapBedViewer.tsx, portal/src/viewers/arbeitszeit/schicht-helpers.ts, portal/src/viewers/arbeitszeit/index.ts, portal/src/viewers/property/type-maps.ts, portal/src/viewers/property/index.ts, portal/src/viewers/arbeitszeit/__tests__/AEinsatzWunschViewer.test.tsx</files>
  <action>
    1. **schicht-helpers.ts** — gemeinsame Utilities:
       - `WEEKDAYS = ['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So']`
       - `HOURS_OF_DAY = [...Array(24).keys()]`
       - `parseEinsatzzeit(node) -> { weekday: number, startMinute: number, endMinute: number }[]`
       - `formatTimeRange(start, end) -> "08:00-12:00"`

    2. **TYPE_MAP ergänzen** für "AEinsatzWunsch", "AKapBed", "AEinsatzzeitWunsch", "ATagPerson". Properties aus AEinsatzWunschViewer.h/AKapBedViewer.h + Engine-Code recherchieren.

    3. **AEinsatzWunschViewer.tsx** — registriert für klass "AEinsatzWunsch":
       - Layout: 7 Spalten (Wochentage) × 24 Zeilen (Stunden), reuse MatrixGrid mit cell-renderer = Checkbox.
       - cellValue = boolean (in dieser Stunde Einsatz gewünscht ja/nein).
       - getCellValue ermittelt sich aus den AEinsatzzeitWunsch-Sub-Nodes (m_iWochentag, m_iVon, m_iBis).
       - onCellChange: editierbar. Wenn cell-toggle von off→on: addChild AEinsatzzeitWunsch (oder erweitere existierenden range, falls anliegend). Wenn off: removeNode oder shrink range. **Phase-1-Discretion:** simple "ein-Block-pro-Stunde"-Modell ohne Range-Coalescing; Optimierung in Phase X.

    4. **AKapBedViewer.tsx** — registriert für "AKapBed":
       - Read-only-Übersicht: Bedarf vs. Verfügbarkeit pro Schicht/Zeit.
       - cell = string (z.B. "12/8" = 12 benötigt, 8 verfügbar, mit Color-Coding rot wenn Bedarf > Verfügbarkeit).
       - In Phase 1: rein read-only, da Kapazitätsbedarf rechnerisch aus dem Modell entsteht und kein Edit-Target ist.

    5. **Tests** — Smoke für AEinsatzWunschViewer mit Mock-AGruppe-Node.
  </action>
  <verify>
    <automated>cd portal && npm run test -- --run AEinsatzWunsch AKapBed && npm run build</automated>
  </verify>
  <done>Beide Viewer rendern und sind erreichbar.</done>
</task>

</tasks>

<verification>
1. `cd portal && npm run test -- --run` alle grün.
2. `cd portal && npm run build` clean.
3. Smoke: Workspace mit Fertigungsstruktur1_mit_AslFj.otx (hat realistischere Schicht/Personal-Strukturen) → alle 4 neuen Viewer via Sidebar erreichbar und rendern Daten korrekt.
4. **Komplettheit-Check:** registry.getRegisteredKlasses() enthält alle 12 erwarteten klass-Namen (PSimulator/ASimulator, PDurchlaufplan, PGObjBase, RESS_BELEG_GROUP, RESS_MENGE_GROUP, RESS_VERKNUEPFUNG_GROUP, DLPL_BETRIEBSMITTEL_GROUP, DLPL_PERSONAL_GROUP, AEinsatzWunsch, AKapBed, AGruppe + PDurchlaufplan-Design als Tab).
</verification>

<success_criteria>
- 12 von 12 Viewern aus D-08 implementiert (Plan 05+06+07+08).
- Read-only-Hinweise klar für die Verknüpfungs-Viewer (die Engine-Erweiterung benötigen).
- Workspace ist mit Fertigungsstruktur1_mit_AslFj.otx voll navigierbar.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/01-vertical-slice/01-08-SUMMARY.md` mit:
- tech_stack: keine neuen Deps (reuse MatrixGrid + reactflow)
- decisions: Verknüpfungs-Viewer (PDlplBetriebsmittel/PDlplPersonal) sind in Phase 1 read-only (Engine-Schema-Erweiterung wartet auf Phase 2)
- patterns: synthetische Folder-Klassen für Cross-Tree-Editoren (analog Plan 06); schicht-helpers für AEinsatzWunsch-Wochen-Raster
- provides: PDlplBetriebsmittelViewer, PDlplPersonalViewer, AEinsatzWunschViewer, AKapBedViewer, schicht-helpers
- affects: portal/src/viewers/linking, portal/src/viewers/arbeitszeit, portal/src/viewers/property/type-maps, portal/src/components/sidebar-tree
</output>

<risks>
- **Verknüpfungs-Edit ist Engine-blockiert:** PAssozBelegLink im _SKIP-Set des otx_loaders. Mitigation: Phase-1-Viewer ist read-only, Hinweis-Banner, Phase-2-Backlog-Item dokumentiert.
- **AEinsatzWunsch-Schicht-Modell ist komplex:** Originale OSim2004-Schicht-Strukturen sind detaillierter (PEinsatzzeit, ATagesEinsatzzeit, PPauseZyklus). Phase 1 macht "happy-path" mit Wochen-Stunden-Raster; komplexe Pause-/Schicht-Wechsel-Editierung kommt später.
- **AKapBed ist computed, kein storage:** die KapBed-Werte entstehen aus dem Modell durch Berechnung. Phase 1 macht read-only ohne Berechnungs-Trigger (die echte Berechnung läuft sonst im Sim-Lauf, der in Phase 1 nicht passiert). Optional: einfache JS-Berechnung clientseitig aus den vorhandenen Daten. Discretion: optional, nur wenn schnell umsetzbar.
</risks>
