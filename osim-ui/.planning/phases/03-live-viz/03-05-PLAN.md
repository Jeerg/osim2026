---
phase: 03-live-viz
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-03
files_modified:
  - portal/src/graph/osim/animation/status-color.ts
  - portal/src/graph/osim/animation/index.ts
  - portal/src/viewers/PDurchlaufplanViewerDesign.tsx
  - portal/src/viewers/PRessBelegMatrixViewer.tsx
  - portal/src/viewers/AEinsatzWunschViewer.tsx
  - portal/src/pages/RunsLivePage.tsx
  - portal/src/graph/osim/animation/__tests__/status-color.test.ts
  - portal/src/viewers/__tests__/PDurchlaufplanViewerDesign.animation.test.tsx
autonomous: true
requirements:
  - LV-02  # Knoten färben sich live während Sim (<500 ms Latenz)

must_haves:
  truths:
    - "status-color.ts mappt Engine-Topics auf {howMuchRed:0..1, optionalColor?:string}."
    - "PDurchlaufplanViewerDesign (Phase 1) wird um eine `liveRouter`-Prop erweitert; bei mount registriert es alle Knoten der View bei dem Router."
    - "PRessBelegMatrixViewer-Zellen bekommen Heatmap-Overlay basierend auf Maschinenbelegung-Events (`proz.bearbeit.start/ende`)."
    - "AEinsatzWunschViewer-Schicht-Blöcke färben sich nach Aktivitätsstatus (aktiv/inaktiv) aus `ptk.*`-Events."
    - "RunsLivePage wired den PDurchlaufplanViewerDesign in den Graph-Slot und übergibt den live-Router (aus useLiveRun)."
    - "Animation-Mapping ist deklarativ (Lookup-Tabelle) und für Plan 04 (Cloud) / Plan 05 (Reports) wiederverwendbar."
  artifacts:
    - path: portal/src/graph/osim/animation/status-color.ts
      provides: "Topic→Animation-State-Mapping"
    - path: portal/src/viewers/PDurchlaufplanViewerDesign.tsx
      provides: "Phase-1-Viewer mit Live-Hook erweitert (rückwärtskompatibel)"
    - path: portal/src/viewers/PRessBelegMatrixViewer.tsx
      provides: "Phase-1-Viewer mit Heatmap-Live-Overlay erweitert"
    - path: portal/src/viewers/AEinsatzWunschViewer.tsx
      provides: "Phase-1-Viewer mit Schicht-Status-Animation erweitert"
  key_links:
    - from: portal/src/graph/osim/animation/status-color.ts
      to: portal/src/graph/core/GObject.ts
      via: "applyToGObject(gobj, topic, data) setzt gobj.howMuchRed und gobj.animate"
      pattern: "howMuchRed|animate"
    - from: portal/src/viewers/PDurchlaufplanViewerDesign.tsx
      to: portal/src/graph/live/event-router.ts
      via: "useEffect: graphView.forEach(node => liveRouter.registerNode(node.id, node))"
      pattern: "registerNode"
    - from: portal/src/pages/RunsLivePage.tsx
      to: portal/src/viewers/PDurchlaufplanViewerDesign.tsx
      via: "<PDurchlaufplanViewerDesign modelId={...} liveRouter={router} readOnly />"
      pattern: "PDurchlaufplanViewerDesign"

user_setup: []
---

<objective>
Bestehende Phase-1-Viewer (PDurchlaufplanViewer-Design, PRessBelegMatrixViewer, AEinsatzWunschViewer) bekommen Live-Animation-Hooks und werden in die RunsLivePage eingebunden.

Purpose: Phase 1 hat die Viewer rein für Modellierung gebaut. Phase 3 hängt sich über eine `liveRouter`-Prop an, ohne die Phase-1-Funktionalität (Edit, Properties, Sidebar-Nav) zu brechen. Read-Only-Modus während Live-View, Edit-Modus weiterhin verfügbar.

Output: Animation-Lookup + 3 Viewer-Erweiterungen + Page-Wiring + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live-viz/03-PRELIMINARY-PLAN.md
@.planning/phases/03-live-viz/03-01-PLAN.md
@.planning/phases/03-live-viz/03-01-SUMMARY.md
@.planning/phases/03-live-viz/03-03-PLAN.md
@.planning/phases/03-live-viz/03-03-SUMMARY.md
@.planning/phases/03-live-viz/03-04-PLAN.md
@.planning/phases/03-live-viz/03-04-SUMMARY.md
@.planning/phases/01-vertical-slice/01-CONTEXT.md
@.planning/research/osim2004-ui-analysis.md

<interfaces>
<!-- Aus Phase 1 (01-CONTEXT.md D-08): existing Viewer-Komponenten -->
// PDurchlaufplanViewerDesign props (Phase 1):
//   { modelId: string; readOnly?: boolean; onChange?: (model)=>void }
// PRessBelegMatrixViewer props:
//   { modelId: string; readOnly?: boolean }
// AEinsatzWunschViewer props:
//   { modelId: string; readOnly?: boolean }

<!-- Phase-3-Erweiterung (additive Prop, optional): -->
type LiveAnimationProps = {
  liveRouter?: EventRouter; // aus Plan 03-03
};

<!-- Aus Plan 03-01: GObject.onSimEvent / GObject.animate / GObject.howMuchRed -->
<!-- Aus osim-engine-api.md §5.1: Standard-Topics -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: status-color Lookup + Tests</name>
  <files>
    portal/src/graph/osim/animation/status-color.ts,
    portal/src/graph/osim/animation/index.ts,
    portal/src/graph/osim/animation/__tests__/status-color.test.ts
  </files>
  <behavior>
    - `applyTopicToNode(gobj, topic, data)`:
      • `proz.bearbeit.start` → gobj.howMuchRed=1, gobj.animate=true
      • `proz.bearbeit.ende` → gobj.howMuchRed=0
      • `plan.ausloesen` → gobj.howMuchRed=0.4 (sanftes Anzeigen aktiver Pläne)
      • `plan.beendet` → gobj.howMuchRed=0
      • `sim.period.*` → keine Knoten-Animation (Periode-Event ist global)
      • unbekanntes topic → no-op
    - `cellHeatmapForUtilization(busyPercent)` → CSS-color-string (gleiche 3-Stufen-Logik wie MachineUtilizationChart aus Plan 03-04: grün/gelb/rot).
    - `shiftStatusForActivity(active: boolean)` → {opacity:1|0.4, borderColor:'#0a0'|'#999'}.
  </behavior>
  <action>
    `status-color.ts`: drei pure Funktionen, keine React-Imports. Lookup-Table als const-Map. `applyTopicToNode` ist die zentrale Entry-Funktion, die der EventRouter aus Plan 03-03 effektiv triggert (via gobj.onSimEvent → konkrete Knoten-Subklasse ruft `applyTopicToNode(this, topic, data)`).

    Konvention: Knoten-Subklassen aus Plan 03-01 Task 2 erhalten in ihrer `onSimEvent`-Methode genau einen Aufruf: `applyTopicToNode(this, topic, data); this.parentCollection?.getView()?.invalidateView();`. Diese Konvention dokumentieren in JSDoc auf `applyTopicToNode`.

    Tests prüfen alle 5 Topic-Pfade + Edge-Case "unknown topic" + cellHeatmap-Grenzen (0.69→grün, 0.71→gelb, 0.89→gelb, 0.91→rot).
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx vitest run src/graph/osim/animation --reporter=dot</automated>
  </verify>
  <done>
    Tests grün; Lookup ist deterministisch und seiteneffektfrei (außer der gewollten gobj-Mutation in applyTopicToNode).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDurchlaufplanViewerDesign + PRessBelegMatrixViewer + AEinsatzWunschViewer um liveRouter erweitern</name>
  <files>
    portal/src/viewers/PDurchlaufplanViewerDesign.tsx,
    portal/src/viewers/PRessBelegMatrixViewer.tsx,
    portal/src/viewers/AEinsatzWunschViewer.tsx,
    portal/src/viewers/__tests__/PDurchlaufplanViewerDesign.animation.test.tsx
  </files>
  <behavior>
    - PDurchlaufplanViewerDesign akzeptiert neue optionale Prop `liveRouter?: EventRouter`. Bei mount mit liveRouter: useEffect registriert alle Knoten der aktuellen GraphView per `router.registerNode(node.id, node)`. Bei unmount: unregisterAll.
    - PRessBelegMatrixViewer akzeptiert optional `liveKpiAggregator?: KpiAggregator`. Aggregator-snapshot bringt `machineUtilization`-Map → Zellen-Hintergrund per `cellHeatmapForUtilization(busyPercent)`.
    - AEinsatzWunschViewer akzeptiert optional `liveRouter?: EventRouter`. Schicht-Blöcke registrieren sich (analog Durchlaufplan) und reagieren auf `ptk.*`-Events.
    - **Rückwärtskompatibilität:** Ohne die neuen Props sind die Viewer Phase-1-identisch (Edit-Mode funktioniert weiter).
  </behavior>
  <action>
    Falls die Phase-1-Viewer-Dateien noch nicht existieren (Phase 1 noch nicht implementiert): erzeuge minimale Stub-Implementation mit dem in 01-CONTEXT.md D-05/D-08 definierten Pattern (ClientCtrl + ChildDialog), die die nötige API für Live-Wiring bereitstellt. Markiere mit `// TODO(phase-1-followup): full implementation per 01-CONTEXT.md D-08` an den noch nicht implementierten Stellen.

    Falls Phase 1 die Viewer geliefert hat: rein additive Edit (neue optional Prop + useEffect-Block + Cleanup). Keine Änderung an Edit-Logik.

    Wichtig: `if (!props.liveRouter) return;` als erste Zeile im useEffect, damit Phase-1-Tests unverändert grün bleiben.

    Test in `PDurchlaufplanViewerDesign.animation.test.tsx`: rendere Viewer mit Stub-Modell (3 Knoten), übergebe einen Mock-Router; assert `router.registerNode` wurde 3-mal aufgerufen; trigger `router.routeEvent({topic:'proz.bearbeit.start', data:{node_id:'n1'}, sim_time:100})`; assert dass die DOM-Repräsentation von n1 die rote Animation-Klasse oder das gewünschte data-howmuchred-Attribut zeigt.
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx vitest run src/viewers/__tests__/PDurchlaufplanViewerDesign.animation.test.tsx --reporter=dot</automated>
  </verify>
  <done>
    Animation-Test grün; bestehende Phase-1-Viewer-Tests (falls vorhanden) bleiben grün (rückwärtskompatibel).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RunsLivePage wired die Viewer + Final-Integration</name>
  <files>
    portal/src/pages/RunsLivePage.tsx
  </files>
  <behavior>
    - In `RunsLivePage` wird der Graph-Slot mit `<PDurchlaufplanViewerDesign modelId={modelId} readOnly liveRouter={router} />` befüllt.
    - Aggregator wird zusätzlich an `<PRessBelegMatrixViewer modelId={modelId} readOnly liveKpiAggregator={aggregator} />` als ausklappbares Sub-Panel weitergegeben (Toggle-Button "Ressourcen-Matrix anzeigen").
    - AEinsatzWunschViewer ist analog togglebar.
    - Toggle-State lebt in URL-Search-Param `?panels=matrix,shifts` (TanStack-Router-Pattern).
    - modelId wird aus dem run-Detail abgerufen (REST-Call beim Page-Mount via `getRun(runId).then(r => setModelId(r.modelId))`).
  </behavior>
  <action>
    Ergänze RunsLivePage aus Plan 03-04 um den Graph-Slot-Inhalt und die zwei optionalen Toggle-Panels. router/aggregator kommen aus dem useLiveRun-Hook (bereits in Plan 03-04 instanziert). Falls useLiveRun-Returntyp router/aggregator NICHT exposed: Plan 03-03 hat sie als interne Refs; hier ggf. via useMemo neu erzeugen und beim Hook-Init mitgeben.

    Test ist nicht zwingend nötig (Visual Check folgt in Plan 03-06 E2E). Wenn ein Test ergänzt wird: render RunsLivePage mit gemocktem useLiveRun, assert dass PDurchlaufplanViewerDesign mit `liveRouter`-Prop !== undefined gerendert wurde.
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    tsc-noEmit fehlerfrei; URL-Toggle funktioniert (manuell prüfbar in Plan 03-06).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kein neuer Boundary in diesem Plan; alle Live-Daten kommen aus Plan 03-02/03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering | EventRouter erhält manipuliertes node_id und ruft auf Knoten X statt Knoten Y | accept | Mapping ist read-only nach Mount; Backend liefert Tenant-isolierte Events (Plan 02 T-03-05). |
| T-03-16 | DoS | 1000 Knoten registrieren gleichzeitig | accept | O(n) bei mount, einmalig; n typisch <100. Stress-Test in Plan 03-06 mit 50 Knoten (Akzeptanzkriterium 10). |
</threat_model>

<verification>
- `cd portal && npx vitest run src/viewers src/graph/osim/animation --reporter=dot` — grün
- Visual Check folgt in Plan 03-06 (E2E)
</verification>

<success_criteria>
1. PDurchlaufplanViewerDesign hat `liveRouter`-Prop und reagiert auf Engine-Events.
2. PRessBelegMatrixViewer-Heatmap wird vom KpiAggregator gefüttert.
3. AEinsatzWunschViewer-Schicht-Status reagiert auf `ptk.*`-Events.
4. Alle drei Viewer bleiben rückwärtskompatibel mit Phase 1 (ohne neue Props → Edit-Mode).
5. RunsLivePage rendert die Live-Animations-Pipeline end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-viz/03-05-SUMMARY.md`
</output>
