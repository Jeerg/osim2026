---
phase: 03-live-viz
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - portal/src/pages/RunsLivePage.tsx
  - portal/src/pages/runs/RunStatusBanner.tsx
  - portal/src/pages/runs/RunControlPanel.tsx
  - portal/src/components/charts/SimTimeChart.tsx
  - portal/src/components/charts/CompletedPlansChart.tsx
  - portal/src/components/charts/MachineUtilizationChart.tsx
  - portal/src/components/charts/index.ts
  - portal/src/router/routes.tsx
  - portal/src/api/runs.ts
  - portal/src/pages/__tests__/RunsLivePage.test.tsx
  - portal/package.json
autonomous: false
requirements:
  - LV-05  # Live-KPI-Charts mit <1 s Update

must_haves:
  truths:
    - "Route `/runs/:runId/live` ist im TanStack-Router registriert und rendert RunsLivePage."
    - "Layout: links Graph (Plan 03-05 wired die Animationen ein), rechts KPI-Charts und Status-Banner (Split-Layout)."
    - "Drei Recharts-Charts zeigen Sim-Zeit (LineChart), abgeschlossene Pläne (LineChart), Maschinen-Auslastung (horizontaler BarChart mit Top-N Ressourcen)."
    - "RunStatusBanner zeigt queued/running/succeeded/failed/cancelled mit eindeutigen Farben und Icons."
    - "RunControlPanel hat Cancel-Button, der POST /api/v1/runs/{run_id}/cancel ruft; nur sichtbar bei status='running' oder 'queued'."
    - "Charts updaten <1s nach Event (siehe Akzeptanzkriterium 8 + LV-05)."
    - "Bei Disconnected-Status zeigt Banner einen 'Verbindung wird wiederhergestellt'-Indikator."
  artifacts:
    - path: portal/src/pages/RunsLivePage.tsx
      provides: "Live-Run-Seite mit Split-Layout"
      min_lines: 80
    - path: portal/src/components/charts/SimTimeChart.tsx
      provides: "Recharts LineChart für Sim-Zeit über Wall-Time"
    - path: portal/src/components/charts/CompletedPlansChart.tsx
      provides: "Recharts LineChart für completedPlans über Sim-Zeit"
    - path: portal/src/components/charts/MachineUtilizationChart.tsx
      provides: "Recharts horizontaler BarChart, Top-N Ressourcen nach Auslastung"
    - path: portal/src/pages/runs/RunStatusBanner.tsx
      provides: "Status-Banner-Component"
    - path: portal/src/pages/runs/RunControlPanel.tsx
      provides: "Cancel-Button + (Phase-4-Backlog) Pause/Resume"
    - path: portal/src/api/runs.ts
      provides: "cancelRun(runId), getRun(runId) — REST-Client-Funktionen"
  key_links:
    - from: portal/src/pages/RunsLivePage.tsx
      to: portal/src/graph/live/useLiveRun.ts
      via: "const {status, kpis} = useLiveRun(runId, {...})"
      pattern: "useLiveRun"
    - from: portal/src/pages/runs/RunControlPanel.tsx
      to: portal/src/api/runs.ts
      via: "await cancelRun(runId)"
      pattern: "cancelRun"
    - from: portal/src/router/routes.tsx
      to: portal/src/pages/RunsLivePage.tsx
      via: "createRoute({path:'/runs/$runId/live', component: RunsLivePage})"
      pattern: "/runs/\\$runId/live"

user_setup: []
---

<objective>
Live-Run-Seite mit Split-Layout, Recharts-KPI-Dashboard, Status-Banner und Cancel-Steuerung.

Purpose: Konsument der Plan-03-03-Hooks. Plan 03-05 hängt sich später in den Graph-Bereich ein. Hier wird der React-State sichtbar gemacht.

Output: Page + 3 Charts + Banner + Control-Panel + Route + Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live-viz/03-PRELIMINARY-PLAN.md
@.planning/phases/03-live-viz/03-03-PLAN.md
@.planning/phases/03-live-viz/03-03-SUMMARY.md
@.planning/research/osim2004-ui-analysis.md

<interfaces>
<!-- Aus Plan 03-03 (useLiveRun): -->
type UseLiveRunResult = {
  status: 'idle'|'connecting'|'connected'|'reconnecting'|'disconnected'|'failed';
  runStatus: 'queued'|'running'|'succeeded'|'failed'|'cancelled' | null;
  simTime: number | null;
  kpis: { completedPlans: number; machineUtilization: Array<{id:string;name:string;busyPercent:number}> } | null;
  errorMsg: string | null;
};

<!-- Recharts (bereits in 3fls-Pattern verwendet — Lib-Wahl bestätigt in 03-PRELIMINARY-PLAN §1) -->
import { LineChart, Line, BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';

<!-- shadcn/ui (aus Phase 1 D-discretion: shadcn übernommen): -->
import { Card } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API-Client + Charts + Banner + Control-Panel</name>
  <files>
    portal/src/api/runs.ts,
    portal/src/components/charts/SimTimeChart.tsx,
    portal/src/components/charts/CompletedPlansChart.tsx,
    portal/src/components/charts/MachineUtilizationChart.tsx,
    portal/src/components/charts/index.ts,
    portal/src/pages/runs/RunStatusBanner.tsx,
    portal/src/pages/runs/RunControlPanel.tsx,
    portal/package.json
  </files>
  <behavior>
    - api/runs.ts exportiert `cancelRun(runId)` (POST /api/v1/runs/{id}/cancel mit Bearer-Token-Header) und `getRun(runId)`.
    - SimTimeChart erhält `data: Array<{wallTime:number, simTime:number}>` und rendert Recharts-LineChart mit ResponsiveContainer (volle Card-Breite, 200px Höhe).
    - CompletedPlansChart erhält `data: Array<{simTime:number, completed:number}>` und rendert Treppen-Linie (Recharts step-Type).
    - MachineUtilizationChart erhält `data: Array<{id,name,busyPercent}>` (Top-10), rendert horizontalen BarChart, Bar-Farbe via `busyPercent` (grün <70%, gelb 70-90%, rot >90%).
    - RunStatusBanner({status, runStatus, errorMsg}) zeigt 5 distinct visual states; bei status='reconnecting' Spinner+Text "Verbindung wird wiederhergestellt …".
    - RunControlPanel({runId, runStatus}) zeigt Cancel-Button nur bei runStatus ∈ {queued, running}; bei click → confirm-dialog → cancelRun.
  </behavior>
  <action>
    Falls recharts noch nicht im `package.json`: `npm install recharts` ergänzen (Lizenz MIT, kein Pro-Feature). Falls Phase 1 bereits installiert: kein Eintrag nötig.

    Charts als reine Presentation-Components (kein eigener Fetch). Tests mit `@testing-library/react`: Component rendert ohne crash; Top-10-Truncation funktioniert; Farb-Logik korrekt.

    Cancel-Button verwendet shadcn-AlertDialog für Bestätigung (analog 3fls-Convention).

    api/runs.ts nutzt den in Phase 1 etablierten API-Client (axios oder fetch mit Auth-Interceptor — vorhandenes Pattern aus `@/api/client` verwenden).
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx vitest run src/components/charts src/pages/runs --reporter=dot</automated>
  </verify>
  <done>
    Component-Tests grün; tsc-noEmit fehlerfrei; recharts in package.json gelistet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunsLivePage + Routing + Integration-Test</name>
  <files>
    portal/src/pages/RunsLivePage.tsx,
    portal/src/router/routes.tsx,
    portal/src/pages/__tests__/RunsLivePage.test.tsx
  </files>
  <behavior>
    - Page liest runId aus URL-Params (TanStack-Router).
    - Ruft `useLiveRun(runId, {token: await user.getIdToken(), router, aggregator})`.
    - Layout: CSS-Grid mit 2 Spalten (60%/40% auf md+, gestackt auf sm). Links: Placeholder-Slot mit data-testid="graph-slot" (Plan 03-05 wired den echten Graph ein). Rechts: vertikaler Stack aus RunStatusBanner, 3 Chart-Cards, RunControlPanel.
    - Wenn runId leer / unauthenticated → Redirect auf /runs.
    - Cleanup beim unmount (über Hook).
  </behavior>
  <action>
    `RunsLivePage.tsx`: Component mit useParams. Token via Firebase-Auth-Hook aus Phase 1 (`useFirebaseUser()` o.ä.). Erzeuge router/aggregator als useMemo. Layout mit Tailwind-CSS-Grid (3fls-Stil).

    `router/routes.tsx`: Ergänze Route `/runs/:runId/live` → RunsLivePage. Falls die in Phase 1 etablierte Router-Datei woanders liegt (z.B. `portal/src/main.tsx`), dort ergänzen.

    Integration-Test mit MSW (Mock Service Worker) oder vi.mock für WebSocket: render Page mit runId='test', simuliere onmessage-Event mit `{topic:'plan.beendet', data:{plan_id:'x'}, sim_time:100}`, assert dass CompletedPlansChart einen Datenpunkt hat (data-testid='completed-chart').
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx vitest run src/pages/__tests__/RunsLivePage.test.tsx --reporter=dot</automated>
  </verify>
  <done>
    Page-Test grün; Route in routes.tsx registriert; Graph-Slot via data-testid für Plan 03-05 verfügbar.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visueller Check des Live-Dashboards</name>
  <what-built>RunsLivePage mit Split-Layout, 3 Charts, Status-Banner, Cancel-Button.</what-built>
  <how-to-verify>
    1. `docker compose up postgres firebase-emulator minio`
    2. `uv run uvicorn app.main:app --reload` (Backend)
    3. `cd portal && npm run dev` (Frontend)
    4. Im Browser: einloggen, ein Modell mit einem Sim-Lauf starten (Phase 2 / oder Test-Stub-Run via `curl POST /api/v1/runs` mit dem Dummy.otx-Modell aus `OSim2004/Vorstellung04/Dummy.otx`).
    5. Auf `/runs/{runId}/live` navigieren.
    6. Verifiziere:
       - Split-Layout ist sichtbar (links Graph-Slot, rechts Charts)
       - Status-Banner zeigt korrekten Status (queued → running → succeeded)
       - SimTimeChart-Linie steigt monoton
       - CompletedPlansChart zeigt Treppenfunktion
       - MachineUtilizationChart sortiert nach busyPercent absteigend
       - Bei `docker stop` des Backends: Banner zeigt "Verbindung wird wiederhergestellt" innerhalb 3s
       - Cancel-Button bei laufendem Run sichtbar; Klick öffnet Bestätigungsdialog; Bestätigung beendet den Run (status → cancelled)
    7. Layout auf 1024×768 und 375×667 prüfen (Stacking funktioniert).
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cancel-Button → POST /cancel | Browser-Request; muss Auth-Token mitschicken und Tenant-Ownership prüfen (Backend) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Elevation of Privilege | User-A cancelt Run von User-B | mitigate | Backend (Phase-3-Plan-02 RunRegistry) prüft tenant_id; Frontend cancelRun-Aufruf landet beim Backend-Endpoint mit Tenant-Check. Test in Backend-Suite (Plan 02 T-03-05). |
| T-03-14 | DoS | User klickt Cancel-Spam | accept | Browser-seitige Disable-after-Click; Backend ist idempotent (cancel auf bereits-cancelled = no-op). |
</threat_model>

<verification>
- `cd portal && npx vitest run src/pages src/components/charts --reporter=dot` — grün
- Manueller Visual Check (Task 3 Checkpoint)
- Lighthouse-Run (informativ): erste sichtbare Charts <2s nach Page-Load
</verification>

<success_criteria>
1. RunsLivePage rendert in Split-Layout mit allen 3 Charts.
2. Status-Banner reagiert auf alle 5 runStatus-Werte und auf reconnecting.
3. Cancel-Button funktioniert end-to-end (gegen echtes Backend in Task 3).
4. Charts updaten <1s nach Event (visuell verifiziert in Checkpoint, technisch in Plan 03-03 garantiert).
5. Route `/runs/$runId/live` ist im Router-Tree.
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-viz/03-04-SUMMARY.md`
</output>
