---
phase: 03-live-viz
plan: 06
type: execute
wave: 4
depends_on:
  - 03-04
  - 03-05
files_modified:
  - portal/e2e/live-run.spec.ts
  - portal/e2e/live-reconnect.spec.ts
  - portal/perf/graph-fps.bench.ts
  - portal/perf/run-fps.mjs
  - tests/integration/test_live_e2e.py
  - tests/integration/test_event_latency.py
  - docs/PHASE-3-GRAPH-OBJECT.md
  - portal/playwright.config.ts
autonomous: false
requirements:
  - LV-02  # <500 ms Event-Latency
  - LV-05  # KPI-Charts <1 s Update
  - LV-06  # Performance 30 FPS @ 50 Knoten
  - LV-07  # Foundation-Bereitschaft dokumentiert

must_haves:
  truths:
    - "Playwright-E2E `live-run.spec.ts` durchläuft den vollen Flow: Login → Run starten → /runs/{id}/live → Animation sichtbar → Status succeeded → Charts haben Datenpunkte."
    - "Playwright-E2E `live-reconnect.spec.ts` simuliert WebSocket-Trennung und prüft Reconnect ≤3s (Akzeptanzkriterium 9)."
    - "Backend-Integration-Test misst Event-Latency: Engine emit → WebSocket-Empfang < 500ms (Akzeptanzkriterium 2)."
    - "Performance-Benchmark misst Frame-Rate des PDurchlaufplanViewerDesign mit 50 Knoten und gleichzeitiger Live-Animation → ≥30 FPS (Akzeptanzkriterium 10, LV-06)."
    - "`docs/PHASE-3-GRAPH-OBJECT.md` dokumentiert: GraphObject-Architektur, Live-Pipeline, Performance-Budget, bekannte Risiken (Multiplexing, CheckNeighbourhood-Simplification), Erweiterungs-Punkte für Phase 4/5/7+."
  artifacts:
    - path: portal/e2e/live-run.spec.ts
      provides: "Playwright Happy-Path-Test gegen lokales docker-compose-Setup"
    - path: portal/e2e/live-reconnect.spec.ts
      provides: "Reconnect-Test mit Network-Throttling"
    - path: portal/perf/graph-fps.bench.ts
      provides: "Frame-Rate-Benchmark via requestAnimationFrame-Timestamps"
    - path: tests/integration/test_event_latency.py
      provides: "End-to-end event latency measurement (Engine → WS → assertion)"
    - path: docs/PHASE-3-GRAPH-OBJECT.md
      provides: "Architektur-Dokumentation der GraphObject-Schicht + Live-Pipeline"
      min_lines: 150
  key_links:
    - from: portal/e2e/live-run.spec.ts
      to: portal/src/pages/RunsLivePage.tsx
      via: "page.goto('/runs/' + runId + '/live') und assert auf data-testid='completed-chart'"
      pattern: "/runs/.*live"
    - from: tests/integration/test_event_latency.py
      to: app/api/v1/runs_live.py
      via: "websockets.connect + timestamp comparison"
      pattern: "ws_connect|websockets"
    - from: docs/PHASE-3-GRAPH-OBJECT.md
      to: portal/src/graph/core/GObject.ts
      via: "Doku referenziert die Code-Module"
      pattern: "graph/core|graph/live"

user_setup: []
---

<objective>
Phase 3 schließen mit E2E-Tests, Latenz-Messung, Performance-Benchmark und Architektur-Dokumentation.

Purpose: Akzeptanzkriterien 2 (<500ms Latency), 8 (<1s KPI), 9 (Reconnect ≤3s), 10 (30 FPS @ 50 Knoten) sind sonst nur indirekt geprüft. Foundation-Doku ist Pflicht für Phase 5/7+ Konsumenten.

Output: 2 Playwright-Specs + 1 Bench + 1 Backend-Integration-Test + 1 Doku-File.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live-viz/03-PRELIMINARY-PLAN.md
@.planning/phases/03-live-viz/03-01-SUMMARY.md
@.planning/phases/03-live-viz/03-02-SUMMARY.md
@.planning/phases/03-live-viz/03-03-SUMMARY.md
@.planning/phases/03-live-viz/03-04-SUMMARY.md
@.planning/phases/03-live-viz/03-05-SUMMARY.md
@.planning/phases/01-vertical-slice/01-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Test-Modell: Fertigungsstruktur1_mit_AslFj.otx (272 KB, ~50 Knoten, mittelgroß) -->
<!--   Pfad: C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\Vorstellung04\Fertigungsstruktur1_mit_AslFj.otx -->
<!--   In CI als Fixture in tests/fixtures/Fertigungsstruktur1.otx kopiert. -->

<!-- Smoke-Modell: Dummy.otx (228 KB, kleinstes funktionsfähiges) -->
<!--   Pfad: C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\Vorstellung04\Dummy.otx -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend-Integration-Test für Event-Latency + Playwright-E2E-Specs</name>
  <files>
    tests/integration/test_event_latency.py,
    tests/integration/test_live_e2e.py,
    portal/e2e/live-run.spec.ts,
    portal/e2e/live-reconnect.spec.ts,
    portal/playwright.config.ts,
    tests/fixtures/Fertigungsstruktur1.otx,
    tests/fixtures/Dummy.otx
  </files>
  <behavior>
    - tests/integration/test_event_latency.py: startet einen echten kleinen Sim-Lauf (Dummy.otx), öffnet WebSocket, misst Zeit zwischen Worker-Emit-Timestamp (im Event als `sim_time` ODER `wall_time` falls vom Wrapper hinzugefügt) und WS-Receive-Timestamp; assert <500ms (lokaler Run, kein Netzwerk).
    - portal/e2e/live-run.spec.ts: startet Backend+Frontend (via webServer-Konfig oder voraus laufendes docker compose), authentifiziert via Firebase-Emulator-Test-User, lädt Dummy.otx hoch, startet Run, navigiert auf /runs/{id}/live, wartet auf data-testid='run-status' mit Text 'succeeded' (timeout 60s), prüft dass `data-testid='completed-chart'` mindestens 1 Datenpunkt hat.
    - portal/e2e/live-reconnect.spec.ts: gleicher Setup, aber via `await context.setOffline(true)` mid-run; assert Banner-Text "Verbindung wird wiederhergestellt" erscheint innerhalb 3s; setOffline(false); assert Banner verschwindet innerhalb 3s.
    - tests/integration/test_live_e2e.py: pytest-Variante des Live-Flows ohne UI (rein API+WS), als CI-Fallback falls Playwright in CI flaky.
  </behavior>
  <action>
    Fixtures: Kopiere die zwei OTX-Files aus OSim2004 nach `tests/fixtures/`. Wenn die Files nicht in den Repo gehören (Größe / Lizenz), per Symlink oder pytest-Fixture-Download-Hook lösen — Sebastian-Style: `if not path.exists(): pytest.skip('OTX fixture missing — copy from OSim2004/Vorstellung04/')`.

    Playwright-Config: setze `webServer` auf `cd .. && uv run uvicorn app.main:app` UND `npm run dev`, mit Health-Check-Wait. Falls Setup-Komplexität zu groß, schreibe README-Hinweis und führe Playwright manuell aus.

    Latency-Test: Worker-Wrapper aus Plan 03-02 ergänzt im JsonlStdoutSink jedes Event um `wall_time: time.time()`. Test berechnet `receive_time - wall_time`. Median <500ms über 10 Events.

    test_live_e2e.py: nutzt httpx + websockets-Library, simuliert Login (Firebase-Emulator-Custom-Token), POST /api/v1/runs, WS-Connect, lese Events bis status=succeeded.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_event_latency.py tests/integration/test_live_e2e.py -x -q</automated>
  </verify>
  <done>
    Latency-Test grün (median <500ms); test_live_e2e.py durchläuft Happy-Path; Playwright-Specs sind ausführbar (manuell oder in CI).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Performance-Benchmark Frame-Rate</name>
  <files>
    portal/perf/graph-fps.bench.ts,
    portal/perf/run-fps.mjs
  </files>
  <behavior>
    - Benchmark erzeugt programmatisch eine GraphView mit 50 Knoten und 70 Kanten (typische Topologie aus Fertigungsstruktur1).
    - Triggert 1000 simulierte `proz.bearbeit.start/ende`-Events pro Sekunde über 10 Sekunden.
    - Misst Frame-Rate via `performance.now()`-Deltas zwischen requestAnimationFrame-Callbacks.
    - Bench-Output: median FPS, p10 FPS, max paint-Lag.
    - Akzeptanz: median FPS ≥30, p10 ≥20.
  </behavior>
  <action>
    `graph-fps.bench.ts`: Vitest-bench oder eigenständige Headless-Browser-Bench (Playwright in headless-mode mit Performance-API). Empfehlung: Playwright-basiert (echtes Browser-Rendering), `portal/perf/run-fps.mjs` als Node-Runner der den Playwright-Test startet und JSON-Result schreibt.

    Falls Playwright zu schwer: jsdom-basierte Vitest-bench mit gemocktem requestAnimationFrame (16ms-Tick) — weniger realistisch, aber CI-tauglich.

    Doku in `portal/perf/README.md` (inline im run-fps.mjs als Kommentar): "Run via `node portal/perf/run-fps.mjs > perf-result.json`. Akzeptanz-Grenze: median ≥30 FPS."
  </action>
  <verify>
    <automated>cd portal &amp;&amp; node perf/run-fps.mjs</automated>
  </verify>
  <done>
    Bench läuft fehlerfrei; median FPS ≥30 in lokalem Run; Output-JSON dokumentiert Werte für CI-Vergleich.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Architektur-Dokumentation</name>
  <files>
    docs/PHASE-3-GRAPH-OBJECT.md
  </files>
  <behavior>
    - Doku enthält 5 Abschnitte: (1) Übersicht & Architektur-Diagramm (ASCII), (2) GraphObject-API mit Beispiel-Subklasse, (3) Live-Pipeline (Worker → Bridge → LiveBus → WS → Hook → Router → GObject), (4) Performance-Budget mit Mess-Methodik, (5) Erweiterungs-Punkte für Phase 4 (Pub/Sub), Phase 5 (Reports), Phase 7+ (Matrix/Gantt) inkl. konkreter API-Referenzen.
    - Bekannte Risiken (aus 03-PRELIMINARY-PLAN.md §6) sind dokumentiert: in-Process-Multiplexing-Grenze, CheckNeighbourhood-Vereinfachung (falls vorgenommen), GLDirection-Vollständigkeit.
    - JWT-im-Query-Param-Sicherheitshinweis (siehe T-03-07 in Plan 03-02).
  </behavior>
  <action>
    Schreibe `docs/PHASE-3-GRAPH-OBJECT.md` als Markdown. ASCII-Diagramm für Live-Pipeline:
    ```
    Engine (PSimulator) ─emit→ EventBus ─sink→ stdout(JSONL)
                                                  │
                                          (Worker-Subprocess)
                                                  ▼
                                         sim_event_bridge.py
                                                  │
                                                  ▼
                                            live_bus.py  ── WS broadcast ──→ Browser
                                                                                 │
                                                                                 ▼
                                                                          ws-client.ts
                                                                                 │
                                                                ┌────────────────┼─────────────────┐
                                                                ▼                ▼                 ▼
                                                        event-router.ts   kpi-aggregator   useLiveRun
                                                                │                │                 │
                                                                ▼                ▼                 ▼
                                                        GObject.onSimEvent  Recharts-Data    RunsLivePage
    ```

    Code-Snippets: kleines Beispiel einer Knoten-Subklasse (analog `PDpKnKonstant` aus 03-PRELIMINARY-PLAN §4) und ein Snippet zur Hook-Verwendung.

    Erweiterungs-Punkte:
    - Phase 4: Ersetze `live_bus.py` In-Process-Singleton durch GCP-Pub/Sub-Backed-Bus; WS-Endpoint bleibt identisch.
    - Phase 5: Reports-Charts können `KpiAggregator` direkt nutzen (gleiche Datenpunkte) oder JSONL-Trace aus Phase 1 als Quelle.
    - Phase 7+: Matrix-Viewer (Gantt) bauen weitere GObject-Subklassen, die `GraphGrid`-Operationen aus Plan 03-01 nutzen.
  </action>
  <verify>
    <automated>test -f docs/PHASE-3-GRAPH-OBJECT.md &amp;&amp; wc -l docs/PHASE-3-GRAPH-OBJECT.md | awk '{exit ($1 &gt;= 150) ? 0 : 1}'</automated>
  </verify>
  <done>
    Doku existiert, mindestens 150 Zeilen, enthält ASCII-Diagramm und Erweiterungs-Punkte; in Repo committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase-3-Abnahme — alle 10 Akzeptanzkriterien</name>
  <what-built>Phase 3 komplett: GraphObject-Foundation, Live-Channel, KPI-Dashboard, Animations-Hooks, Tests, Doku.</what-built>
  <how-to-verify>
    Gehe die 10 Akzeptanzkriterien aus `03-PRELIMINARY-PLAN.md §3` manuell durch:

    1. ✅ Sim-Lauf zeigt graphischen Durchlaufplan mit Kanten → öffne /runs/{id}/live mit Fertigungsstruktur1.otx
    2. ✅ Knoten färben sich live, Latency <500ms → `uv run pytest tests/integration/test_event_latency.py` grün
    3. ✅ Multi-Waypoint-Links editierbar → manuell im PDurchlaufplanViewerDesign Edit-Mode (Phase 1 + Plan 03-01)
    4. ✅ Region-Check (Mitte=Edit, Rand=Link) → manuell in Edit-Mode
    5. ✅ GObjSub-Doppelklick öffnet Sub-Plan → manuell mit Modell, das einen `GObjSub` enthält (oder Test-Stub)
    6. ✅ Grid-Layout: Spalten/Zeilen einfügen/entfernen → manuell in Edit-Mode mit Grid-Toggle
    7. ✅ Copy/Paste eines Subgraphen → manuell, prüfe Link-Restore
    8. ✅ Live-KPI-Charts: Sim-Zeit, completed, Auslastung → visuell in RunsLivePage
    9. ✅ WS-Reconnect ≤3s → `cd portal && npx playwright test e2e/live-reconnect.spec.ts` grün
    10. ✅ 30 FPS @ 50 Knoten → `node portal/perf/run-fps.mjs` zeigt median ≥30

    Wenn ein Kriterium NICHT grün: dokumentiere in `.planning/phases/03-live-viz/03-VERIFICATION.md`, ob Gap (→ `/gsd-plan-phase 3 --gaps`) oder akzeptierte Abweichung (mit Begründung).
  </how-to-verify>
  <resume-signal>Type "approved" (alle 10 grün) ODER liste fehlende Kriterien mit Begründung</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI vs. local dev environment | E2E-Tests benötigen Firebase-Emulator + docker compose; in CI ggf. nur Backend-Integration-Test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | DoS | Performance-Bench übersteigt CI-Budget (Headless-Browser ressourcen-intensiv) | accept | run-fps.mjs primär für lokale Ausführung; CI führt nur den jsdom-Fallback aus oder skipped. |
| T-03-18 | Info Disclosure | OTX-Fixtures enthalten Firmen-Daten | mitigate | Nur `Dummy.otx` (228 KB, OSim2004-Beispiel) und `Fertigungsstruktur1_mit_AslFj.otx` (anonymisiertes Lehrmaterial) verwenden — keine Kunden-OTX in `tests/fixtures/`. |
</threat_model>

<verification>
- `uv run pytest tests/integration -x -q` — Backend-Tests grün
- `cd portal && npx playwright test e2e/` — E2E grün (manuell oder CI)
- `node portal/perf/run-fps.mjs` — FPS ≥30
- `docs/PHASE-3-GRAPH-OBJECT.md` ≥150 Zeilen
- Checkpoint Task 4 abgenommen
</verification>

<success_criteria>
1. Alle 10 Akzeptanzkriterien aus 03-PRELIMINARY-PLAN.md §3 grün (Checkpoint).
2. Event-Latency-Test misst <500ms median.
3. Performance-Benchmark zeigt ≥30 FPS @ 50 Knoten.
4. Reconnect-Test zeigt ≤3s.
5. Doku PHASE-3-GRAPH-OBJECT.md erklärt Architektur + Erweiterungs-Punkte.
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-viz/03-06-SUMMARY.md`
</output>
