---
phase: 03-live-viz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portal/src/graph/core/types.ts
  - portal/src/graph/core/GObject.ts
  - portal/src/graph/core/GObjLink.ts
  - portal/src/graph/core/GLink.ts
  - portal/src/graph/core/GLinkPoint.ts
  - portal/src/graph/core/GraphCollection.ts
  - portal/src/graph/core/GraphList.ts
  - portal/src/graph/core/GraphGrid.ts
  - portal/src/graph/core/GraphView.ts
  - portal/src/graph/core/GObjSub.ts
  - portal/src/graph/osim/nodes/index.ts
  - portal/src/graph/osim/nodes/PDpKnKonstant.ts
  - portal/src/graph/osim/nodes/PDpKnZeitvorgabe.ts
  - portal/src/graph/osim/nodes/PDpKnVerteilung.ts
  - portal/src/graph/osim/nodes/PDpKnMenge.ts
  - portal/src/graph/osim/nodes/PDpKnMengeRuesten.ts
  - portal/src/graph/osim/nodes/PDpKnRuecksprung.ts
  - portal/src/graph/osim/nodes/PDpKnRueckKonstant.ts
  - portal/src/graph/osim/nodes/PDpKnRueckVerteilung.ts
  - portal/src/graph/osim/nodes/PDpKnAlternativ.ts
  - portal/src/graph/osim/nodes/PDpKnAlternativTypId.ts
  - portal/src/graph/osim/nodes/PDpKnAlternativeVerteilung.ts
  - portal/src/graph/osim/nodes/PDpKnAlternativLogik.ts
  - portal/src/graph/osim/nodes/PDlplKnoten.ts
  - portal/src/graph/osim/edges/index.ts
  - portal/src/graph/osim/edges/PDlplKante.ts
  - portal/src/graph/osim/edges/PDpKaUebergang.ts
  - portal/src/graph/osim/edges/PDpKaExtern.ts
  - portal/src/graph/osim/edges/PDpKaEntitaet.ts
  - portal/src/graph/osim/edges/PDpKaEntitaetAblage.ts
  - portal/src/graph/persistence/clipboard.ts
  - portal/src/graph/persistence/serializer.ts
  - portal/src/graph/react-flow-adapter/usePhantomDrag.ts
  - portal/src/graph/core/__tests__/GObject.test.ts
  - portal/src/graph/core/__tests__/GObjLink.test.ts
  - portal/src/graph/core/__tests__/GLinkPoint.test.ts
  - portal/src/graph/core/__tests__/GraphGrid.test.ts
  - portal/src/graph/core/__tests__/GObjSub.test.ts
  - portal/src/graph/osim/__tests__/nodes.test.ts
  - portal/src/graph/persistence/__tests__/clipboard.test.ts
autonomous: true
requirements:
  - LV-01  # Durchlaufplan graphisch mit ein-/ausgehenden Kanten
  - LV-03  # Sub-Pläne (GObjSub) via Doppelklick
  - LV-04  # Region-Check (Klick-Mitte=Edit, Rand=Link)
  - LV-07  # Foundation-Bereitschaft für Phase 5/7+

must_haves:
  truths:
    - "Alle 18 GObjType-Klassen (13 Knoten + 5 Kanten) sind als TypeScript-Klassen instanziierbar."
    - "GObjLink hält In-/Out-Link-Listen synchron (add/remove auf einer Seite aktualisiert die Gegenseite)."
    - "GLinkPoint.checkNeighbourhood produziert für jede Kombination aus Vor-/Nach-GLDirection eine deterministische Wegpunkt-Liste (1–6 Punkte)."
    - "GraphGrid unterstützt Insert/Remove von Reihen/Spalten ohne Verlust von Objekten in anderen Reihen/Spalten."
    - "GObjSub kapselt eine eigene GraphView und referenziert sie als kind."
    - "GraphView hält 4 separate Layer (BG, Grid, Links, FG) und liefert getrennte Iterations-API."
    - "Phantom-Drawing speichert/restauriert Rect für Drag-Preview."
    - "Copy/Paste eines Subgraphen (Knoten + Kanten) erzeugt einen identisch verdrahteten Klon."
  artifacts:
    - path: portal/src/graph/core/types.ts
      provides: "GLDirection (18 Werte), GORegion (6 Werte), GObjType (18 Werte), GObjState (4), DrawLayer-Type"
    - path: portal/src/graph/core/GObject.ts
      provides: "Abstrakte Basisklasse mit Draw/HitTest/CheckRegion/Phantom/onSimEvent"
      min_lines: 120
    - path: portal/src/graph/core/GObjLink.ts
      provides: "GObject + In/Out-Link-Listen, getLinkStartPos je GLDirection"
    - path: portal/src/graph/core/GLinkPoint.ts
      provides: "Multi-Waypoint-Routing mit CheckNeighbourhood-Port (bis 6 Punkte)"
      min_lines: 180
    - path: portal/src/graph/core/GraphGrid.ts
      provides: "Row/Col-Operationen: goIns, goRemove, insertColBefore, insertRowBefore, removeCol, removeRow, getNextFreeGridPlace, copyCell"
    - path: portal/src/graph/core/GObjSub.ts
      provides: "Hierarchischer Sub-Container (GObjLink + GraphView)"
    - path: portal/src/graph/persistence/clipboard.ts
      provides: "copyToClipboard / restoreFromClipboard mit RestoreInLink-Logik"
  key_links:
    - from: portal/src/graph/core/GObjLink.ts
      to: portal/src/graph/core/GLink.ts
      via: "addInLink/addOutLink ruft GLink.setPrev/setNext UND umgekehrt; bei remove wird beide Seiten konsistent gehalten"
      pattern: "addInLink|addOutLink|removeInLink|removeOutLink"
    - from: portal/src/graph/core/GLinkPoint.ts
      to: portal/src/graph/core/types.ts
      via: "Switch über GLDirection-Paare bestimmt usedPointNum"
      pattern: "checkNeighbourhood|GLDirection\\."
    - from: portal/src/graph/osim/nodes/PDpKnKonstant.ts
      to: portal/src/graph/core/GObjLink.ts
      via: "extends GObjOSimDlp (Subklasse von GObjLink) + überschreibt draw/onSimEvent"
      pattern: "extends GObj|onSimEvent"

user_setup: []
---

<objective>
GraphObject-Schicht vervollständigen, sodass alle 18 OSim-Typen (13 Knoten + 5 Kanten) als TypeScript-Klassen vorliegen, Multi-Waypoint-Routing, hierarchische Sub-Container und Grid-Operationen implementiert sind und Copy/Paste mit Link-Restore funktioniert.

Purpose: Phase 1 hat im Rahmen von `PDurchlaufplanViewer-Design` (D-08 Punkt 3 in 01-CONTEXT.md) nur die Basis-Klassen `GObject`, `GObjLink`, `GLink` plus ein paar konkrete Knoten-Renderer geliefert (siehe Specifics in 01-CONTEXT.md). Phase 3 macht die Foundation für ALLE graphischen Viewer voll — Voraussetzung dafür, dass spätere Phasen (5: Reports, 7+: Matrix/Gantt) ohne Bruch konsumieren können.

Output: Komplette `portal/src/graph/core/`- und `portal/src/graph/osim/`-Hierarchie + Persistenz-Modul + Vitest-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vertical-slice/01-CONTEXT.md
@.planning/phases/03-live-viz/03-PRELIMINARY-PLAN.md
@.planning/research/osim2004-ui-analysis.md

# Original C++ Source (Pflichtlektüre für Algorithmen-Port)
@C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\inc\GraphObj.h

<interfaces>
<!-- Phase 1 hat folgendes vorgegeben (siehe 01-CONTEXT.md D-05/D-08 Punkt 3); Phase 3 baut darauf auf. -->
<!-- Falls Phase 1 noch nicht implementiert ist, sind die Basis-Klassen aus diesem Plan zu erzeugen. -->

Aus Phase 1 erwartete (oder hier neu/voll zu definierende) Basis:
- portal/src/graph/core/GObject.ts (Basis, evtl. minimal aus Phase 1)
- portal/src/graph/core/GObjLink.ts (Phase-1-Erstauszug)
- portal/src/graph/core/GLink.ts (Phase-1-Erstauszug)
- portal/src/graph/react-flow-adapter/GraphObjectNode.tsx (Phase 1)

Falls Phase 1 die Klassen nur teilweise lieferte: Plan-Ausführung erweitert sie auf den hier definierten Sollstand; bestehende öffentliche API bleibt rückwärtskompatibel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core-Module komplettieren (Enums, GObject, GObjLink, GLink, GLinkPoint)</name>
  <files>
    portal/src/graph/core/types.ts,
    portal/src/graph/core/GObject.ts,
    portal/src/graph/core/GObjLink.ts,
    portal/src/graph/core/GLink.ts,
    portal/src/graph/core/GLinkPoint.ts,
    portal/src/graph/core/GraphCollection.ts,
    portal/src/graph/core/GraphList.ts,
    portal/src/graph/core/GraphGrid.ts,
    portal/src/graph/core/GraphView.ts,
    portal/src/graph/core/GObjSub.ts,
    portal/src/graph/core/__tests__/GObject.test.ts,
    portal/src/graph/core/__tests__/GObjLink.test.ts,
    portal/src/graph/core/__tests__/GLinkPoint.test.ts,
    portal/src/graph/core/__tests__/GraphGrid.test.ts,
    portal/src/graph/core/__tests__/GObjSub.test.ts
  </files>
  <behavior>
    - GObject: SetState/GetState halten GObjState konsistent; CheckRegion liefert R_LINK_EDIT am Rand (≤ margin=10), R_EDIT im Innenraum, R_NO außerhalb der virtRect.
    - GObjLink: addInLink fügt nicht zweimal denselben GLink hinzu und ruft link.setNext(this) auf; removeInLink trennt link.next; getLinkStartPos liefert für GLDirection.NORTH den Mittelpunkt der oberen Kante.
    - GLinkPoint.checkNeighbourhood(NORTH, SOUTH) → usedPointNum=2 (gerader Pfad); (EAST, SOUTH) → 3-Punkt-Knick; jede der 18×18 Direction-Kombinationen liefert deterministisch eine Wegpunkt-Anzahl zwischen 1 und 6 (volle Logik aus GraphObj.h portiert; sofern Algorithmus zu komplex, vereinfachte 3-Punkt-Variante implementieren UND `TODO: full 6-point port` markieren mit Issue-Referenz auf das Risiko aus 03-PRELIMINARY-PLAN.md §6).
    - GraphGrid: insertColBefore(2) verschiebt alle GObjPositionen mit colIdx>=2 um +1; removeRow(3) entfernt nur die Zeile-3-Objekte; goIns mit besetzter Zelle wirft Error oder liefert false (deterministisch).
    - GraphView: 4 Layer (bgList, stdGrid, linkList, fgList) sind separat iterierbar; isHit() prüft FG zuerst, dann Links, Grid, BG (Z-Ordering laut OGraphView).
    - GObjSub: getKindView() liefert die kapselte GraphView; isParentFrom(child, recursive=true) erkennt Verschachtelung.
  </behavior>
  <action>
    Implementiere `portal/src/graph/core/*` als TypeScript-Port von `GraphObj.h`. Reihenfolge: types.ts (alle Enums) → GObject (abstract, Draw-/HitTest-/CheckRegion-/Phantom-API als virtuelle Methoden) → GObjLink (In-/Out-Listen mit Dup-Check und Notify-an-Link) → GLink (prev/next + stdGLDirPrev/stdGLDirNext) → GLinkPoint (extends GLink, Multi-Waypoint mit CheckNeighbourhood) → GraphCollection (abstract) → GraphList → GraphGrid (Row/Col mit goIns, goRemove, insertColBefore, insertRowBefore, removeCol, removeRow, getNextFreeGridPlace, isGridPlaceTaken, copyCell) → GraphView (4-Layer-Container, invalidateView-Hook, isHit() in FG→Links→Grid→BG-Reihenfolge) → GObjSub (extends GObjLink, hält eine eigene GraphView).

    CheckNeighbourhood-Port: lies GraphObj.h §GLinkPoint im Original (Such-Pattern `class.*GLinkPoint` und `CheckNeighbourhood`). Falls die C++-Methode zu komplex für 1:1-Port ist, implementiere die vereinfachte 3-Punkt-Variante (Vor-Direction-Austritt → orthogonaler Knick → Eintritt-Direction-Ziel) und markiere im Code `// FIXME(phase-3-followup): full 6-point port pending; tracked as risk in 03-PRELIMINARY-PLAN.md §6`. Risiko ist explizit in der Phase-Skizze adressiert.

    Verwende Vitest. Tests prüfen alle in `<behavior>` aufgezählten Verhaltensweisen; mindestens 1 Test pro öffentliche Methode.

    Keine React-Imports in `core/*`. Pure TypeScript. Punkte/Rechtecke als `{x:number;y:number}` / `{x:number;y:number;w:number;h:number}`.
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx vitest run src/graph/core --reporter=dot</automated>
  </verify>
  <done>
    Alle Tests grün; tsc-noEmit über `portal/src/graph/core/` läuft fehlerfrei; mindestens 30 Tests; `grep -rn 'export class' portal/src/graph/core | grep -v __tests__ | wc -l` >= 10.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OSim-Subklassen (13 Knoten + 5 Kanten) + Phantom-Drag-Hook</name>
  <files>
    portal/src/graph/osim/nodes/index.ts,
    portal/src/graph/osim/nodes/PDlplKnoten.ts,
    portal/src/graph/osim/nodes/PDpKnZeitvorgabe.ts,
    portal/src/graph/osim/nodes/PDpKnKonstant.ts,
    portal/src/graph/osim/nodes/PDpKnVerteilung.ts,
    portal/src/graph/osim/nodes/PDpKnMenge.ts,
    portal/src/graph/osim/nodes/PDpKnMengeRuesten.ts,
    portal/src/graph/osim/nodes/PDpKnRuecksprung.ts,
    portal/src/graph/osim/nodes/PDpKnRueckKonstant.ts,
    portal/src/graph/osim/nodes/PDpKnRueckVerteilung.ts,
    portal/src/graph/osim/nodes/PDpKnAlternativ.ts,
    portal/src/graph/osim/nodes/PDpKnAlternativTypId.ts,
    portal/src/graph/osim/nodes/PDpKnAlternativeVerteilung.ts,
    portal/src/graph/osim/nodes/PDpKnAlternativLogik.ts,
    portal/src/graph/osim/edges/index.ts,
    portal/src/graph/osim/edges/PDlplKante.ts,
    portal/src/graph/osim/edges/PDpKaUebergang.ts,
    portal/src/graph/osim/edges/PDpKaExtern.ts,
    portal/src/graph/osim/edges/PDpKaEntitaet.ts,
    portal/src/graph/osim/edges/PDpKaEntitaetAblage.ts,
    portal/src/graph/react-flow-adapter/usePhantomDrag.ts,
    portal/src/graph/osim/__tests__/nodes.test.ts
  </files>
  <behavior>
    - Jede der 13 Knoten-Klassen extends GObjLink (oder eine osim-Zwischenklasse `GObjOSimDlp`), hat `type: GObjType.PDPKN...` und liefert einen typ-spezifischen Symbol-/Domain-Marker (z.B. `PDpKnKonstant` hält `durchfuehrungsZeit:number`, `PDpKnVerteilung` hält `verteilungRef:string`).
    - Jede der 5 Kanten-Klassen extends GLinkPoint (oder GLink für `PDpKaExtern`) mit korrektem GObjType-Wert.
    - Konstruktion via `new PDpKnKonstant({id, name, x, y})` liefert ein valides GObjLink-Objekt mit korrekt initialisiertem virtRect und styles.
    - usePhantomDrag-Hook (React-Hook): onMouseDown speichert OldPhantomRect, onMouseMove ruft gobj.showPhantom mit aktualisiertem virtP, onMouseUp ruft hidePhantom und gobj.onRectUpdate; Hook ist testbar isoliert mit @testing-library/react.
  </behavior>
  <action>
    Pro Knoten-/Kanten-Typ eine eigene Datei mit minimaler Klasse (Konstruktor + Type + domain-spezifische Properties, KEINE Render-Logik in dieser Welle — Rendering bleibt im react-flow-adapter aus Phase 1). `nodes/index.ts` und `edges/index.ts` re-exportieren alle und stellen eine `nodeFactory(type: GObjType, opts)`-Funktion bereit, die nach Typ die richtige Klasse instanziiert (Switch über GObjType-Enum).

    Eine optional vorgeschaltete Zwischenklasse `GObjOSimDlp extends GObjLink` darf eingeführt werden, wenn gemeinsame Defaults (backColor, styles, Standard-Größe 200×80) das Erstellen vereinfachen.

    usePhantomDrag implementiert das C++-`ShowPhantom`/`HidePhantom`-Verhalten als React-Hook (siehe GraphObj.h §GObject Phantom-Funktionen). Greift auf `gobj.showPhantom(ctx, virtP)` und `gobj.hidePhantom(ctx)` aus Task 1 zu — verwendet als ctx ein einfaches `CanvasRenderingContext2D`-kompatibles Stub-Interface, das im React-Flow-Adapter konkret implementiert wird.

    Tests prüfen: factory(GObjType.PDPKNKONSTANT) liefert Instanz von PDpKnKonstant; jede Klasse hat das richtige `type`-Feld; usePhantomDrag-Lifecycle (mousedown → move → up) ruft die 3 erwarteten gobj-Methoden in richtiger Reihenfolge.
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx vitest run src/graph/osim src/graph/react-flow-adapter/usePhantomDrag --reporter=dot</automated>
  </verify>
  <done>
    Alle 18 GObjType-Werte sind durch eine konkrete TS-Klasse abgedeckt; factory-Test grün; usePhantomDrag-Test grün; `grep -c 'extends GObj' portal/src/graph/osim/nodes/*.ts portal/src/graph/osim/edges/*.ts | grep -v ':0$' | wc -l` >= 18.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistenz (Serializer + Clipboard mit Link-Restore)</name>
  <files>
    portal/src/graph/persistence/serializer.ts,
    portal/src/graph/persistence/clipboard.ts,
    portal/src/graph/persistence/__tests__/clipboard.test.ts
  </files>
  <behavior>
    - serializer.toJSON(view: GraphView) → JSON-Objekt mit `{nodes:[{id,type,virtRect,...}], edges:[{id,type,prevId,nextId,waypoints}], subFlows:[]}`.
    - serializer.fromJSON(json) → GraphView mit identischem Nodes/Edges-Set; round-trip ist verlustfrei für alle Properties die im Domain-Klassen-Konstruktor definiert sind.
    - clipboard.copyToClipboard(selection: GObject[]) → liefert eine Container-Struktur mit Nodes + Edges, wobei Cross-Referenzen via `user`-ID neu erzeugt werden (analog C++ `m_User`-Variable).
    - clipboard.restoreFromClipboard(payload, insertPos: Point) → fügt Klone aller Knoten ein (neue IDs), restoreInLink/restoreOutLink wirt Cross-Refs zu den neuen IDs um, sodass kopierte Kanten zu den kopierten Knoten zeigen, nicht zu den Originalen.
  </behavior>
  <action>
    serializer.ts: implementiere `toJSON` und `fromJSON` als reine Funktionen. Nutzt die `nodeFactory`/`edgeFactory` aus Task 2 für fromJSON. Sub-Flows (GObjSub) werden rekursiv serialisiert.

    clipboard.ts: portiere `CopyToClipboard`/`RestoreFromClipboard`/`RestoreInLink` aus GraphObj.h. Erst Phase: alle selektierten Objekte rekursiv durchgehen, neue IDs vergeben, ID-Mapping merken. Zweite Phase: Kanten neu verdrahten (Lookup über ID-Mapping). Wenn eine Kante auf einen Knoten zeigt, der NICHT im Clipboard war, wird die Kante verworfen (mit Warning-Log) — analog `copyExtern=FALSE` im Original.

    Test: 3-Knoten-Kette A→B→C kopieren, einfügen, prüfen dass A'→B'→C' entstand (neue IDs) und Original unverändert ist.
  </action>
  <verify>
    <automated>cd portal &amp;&amp; npx vitest run src/graph/persistence --reporter=dot</automated>
  </verify>
  <done>
    Round-Trip-Test (`toJSON → fromJSON` ergibt strukturell identische View) grün; Clipboard-Test mit 3-Knoten-Kette grün; mindestens 8 Tests in `persistence/__tests__/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-Speicher → React-State | clipboard.ts hält rohen Sub-Graph in JS-Memory; keine externe Quelle. Risiko klein. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | clipboard.ts `restoreFromClipboard` | mitigate | Validate payload shape (nodes:array, edges:array) bevor Klone erzeugt werden; Reject mit Error bei unbekanntem GObjType. |
| T-03-02 | DoS | GLinkPoint.checkNeighbourhood mit pathologischen Inputs | accept | Pure deterministische Funktion; max. 6 Output-Punkte hardcoded — kein unbounded loop. Risiko vernachlässigbar. |
| T-03-03 | Information Disclosure | serializer.toJSON exportiert ALLE virtRect/styles/textColor | accept | Domänenmodell enthält keine PII (technische OSim-Modelldaten). Out-of-Scope. |
</threat_model>

<verification>
- `cd portal && npx vitest run src/graph --reporter=dot` — alle Tests grün
- `cd portal && npx tsc --noEmit -p tsconfig.json` — keine Type-Errors in `src/graph/`
- Manuelle Inspektion: `grep -c 'class P' portal/src/graph/osim/nodes/*.ts portal/src/graph/osim/edges/*.ts` ergibt 18+
</verification>

<success_criteria>
1. Alle Tests in `portal/src/graph/**/__tests__/*` grün.
2. Alle 18 GObjType-Werte haben eine konkrete TypeScript-Klasse.
3. GraphGrid Insert/Remove ändert keine fremden Reihen/Spalten (Test verifiziert).
4. CheckNeighbourhood liefert für alle 324 (18×18) Direction-Paare einen deterministischen, getesteten Wegpunkt-Count (mindestens Stichprobe von 20 Paaren in Tests).
5. Copy/Paste 3-Knoten-Kette rundläuft (Test grün).
6. Foundation ist API-stabil: Phase 3 Plan 02–05 + Phase 5/7+ können auf `import { GObject, GObjLink, GLink, GLinkPoint, GraphView, GraphGrid, GObjSub } from '@/graph/core'` zugreifen.
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-viz/03-01-SUMMARY.md`
</output>
