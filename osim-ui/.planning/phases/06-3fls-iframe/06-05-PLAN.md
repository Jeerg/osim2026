---
phase: 06-3fls-iframe
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - e2e/3fls-integration/playwright.config.ts
  - e2e/3fls-integration/specs/embedded-login.spec.ts
  - e2e/3fls-integration/specs/embedded-run.spec.ts
  - e2e/3fls-integration/specs/standalone-mode.spec.ts
  - e2e/3fls-integration/fixtures/3fls-stub.ts
  - e2e/3fls-integration/README.md
  - docs/PHASE-6-3FLS-INTEGRATION.md
  - docs/FAILURE-MODES.md
  - .planning/phases/06-3fls-iframe/06-VERIFICATION.md
autonomous: false
requirements:
  - P6-E2E-EMBEDDED
  - P6-E2E-STANDALONE
  - P6-DOCS
  - P6-FAILURE-MODES

must_haves:
  truths:
    - "Playwright-Test 'embedded-login' deckt den vollen Iframe-Login-Flow ab (3fls-Stub-Login -> Sim-Tab -> Iframe lädt -> Token-Exchange -> osim-ui-Inhalt sichtbar)"
    - "Playwright-Test 'embedded-run' startet einen Sim-Lauf im Iframe (oder triggert das relevante UI-Element falls Phase 1 noch keinen vollständigen Run liefert)"
    - "Playwright-Test 'standalone-mode' verifiziert, dass osim-ui OHNE `?embedded=1` mit eigener Login-Form, eigenem Logout und Top-Nav weiterhin funktioniert"
    - "docs/PHASE-6-3FLS-INTEGRATION.md beschreibt das Gesamtbild End-to-End (Sequenz-Diagramm, Komponenten, Deploy-Schritte)"
    - "docs/FAILURE-MODES.md listet die 6+ dokumentierten Fehlerzustände mit User-sichtbarem Verhalten und Recovery"
    - "06-VERIFICATION.md fasst zusammen, welche Akzeptanzkriterien aus 06-PRELIMINARY-PLAN.md grün sind (1..9)"
  artifacts:
    - path: e2e/3fls-integration/playwright.config.ts
      provides: "Playwright-Config mit zwei Projekten (osim-standalone, osim-embedded-in-3fls-stub)"
      contains: "projects:"
    - path: e2e/3fls-integration/fixtures/3fls-stub.ts
      provides: "Minimal-3fls-Stub-Page als Express- oder Vite-Statisch-Server, der die SimulationFrame-Logik nachstellt (Token-Exchange + postMessage) — verwendet wenn echtes tbx_stzrim lokal nicht verfügbar"
      min_lines: 50
    - path: e2e/3fls-integration/specs/embedded-login.spec.ts
      provides: "E2E: 3fls-Stub-Login -> Sim-Tab -> Iframe-Token-Exchange -> Iframe-Auth-Success"
    - path: e2e/3fls-integration/specs/standalone-mode.spec.ts
      provides: "Regression-Test: Standalone-Flow"
    - path: docs/PHASE-6-3FLS-INTEGRATION.md
      provides: "End-to-End-Doku für osim-ui-Seite"
    - path: docs/FAILURE-MODES.md
      provides: "Fehlerzustands-Matrix"
    - path: .planning/phases/06-3fls-iframe/06-VERIFICATION.md
      provides: "Phase-Verifikation mit Akzeptanzkriterien-Mapping"
  key_links:
    - from: e2e/3fls-integration/specs/embedded-login.spec.ts
      to: fixtures/3fls-stub.ts
      via: "Test setzt 3fls-Stub auf Port 3000 auf, osim-ui auf 3001, osim-ui-Backend auf 8000; Stub mocked Firebase-getIdToken"
      pattern: "3fls-stub"
    - from: e2e/3fls-integration/specs/embedded-login.spec.ts
      to: portal/src/embed/PostMessageBridge.ts
      via: "Test wartet auf Bridge-ready, dann auf erfolgreichen Token-Exchange-Network-Call, dann auf authenticated-State im Iframe (DOM-Indikator)"
      pattern: "exchange-3fls-token"
---

<objective>
End-to-End-Verifikation und Dokumentation der gesamten Phase-6-Integration. Playwright-Suite, die den Iframe-Flow inkl. Token-Exchange automatisiert testet, plus User-Doku, Failure-Mode-Katalog und Phase-Verifikations-Mapping.

Purpose: Phase 6 ist erst dann abgeschlossen, wenn der Flow reproduzierbar funktioniert und alle 9 Akzeptanzkriterien aus 06-PRELIMINARY-PLAN.md grün gemeldet werden können. Doku stellt sicher, dass spätere Phasen (oder Wartung) den Mechanismus verstehen.

Output: Playwright-Suite mit 2 Test-Projekten, 3 Spec-Files, 1 Stub-Fixture, 3 Doku-Files (Integration, Failure-Modes, Verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-3fls-iframe/06-PRELIMINARY-PLAN.md
@.planning/phases/06-3fls-iframe/06-01-PLAN.md
@.planning/phases/06-3fls-iframe/06-02-PLAN.md
@.planning/phases/06-3fls-iframe/06-03-PLAN.md
@.planning/phases/06-3fls-iframe/06-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playwright-Setup mit 3fls-Stub und Standalone-Spec</name>
  <files>e2e/3fls-integration/playwright.config.ts, e2e/3fls-integration/fixtures/3fls-stub.ts, e2e/3fls-integration/specs/standalone-mode.spec.ts, e2e/3fls-integration/README.md, package.json</files>
  <behavior>
    - Playwright-Config startet via `webServer`-Option drei Prozesse: osim-ui-Portal (`npm run dev -- --port 3001`), osim-ui-Backend (`uv run uvicorn main:app --port 8000`), 3fls-Stub (`tsx fixtures/3fls-stub.ts --port 3000`).
    - 3fls-Stub: Vite oder einfache Express-App, die auf `/simulation` eine HTML-Seite mit `<iframe src="http://localhost:3001/?embedded=1">` rendert UND beim Mount per `fetch("http://localhost:8000/api/v1/auth/exchange-3fls-token", {body: JSON.stringify({external_token: STUB_TOKEN})})` einen Token-Exchange triggert (Backend muss STUB_TOKEN verifizieren — via Firebase-Emulator oder Mock-Modus in Backend, das ist vorausgesetzt durch Phase-1-Test-Setup).
    - Spec `standalone-mode.spec.ts`:
      - Test 1: Navigate to `http://localhost:3001/` -> Login-Form sichtbar, kein Iframe-Wrapper.
      - Test 2: Navigate to `http://localhost:3001/?embedded=0` -> identisch zu Test 1.
      - Test 3: DOM enthält `nav[role="navigation"]` (Top-Nav-Selector — Executor wählt nach Inspektion des Phase-1-Layouts).
  </behavior>
  <action>
    Neuer Top-Level-Ordner `e2e/3fls-integration/` (NICHT in portal/, NICHT in app/ — separates E2E-Modul). `package.json` (Root): füge `"e2e": "playwright test --config e2e/3fls-integration/playwright.config.ts"` Script hinzu; devDeps `@playwright/test`, `express`, `tsx`. Stub: minimal Express-App, eine HTML-Datei + ein POST-Handler-Proxy zum osim-ui-Backend. Firebase-Token-Stub: entweder Phase-1-Test-Backend liefert einen "test-mode-bypass" der Token-Verify überspringt, ODER die Pentest-Suite aus Welle 4 hat einen Mock-Modus-Schalter, der hier wiederverwendet wird. Executor wählt nach Inspektion. README erklärt: lokale Voraussetzungen, Start-Sequenz, Debug-Hinweise (`playwright test --debug`).
  </action>
  <verify>
    <automated>cd . && npx playwright install --with-deps chromium && npx playwright test --config e2e/3fls-integration/playwright.config.ts --project=osim-standalone</automated>
  </verify>
  <done>Standalone-Spec grün; Playwright-Config startet alle 3 Services; README ist gepflegt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embedded-Login- und Embedded-Run-Specs</name>
  <files>e2e/3fls-integration/specs/embedded-login.spec.ts, e2e/3fls-integration/specs/embedded-run.spec.ts</files>
  <behavior>
    - `embedded-login.spec.ts`:
      1. Navigate to `http://localhost:3000/simulation` (3fls-Stub).
      2. Warte auf iframe[src*="localhost:3001"] zu existieren.
      3. Warte auf network-Request POST /api/v1/auth/exchange-3fls-token mit Status 200.
      4. Wechsle in iframe-Context (page.frameLocator), assert: KEIN Top-Nav-Element, KEIN Login-Form-Element, sondern authenticated-State-DOM-Indikator (z. B. ein `[data-testid="osim-app-content"]`).
    - `embedded-run.spec.ts`:
      1. Wie embedded-login, aber zusätzlich: im iframe ein UI-Element triggern, das nach Phase 1 verfügbar ist (z. B. ein "Modell hochladen"-Button-Click oder eine Navigation auf eine vorhandene Subroute).
      2. Skip-mit-Kommentar wenn Phase 1 noch keinen vollständigen Run liefert — Executor entscheidet basierend auf Phase-1-SUMMARY.md.
      3. Assertions: Network-Call zum Backend, der mit `Authorization: Bearer ...`-Header geht (verifiziert dass der Custom-Token im Frontend angekommen ist und verwendet wird).
  </behavior>
  <action>
    Playwright `page.frameLocator(...)` für Iframe-Zugriff. `page.waitForRequest(req => req.url().includes("/api/v1/auth/exchange-3fls-token") && req.method() === "POST")` für Network-Assertion. data-testid-Selektoren bevorzugen über CSS-Klassen. `embedded-run.spec.ts` darf mit `test.skip(condition, "Phase 1 has not delivered full run flow yet; revisit")` markiert werden wenn Phase 1 noch keinen Run liefert. README erweitern um Test-Matrix.
  </action>
  <verify>
    <automated>cd . && npx playwright test --config e2e/3fls-integration/playwright.config.ts --project=osim-embedded-in-3fls-stub</automated>
  </verify>
  <done>embedded-login grün; embedded-run grün oder bewusst skipped mit Begründung im Code; Playwright-Report-HTML zeigt alle Specs.</done>
</task>

<task type="auto">
  <name>Task 3: Doku, Failure-Modes und Phase-Verifikation</name>
  <files>docs/PHASE-6-3FLS-INTEGRATION.md, docs/FAILURE-MODES.md, .planning/phases/06-3fls-iframe/06-VERIFICATION.md</files>
  <action>
    `docs/PHASE-6-3FLS-INTEGRATION.md`:
      - Abschnitt 1: Architektur-Bild (Mermaid `graph LR`) mit 3fls-Portal, osim-ui-Iframe, osim-ui-Backend, 3fls-Firebase, osim-ui-Firebase.
      - Abschnitt 2: Sequenz-Diagramm (Mermaid `sequenceDiagram`) für den Token-Exchange-Flow.
      - Abschnitt 3: Komponenten-Übersicht (osim-ui-Embed, Backend-Endpoint, 3fls-SimulationFrame, Security-Middleware).
      - Abschnitt 4: Deploy-Reihenfolge (Welle 1 -> 2 -> 4 -> 3 -> 5; Begründung: 3fls-PR-Merge erst nach osim-ui-Prod-Deploy).
      - Abschnitt 5: ENV-Variablen-Konsolidierung (Tabelle aller VITE_*-, TFLS_*-, CSP_*-Variablen mit Beschreibung und Beispiel).
      - Abschnitt 6: Verlinkung zu allen Welle-Plans und Welle-Summaries.
    `docs/FAILURE-MODES.md`: Tabelle mit Spalten: Failure | Trigger | User-sichtbar | Log-Eintrag | Recovery. Mindestens 8 Einträge:
      1. 3fls-Token-Exchange schlägt fehl (401) — Iframe zeigt Error-Card mit Retry
      2. 3fls-Token abgelaufen mitten in Session — Custom-Token wird nicht refreshed (Backlog); User landet auf "session expired"-Hinweis und muss Sim-Tab neu laden
      3. PostMessage-Bridge erhält Message von fremder Origin — silently dropped + warn-Log
      4. Iframe-Refused-by-CSP (frame-ancestors-Violation) — Browser-Console-Error, Iframe leer; User-sichtbar: leeres Frame; Recovery: Operator-Config-Fix
      5. osim-ui-Backend down — 3fls-Frontend zeigt Network-Error in Exchange-Call, Error-Card mit Retry
      6. 3fls-Service-Account-Key rotiert ohne Cloud-Run-Update — Backend wirft InvalidExternalTokenError("audience-mismatch") für alle Requests; User-sichtbar: 401-Error-Card; Recovery: Secret aktualisieren
      7. Unknown 3fls-tenant mit auto_provision=False — User-sichtbar: "Kein Zugriff"-Hinweis; Recovery: Operator legt Mapping-Eintrag an
      8. Resize-Bridge spammt (kein Debounce) — Performance-Degradation; aktuelle Mitigation: 100ms-Debounce in Bridge; bei Symptom Investigation
    `06-VERIFICATION.md`: Tabelle der 9 Akzeptanzkriterien aus 06-PRELIMINARY-PLAN.md, jede Zeile mit Status (✅ grün / 🟡 partial / ❌ rot), Verifikations-Methode (welcher Test/Welcher Checkpoint hat es bewiesen), Verweis auf SUMMARY.
  </action>
  <verify>
    <automated>test -f docs/PHASE-6-3FLS-INTEGRATION.md && test -f docs/FAILURE-MODES.md && test -f .planning/phases/06-3fls-iframe/06-VERIFICATION.md && grep -q "sequenceDiagram" docs/PHASE-6-3FLS-INTEGRATION.md && grep -cE "^\| [0-9]" docs/FAILURE-MODES.md</automated>
  </verify>
  <done>Alle drei Dateien existieren; Mermaid-Diagramme vorhanden; Failure-Modes-Tabelle hat ≥8 Einträge; Verifikation deckt alle 9 Akzeptanzkriterien.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-End-Akzeptanz und Phase-6-Sign-off</name>
  <what-built>
    - Playwright-E2E-Suite läuft erfolgreich.
    - Doku ist gepflegt.
    - 06-VERIFICATION.md zeigt 9 grüne Akzeptanzkriterien.
  </what-built>
  <how-to-verify>
    1. `npm run e2e` -> alle Specs grün (oder bewusst skipped mit Begründung).
    2. Manueller End-to-End-Run mit echtem tbx_stzrim (nicht Stub): tbx_stzrim-Portal lokal, Login, Sidebar "Simulation", Iframe lädt, Token-Exchange-Network-Call sichtbar, osim-ui-Inhalt sichtbar, Logout funktioniert.
    3. Standalone-Check: osim-ui ohne Iframe weiterhin voll funktional.
    4. Doku: docs/PHASE-6-3FLS-INTEGRATION.md gelesen und plausibel; Mermaid-Diagramme rendern in der GitHub-Vorschau.
    5. Failure-Modes Stichprobe: einen der 8 Failure-Modes provozieren (z. B. 3fls-Service-Account-Env entfernen) und User-sichtbares Verhalten gegen die Doku-Beschreibung abgleichen.
    6. 06-VERIFICATION.md: 9 Kriterien geprüft; alle ✅ oder mit Begründung dokumentierte 🟡 (Skip-Bedingungen aus Phase-1-Status).
    7. Bereitschaft zum Merge: alle PRs (osim-ui + tbx_stzrim) review-bereit, CI grün, kein offener TODO im Code.
  </how-to-verify>
  <resume-signal>"approved" wenn 9/9 grün ODER alle 🟡 mit dokumentierter Folge-Phase; sonst Befund + Welle-Plan zum Fix nennen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E-Tests vs. Produktion | Test-Setup darf keine Prod-Credentials anfassen; nutzt Firebase-Emulator und Stub-Tokens |
| Doku ist Vertrauensartefakt | Operator vertraut Doku bei Deploy-Schritten; falsche Reihenfolge = Outage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-01 | Tampering | Doku-Drift (Code ändert sich, Doku nicht) | mitigate | docs/PHASE-6-*.md verlinken auf konkrete Code-Pfade; CI-Hook (Backlog) für Link-Check |
| T-06.5-02 | Information Disclosure | E2E-Test-Logs enthalten Tokens | mitigate | Playwright `--reporter=html` standardmäßig ohne Network-Body-Capture; Stub-Token ist nicht prod-gültig |
| T-06.5-03 | Repudiation | Phase als "abgeschlossen" gemeldet ohne Beweis | mitigate | 06-VERIFICATION.md zwingt zur Pro-Kriterium-Methoden-Dokumentation |
</threat_model>

<verification>
- `npm run e2e` grün (alle Specs ODER mit dokumentierter Skip-Begründung).
- Manuelle Checkpoint-Liste Task 4 abgearbeitet.
- 06-VERIFICATION.md zeigt vollständiges Mapping.
</verification>

<success_criteria>
- Playwright-Suite läuft reproduzierbar.
- Doku (Integration + Failure-Modes + Verification) ist vollständig und konsistent.
- Alle 9 Akzeptanzkriterien aus dem Preliminary-Plan dokumentiert grün.
- Sign-off erfolgt im Checkpoint Task 4.
</success_criteria>

<output>
After completion, create `.planning/phases/06-3fls-iframe/06-05-SUMMARY.md` mit:
- Test-Matrix (Standalone, Embedded-Login, Embedded-Run — pro Spec Status)
- Verlinkung auf alle Doku-Artefakte
- 06-VERIFICATION.md-Final-Status (X/9 grün)
- Empfehlungen für Phase-7-Backlog: Module Federation, Token-Refresh-Flow, URL-Deep-Linking
</output>
</content>
</invoke>