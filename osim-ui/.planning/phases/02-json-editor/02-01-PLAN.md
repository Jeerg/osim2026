---
phase: 02-json-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../engine/src/osim_engine/schema/__init__.py
  - ../engine/src/osim_engine/schema/registry.py
  - ../engine/src/osim_engine/schema/fields.py
  - ../engine/src/osim_engine/schema/generator.py
  - ../engine/src/osim_engine/schema/__main__.py
  - ../engine/src/osim_engine/io/json_loader.py
  - ../engine/src/osim_engine/io/json_dumper.py
  - ../engine/tests/schema/test_registry.py
  - ../engine/tests/schema/test_generator.py
  - ../engine/tests/schema/test_cli_dump.py
  - ../engine/tests/io/test_json_loader.py
  - ../engine/tests/io/test_json_dumper.py
  - ../engine/tests/io/test_otx_json_round_trip.py
autonomous: true
requirements:
  - E2.1
  - E2.2
  - E2.3
  - E2.4
  - E2.5
  - E2.6

must_haves:
  truths:
    - "Engine kennt eine maschinell abfragbare Class-Registry aller im UI editierbaren Modell-Klassen (PSimulator, alle PAusloeser-, PDlplKnoten-, PRess*- und Kanten-Subklassen)."
    - "Jede registrierte Klasse exponiert ein `__sim_fields__`-Dict mit Name, Typ, Default, Pflicht-Flag und Beschreibung pro Public-Attribut."
    - "`python -m osim_engine.schema dump` schreibt ein vollständiges JSON-Schema (Draft 2020-12) auf stdout, das alle registrierten Klassen samt Discriminator (`$type`) enthält."
    - "`osim_engine.io.json_loader.load_model_from_json(payload) -> PSimulator` rekonstruiert einen lauffähigen Simulator aus dem JSON-Tree, inklusive Wire-Pass für Referenzen."
    - "`osim_engine.io.json_dumper.dump_simulator_to_json(sim) -> dict` serialisiert einen lauffähigen Simulator in den gleichen JSON-Tree."
    - "Round-Trip-Test `parse_otx -> dump_json -> load_json -> run` erzeugt eine Trace, die mit `parse_otx -> run` bit-identisch ist (gleiche Event-Reihenfolge, gleiche Counter)."
  artifacts:
    - path: "../engine/src/osim_engine/schema/registry.py"
      provides: "Klassen-Registry, Decorator `@sim_class`, `iter_registered() -> Iterable[type]`"
      min_lines: 60
    - path: "../engine/src/osim_engine/schema/fields.py"
      provides: "`SimField`-Dataclass, `sim_field(...)` Helper zur Definition introspectabler Felder"
      min_lines: 40
    - path: "../engine/src/osim_engine/schema/generator.py"
      provides: "`build_json_schema() -> dict` (Draft 2020-12), Discriminator via `$type`"
      min_lines: 80
    - path: "../engine/src/osim_engine/schema/__main__.py"
      provides: "CLI: `python -m osim_engine.schema dump`"
      contains: "if __name__"
    - path: "../engine/src/osim_engine/io/json_loader.py"
      provides: "`load_model_from_json(payload: dict) -> PSimulator` (Two-Pass: instantiate + wire)"
    - path: "../engine/src/osim_engine/io/json_dumper.py"
      provides: "`dump_simulator_to_json(sim: PSimulator) -> dict`"
    - path: "../engine/tests/io/test_otx_json_round_trip.py"
      provides: "Bit-Genauigkeitstest OTX vs. OTX->JSON->LOAD"
      contains: "test_round_trip_bit_identical"
  key_links:
    - from: "../engine/src/osim_engine/schema/generator.py"
      to: "../engine/src/osim_engine/schema/registry.py"
      via: "iter_registered() -> Schema-Properties"
      pattern: "iter_registered"
    - from: "../engine/src/osim_engine/io/json_loader.py"
      to: "../engine/src/osim_engine/schema/registry.py"
      via: "Lookup `$type` -> Klasse"
      pattern: "registry"
    - from: "../engine/tests/io/test_otx_json_round_trip.py"
      to: "../engine/src/osim_engine/io/otx_loader.py + io/json_dumper.py + io/json_loader.py"
      via: "Sequenz load_otx -> dump_json -> load_model_from_json -> sim.start()"
      pattern: "load_otx_file|dump_simulator_to_json|load_model_from_json"
---

<objective>
Engine-Sprint Phase 2 / Welle 0: Macht die `osim-engine` introspektierbar und JSON-Round-Trip-fähig. Liefert die Voraussetzung für alle nachfolgenden Pläne dieser Phase (Schema-Endpoint, Form-Editor, OTX->JSON-Konvertierung).

Purpose: Ohne maschinell abfragbares Schema und ohne JSON-Loader/Dumper bleibt das UI auf hand-geschriebenes Schema angewiesen — Schema-Drift wäre unausweichlich. Single Source of Truth muss die Engine bleiben (PROJECT.md, ROADMAP-Foundation 2).

Output: Drei neue Engine-Module (`schema/`, erweiterte `io/json_loader.py`, neues `io/json_dumper.py`), CLI `python -m osim_engine.schema dump`, sechs Test-Files inkl. bit-genauem Round-Trip-Test.

WICHTIG: Diese Arbeit findet im Engine-Sub-Repo statt (`C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\`), NICHT in `osim-ui/`. Pfade in `files_modified` sind relativ zum osim-ui-Working-Directory (`../engine/...`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-json-editor/02-PRELIMINARY-PLAN.md
@.planning/research/osim-engine-api.md

# Engine-Quellen (Pflichtlektüre vor Implementierung — alle einmal lesen, dann extrahieren)
@../engine/src/osim_engine/pps/simulator.py
@../engine/src/osim_engine/pps/ausloeser/base.py
@../engine/src/osim_engine/pps/ausloeser/einzel.py
@../engine/src/osim_engine/pps/knoten/base.py
@../engine/src/osim_engine/pps/knoten/zeitvorgabe.py
@../engine/src/osim_engine/pps/knoten/alternativ.py
@../engine/src/osim_engine/pps/kante/base.py
@../engine/src/osim_engine/pps/kante/uebergang.py
@../engine/src/osim_engine/io/otx_loader.py
@../engine/src/osim_engine/io/json_loader.py
@../engine/src/osim_engine/resources/ressource.py

<interfaces>
<!-- Kontrakte, die in dieser Welle entstehen und in nachfolgenden Plänen konsumiert werden. -->

Geplante Signaturen (Wave 0 dieses Plans — als allererster Task festzuschreiben):

```python
# osim_engine/schema/fields.py
@dataclass(frozen=True)
class SimField:
    name: str
    py_type: type
    default: Any | None
    required: bool
    description: str
    enum: tuple | None = None

def sim_field(name, *, py_type, default=None, required=False, description="", enum=None) -> SimField: ...

# osim_engine/schema/registry.py
def sim_class(*, type_id: str, category: str) -> Callable[[type], type]: ...
def iter_registered() -> Iterable[type]: ...
def get_by_type_id(type_id: str) -> type: ...
def category_of(cls: type) -> str: ...
# category in {"simulator", "ausloeser", "knoten", "kante", "ressource"}

# osim_engine/schema/generator.py
def build_json_schema() -> dict: ...    # Draft 2020-12, Discriminator-Property "$type"

# osim_engine/io/json_loader.py (produktiv)
def load_model_from_json(payload: dict) -> "PSimulator": ...

# osim_engine/io/json_dumper.py (neu)
def dump_simulator_to_json(sim: "PSimulator") -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema-Foundation — fields.py + registry.py + Decorator-Annotation der Engine-Klassen</name>
  <files>../engine/src/osim_engine/schema/__init__.py, ../engine/src/osim_engine/schema/fields.py, ../engine/src/osim_engine/schema/registry.py, ../engine/tests/schema/test_registry.py, ../engine/src/osim_engine/pps/simulator.py, ../engine/src/osim_engine/pps/ausloeser/base.py, ../engine/src/osim_engine/pps/ausloeser/einzel.py, ../engine/src/osim_engine/pps/ausloeser/ent_extern.py, ../engine/src/osim_engine/pps/knoten/base.py, ../engine/src/osim_engine/pps/knoten/zeitvorgabe.py, ../engine/src/osim_engine/pps/knoten/alternativ.py, ../engine/src/osim_engine/pps/knoten/extern.py, ../engine/src/osim_engine/pps/knoten/ruecksprung.py, ../engine/src/osim_engine/pps/kante/base.py, ../engine/src/osim_engine/pps/kante/uebergang.py, ../engine/src/osim_engine/pps/kante/verteilung.py, ../engine/src/osim_engine/resources/ressource.py, ../engine/src/osim_engine/resources/beleg.py, ../engine/src/osim_engine/resources/menge.py</files>
  <behavior>
    - `SimField` ist eine frozen dataclass mit Feldern (name, py_type, default, required, description, enum).
    - `sim_field(name, *, py_type, default=None, required=False, description="", enum=None)` baut eine `SimField`-Instanz.
    - `sim_class(*, type_id, category)` ist ein Klassen-Decorator. Er traegt die Klasse in eine modulglobale Registry ein und prueft Eindeutigkeit von `type_id`. Doppelte `type_id` -> `ValueError`.
    - `iter_registered()` gibt die Klassen in Reihenfolge der Registrierung zurueck.
    - `get_by_type_id("ausloeser.einzel")` liefert genau `PAslEinzel`.
    - `category_of(PAslEinzel) == "ausloeser"`.
    - Jede dekorierte Klasse besitzt nach Decorator-Pass ein Attribut `__sim_fields__: tuple[SimField, ...]` (aus einem Klassen-Attribut `_sim_fields = (...)` oder Default leer).
    - Decorator-Annotation: `PSimulator (type_id="simulator", category="simulator")`, `PAusloeser`-Subklassen (`ausloeser.einzel`, `ausloeser.ent_extern`, `ausloeser.aco_ant`), `PDlplKnoten`-Subklassen (`knoten.konstant`, `knoten.alternativ`, `knoten.extern`, `knoten.ruecksprung`), Kanten (`kante.uebergang`, `kante.verteilung`), Ressourcen (`ressource.beleg`, `ressource.menge`).
    - Pro Klasse mindestens die Felder, die der OTX-Loader heute setzt (z.B. `m_sName`, `m_iBeginTermin`, `m_iDurchfuehrungszeit`) als `_sim_fields` deklariert.
    - Test `test_registry.py` deckt ab: Eindeutigkeits-Check, `iter_registered`-Reihenfolge, `category_of`, Vorhandensein der erwarteten `__sim_fields__` an PSimulator und PAslEinzel, leere Registry-Initialzustand nach `registry._reset()` (Test-Helper).
  </behavior>
  <action>
    Pflicht-Lektuere zuerst (jede Datei genau einmal lesen, Patterns extrahieren): die in `<context>` aufgefuehrten Engine-Quellen. Danach NICHT erneut lesen — alle benoetigten Public-Attribute aus dem ersten Read extrahieren.

    Lege `engine/src/osim_engine/schema/` an. Implementiere `fields.py` und `registry.py` gemaess `<interfaces>`. Stelle sicher, dass der Decorator keine Seiteneffekte ueber das Eintragen hinaus erzeugt (KEINE Pydantic-Mirror-Modelle — pro D-Vorgabe: Plain Python + `__sim_fields__`-Spec). Die Decorator-Reihenfolge in den Engine-Modulen muss deterministisch sein (Import-Order via expliziter Import-Sammlung in `schema/__init__.py`: importiere alle annotierten Module, damit `iter_registered()` reproduzierbar ist).

    Annotiere die 13 Engine-Klassen mit `@sim_class(type_id=..., category=...)`. Setze pro Klasse `_sim_fields = (sim_field(...), ...)` mit allen Public-Attributen, die der OTX-Loader heute belegt — Quelle: lies `engine/src/osim_engine/io/otx_loader.py` einmal vollstaendig zur Extraktion der Attribut-Listen. Verwende deutsche `description`-Strings (Lehrmaterial-tauglich).

    Tests in `engine/tests/schema/test_registry.py`. Importiere `osim_engine.schema` als Side-Effect zum Triggern der Registrierungen.
  </action>
  <verify>
    <automated>cd ../engine && uv run pytest tests/schema/test_registry.py -x -v</automated>
  </verify>
  <done>13 Engine-Klassen sind registriert mit eindeutigen `type_id`s. `iter_registered()` ist deterministisch sortiert. `__sim_fields__` enthaelt mindestens die OTX-Loader-Attribute. Alle Tests gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema-Generator + CLI `python -m osim_engine.schema dump`</name>
  <files>../engine/src/osim_engine/schema/generator.py, ../engine/src/osim_engine/schema/__main__.py, ../engine/tests/schema/test_generator.py, ../engine/tests/schema/test_cli_dump.py</files>
  <behavior>
    - `build_json_schema()` liefert ein dict konform JSON Schema Draft 2020-12.
    - Top-Level: `$schema`, `$id`, `title`, `type: "object"`, `$defs` mit einer Definition pro registrierter Klasse.
    - Jede Klassen-Definition: `type: "object"`, `properties` aus `__sim_fields__` (py_type-Mapping: `int -> integer`, `float -> number`, `str -> string`, `bool -> boolean`, `list -> array`, `enum -> string mit enum-Liste`), `required` aus required-Flag, `additionalProperties: false`, eine Property `"$type": {"const": "<type_id>"}`.
    - Kategorien (`ausloeser`, `knoten`, `kante`, `ressource`) werden als `oneOf`-Unions exportiert: `$defs/_AusloeserAny`, `_KnotenAny`, `_KanteAny`, `_RessourceAny`. Discriminator: `{"propertyName": "$type", "mapping": {<type_id>: <$ref>}}`.
    - Root-Property von `PSimulator` referenziert `_AusloeserAny`-Listen etc. via `$ref`.
    - CLI: `python -m osim_engine.schema dump` schreibt `json.dumps(build_json_schema(), indent=2, ensure_ascii=False)` auf stdout, Exit-Code 0. `python -m osim_engine.schema dump --output schema.json` schreibt in Datei.
    - Tests: schema enthaelt `$defs.PSimulator`, schema-Property `properties.m_iPeriodenLaenge`, schema-Validation eines Minimal-Modell-Dicts gegen jsonschema-Lib gruen, CLI-Subprocess-Test mit `subprocess.run([sys.executable, "-m", "osim_engine.schema", "dump"])` liefert parsebares JSON.
  </behavior>
  <action>
    Implementiere `generator.py` mit reinem Stdlib-`typing`-Mapping (kein Pydantic). Nutze `jsonschema` (dev-dependency in `engine/pyproject.toml` ergaenzen, falls fehlt) NUR fuer Tests, nicht fuer Generator.

    `__main__.py` parsed argv via `argparse`: `dump` als einziger Subcommand, optional `--output PATH`. Bei `--output` schreibt es die Datei, sonst stdout.

    Tests: `test_generator.py` mit jsonschema-Validation eines per Hand gebauten Minimal-Modells (1 Auslöser + 1 Knoten). `test_cli_dump.py` via `subprocess.run` mit `cwd=engine_root`, prueft Exit-Code 0 und parsebares JSON mit `$defs.PSimulator`.
  </action>
  <verify>
    <automated>cd ../engine && uv run pytest tests/schema/test_generator.py tests/schema/test_cli_dump.py -x -v</automated>
  </verify>
  <done>`python -m osim_engine.schema dump` liefert ein valides JSON-Schema. jsonschema-Validation gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: JSON-Loader (produktiv) + JSON-Dumper + Round-Trip-Test bit-genau</name>
  <files>../engine/src/osim_engine/io/json_loader.py, ../engine/src/osim_engine/io/json_dumper.py, ../engine/tests/io/test_json_loader.py, ../engine/tests/io/test_json_dumper.py, ../engine/tests/io/test_otx_json_round_trip.py</files>
  <behavior>
    - `load_model_from_json(payload: dict) -> PSimulator`: Two-Pass. Pass 1 (instantiate): fuer jeden Eintrag mit `"$type"` die Klasse aus `registry.get_by_type_id` ziehen, instanziieren, Public-Attribute aus `__sim_fields__` per `setattr` setzen (Referenzen vorerst als string-id parken). Pass 2 (wire): String-IDs durch echte Objekt-Referenzen ersetzen (z.B. `m_lDlpl: "knoten-1"` -> Knoten-Objekt).
    - `dump_simulator_to_json(sim: PSimulator) -> dict`: invertiert load_model_from_json. Referenzen werden als string-ids (`obj.m_sName` oder neue Id) exportiert, `$type` aus `registry`-Lookup gesetzt, alle Felder aus `__sim_fields__` per `getattr` gelesen.
    - Round-Trip-Invariante: `dump(load(payload)) == payload` (struktureller Vergleich, Reihenfolge der Listen stabil).
    - Sim-Reproduzierbarkeit: `parse_otx -> dump_json -> load_json -> sim.start()` produziert exakt die gleiche Sequenz von EventBus-Events wie `parse_otx -> sim.start()` (TraceCaptureSink-Vergleich, Topic + sim_time + sub_time + alle key/value-Paare in den Event-Daten).
    - Tests: `test_json_loader.py` baut payload-dict, loadet, prueft `sim.m_lAusl[0].m_sName == "Erzeugnis-1"`. `test_json_dumper.py` baut Sim per Hand, dumpt, prueft dict-Struktur. `test_otx_json_round_trip.py` lädt `Dummy.otx` (Fixture-Pfad: `OSim2004/Vorstellung04/Dummy.otx`), Round-Trip + Trace-Vergleich.
  </behavior>
  <action>
    Loescht/ersetzt das Skelett in `io/json_loader.py` (heute basiert es auf `model.sim_model.SimModel` — nicht passend, wir wollen den echten `PSimulator`). Behalte fuer Kompat-Aliase `load_model`/`dump_model` mit DeprecationWarning, falls extern referenziert.

    Schreibe einen Test-Helper `tests/io/_fixtures.py` mit `OTX_DUMMY_PATH = Path(...) / "OSim2004" / "OSimV01(Fj)" / "Vorstellung04" / "Dummy.otx"`. Falls die Datei nicht existiert (CI), markiere den Round-Trip-Test mit `pytest.skip("OSim2004-Fixture nicht verfuegbar")` — aber lokal (Entwickler-Maschine) MUSS er laufen.

    Trace-Vergleich: subscribe `TraceCaptureSink` auf `*` bei beiden Sim-Instanzen, vergleiche `sink.events` (Liste von dicts) deep-equal. Bei Mismatch: pytest-assertion mit aussagekraeftigem Diff (zuerst Counts pro Topic, dann erste Abweichung).
  </action>
  <verify>
    <automated>cd ../engine && uv run pytest tests/io/test_json_loader.py tests/io/test_json_dumper.py tests/io/test_otx_json_round_trip.py -x -v</automated>
  </verify>
  <done>Round-Trip-Test `test_round_trip_bit_identical` gruen mit `Dummy.otx`. Alle Loader/Dumper-Unit-Tests gruen. `dump(load(payload)) == payload` invariant.</done>
</task>

</tasks>

<verification>
1. `cd ../engine && uv run pytest tests/schema/ tests/io/ -x` — alle Tests gruen.
2. `cd ../engine && uv run python -m osim_engine.schema dump | python -c "import json,sys; s=json.load(sys.stdin); assert '$defs' in s and 'PSimulator' in s['$defs']"` — Schema enthaelt PSimulator-Definition.
3. `cd ../engine && uv run ruff check src/osim_engine/schema/ src/osim_engine/io/json_loader.py src/osim_engine/io/json_dumper.py` — clean.
4. Round-Trip-Bit-Genauigkeit mit `Dummy.otx` verifiziert (siehe Task 3).
</verification>

<success_criteria>
- 13 Engine-Klassen via `@sim_class` registriert.
- CLI `python -m osim_engine.schema dump` liefert Draft-2020-12-Schema.
- `load_model_from_json` und `dump_simulator_to_json` sind produktiv, kein Skelett.
- Bit-genauer Round-Trip-Test gruen.
- Engine-Test-Suite (`uv run pytest`) bleibt gruen (keine bestehenden Tests gebrochen).
</success_criteria>

<output>
After completion, create `.planning/phases/02-json-editor/02-01-SUMMARY.md` (im osim-ui-Repo) mit:
- Liste der annotierten Klassen + `type_id`s
- Pfad zur generierten Schema-Datei (falls in CI eingecheckt)
- Round-Trip-Test-Status
- Engine-Commit-Hash, der gegen osim-ui Phase 2 gepinnt werden kann
- Bekannte Lücken (z.B. nicht-introspectable Felder, die als Backlog für spätere Phasen markiert sind)
</output>
</content>
</invoke>