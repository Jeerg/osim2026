---
phase: 02-json-editor
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - tests/integration/test_phase2_round_trip.py
  - tests/integration/test_phase2_end_to_end.py
  - tests/integration/conftest.py
  - portal/tests/e2e/phase2-flow.spec.ts
  - portal/playwright.config.ts
  - docs/PHASE-2-JSON-EDITOR.md
  - docs/ENGINE-SYNC.md
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "Integration-Test belegt: OTX-Modell -> Konvertierung -> JSON-Sim erzeugt bit-identisches Trace wie OTX-Sim (Backend-Ende-zu-Ende, nicht nur Engine-Unit-Test aus Plan 01)."
    - "Pytest-Integration-Suite faehrt den ganzen Backend-Pfad: Upload OTX -> Convert -> JSON-Modell speichern -> Submit Run -> Polling bis succeeded -> Trace-Download."
    - "Playwright-E2E faehrt den ganzen UI-Pfad: Login (Test-Auth) -> Modell anlegen -> Submit -> Status-Wechsel -> Trace-Download."
    - "Schema-Validation-Test: Submit eines invaliden Modells (unbekannter $type, fehlendes Pflichtfeld, falscher Typ) liefert klare field-level Errors."
    - "`docs/PHASE-2-JSON-EDITOR.md` beschreibt Architektur, REST-Endpoints, JSON-Schema-Beispiel, Konvertierungs-Bericht."
    - "`docs/ENGINE-SYNC.md` beschreibt den Schema-Refresh-Workflow zwischen osim-engine und osim-ui."
  artifacts:
    - path: "tests/integration/test_phase2_round_trip.py"
      provides: "Bit-genauer Round-Trip ueber den FULL Backend-Stack"
    - path: "tests/integration/test_phase2_end_to_end.py"
      provides: "REST-End-to-End-Test"
    - path: "portal/tests/e2e/phase2-flow.spec.ts"
      provides: "Playwright-E2E-Test"
    - path: "docs/PHASE-2-JSON-EDITOR.md"
      provides: "User-/Dev-Doku Phase 2"
    - path: "docs/ENGINE-SYNC.md"
      provides: "Schema-Sync-Prozessbeschreibung"
  key_links:
    - from: "tests/integration/test_phase2_round_trip.py"
      to: "POST /models/upload-otx + POST /models/{id}/convert-to-json + POST /runs"
      via: "httpx AsyncClient ueber den vollen REST-Stack"
      pattern: "convert-to-json|/runs"
    - from: "portal/tests/e2e/phase2-flow.spec.ts"
      to: "Backend lokaler Dev-Server + Frontend Vite-Dev"
      via: "Playwright-fixture: web-server config"
      pattern: "webServer"
---

<objective>
Verifikations- und Doku-Plan: Phase 2 wird durch zwei Integration-Test-Suites + einen E2E-Test verifiziert; ARCHITECTURE und User-Doku werden auf den neuen Stand gebracht.

Purpose: Definition-of-Done der Phase. Macht Schema-Drift, Konvertierungs-Verlust und UI-Regressions im naechsten Phase-Pass sofort sichtbar.

Output: Drei Test-Suites + zwei Markdown-Dokumente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-json-editor/02-PRELIMINARY-PLAN.md

# Vorgaenger-Summaries (Pflichtlektuere)
@.planning/phases/02-json-editor/02-01-SUMMARY.md
@.planning/phases/02-json-editor/02-02-SUMMARY.md
@.planning/phases/02-json-editor/02-03-SUMMARY.md
@.planning/phases/02-json-editor/02-04-SUMMARY.md
@.planning/phases/02-json-editor/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration-Tests (Backend-Round-Trip + End-to-End)</name>
  <files>tests/integration/conftest.py, tests/integration/test_phase2_round_trip.py, tests/integration/test_phase2_end_to_end.py</files>
  <behavior>
    - `tests/integration/conftest.py`:
       * Pytest-Fixture `app_client` (httpx AsyncClient gegen die FastAPI-App mit Test-DB + Test-Storage).
       * Pytest-Fixture `tenant_headers` mit Test-JWT (Test-Auth-Helper aus Plan 02 wiederverwendet).
       * Pytest-Fixture `dummy_otx_path` -> Pfad zu `OSim2004/Vorstellung04/Dummy.otx`; bei Nicht-Existenz `pytest.skip`.
    - `test_phase2_round_trip.py`:
       * Lade Dummy.otx als bytes.
       * `POST /api/v1/models/upload-otx` (multipart) -> 201, otx_model_id.
       * `POST /api/v1/models/{otx_model_id}/convert-to-json` -> 201, json_model_id, coverage_ratio >= 0.8.
       * Submit zwei parallele Runs (gleicher Seed): einer auf otx_model_id, einer auf json_model_id.
       * Polling bis beide `succeeded`.
       * Download beide Traces.
       * `assert trace_otx == trace_json` (event-fuer-event Vergleich; bei Mismatch: erstes Delta ausgeben).
    - `test_phase2_end_to_end.py`:
       * Anlegen eines Minimal-Modells via `POST /api/v1/models/json` (kein OTX).
       * Submit, Polling, Download Trace -> nicht-leer, mindestens ein `sim.begin`.
       * Schema-Validation-Tests: vier negative Cases (unbekannter $type, fehlendes Pflichtfeld, falscher Typ, ungueltige Knoten-Referenz) -> jeweils 422 mit field-level errors.
       * Schema-Hash-Stabilitaet ueber zwei Requests.
       * Run-Liste enthaelt alle gerade angelegten Runs.
  </behavior>
  <action>
    Pytest-Marker: alle Tests `@pytest.mark.integration`. CI-Konfig (`pyproject.toml` `[tool.pytest.ini_options]`) so, dass `pytest` ohne Marker nur Unit-Tests laufen laesst; `pytest -m integration` triggert diese.

    Test-DB: nutze `pytest-postgresql` oder einen Test-DB-Fixture aus dem bestehenden 3fls-Pattern. Lese `tests/conftest.py` (existiert) einmal und reuse den vorhandenen DB-Fixture-Code.

    Storage in Tests: tmp_path-Backend, nicht echtes Minio.

    Bit-Vergleich: lade beide JSONL-Traces zeilenweise, parse zu dicts, vergleiche Sequenz. Bei Mismatch logge Index + beide Events.
  </action>
  <verify>
    <automated>uv run pytest tests/integration -m integration -x -v</automated>
  </verify>
  <done>Beide Integration-Tests gruen. Bit-Genauigkeit OTX vs. JSON belegt. Schema-Validation 422-Cases korrekt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright-E2E + Doku</name>
  <files>portal/tests/e2e/phase2-flow.spec.ts, portal/playwright.config.ts, docs/PHASE-2-JSON-EDITOR.md, docs/ENGINE-SYNC.md</files>
  <behavior>
    - Falls `portal/playwright.config.ts` noch nicht existiert: anlegen mit `webServer`-Config, die Backend (`uv run uvicorn app.main:app --port 8001`) UND Frontend (`npm run dev -- --port 5173`) startet, Reuse `existing` falls schon laufend.
    - Test `phase2-flow.spec.ts`:
       1. Goto `/login`, Test-Auth-Login (durch Test-Bypass oder Firebase-Emulator-Helper).
       2. Goto `/models/new`.
       3. Form ausfuellen: Name "E2E-Test", Auslöser hinzufuegen (1 PAslEinzel, m_iBeginTermin=100), Knoten hinzufuegen (1 PDpKnKonstant, m_iDurchfuehrungszeit=500), Verknuepfung setzen.
       4. Save klicken -> erwartet Toast "Modell gespeichert", URL wechselt auf `/models/{uuid}/edit`.
       5. "Simulation starten" klicken -> Modal: seed=42, periods=1, submit.
       6. URL wechselt auf `/runs/{uuid}`.
       7. Warte bis Status-Badge "succeeded" (max 30 s).
       8. Klick "Trace herunterladen" -> Playwright-Download-Promise, Datei ist nicht-leer.
       9. Goto `/runs` -> Tabelle enthaelt den Run.
    - `docs/PHASE-2-JSON-EDITOR.md`:
       * Architektur-Diagramm (Mermaid): Engine -> Schema-Generator -> Backend -> Form-Editor.
       * REST-API-Tabelle (alle neuen Endpoints aus Plan 02-05).
       * JSON-Schema-Beispiel (Minimal-Modell).
       * Konvertierungs-Anleitung (OTX -> JSON).
       * Bekannte Limitationen (nicht-konvertierbare Klassen, Phase-3-Live-Viz-Stub).
    - `docs/ENGINE-SYNC.md`:
       * Wann muss `python -m osim_engine.schema dump` neu laufen? Antwort: nach jedem Engine-Release mit Aenderungen an `@sim_class`-Klassen.
       * Wie wird der Cache invalidiert? Beschreibung von `schema_service.invalidate()`.
       * CI-Hook-Vorschlag: pre-deploy `schema_hash`-Vergleich zwischen Engine und Backend.
       * Migration-Strategie bei Schema-Breaks (vorerst nur Warning, Phase 5+ Backlog).
  </behavior>
  <action>
    Playwright nutzt eigenen Test-Tenant + eigene Test-DB-Datenbank, damit E2E-Tests keine Prod-Daten beruehren. Setze `osim_TEST_MODE=1` Env-Var, die das Backend auf Test-Auth-Bypass schaltet (Phase 2 Vereinfachung — Firebase-Emulator-Integration ist Phase-1-Restposten).

    `docs/PHASE-2-JSON-EDITOR.md` und `docs/ENGINE-SYNC.md` sind die einzigen .md-Dateien, die in diesem Plan erlaubt sind (sie sind explizit angefordert, nicht "Documentation for documentation's sake"). Keine weiteren .md-Dateien anlegen.

    Mermaid-Diagramm: nutze ```mermaid-Fences. Halte es kompakt (<= 15 Knoten).
  </action>
  <verify>
    <automated>cd portal && npx playwright test phase2-flow.spec.ts</automated>
  </verify>
  <done>E2E-Test gruen. Beide Docs geschrieben mit Mermaid-Diagramm und vollstaendiger REST-API-Tabelle.</done>
</task>

</tasks>

<verification>
1. `uv run pytest -m integration -x` gruen.
2. `cd portal && npx playwright test` gruen.
3. `uv run ruff check . && cd portal && npm run lint` clean.
4. Manuelle Doku-Review: `docs/PHASE-2-JSON-EDITOR.md` und `docs/ENGINE-SYNC.md` sind in MD-Viewer lesbar, Diagramm rendert.
5. Smoke-Test mit `Fertigungsstruktur1_mit_AslFj.otx` (mittelgrosses Realmodell): Upload -> Convert -> Sim -> succeeded.
</verification>

<success_criteria>
- Bit-genauer Round-Trip auf Backend-Ebene verifiziert.
- E2E-UI-Flow gruen.
- Schema-Validation-Negative-Tests gruen.
- Doku reflektiert tatsaechlich gebaute Architektur.
- ROADMAP.md kann nach dieser Phase mit `[x] Phase 2` markiert werden.
</success_criteria>

<output>
After completion, create `.planning/phases/02-json-editor/02-06-SUMMARY.md` mit:
- Test-Counts (Unit, Integration, E2E)
- Tatsaechliche End-zu-End-Latenz fuer den E2E-Flow
- Liste der dokumentierten REST-Endpoints
- Verweis auf ROADMAP-Update (Phase 2 abgeschlossen, Foundation 2 erstmals wirksam)
- Ungeloeste TODOs, die in Phase 3 (Live-Viz) adressiert werden muessen
</output>
</content>
</invoke>