---
phase: 01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-
plan: 06
type: execute
wave: 4
depends_on: ["01.3-05"]
files_modified:
  - osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx
  - osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx
  - osim-ui/portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx
  - osim-ui/portal/src/viewers/PRess/matrix-common.tsx
  - osim-ui/portal/src/viewers/registry.ts
autonomous: false
requirements: [SC-7, SC-9]
must_haves:
  truths:
    - "Neuer Pfad osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx existiert (analog PRessBelegMatrix-Layout)."
    - "Neuer Pfad osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx existiert (Combobox-Toolbar)."
    - "Alter Pfad osim-ui/portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx ist gelöscht."
    - "Alter Pfad osim-ui/portal/src/viewers/PRess/matrix-common.tsx ist gelöscht (kein Konsument mehr)."
    - "Neuer PRessMengeMatrixViewer nutzt MatrixGrid + useBlockSelection + writeToDataTransfer/readFromDataTransfer aus @osim/graphobject (NICHT mehr matrix-common)."
    - "Viewer-Registry (registry.ts) ist auf neuen Pfad PRessMenge/ umgestellt."
    - "tsc 0 errors, ESLint 0 errors."
    - "User kann im Browser auf localhost:3002 ein Modell mit PAssozMenge-Instanzen laden, im Tree auf einen Durchlaufplan klicken, in der ChildDialog-Liste den PRessMengeMatrixViewer als Tab öffnen, eine Cell durch Linksklick anlegen (Toolbar-gesetzter Subklasse-Typ + Menge), eine Cell durch Rechtsklick löschen."
  artifacts:
    - path: "osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx"
      provides: "2D-Matrix-Viewer mit PAssozMenge-Cell-Edit"
      min_lines: 300
    - path: "osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx"
      provides: "Combobox-Toolbar: Subklassen-Typ + Mengen-Input"
      min_lines: 80
  key_links:
    - from: "PRessMenge/PRessMengeMatrixViewer.tsx"
      to: "@osim/graphobject MatrixGrid"
      via: "Import + Component-Aufruf"
      pattern: "from \"@osim/graphobject\""
    - from: "registry.ts"
      to: "PRessMenge/PRessMengeMatrixViewer"
      via: "Import-Pfad"
      pattern: "viewers/PRessMenge/PRessMengeMatrixViewer"
    - from: "PRessMengeMatrixViewer onCellEdit"
      to: "useModelStore.createObject(\"PAssozMengeXXX\", ...)"
      via: "Store-Dispatch beim Cell-Create"
      pattern: "createObject\\(\"PAssozMenge"
---

<objective>
PRessMengeMatrixViewer von der Legacy-`matrix-common.tsx` (1D-Tabelle) auf
die `@osim/graphobject` Matrix-Foundation migrieren — als 2D-Matrix
analog `PRessBelegMatrixViewer` (Welle 01.2-06):

  Zeilen   = PRessMenge-Objekte (Lager/Bestände) des Modells
  Spalten  = PDlplKnoten (m_lKnoten des aktuellen PDurchlaufplan)
  Cells    = PAssozMenge-Subklassen-Instanz (Erzgt / Verbr / VerbrZwischen / Abfr)
             mit Mengen-Wert; oder leer.

Interaktion 1:1 zum PRessBelegMatrixViewer + C++-Original-Pattern (siehe
PRessMengeMatrixViewer.cpp Z.861-1000):
  - Linksklick auf leere Cell  → Belegung anlegen mit Toolbar-Subklasse + Toolbar-Menge
  - Linksklick auf belegte Cell → No-Op
  - Rechtsklick auf belegte Cell → Belegung löschen
  - Ctrl/Shift + Klick → Block-Select (kein Paint)
  - Ctrl+C / Ctrl+V → Block-Copy/Paste über document-Level-Listener

Toolbar-Modi (1:1 zum C++ aus PRessMengeMatrixViewer.cpp Z.1419-1438):
  - m_cbMenge   → Zahl-Eingabe (im Original Dropdown 1/5/10/20/50/100; wir
                  machen ein <input type="number"> mit Default 1)
  - m_cbTyp     → Dropdown: Erzgt / Verbr / VerbrZwischen / Abfr
                  (im Original via Fill(pMeta=PAssozMenge::META, ...)
                  programmatisch aus Subklassen-MetaClass-Liste gebaut)

KEINE Generalisierung Richtung "ResourceMatrix"-HOC. Das ist Phase-4-Backlog
laut PRess/PRessMengeMatrixViewer.tsx Kommentar Z.10-21.

Output: 2 neue Files unter PRessMenge/ + Registry-Update + 2 alte Files
gelöscht. Erforderlich: User-Browser-UAT (Checkpoint), weil die Persistenz-
Pfade (Lock/Save) sich identisch zu Welle 01.2-H verhalten — wenn dort ein
Bug offen ist, vererbt er sich hier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md
@osim-ui/CLAUDE.md
</context>

<interfaces>
<!-- @osim/graphobject Matrix-API (aus packages/graphobject/src/matrix/): -->
import {
  MatrixGrid,                      // <MatrixGrid rows cols rowKey colKey cellLookup renderCell revision disabled cornerLabel />
  useBlockSelection,               // returns { selection, isSelected, handleCellPointerDown, clear }
  readFromDataTransfer,            // (dt) => MatrixClipboardPayload | null
  writeToDataTransfer,             // (dt, payload) => void
  type MatrixClipboardPayload,
  MATRIX_CLIPBOARD_MIME,
} from "@osim/graphobject";

<!-- Existierender PRessBelegMatrixViewer (Schablone für Migration, siehe Read-Datei): -->
osim-ui/portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx

<!-- Existierender PRessBelegMatrixToolbar (Schablone für Toolbar): -->
osim-ui/portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx

<!-- ModelStore-API (osim-ui/portal/src/stores/model-store.ts): -->
useModelStore.getState()
  .createObject(klass: string, attrs: object) -> oid
  .patchObject(oid: number, attrs: object) -> void
  .deleteObject(oid: number) -> void
  .appendSubRef(parent_oid: number, slot: 0, child_oid: number) -> void
  .removeSubRef(parent_oid: number, slot: 0, child_oid: number) -> void
  .wire.objects: Record<number, OBaseObj>

<!-- ViewerProps (osim-ui/portal/src/viewers/core/types.ts): -->
interface ViewerProps {
  obj: OBaseObj;                       // hier: das aktuelle PDurchlaufplan-Objekt
  allObjects: Record<number, OBaseObj>;
  disabled?: boolean;
}

<!-- Existierender Registry-Eintrag (osim-ui/portal/src/viewers/registry.ts): -->
// Aktuelle Importzeile zu PRessMengeMatrixViewer suchen und ersetzen:
// from "@/viewers/PRess/PRessMengeMatrixViewer"
//   → from "@/viewers/PRessMenge/PRessMengeMatrixViewer"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PRessMengeMatrixToolbar (Combobox + Mengen-Input) anlegen</name>
  <files>osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\src\viewers\PRessBelegMatrix\PRessBelegMatrixToolbar.tsx (Stil-Schablone 1:1)
    - C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\OSimPro\PRessMengeMatrixViewer.cpp Zeilen 1408-1450 (Original OViewInit-Pattern für m_cbMenge + m_cbTyp)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\CLAUDE.md (Brand-/Style-Guide-Pflichten)
    - osim-ui/app/static/schemas/v1/schemas.json nach Plan 05 (für die Klassen-Labels)
  </read_first>
  <behavior>
    - PRESSMENGE_TYPES exportierte Konstante enthält genau 4 Einträge:
      { value: "PAssozMengeErzgt", label: "Erzeuger", mengen_attr: "m_iMengeAus" },
      { value: "PAssozMengeVerbr", label: "Verbraucher", mengen_attr: "m_iMengeEin" },
      { value: "PAssozMengeVerbrZwischen", label: "Zwischen-Verbrauch", mengen_attr: "m_iMengeEin" },
      { value: "PAssozMengeAbfr", label: "Abfrage", mengen_attr: "m_iMengeAbfr" }
    - Default-Typ ist "PAssozMengeErzgt" (entspricht dem C++ m_cbTyp.SetCurSel(0) Z.1438).
    - Default-Menge ist 1 (Z.1422 m_cbMenge.SetWindowText("1")).
    - Toolbar rendert: 1 Combobox "Assoziations-Typ" + 1 Input "Menge"
      mit den shadcn-Tokens identisch zu PRessBelegMatrixToolbar.
  </behavior>
  <action>
    Datei `osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx`
    neu anlegen (Verzeichnis PRessMenge/ ggf. neu erstellen).

    Struktur 1:1 zur PRessBelegMatrixToolbar.tsx, aber mit
    PRessMenge-spezifischen Modi:

    ```tsx
    import * as React from "react";
    import { cn } from "@/lib/utils";

    /**
     * Subklassen-Typ-Optionen für die Cell-Create-Logik.
     * 1:1 aus C++ PRessMengeMatrixViewer::OViewInit (Z.1430-1438):
     * Fill(PAssozMenge::META, &m_cbTyp) iteriert über die META-Subklassen
     * und füllt die Combobox. Hier hart kodiert auf die 4 PAssozMenge-
     * Subklassen aus schemas.json (Plan 05).
     */
    export const PRESSMENGE_TYPES: Array<{
      value: "PAssozMengeErzgt" | "PAssozMengeVerbr" | "PAssozMengeVerbrZwischen" | "PAssozMengeAbfr";
      label: string;
      mengen_attr: "m_iMengeAus" | "m_iMengeEin" | "m_iMengeAbfr";
    }> = [
      { value: "PAssozMengeErzgt",        label: "Erzeuger",          mengen_attr: "m_iMengeAus" },
      { value: "PAssozMengeVerbr",        label: "Verbraucher",       mengen_attr: "m_iMengeEin" },
      { value: "PAssozMengeVerbrZwischen", label: "Zwischen-Verbrauch", mengen_attr: "m_iMengeEin" },
      { value: "PAssozMengeAbfr",         label: "Abfrage",           mengen_attr: "m_iMengeAbfr" },
    ];

    export type PRessMengeTypeValue = (typeof PRESSMENGE_TYPES)[number]["value"];

    export interface PRessMengeMatrixToolbarProps {
      typ: PRessMengeTypeValue;
      onTypChange: (v: PRessMengeTypeValue) => void;
      menge: number;
      onMengeChange: (v: number) => void;
      disabled?: boolean;
    }

    export const PRessMengeMatrixToolbar: React.FC<PRessMengeMatrixToolbarProps> = ({
      typ, onTypChange, menge, onMengeChange, disabled = false,
    }) => {
      // Layout 1:1 zur PRessBelegMatrixToolbar (label + select, label + input)
      // mit shadcn-Tokens (bg-card, border-border, focus:ring-primary/40).
    };
    ```

    Stil-Regeln aus CLAUDE.md (Brand-/Style-Guide-Pflichten):
    - Tokens (bg-card, border-border, focus:ring-primary/40), keine ad-hoc Hex.
    - Segoe UI als Body-Font (über font-sans-Default).
    - 4 px-Grid-Spacing.
    - `data-testid="matrix-toolbar"` am Wrapper für E2E.
    - `data-testid="combo-typ"` + `data-testid="combo-typ-select"` für die Typ-Combobox.
    - `data-testid="combo-menge"` + `data-testid="combo-menge-input"` für die Menge-Input.

    Menge-Input: `<input type="number" min="1" step="1">`. Bei Eingabe wird
    onMengeChange(Number(e.target.value)) aufgerufen. Falls Number Invalid →
    bleibt auf altem Wert.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path 'osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx'"</automated>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npx tsc --noEmit -p ."</automated>
  </verify>
  <done>
    File existiert, exportiert PRESSMENGE_TYPES + PRessMengeMatrixToolbar. tsc 0 errors.
  </done>
  <acceptance_criteria>
    - Source: Datei `osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx` existiert.
    - Source: Datei exportiert die Konstante `PRESSMENGE_TYPES` mit genau 4 Einträgen.
    - Source: Datei exportiert die React-Komponente `PRessMengeMatrixToolbar`.
    - Source: PRESSMENGE_TYPES enthält die 4 String-Substrings: "PAssozMengeErzgt", "PAssozMengeVerbr", "PAssozMengeVerbrZwischen", "PAssozMengeAbfr".
    - Source: Datei verwendet KEINE ad-hoc Hex-Strings (keine `#[0-9A-Fa-f]{6}`-Treffer im File).
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx tsc --noEmit -p ."` exit-code 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Neuer PRessMengeMatrixViewer (2D-Matrix) + alte Files löschen + Registry-Update</name>
  <files>
    osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx
    osim-ui/portal/src/viewers/registry.ts
    osim-ui/portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx   (DELETE)
    osim-ui/portal/src/viewers/PRess/matrix-common.tsx            (DELETE)
  </files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\src\viewers\PRessBelegMatrix\PRessBelegMatrixViewer.tsx (kanonische Schablone — komplett lesen)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\src\viewers\PRess\PRessMengeMatrixViewer.tsx (zu ersetzen — Soll-Spalte für Vergleichen)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\src\viewers\PRess\matrix-common.tsx (zu löschen)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\src\viewers\registry.ts (Import-Update)
    - .planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md (für Subklassen-Attr-Mapping)
    - C:\Users\JörgWFischer\PycharmProjects\OSim2004\OSimV01(Fj)\OSimPro\PRessMengeMatrixViewer.cpp Zeilen 787-820 (GetAssozMenge — Cell-Create-Pfad-Soll)
  </read_first>
  <behavior>
    - Test 1: PRessMengeMatrixViewer rendert mit leerem allObjects → Empty-State-Hinweis "Keine Mengen-Ressourcen im Modell" oder "Keine Knoten im Durchlaufplan".
    - Test 2: PRessMengeMatrixViewer rendert mit 2 PRessMenge + 3 PDlplKnoten + 1 PAssozMengeErzgt-Assoz im Wire → MatrixGrid mit 2 Zeilen, 3 Spalten, 1 Cell hat einen Status-Pill (oder Wert).
    - Test 3: Linksklick auf eine leere Cell → useModelStore.createObject("PAssozMengeErzgt", { m_sName: <auto>, m_iMengeAus: 1 }) wird aufgerufen.
    - Test 4: Linksklick mit Toolbar-Typ="PAssozMengeVerbr" + Toolbar-Menge=5 → createObject("PAssozMengeVerbr", { m_iMengeEin: 5 }).
    - Test 5: Rechtsklick auf belegte Cell → deleteObject(assoc.oid) wird aufgerufen.
    - Test 6: Ctrl+Klick → Block-Select (kein Paint via createObject).
    - Test 7: PRess/PRessMengeMatrixViewer.tsx existiert NICHT mehr nach dem Plan.
    - Test 8: PRess/matrix-common.tsx existiert NICHT mehr nach dem Plan.
    - Test 9: registry.ts importiert PRessMengeMatrixViewer vom neuen Pfad PRessMenge/.
  </behavior>
  <action>
    Hauptarbeit: neuer 2D-Matrix-Viewer + Cell-Create/Delete-Logik +
    Document-Level-Clipboard-Listener — komplett 1:1 zur PRessBelegMatrixViewer-
    Struktur, aber mit PRessMenge-spezifischer Cell-Semantik.

    ### Subtask 2a: Neuen Viewer-File anlegen

    Datei `osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx`
    neu anlegen. Struktur folgt PRessBelegMatrixViewer.tsx 1:1 mit folgenden
    Anpassungen:

    1. **Filter-Funktion** (analog `filterRows` in PRessBelegMatrixViewer):
       ```tsx
       function filterMengenRess(allObjects: Record<number, OBaseObj>): OBaseObj[] {
         return Object.values(allObjects)
           .filter((o) => o.klass === "PRessMenge")
           .sort((a, b) => a.oid - b.oid);
       }
       ```

    2. **Spalten-Resolution** (analog für PDurchlaufplan.m_lKnoten):
       Wire-Struktur ist dieselbe wie bei PRessBelegMatrixViewer.tsx
       (Z.366-372). NUR die Klass-Filterung der resultierenden Knoten ggf.
       lockern — im C++-Original werden alle PDlplKnoten erlaubt, kein
       Filter auf Subklassen (PRessMengeMatrixViewer.cpp Z.452-470
       walked m_lKnoten via m_lKnotenOber !=ONULL).

    3. **cellMap-Build** (analog Z.376-391, aber mit PAssozMenge):
       ```tsx
       // Für jeden Knoten: lies m_lAssozRess-Wrapper → für jeden PAssozMenge*-
       // Eintrag → setze cellMap[ressOid:colOid] = { assoc, menge }
       // Statt m_lRessourcen-LList-Indirektion (PAssozBeleg) ist bei PAssozMenge
       // m_lMengRess ein SCALAR-POINTER direkt auf die PRessMenge.
       const cellMap = new Map<string, MatrixCellValue>();
       const PASSOZMENGE_KLASSEN = new Set([
         "PAssozMengeErzgt", "PAssozMengeVerbr",
         "PAssozMengeVerbrZwischen", "PAssozMengeAbfr",
       ]);
       for (const col of cols) {
         const assozOids = readWrapperList(col, "m_lAssozRess", allObjects);
         for (const assozOid of assozOids) {
           const assoz = allObjects[assozOid];
           if (!assoz || !PASSOZMENGE_KLASSEN.has(assoz.klass)) continue;
           const mengeRessOidRaw = assoz.attrs.m_lMengRess;
           if (typeof mengeRessOidRaw !== "number") continue;
           cellMap.set(`${mengeRessOidRaw}:${col.oid}`, {
             assoc: assoz,
             menge: extractMenge(assoz),  // siehe unten
           });
         }
       }
       ```
       `readWrapperList` wird 1:1 aus PRessBelegMatrixViewer.tsx kopiert
       (oder in einen geteilten Helper extrahiert — Plan-Pragma: KOPIEREN,
       weil Generalisierung Phase-4-Backlog).

       `extractMenge(assoz)` schlägt das passende Mengen-Attr per
       PRESSMENGE_TYPES-Mapping nach:
       ```tsx
       function extractMenge(assoz: OBaseObj): number {
         const def = PRESSMENGE_TYPES.find((t) => t.value === assoz.klass);
         if (!def) return 0;
         const raw = assoz.attrs[def.mengen_attr];
         return typeof raw === "number" ? raw : 0;
       }
       ```

    4. **MatrixCellValue**:
       ```tsx
       interface MatrixCellValue {
         assoc: OBaseObj;
         menge: number;
       }
       ```

    5. **MatrixMengeCell** (analog MatrixStatusCell aus PRessBeleg):
       Zeigt die Menge als Pill mit Subklassen-Farbe an. Farb-Mapping:
       Erzgt → grün (positiv/Erzeuger), Verbr → amber (Verbrauch),
       VerbrZwischen → blau (neutral), Abfr → grau (nur Abfrage).
       Verwendet shadcn-Tokens (bg-[var(--status-finished-bg)] etc.).
       Klick-Handler:
       - Linksklick auf leere Cell → onCellEdit(rowOid, colOid, "create")
       - Linksklick auf belegte Cell → No-Op (wie PRessBeleg)
       - Rechtsklick auf belegte Cell → onCellEdit(rowOid, colOid, "delete")
       - Ctrl/Shift → Block-Select via onPointerDown.

    6. **onCellEdit** (Cell-Create-Pfad 1:1 zu C++ Z.787-820 GetAssozMenge):
       ```tsx
       const onCellEdit = React.useCallback(
         (rowOid: number, colOid: number, action: "create" | "delete") => {
           const store = useModelStore.getState();
           const rowRess = allObjects[rowOid];
           const colKnoten = allObjects[colOid];
           if (!rowRess || !colKnoten) return;

           const existing = matrix.cellMap.get(`${rowOid}:${colOid}`) ?? null;

           if (action === "delete" && existing) {
             // 1:1 OnRButtonDown → knoten->RemovePSimObj(assoz)
             const assoc = existing.assoc;
             // Aus dem Knoten-Wrapper m_lAssozRess entfernen:
             const knotenWrapperOidRaw = colKnoten.attrs.m_lAssozRess;
             if (typeof knotenWrapperOidRaw === "number") {
               store.removeSubRef(knotenWrapperOidRaw, 0, assoc.oid);
             }
             store.deleteObject(assoc.oid);
             bumpRevision();
             return;
           }

           if (action === "create" && !existing) {
             // 1:1 GetAssozMenge: pMeta = m_cbTyp; oobj = pMeta->New(...);
             //                    aress->m_lMengRess = menge;
             // Schritt 1: Knoten-Wrapper sicherstellen (PAssozRessourceLList)
             const knotenWrapperOidRaw = colKnoten.attrs.m_lAssozRess;
             let knotenWrapperOid: number;
             if (typeof knotenWrapperOidRaw === "number") {
               knotenWrapperOid = knotenWrapperOidRaw;
             } else {
               knotenWrapperOid = store.createObject("PAssozRessourceLList", {});
               store.patchObject(colKnoten.oid, { m_lAssozRess: knotenWrapperOid });
             }
             // Schritt 2: neue PAssozMenge<Typ> mit Mengen-Attr und m_lMengRess
             const typeDef = PRESSMENGE_TYPES.find((t) => t.value === typ);
             if (!typeDef) return;
             const newAssoz = store.createObject(typ, {
               m_sName: `${typeDef.label} ${colKnoten.attrs.m_sName ?? ""}→${rowRess.attrs.m_sName ?? ""}`,
               m_lMengRess: rowOid,
               [typeDef.mengen_attr]: menge,
             });
             // Schritt 3: in Knoten-Wrapper einhängen
             store.appendSubRef(knotenWrapperOid, 0, newAssoz);
             bumpRevision();
           }
         },
         [allObjects, matrix.cellMap, typ, menge, bumpRevision],
       );
       ```

    7. **Document-Level-Clipboard-Listener** (1:1 aus PRessBeleg
       Z.487-546, aber Payload-Typ ist die Menge + Subklasse statt nur Status):
       ```tsx
       interface ClipboardCellPayload {
         klass: string;    // "PAssozMengeErzgt" etc.
         menge: number;
       }
       ```
       Auf Paste: für jede Quell-Cell wird onCellEdit (zwei-Schritte:
       delete + create) ausgeführt mit den Payload-Werten — NICHT mit den
       Toolbar-Modi. Wenn die Subklasse im Payload nicht in PRESSMENGE_TYPES
       → skip.

    ### Subtask 2b: Alte Files löschen + Registry-Update

    1. `osim-ui/portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx` löschen.
    2. `osim-ui/portal/src/viewers/PRess/matrix-common.tsx` löschen.
       (Vorab via `grep -r "matrix-common" osim-ui/portal/src` verifizieren,
       dass keine anderen Importe darauf zeigen. Falls ja: das wäre Plan-Risiko;
       in das SUMMARY aufnehmen und auf den Auftraggeber zugehen.)
    3. `osim-ui/portal/src/viewers/registry.ts` öffnen und die Import-
       Pfad-Zeile für PRessMengeMatrixViewer ändern von
       `@/viewers/PRess/PRessMengeMatrixViewer` auf
       `@/viewers/PRessMenge/PRessMengeMatrixViewer`.

    ### Subtask 2c: tsc + lint clean

    Nach allen Änderungen:
    ```
    docker exec osim-ui-portal-1 sh -c "cd /app && npx tsc --noEmit -p ."
    docker exec osim-ui-portal-1 sh -c "cd /app && npm run lint"
    ```
    Beide müssen 0 errors liefern. Falls neue ESLint-Warnings auftauchen
    (z.B. unused imports nach Löschen), aufräumen.

    ### Wichtig: KEINE Touch an Save-/Lock-Pfaden

    Die existierenden Save-/Lock-Pfade (Welle 01.2-H Status laut ROADMAP-
    SC-9: "Persistenz hängt am gleichen Save-/Lock-Stand wie Welle 1.2-H —
    falls Bug noch offen, übernehmen wir denselben Stand") werden NICHT
    angefasst. Falls Auto-Save dort hängt, ist das ein bekanntes Risiko
    und wird in Plan 07 dokumentiert (Persistenz-E2E darf `test.fixme` sein).
  </action>
  <verify>
    <automated>powershell -Command "Test-Path 'osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx'"</automated>
    <automated>powershell -Command "(Test-Path 'osim-ui/portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx') -or (Test-Path 'osim-ui/portal/src/viewers/PRess/matrix-common.tsx')"</automated>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npx tsc --noEmit -p ."</automated>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npm run lint"</automated>
  </verify>
  <done>
    Neuer Viewer im PRessMenge/-Verzeichnis. Alte Files weg. Registry
    pointing auf neuen Pfad. tsc 0, lint 0. Vitest-Lauf (vor Plan 07
    Test-Erweiterung) zeigt KEINE Regressionen gegenüber Stand Vor-Plan-06.
  </done>
  <acceptance_criteria>
    - Source: Datei `osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx` existiert.
    - Source: Datei importiert `MatrixGrid`, `useBlockSelection`, `readFromDataTransfer`, `writeToDataTransfer` aus `"@osim/graphobject"`.
    - Source: Datei enthält das Substring `createObject("PAssozMengeErzgt"` ODER `createObject(typ` (Cell-Create-Pfad).
    - Source: Datei importiert `PRessMengeMatrixToolbar` und `PRESSMENGE_TYPES` aus `"./PRessMengeMatrixToolbar"`.
    - Source: Datei `osim-ui/portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx` existiert NICHT mehr.
    - Source: Datei `osim-ui/portal/src/viewers/PRess/matrix-common.tsx` existiert NICHT mehr.
    - Source: registry.ts enthält das Substring `@/viewers/PRessMenge/PRessMengeMatrixViewer`.
    - Source: registry.ts enthält KEIN Substring `@/viewers/PRess/PRessMengeMatrixViewer`.
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx tsc --noEmit -p ."` exit-code 0.
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npm run lint"` exit-code 0.
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run"` exit-code 0 (keine Regressionen — alte PRess/matrix-common-Specs werden ggf. obsolet, in dem Fall in Plan 07 ersetzt).
    - Behavior: PRessMengeMatrixViewer.tsx hat ≥ 300 Zeilen (signifikante 2D-Logik).
    - Behavior: Datei enthält KEINE ad-hoc Hex-Strings (`#[0-9A-Fa-f]{6}`).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: User-Browser-UAT PRessMengeMatrixViewer</name>
  <what-built>
    Neuer 2D-Matrix-Viewer für PRessMenge × PDlplKnoten × PAssozMenge*
    unter osim-ui/portal/src/viewers/PRessMenge/. Alte 1D-Tabelle und
    matrix-common.tsx gelöscht. Toolbar mit Subklassen-Dropdown + Mengen-Input.
    Cell-Create / Cell-Delete / Block-Select / Block-Copy/Paste analog
    PRessBelegMatrixViewer (Welle 01.2-E).
  </what-built>
  <how-to-verify>
    1. Container-Stack ist up: `docker compose ps` zeigt portal + api running.
    2. Browser http://localhost:3002 öffnen, Login mit jwfischer69@gmail.com / 123456.
    3. Modell mit PAssozMenge-Instanzen aus der Bibliothek laden (oder
       eines hochladen — siehe Plan 04 Fixture passozmenge_minimal.otx als
       Quelle, sofern die Wire-API es akzeptiert).
    4. Im Tree einen PDurchlaufplan auswählen, dann via ChildDialog-Liste
       (oder Hint-Switcher) den PRessMengeMatrixViewer-Tab öffnen.
    5. Verifiziere:
       - 2D-Matrix mit Zeilen = PRessMenge, Spalten = PDlplKnoten
       - Vorhandene PAssozMenge-Instanzen erscheinen als gefüllte Cells
         mit Subklasse-Pill und Mengen-Wert
       - Toolbar: Dropdown wechselt zwischen Erzgt/Verbr/VerbrZwischen/Abfr.
       - Menge-Input akzeptiert Zahlen.
       - Linksklick auf leere Cell legt eine Belegung an (vom in Toolbar
         gewählten Typ + Menge). Cell sollte sich sofort visuell füllen.
       - Rechtsklick auf belegte Cell löscht sie.
       - Ctrl+Klick selektiert Cells in einem Block (visuelles Highlight).
       - Ctrl+C dann Ctrl+V kopiert die Selektion an die nächste angeklickte
         Cell.
    6. Browser-Console: keine roten Errors.
    7. Persistenz (F5-Reload) ist NICHT zwingend in diesem Checkpoint —
       laut SC-9 hängt sie am Welle 01.2-H Save/Lock-Stand. Falls Reload
       die Cells verliert, dokumentieren wir das im SUMMARY und in Plan 07
       als `test.fixme`.
    8. Link mit dabei: http://localhost:3002
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend ↔ Wire-API | Cell-Create/Delete mutiert wire über useModelStore — denselbe Pfad wie PRessBelegMatrixViewer (SC-Status: Welle 01.2-H bekannt). |
| User-Input ↔ React-DOM | Toolbar-Mengen-Input + Cell-Pills rendern user-attributed Strings (z.B. m_sName); XSS-Risiko durch React's escapes mitigated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3.06-01 | Cross-Site Scripting | Cell-Pill rendert assoz.attrs.m_sName | mitigate | React's JSX-Escape kümmert sich automatisch (kein dangerouslySetInnerHTML). Cell-Pill rendert `{statusLabel}` als TextContent, kein HTML-Parsing. |
| T-01.3.06-02 | Tampering | Toolbar-Mengen-Input akzeptiert negative Zahlen | mitigate | `<input type="number" min="1" step="1">` blocked Browser-seitig; PRessMengeMatrixToolbar onMengeChange-Handler verifiziert Number-Validity. |
| T-01.3.06-03 | Tampering | Cell-Create dispatched mit falscher Subklasse | mitigate | typeDef-Lookup über PRESSMENGE_TYPES, falls nicht gefunden → early return; getypt via TypeScript-Discriminated-Union. |
| T-01.3.06-04 | Information Disclosure | Container-Sync via docker compose cp | accept | Schema-Datei enthält nur Klassen-Strukturen, keine Secrets. |
| T-01.3.06-SC | Tampering | Package-Installs | accept | Plan modifiziert nur bestehende Source-Files; keine neuen npm-Pakete. |
</threat_model>

<verification>
- tsc 0 errors, lint 0 errors, Vitest grün (kein Regressions-Crash).
- Browser-UAT durch User approved.
- Alte Files weg, Registry korrekt.
</verification>

<success_criteria>
- SC-7 (PRessMengeMatrixViewer migriert, matrix-common.tsx gelöscht).
- SC-9 (UI-Cell-Edit funktioniert im Browser).
</success_criteria>

<output>
Create `.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-06-SUMMARY.md` when done.
</output>
</content>
</invoke>