---
phase: 01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-
plan: 02
type: execute
wave: 2
depends_on: ["01.3-01"]
files_modified:
  - engine/src/osim_engine/io/otx_loader.py
  - engine/tests/unit/io/test_otx_loader.py
autonomous: true
requirements: [SC-2, SC-5]
must_haves:
  truths:
    - "otx_loader._HANDLERS enthält Einträge für PAssozMenge, PAssozMengeErzgt, PAssozMengeVerbr, PAssozMengeVerbrZwischen, PAssozMengeAbfr."
    - "Jeder Handler instanziiert die korrespondierende Klasse aus osim_engine.resources.assoziation.menge."
    - "Jeder Handler kopiert m_sName via copy_scalars in das Sim-Objekt."
    - "Konkrete Subklassen-Handler kopieren zusätzlich ihre jeweiligen Mengen-Attrs: Erzgt → m_iMengeAus; Verbr/VerbrZwischen → m_iMengeEin; Abfr → m_iMengeAbfr."
    - "Jeder Handler resolved m_lMengRess via resolve_ref → py.m_lMengRess (Scalar-Pointer auf PRessMenge)."
    - "Existierende Loader-Tests bleiben grün (Stand: 70+ Tests)."
  artifacts:
    - path: "engine/src/osim_engine/io/otx_loader.py"
      provides: "5 neue Handler-Klassen + 5 register_handler-Aufrufe"
      contains: "register_handler(\"PAssozMengeErzgt\")"
    - path: "engine/tests/unit/io/test_otx_loader.py"
      provides: "Unit-Tests dass die 5 Handler registriert sind und ihre Attrs lesen"
      contains: "PAssozMengeAbfr"
  key_links:
    - from: "otx_loader.py PAssozMengeErzgt-Handler"
      to: "osim_engine.resources.assoziation.menge.PAssozMengeErzgt"
      via: "from-Import + Konstruktor-Aufruf"
      pattern: "from osim_engine.resources.assoziation.menge import"
    - from: "otx_loader.py m_lMengRess-Resolution"
      to: "PRessMenge-Instanz aus loader.instances"
      via: "resolve_ref(loader, obj, \"m_lMengRess\")"
      pattern: "resolve_ref\\(loader, obj, \"m_lMengRess\""
---

<objective>
Engine-Loader-Handler für die 5 PAssozMenge-Klassen (1 abstract + 4 konkret)
in `engine/src/osim_engine/io/otx_loader.py` registrieren.

Pattern-Quelle 1:1: `_PAssozBelegHandler` in otx_loader.py:782-796 (Welle
2026-05-24 PAssozBeleg-Roundtrip) — copy_scalars + resolve_list/resolve_ref
im wire()-Pass.

Wichtige Abweichung zum PAssozBeleg-Pattern: PAssozMenge hat KEINEN
LList-Wrapper auf eine Liste von Ressourcen (wie PAssozBeleg.m_lRessBeleg),
sondern einen SCALAR-POINTER `m_lMengRess` auf eine EINZELNE `PRessMenge`.
Daher resolve_ref (nicht resolve_list).

Pattern-Quelle für Scalar-Pointer-Resolution: `_PTagRessHandler` in
otx_loader.py:717-727 — `py.m_oRessBeleg = resolve_ref(loader, obj, "m_oRessBeleg")`.

Output: 5 neue Handler-Klassen (Decorator `@register_handler`) + Unit-Tests
in `engine/tests/unit/io/test_otx_loader.py`.

KEIN Writer-Code in diesem Plan. Writer ist Plan 03.
KEIN Round-Trip-Test. Round-Trip ist Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md
@osim-ui/CLAUDE.md
</context>

<interfaces>
<!-- ClassHandler-Basis (otx_loader.py Z.43-54) -->
class ClassHandler:
    def instantiate(self, loader: "OtxLoader", obj: OtxObject) -> Any | None
    def wire(self, loader: "OtxLoader", py_obj: Any, obj: OtxObject) -> None

<!-- Decorator (otx_loader.py Z.57-64) -->
def register_handler(*klass_names: str) -> Callable[[type], type]

<!-- Helpers (otx_loader.py Z.212-253) -->
def resolve_list(loader, obj, list_attr: str) -> list[Any]
def resolve_ref(loader, obj, attr: str) -> Any | None  # liefert None wenn fehlt
def copy_scalars(py_obj, otx_obj, attrs: tuple[str, ...]) -> None  # nur wenn key existiert

<!-- Pattern-Schablone PAssozBeleg (otx_loader.py Z.782-796) -->
@register_handler("PAssozBeleg")
class _PAssozBelegHandler(ClassHandler):
    def instantiate(self, loader, obj):
        from osim_engine.resources.assoziation.beleg import PAssozBeleg
        a = PAssozBeleg(loader.simulator)
        copy_scalars(a, obj, ("m_sName",))
        return a
    def wire(self, loader, py, obj):
        for ress in resolve_list(loader, obj, "m_lRessBeleg"):
            if hasattr(py, "m_lRessBeleg") and isinstance(py.m_lRessBeleg, list):
                if ress not in py.m_lRessBeleg:
                    py.m_lRessBeleg.append(ress)

<!-- Pattern-Schablone für Scalar-Pointer (otx_loader.py Z.717-727 PTagRess): -->
@register_handler("PTagRess")
class _PTagRessHandler(ClassHandler):
    def instantiate(self, loader, obj):
        from osim_engine.resources.einsatzzeit import PTagRess
        tag = obj.attrs.get("m_iTag", 0)
        return PTagRess(m_iTag=int(tag) if isinstance(tag, int) else 0)
    def wire(self, loader, py, obj):
        py.m_oRessBeleg = resolve_ref(loader, obj, "m_oRessBeleg")

<!-- Soll-Klassen (engine/src/osim_engine/resources/assoziation/menge.py): -->
class PAssozMenge(PAssozRessource):
    m_lMengRess: PRessMenge | None = None  # in __init__

class PAssozMengeErzgt(PAssozMenge):
    m_iMengeAus: int = 1

class PAssozMengeVerbr(PAssozMenge):
    m_iMengeEin: int = 1

class PAssozMengeVerbrZwischen(PAssozMengeVerbr):
    pass  # erbt m_iMengeEin

class PAssozMengeAbfr(PAssozMenge):
    m_iMengeAbfr: int = 1
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 5 register_handler-Klassen in otx_loader.py einfügen + import</name>
  <files>engine/src/osim_engine/io/otx_loader.py</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_loader.py
      (komplett — minimum die Sektionen "Helpers" Z.207-253 und "Assoziationen
       (Beleg + Menge)" Z.741-799, plus die _PTagRessHandler-Sektion Z.717-728)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\resources\assoziation\menge.py
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\resources\assoziation\base.py
    - .planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md (Sektionen 2 + 6 — verbindliche Attr-Tupel)
  </read_first>
  <behavior>
    - Test 1: Nach Plan-Execute enthält otx_loader._HANDLERS die 5 Schlüssel "PAssozMenge", "PAssozMengeErzgt", "PAssozMengeVerbr", "PAssozMengeVerbrZwischen", "PAssozMengeAbfr".
    - Test 2: Handler-Klasse für PAssozMengeErzgt liefert bei instantiate(loader_stub, obj={attrs={m_sName:"E1"}}) ein PAssozMengeErzgt-Objekt mit m_sName == "E1".
    - Test 3: Handler-Klasse für PAssozMengeErzgt liest m_iMengeAus aus obj.attrs.
    - Test 4: Handler-Klasse für PAssozMengeVerbr liest m_iMengeEin aus obj.attrs.
    - Test 5: Handler-Klasse für PAssozMengeVerbrZwischen liest m_iMengeEin aus obj.attrs (geerbt).
    - Test 6: Handler-Klasse für PAssozMengeAbfr liest m_iMengeAbfr aus obj.attrs.
    - Test 7: wire() für jeden konkreten Handler setzt py.m_lMengRess auf das Ergebnis von resolve_ref(loader, obj, "m_lMengRess") (kann None sein wenn Attr fehlt).
  </behavior>
  <action>
    In `engine/src/osim_engine/io/otx_loader.py` im Abschnitt
    "Assoziationen (Beleg + Menge)" (nach `register_skip("PAssozBelegLinkInfo")`,
    aktuell Z.798) FÜNF neue Handler-Klassen einfügen, exakt im Stil von
    `_PAssozBelegHandler` (Z.782-796) — copy_scalars + import-im-instantiate.

    Klasse 1: PAssozMenge (abstract — wird i.A. in OTX-Files nicht als
    direkter Typ vorkommen, aber laut SC-2 wird sie gefordert; instantiate
    wirft `NotImplementedError` ist falsch — wir instanziieren ganz normal,
    weil PAssozMenge konkret-instanzierbar ist als Python-Klasse, aber
    semantisch nie genutzt würde; der Handler ist da, falls eine OTX-Datei
    sie explizit nennt). Implementation:
    ```
    @register_handler("PAssozMenge")
    class _PAssozMengeHandler(ClassHandler):
        def instantiate(self, loader, obj):
            from osim_engine.resources.assoziation.menge import PAssozMenge
            a = PAssozMenge(loader.simulator)
            copy_scalars(a, obj, ("m_sName",))
            return a
        def wire(self, loader, py, obj):
            py.m_lMengRess = resolve_ref(loader, obj, "m_lMengRess")
    ```

    Klassen 2-5 (PAssozMengeErzgt / PAssozMengeVerbr / PAssozMengeVerbrZwischen / PAssozMengeAbfr):
    Identisches Muster, mit unterschiedlichen SCALAR-Tupeln je AUDIT.md Sektion 2:
    - Erzgt: copy_scalars(a, obj, ("m_sName", "m_iMengeAus"))
    - Verbr: copy_scalars(a, obj, ("m_sName", "m_iMengeEin"))
    - VerbrZwischen: copy_scalars(a, obj, ("m_sName", "m_iMengeEin"))
       — auch wenn es das Attr erbt, der Loader muss es per copy_scalars
         setzen (sonst bleibt der Default 1)
    - Abfr: copy_scalars(a, obj, ("m_sName", "m_iMengeAbfr"))

    Alle vier konkreten Handler haben dasselbe wire():
    ```
    def wire(self, loader, py, obj):
        py.m_lMengRess = resolve_ref(loader, obj, "m_lMengRess")
    ```

    Die Imports erfolgen WIE bei PAssozBeleg im instantiate (lazy/inside),
    NICHT auf Modul-Ebene — Konsistenz mit existierender Convention im File.

    KEINE Sub-Listen-Resolution für m_lKnoten / m_lOberAssoz / m_lUnterAssoz
    in diesem Plan — PAssozBeleg macht das auch nicht, das ist eine eigene
    Question (PAssozRessource-Basis-Refs). Falls in AUDIT.md Sektion 6 als
    notwendig markiert, dann auch hier, sonst weglassen (Konsistenz mit
    bestehendem Stand der Bel'-Familie).

    Reihenfolge der 5 register_handler-Blöcke: PAssozMenge zuerst, dann
    Erzgt / Verbr / VerbrZwischen / Abfr in dieser Reihenfolge (analog odh).
  </action>
  <verify>
    <automated>cd engine && uv run python -c "from osim_engine.io import otx_loader; assert all(k in otx_loader._HANDLERS for k in ('PAssozMenge','PAssozMengeErzgt','PAssozMengeVerbr','PAssozMengeVerbrZwischen','PAssozMengeAbfr')), sorted(otx_loader._HANDLERS.keys())"</automated>
    <automated>cd engine && uv run pytest tests/integration/test_otx_loader.py tests/integration/io/ -x -q</automated>
  </verify>
  <done>
    `_HANDLERS` enthält die 5 neuen Klassen-Schlüssel. Bestehende Loader-Tests grün (0 Regressionen).
  </done>
  <acceptance_criteria>
    - Source: otx_loader.py enthält 5 Zeilen mit dem Pattern `@register_handler("PAssozMenge...")`.
    - Source: otx_loader.py enthält 5 Klassen-Definitionen mit dem Pattern `class _PAssozMenge.*Handler(ClassHandler):`.
    - Source: Jede der 5 Handler-Klassen hat eine `instantiate`- und `wire`-Methode.
    - Source: Die 4 konkreten Handler kopieren ihr jeweiliges m_iMenge*-Attr via copy_scalars.
    - Test: `uv run python -c "from osim_engine.io import otx_loader; assert all(k in otx_loader._HANDLERS for k in ('PAssozMenge','PAssozMengeErzgt','PAssozMengeVerbr','PAssozMengeVerbrZwischen','PAssozMengeAbfr'))"` exit-code 0.
    - Test: `uv run pytest tests/integration/test_otx_loader.py -q` exit-code 0 (keine Regressionen).
    - Behavior: import-Statements für die 5 Sim-Klassen sind innerhalb der `instantiate`-Methode (lazy, analog PAssozBeleg).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-Tests für die 5 Handler in test_otx_loader.py</name>
  <files>engine/tests/unit/io/test_otx_loader.py</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\unit\io\test_otx_writer.py (Stilvorlage; ist im selben Verzeichnis)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\integration\test_otx_loader.py (Pattern für Loader-Tests)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_loader.py (insbesondere die geänderte Sektion und die OtxObject-Datenstruktur)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_reader.py (für OtxObject- und OtxFile-Konstruktion in Tests)
  </read_first>
  <behavior>
    - Test test_passozmenge_handlers_registered: Importiert otx_loader, prüft 5 Keys in _HANDLERS.
    - Test test_passozmenge_erzgt_handler_reads_m_iMengeAus: Baut OtxObject-Stub mit attrs={m_sName:"E1",m_iMengeAus:5}, ruft handler.instantiate(loader_stub, obj), prüft typ + attrs.
    - Test test_passozmenge_verbr_handler_reads_m_iMengeEin: analog mit m_iMengeEin.
    - Test test_passozmenge_verbr_zwischen_handler_reads_m_iMengeEin: analog (verifiziert dass auch die geerbte Subklasse das Attr nimmt).
    - Test test_passozmenge_abfr_handler_reads_m_iMengeAbfr: analog.
    - Test test_passozmenge_handler_wires_m_lMengRess: Baut zwei OtxObjects — PRessMenge mit oid=10, PAssozMengeErzgt mit attrs={m_sName:"E1",m_iMengeAus:3,m_lMengRess:10}; pre-populate loader.instances[10] mit einem Stub-PRessMenge; ruft handler.wire(...); prüft py.m_lMengRess is the stub.
    - Test test_passozmenge_handler_wires_m_lMengRess_none_when_missing: wire() ohne m_lMengRess in attrs → py.m_lMengRess is None (resolve_ref-Default).
  </behavior>
  <action>
    Datei `engine/tests/unit/io/test_otx_loader.py` neu anlegen (falls noch
    nicht existent) oder erweitern. Mindestens 6 Test-Funktionen schreiben,
    Pattern wie im Behavior-Block.

    Verwendet werden OtxObject-Stubs (siehe otx_reader.py für die Datenklasse;
    Felder: klass, oid, attrs, sub_refs). Loader-Stub: ein OtxLoader-Objekt
    mit minimal befülltem `instances`-Dict und `simulator=PSimulator()`.

    Beispiel-Skeleton (Wortlaut anpassen):
    ```python
    from osim_engine.io import otx_loader
    from osim_engine.io.otx_reader import OtxObject, OtxFile
    from osim_engine.pps.simulator import PSimulator

    def _stub_loader_with(instances=None):
        from osim_engine.io.otx_loader import OtxLoader
        ld = OtxLoader()
        ld.simulator = PSimulator()
        ld.instances = instances or {}
        ld.otx = OtxFile(by_oid={}, header={}, declarations=[])
        return ld

    def test_passozmenge_handlers_registered():
        for k in ("PAssozMenge","PAssozMengeErzgt","PAssozMengeVerbr",
                  "PAssozMengeVerbrZwischen","PAssozMengeAbfr"):
            assert k in otx_loader._HANDLERS

    def test_passozmenge_erzgt_handler_reads_m_iMengeAus():
        h = otx_loader._HANDLERS["PAssozMengeErzgt"]
        obj = OtxObject(klass="PAssozMengeErzgt", oid=42,
                        attrs={"m_sName": "E1", "m_iMengeAus": 5},
                        sub_refs=[])
        ld = _stub_loader_with()
        py = h.instantiate(ld, obj)
        assert type(py).__name__ == "PAssozMengeErzgt"
        assert py.m_sName == "E1"
        assert py.m_iMengeAus == 5
    ```

    Beim Wire-Test: PRessMenge-Stub-Instanz vor-erstellen, in
    `ld.instances[10] = <stub>` legen, ld.otx.by_oid[10] = OtxObject-für-PRessMenge.
    Dann handler.wire(ld, py, obj) aufrufen und prüfen.

    Verzeichnis-Vorbereitung: Falls `engine/tests/unit/io/__init__.py` oder
    `engine/tests/unit/__init__.py` nicht existieren, anlegen (leere Files).
    Die existierende Konvention prüft sich aus `engine/tests/unit/io/test_otx_writer.py`.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/unit/io/test_otx_loader.py -v</automated>
    <automated>cd engine && uv run pytest -x -q</automated>
  </verify>
  <done>
    Mindestens 6 neue Tests im File, alle grün. Gesamtes Test-Set (uv run pytest) grün, keine Regressionen.
  </done>
  <acceptance_criteria>
    - Source: Datei `engine/tests/unit/io/test_otx_loader.py` existiert.
    - Source: Datei enthält mindestens 6 Funktionen, die mit `def test_passozmenge` beginnen.
    - Test: `cd engine && uv run pytest tests/unit/io/test_otx_loader.py -v` exit-code 0.
    - Test: `cd engine && uv run pytest -x -q` exit-code 0 (Gesamt-Suite grün).
    - Behavior: test_passozmenge_handlers_registered prüft alle 5 Klassen-Schlüssel.
    - Behavior: test_passozmenge_handler_wires_m_lMengRess setzt py.m_lMengRess auf eine Instanz aus loader.instances.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OTX-File ↔ Loader | Malformed OTX-Input könnte beim Loader-Run einen Crash provozieren (DoS via Modell-Upload, später relevant für UI). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3.02-01 | Denial of Service | Handler.instantiate bei fehlenden Attrs | mitigate | copy_scalars + resolve_ref tolerieren fehlende Keys (kein KeyError) — bestehendes Pattern aus _PAssozBelegHandler. Tests verifizieren das via test_passozmenge_handler_wires_m_lMengRess_none_when_missing. |
| T-01.3.02-02 | Tampering | Phantasie-Attr-Namen statt C++-Original | mitigate | AUDIT.md (Plan 01) ist verbindliche Quelle; Acceptance-Criteria zwingen genau die m_iMengeAus/Ein/Abfr-Identifier. |
| T-01.3.02-03 | Tampering | npm/pip-Package-Install | accept | Plan modifiziert nur bestehende Source-Files, kein neuer Package-Install. RESEARCH.md Package-Audit nicht erforderlich. |
</threat_model>

<verification>
- `cd engine && uv run pytest` ist grün mit keinen Regressionen ggü. Stand 2026-05-28 (70+ Tests).
- Die 5 neuen Handler sind über `otx_loader._HANDLERS.keys()` listbar.
- Loader-Set-Equality-Check in tests/integration/io/test_otx_roundtrip.py (`test_writer_handles_all_known_loader_classes`) zeigt nach Plan 02 die 4 KONKRETEN PAssozMenge-Subklassen als "Writer fehlt"-Diff — das wird in Plan 03 grün gemacht (per Design).
</verification>

<success_criteria>
- 5 Handler registriert, alle Unit-Tests grün, alle bestehenden Engine-Tests grün.
- Plan 03 kann darauf aufbauen, ohne otx_loader.py erneut anzufassen.
</success_criteria>

<output>
Create `.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-02-SUMMARY.md` when done.
</output>
</content>
</invoke>