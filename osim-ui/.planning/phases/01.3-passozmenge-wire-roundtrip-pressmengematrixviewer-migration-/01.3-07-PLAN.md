---
phase: 01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-
plan: 07
type: execute
wave: 5
depends_on: ["01.3-06"]
files_modified:
  - osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx
  - osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewerClipboard.spec.tsx
  - osim-ui/portal/e2e/passozmenge-block-copy-paste.spec.ts
autonomous: false
requirements: [SC-8, SC-10]
must_haves:
  truths:
    - "PRessMengeMatrixViewer.spec.tsx hat ≥ 8 Vitest-Specs für 2D-Matrix-Rendering + Cell-Edit + Cell-Delete + Toolbar-Modus-Switch."
    - "PRessMengeMatrixViewerClipboard.spec.tsx hat ≥ 1 Spec für Document-Level-Copy/Paste-Listener (analog PRessBeleg-Clipboard-Spec)."
    - "Playwright-E2E passozmenge-block-copy-paste.spec.ts läuft mindestens den Block-Copy/Paste-Cycle gegen den Live-Stack (analog matrix-cell-edit-persistence.spec.ts Test 2)."
    - "Persistenz-E2E darf test.fixme sein, falls Save/Lock-Bug aus Welle 01.2-H noch offen ist."
    - "Vitest gesamt grün (Stand: 379+, kein Regression-Crash)."
  artifacts:
    - path: "osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx"
      provides: "≥ 8 Vitest-Specs"
      contains: "PRessMengeMatrixViewer"
    - path: "osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewerClipboard.spec.tsx"
      provides: "≥ 1 Clipboard-Spec mit Document-Listener-Verdrahtung"
      contains: "MATRIX_CLIPBOARD_MIME"
    - path: "osim-ui/portal/e2e/passozmenge-block-copy-paste.spec.ts"
      provides: "Playwright-Smoke gegen Live-Stack"
      contains: "PRessMengeMatrixViewer"
  key_links:
    - from: "PRessMengeMatrixViewer.spec.tsx"
      to: "PRessMengeMatrixViewer-Component (Plan 06)"
      via: "Import + Render-Test"
      pattern: "import.*PRessMengeMatrixViewer"
    - from: "passozmenge-block-copy-paste.spec.ts"
      to: "Live-Stack auf localhost:3002"
      via: "Playwright page.goto"
      pattern: "localhost:3002"
---

<objective>
Test-Coverage für den in Plan 06 migrierten PRessMengeMatrixViewer:

  - **Vitest-Component-Specs** (≥ 8): 2D-Matrix-Rendering, Cell-Edit-via-Linksklick
    + Toolbar-Subklassen-Typ + Toolbar-Menge, Cell-Delete-via-Rechtsklick,
    Empty-States, Block-Select-Highlight, Disabled-State.
  - **Vitest-Clipboard-Spec** (≥ 1): Document-Level-Copy/Paste-Listener
    dispatched onCellEdit für jede Range-Cell (analog PRessBelegMatrixViewerClipboard.spec.tsx).
  - **Playwright-E2E** (1): Block-Copy/Paste-Cycle gegen Live-Stack;
    Persistenz-Reload-Test als optional `test.fixme` (siehe SC-10).

Schablonen:
  - PRessBelegMatrixViewer.spec.tsx (8 Specs, vollständige cellMap-Tests)
  - PRessBelegMatrixViewerClipboard.spec.tsx (5 T1-T5-Specs für Document-Listener)
  - matrix-cell-edit-persistence.spec.ts (Playwright-Pattern)

Output: 3 neue Test-Dateien. Final ein Sign-Off-Checkpoint, der den
Phase-01.3-Abschluss bestätigt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@osim-ui/CLAUDE.md
</context>

<interfaces>
<!-- Vitest-Spec-Pattern aus PRessBelegMatrixViewer.spec.tsx (Welle 01.2-E): -->
import { render, fireEvent } from "@testing-library/react";
import { describe, it, expect, vi, beforeEach } from "vitest";
vi.mock("@/stores/model-store", ...);
import { PRessBelegMatrixViewer } from "@/viewers/PRessBelegMatrix/PRessBelegMatrixViewer";

<!-- Playwright-Pattern aus matrix-cell-edit-persistence.spec.ts -->
test("...", async ({ page }) => {
  await page.goto("http://localhost:3002/");
  await page.fill('[data-testid="login-email"]', "jwfischer69@gmail.com");
  await page.fill('[data-testid="login-password"]', "123456");
  await page.click('[data-testid="login-submit"]');
  // ...navigate to model, open PRessMengeMatrixViewer...
});

<!-- Clipboard-Listener-Test-Pattern aus PRessBelegMatrixViewerClipboard.spec.tsx -->
const dt = new (globalThis as any).DataTransfer();
const event = new Event("copy") as ClipboardEvent;
Object.defineProperty(event, "clipboardData", { value: dt });
document.dispatchEvent(event);
expect(writeToDataTransferMock).toHaveBeenCalled();
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ≥ 8 Vitest-Specs für PRessMengeMatrixViewer</name>
  <files>osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\src\viewers\__tests__\PRessBelegMatrixViewer.spec.tsx (Schablone — komplett)
    - osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx (Soll-Komponente nach Plan 06)
    - osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixToolbar.tsx (Toolbar-Modus für Tests)
  </read_first>
  <behavior>
    Tests-Set (mind. 8 Stück):

    1. test_renders_empty_state_no_pressmenge: allObjects ohne PRessMenge → Empty-Hinweis.
    2. test_renders_empty_state_no_knoten: PRessMenge da, aber PDurchlaufplan.m_lKnoten leer → Empty-Hinweis.
    3. test_renders_2d_matrix_with_existing_assozmenge: 2 PRessMenge + 3 Knoten + 1 PAssozMengeErzgt → Matrix mit 2×3 cells, 1 davon hat einen Pill mit Subklasse-Label.
    4. test_linksklick_leere_cell_dispatcht_createObject_passozmengeerzgt: default Toolbar (Erzgt + Menge 1). Linksklick auf leere Cell → createObject("PAssozMengeErzgt", { m_lMengRess: <rowOid>, m_iMengeAus: 1, ... }) wird aufgerufen.
    5. test_linksklick_mit_toolbar_verbr_und_menge_5: Toolbar-Typ auf Verbr setzen, Menge auf 5 setzen. Linksklick → createObject("PAssozMengeVerbr", { m_iMengeEin: 5, ... }).
    6. test_linksklick_belegte_cell_no_op: belegte Cell wird angeklickt → createObject NICHT aufgerufen.
    7. test_rechtsklick_belegte_cell_dispatcht_delete: Rechtsklick auf belegte Cell → deleteObject(assoc.oid) UND ggf. removeSubRef vom Knoten-Wrapper.
    8. test_disabled_prop_blockiert_clicks: disabled=true → weder Linksklick (Create) noch Rechtsklick (Delete) dispatched.
    9. (Bonus) test_toolbar_typ_dropdown_default_ist_erzgt: ohne Interaktion ist Toolbar-Combo auf PAssozMengeErzgt.
    10. (Bonus) test_block_select_via_ctrl_klick_setzt_data_matrix_cell_selected: Ctrl+Klick auf 2 Cells → beide haben das data-matrix-cell-selected="true" Attribut.
  </behavior>
  <action>
    Datei `osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx`
    neu anlegen. Struktur 1:1 zur PRessBelegMatrixViewer.spec.tsx.

    Fixtures: Wire-Struktur analog der PRessBeleg-Spec. Konkret z.B.:
    ```ts
    const allObjects: Record<number, OBaseObj> = {
      1: { oid: 1, klass: "PRessMenge", attrs: { m_sName: "L1" }, sub_refs: [[]] },
      2: { oid: 2, klass: "PRessMenge", attrs: { m_sName: "L2" }, sub_refs: [[]] },
      // PAssozRessourceLList als Wrapper für m_lAssozRess am Knoten 100:
      99: { oid: 99, klass: "PAssozRessourceLList", attrs: {}, sub_refs: [[200]] },
      100: { oid: 100, klass: "PDpKnKonstant", attrs: { m_sName: "K1", m_lAssozRess: 99 }, sub_refs: [[]] },
      101: { oid: 101, klass: "PDpKnKonstant", attrs: { m_sName: "K2" }, sub_refs: [[]] },
      102: { oid: 102, klass: "PDpKnKonstant", attrs: { m_sName: "K3" }, sub_refs: [[]] },
      // PAssozMengeErzgt verknüpft L1 (oid=1) mit K1 (oid=100):
      200: { oid: 200, klass: "PAssozMengeErzgt",
             attrs: { m_sName: "E1", m_lMengRess: 1, m_iMengeAus: 5 }, sub_refs: [[]] },
      // PDurchlaufplan als obj-Prop:
      500: { oid: 500, klass: "PDurchlaufplan",
             attrs: { m_sName: "Plan1", m_lKnoten: [100, 101, 102] }, sub_refs: [[]] },
    };
    ```

    Mocks für useModelStore wie in der PRessBeleg-Spec.

    Falls Plan 06 das Cell-Selektor-Attribut `data-cell-id` setzt, dann
    `screen.getByTestId("matrix-cell")` oder `container.querySelector('[data-cell-id="oid:1:oid:100"]')` für Cell-Lookups verwenden.

    Wichtig: Tests testen die KOMPONENTE, nicht die wire-API. createObject etc.
    werden gemockt und auf vi.toHaveBeenCalledWith geprüft.
  </action>
  <verify>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx"</automated>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run"</automated>
  </verify>
  <done>
    ≥ 8 Specs grün in der neuen Datei. Gesamt-Vitest grün ohne Regressionen.
  </done>
  <acceptance_criteria>
    - Source: Datei `osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx` existiert.
    - Source: Datei enthält mindestens 8 Funktionen mit dem Pattern `it("...` oder `test("...` innerhalb von `describe(`-Blöcken.
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx"` exit-code 0 mit ≥ 8 passed.
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run"` exit-code 0 (Gesamt grün).
    - Behavior: Mindestens 1 Test prüft expliziten Aufruf von `createObject` mit Klassen-Substring "PAssozMengeErzgt" oder "PAssozMengeVerbr".
    - Behavior: Mindestens 1 Test prüft `disabled`-Verhalten.
    - Behavior: Mindestens 1 Test prüft Rechtsklick-Delete (`deleteObject` oder `removeSubRef`).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clipboard-Spec für Document-Listener + Playwright-E2E</name>
  <files>
    osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewerClipboard.spec.tsx
    osim-ui/portal/e2e/passozmenge-block-copy-paste.spec.ts
  </files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\src\viewers\__tests__\PRessBelegMatrixViewerClipboard.spec.tsx (Schablone)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\portal\e2e\matrix-cell-edit-persistence.spec.ts (Playwright-Pattern)
    - osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx (zu testendes Subject)
  </read_first>
  <behavior>
    Clipboard-Spec — mindestens 1 Spec, idealerweise 4 (analog PRessBeleg-Pattern T1-T4):
    - T1_copy_mit_aktiver_block_selection: Block-Selection setzen, Copy-Event dispatchen → setData mit MATRIX_CLIPBOARD_MIME wird aufgerufen.
    - T2_paste_mit_gueltiger_payload: Mock-DataTransfer hat eine valide
      MatrixClipboardPayload mit PAssozMengeErzgt-Cell. Paste-Event → onCellEdit
      (oder createObject) wird für die Range-Cell aufgerufen.
    - T3_copy_ohne_selection_pass_through: kein selectedRange → preventDefault wird NICHT aufgerufen.
    - T4_disabled_blockiert_listener: disabled=true → Copy- und Paste-Event triggern KEINE Side-Effects.

    Playwright-E2E — mindestens 1 Test (Block-Copy/Paste-Cycle):
    - Stack-Bootstrap: docker-compose up + seed_firebase_emulator.
    - Login.
    - Navigation zu einem Modell, PDurchlaufplan auswählen,
      PRessMengeMatrixViewer öffnen.
    - 1 Cell durch Linksklick anlegen (Toolbar-Default Erzgt+1).
    - Ctrl+Klick erweitert Selektion. Ctrl+C kopiert.
    - Klick auf eine andere Cell (Ziel).
    - Ctrl+V paste → Ziel-Cell hat jetzt einen Pill mit derselben Subklasse.
    - Assertion: data-cell-id="..." → data-oid existiert (also belegt).

    Optional: Persistenz-Test (Cell anlegen → 30s warten → F5 → Cell noch da)
    DARF als `test.fixme` markiert sein wenn Welle 01.2-H Save/Lock-Bug noch
    offen.
  </behavior>
  <action>
    ### Subtask 2a: Clipboard-Spec
    Datei `osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewerClipboard.spec.tsx`
    neu anlegen. JSDOM-DataTransfer-Polyfill wie in der PRessBeleg-Clipboard-
    Spec (Z.20-58) kopieren — dasselbe Polyfill-Pattern.

    Mocks für useModelStore + createObject etc. wie in PRessBeleg-Spec.

    Minimum-Spec-Sammlung: T1 + T2 + T3 + T4 (= 4 Tests). Falls Plan-06-
    Logik einen Cell-Create-Pfad mit zwei Schritten hat (delete + create),
    Paste-T2 prüfen, dass beide Calls passieren.

    ### Subtask 2b: Playwright-E2E
    Datei `osim-ui/portal/e2e/passozmenge-block-copy-paste.spec.ts` neu anlegen,
    Pattern aus `matrix-cell-edit-persistence.spec.ts` mit folgenden Anpassungen:

    1. Navigation zum PRessMengeMatrixViewer-Tab statt PRessBeleg.
    2. Cell-Selektoren auf `[data-viewer="PRessMengeMatrixViewer"] [data-testid="matrix-cell"]`.
    3. Toolbar-Sicherheits-Check: combo-typ-select existiert mit Default
       "PAssozMengeErzgt".

    Cycle:
    ```ts
    test("Block-Copy/Paste-Cycle PRessMenge", async ({ page }) => {
      // ... setup, login, navigate ...
      // (1) Cell anlegen
      const sourceCell = page.locator('[data-viewer="PRessMengeMatrixViewer"] [data-cell-id="..."]');
      await sourceCell.click();
      await expect(sourceCell.locator('[data-oid]')).toBeVisible();

      // (2) Ctrl+Klick auf 2. Cell → Block-Selektion
      await page.keyboard.down("Control");
      await sourceCell.click();
      await page.keyboard.up("Control");

      // (3) Ctrl+C
      await page.keyboard.press("Control+C");

      // (4) Klick auf Ziel-Cell + Ctrl+V
      const targetCell = page.locator('[data-cell-id="..."]');
      await targetCell.click();
      await page.keyboard.press("Control+V");

      // (5) Ziel-Cell ist belegt
      await expect(targetCell.locator('[data-oid]')).toBeVisible();
    });
    ```

    Persistenz-Test (Reload nach F5):
    ```ts
    test.fixme("Cell-Persistenz nach Reload", async ({ page }) => {
      // hängt am Welle 01.2-H Save/Lock-Stand — überspringen bis SC-9 grün ist.
    });
    ```

    Vor dem Playwright-Run ist optional ein Sanity-Check angemessen:
    ```powershell
    docker compose ps; bash scripts/wait-healthy.sh 90
    ```
  </action>
  <verify>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run src/viewers/__tests__/PRessMengeMatrixViewerClipboard.spec.tsx"</automated>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run"</automated>
    <automated>docker exec osim-ui-portal-1 sh -c "cd /app && npx playwright test e2e/passozmenge-block-copy-paste.spec.ts --reporter=line"</automated>
  </verify>
  <done>
    Clipboard-Spec mit ≥ 1 grünen Test. Playwright-E2E mit dem
    Block-Copy/Paste-Test grün. Persistenz-Test darf `test.fixme` sein
    (kein RED-Status, kein PASS-Status).
  </done>
  <acceptance_criteria>
    - Source: Datei `osim-ui/portal/src/viewers/__tests__/PRessMengeMatrixViewerClipboard.spec.tsx` existiert.
    - Source: Datei `osim-ui/portal/e2e/passozmenge-block-copy-paste.spec.ts` existiert.
    - Source: Beide Dateien enthalten `MATRIX_CLIPBOARD_MIME` als Substring (Clipboard-Spec) bzw. `PRessMengeMatrixViewer` (E2E).
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run src/viewers/__tests__/PRessMengeMatrixViewerClipboard.spec.tsx"` exit-code 0.
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run"` exit-code 0.
    - Test: `docker exec osim-ui-portal-1 sh -c "cd /app && npx playwright test e2e/passozmenge-block-copy-paste.spec.ts --reporter=line"` exit-code 0 (mit mindestens dem Block-Copy/Paste-Test als passed).
    - Behavior: Clipboard-Spec enthält mindestens 1 Test mit `dispatchEvent(new Event("copy"))` oder Aufruf von `writeToDataTransfer`.
    - Behavior: E2E enthält den Selektor-Substring `[data-viewer="PRessMengeMatrixViewer"]`.
    - Behavior: Persistenz-Test (falls vorhanden) ist mit `test.fixme(` markiert (nicht as `test.skip` und nicht als active test).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 01.3 Sign-Off</name>
  <what-built>
    Phase 01.3 abgeschlossen:
    - C++-Audit AUDIT.md (Plan 01)
    - Engine Loader+Writer für 5 PAssozMenge-Klassen + Roundtrip-Tests (Plan 02-04)
    - Schema-Patch (Plan 05)
    - UI-Migration PRessMengeMatrixViewer auf 2D-MatrixGrid (Plan 06)
    - Vitest+E2E-Coverage (Plan 07)
  </what-built>
  <how-to-verify>
    Volle Phase-Verifikation gegen die 10 SCs aus der ROADMAP:

    1. **SC-1 (Audit)**: Existiert .planning/phases/01.3.../01.3-01-AUDIT.md mit allen 5 Klassen-Sektionen?
    2. **SC-2 (Loader)**: `uv run python -c "from osim_engine.io import otx_loader; print(sorted(k for k in otx_loader._HANDLERS if 'PAssozMenge' in k))"` listet 5 Klassen.
    3. **SC-3 (Writer)**: dito für otx_writer._WRITERS.
    4. **SC-4 (Roundtrip-Tests)**: `cd engine && uv run pytest tests/integration/io/test_otx_passozmenge_roundtrip.py -v` zeigt ≥ 8 grüne Tests.
    5. **SC-5 (Alle Engine-Tests grün)**: `cd engine && uv run pytest -q` exit-code 0, kein Regression.
    6. **SC-6 (Schema-Export)**: `docker compose exec api cat /app/app/static/schemas/v1/schemas.json | grep -c "PAssozMenge"` liefert mindestens 5.
    7. **SC-7 (Viewer migriert + matrix-common.tsx gelöscht)**:
       - Test-Path osim-ui/portal/src/viewers/PRessMenge/PRessMengeMatrixViewer.tsx → existiert.
       - Test-Path osim-ui/portal/src/viewers/PRess/matrix-common.tsx → NICHT existiert.
       - Test-Path osim-ui/portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx → NICHT existiert.
    8. **SC-8 (Vitest ≥ 8 Specs)**: `docker exec osim-ui-portal-1 sh -c "cd /app && npx vitest run src/viewers/__tests__/PRessMengeMatrixViewer.spec.tsx --reporter=verbose"` listet ≥ 8 passed.
    9. **SC-9 (Browser-Edit funktioniert)**: User-Browser-Check (Plan 06 Checkpoint hat das schon ein erstes Mal verifiziert; hier nochmal nach Plan 07 Test-Stand).
    10. **SC-10 (Playwright-Smoke)**: `docker exec osim-ui-portal-1 sh -c "cd /app && npx playwright test e2e/passozmenge-block-copy-paste.spec.ts --reporter=list"` zeigt den Block-Copy/Paste-Test als passed.

    Wenn alle 10 SCs erfüllt: "approved" zurückgeben + Phase 01.3 in
    ROADMAP.md auf "[x]" setzen.

    Falls Persistenz-E2E als `test.fixme` markiert ist (wegen Welle 01.2-H
    Save/Lock-Stand): das ist explizit erlaubt laut SC-10; bei Sign-Off
    in SUMMARY dokumentieren.

    Link: http://localhost:3002
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test ↔ Component | Tests dispatchen User-Events gegen die Live-Komponente; Mocks für ModelStore verhindern, dass Tests Realdaten verändern. |
| Playwright ↔ Live-Stack | E2E läuft gegen den Container-Stack auf localhost:3002 + 8000; Test-User (jwfischer69@gmail.com / 123456) ist seedet im Firebase-Emulator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3.07-01 | Tampering | Mock-Store kommt Hot-Path-Logik nicht nach | mitigate | Tests gegen die GENAUE Mock-API (createObject etc.) und expliziter expect-Vergleich der Argumente. |
| T-01.3.07-02 | Denial of Service | Playwright hängt am Stack-Sync | mitigate | wait-healthy.sh 90s + standard Playwright-Timeouts; im worst case test.fixme. |
| T-01.3.07-03 | Information Disclosure | E2E mit Real-User-Credentials in Test-File | accept | jwfischer69@gmail.com / 123456 ist ein Seed-User im LOKALEN Firebase-Emulator (siehe Memory feedback-default-test-user); kein produktives Konto. |
</threat_model>

<verification>
- Vitest gesamt grün, Specs neu hinzugefügt.
- Playwright-E2E grün (Block-Copy/Paste-Cycle).
- Phase 01.3 Sign-Off durch User.
</verification>

<success_criteria>
- SC-8 (≥ 8 Vitest-Specs), SC-10 (Playwright-Smoke) erfüllt.
- Phase 01.3 komplett abgeschlossen — ROADMAP.md markiert sie als done.
</success_criteria>

<output>
Create `.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-07-SUMMARY.md` when done.
Create `.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/PHASE-SUMMARY.md` (Roll-up aller 7 Pläne) when done.
</output>
</content>
</invoke>