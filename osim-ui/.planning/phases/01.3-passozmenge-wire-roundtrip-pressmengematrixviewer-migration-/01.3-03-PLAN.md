---
phase: 01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-
plan: 03
type: execute
wave: 2
depends_on: ["01.3-01"]
files_modified:
  - engine/src/osim_engine/io/otx_writer.py
  - engine/tests/unit/io/test_otx_writer.py
autonomous: true
requirements: [SC-3, SC-5]
must_haves:
  truths:
    - "otx_writer._WRITERS enthält Einträge für PAssozMenge, PAssozMengeErzgt, PAssozMengeVerbr, PAssozMengeVerbrZwischen, PAssozMengeAbfr (5 Klassen)."
    - "Jeder Writer serialisiert m_sName via _serialize_scalars."
    - "Konkrete Subklassen-Writer serialisieren zusätzlich ihr jeweiliges Mengen-Attr: Erzgt → m_iMengeAus; Verbr/VerbrZwischen → m_iMengeEin; Abfr → m_iMengeAbfr."
    - "Jeder Writer serialisiert m_lMengRess als Scalar-OID-Pointer via writer.get_oid(py.m_lMengRess) (None-tolerant: nicht setzen wenn None)."
    - "test_writer_handles_all_known_loader_classes (set-equality-Check) ist grün, weil alle PAssozMenge-Klassen aus Plan 02 jetzt einen Writer haben."
    - "Existierende Writer-Tests bleiben grün (Stand: 70+ Tests)."
  artifacts:
    - path: "engine/src/osim_engine/io/otx_writer.py"
      provides: "5 neue Writer-Klassen + 5 register_writer-Aufrufe"
      contains: "register_writer(\"PAssozMengeErzgt\")"
    - path: "engine/tests/unit/io/test_otx_writer.py"
      provides: "Unit-Tests dass die 5 Writer registriert sind und ihre Attrs schreiben"
      contains: "m_iMengeAus"
  key_links:
    - from: "otx_writer.py PAssozMengeErzgt-Writer.serialize"
      to: "_serialize_scalars-Helper"
      via: "Aufruf-Argument-Tupel"
      pattern: "_serialize_scalars\\(py, \\(.*m_iMengeAus.*\\)\\)"
    - from: "otx_writer.py m_lMengRess-Serialisierung"
      to: "writer.get_oid(py.m_lMengRess)"
      via: "Funktions-Aufruf"
      pattern: "writer.get_oid\\(getattr\\(py, \"m_lMengRess\""
---

<objective>
Engine-Writer-Handler für die 5 PAssozMenge-Klassen in
`engine/src/osim_engine/io/otx_writer.py` registrieren — als 1:1-Pendant
zu den Loader-Handlern aus Plan 02. Damit ist der `test_writer_handles_all_known_loader_classes`
set-equality-Check (tests/integration/io/test_otx_roundtrip.py Z.28-43)
wieder grün.

Pattern-Quelle 1:1: `_make_assoz_writer` in otx_writer.py:917-944 (PAssozBeleg-
Factory) — `_serialize_scalars(py, ("m_sName",))` und der m_LinkStatusList-
Spezialfall. WICHTIG: AUDIT.md Sektion 4 hat geklärt ob für PAssozMenge
ein analoger Spezialfall nötig ist — Erwartung NEIN (kein m_L*-Pointer
in den 4 Subklassen), aber das Plan-Verhalten DARF NICHT von dieser Erwartung
abhängen: was in AUDIT.md Sektion 4 als verbindliche JA/NEIN-Aussage steht,
ist umzusetzen.

Pattern-Quelle für Scalar-Pointer-Serialisierung: `_PTagRessWriter` in
otx_writer.py:892-901 — `ref = writer.get_oid(getattr(py, "m_oRessBeleg", None));
if ref is not None: props["m_oRessBeleg"] = ref`.

Output: 5 neue WriterHandler-Klassen + 5 register_writer-Aufrufe + Unit-Tests
in `engine/tests/unit/io/test_otx_writer.py`.

KEIN Round-Trip-Test im Sinne von "OTX-Datei laden → schreiben → laden". Das
ist Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md
@osim-ui/CLAUDE.md
</context>

<interfaces>
<!-- WriterHandler-Basis (otx_writer.py Z.53-71) -->
class WriterHandler:
    def serialize(self, writer: "OtxWriter", py_obj: Any, oid: int
                  ) -> tuple[dict[str, Any], list[int]]:
        # returns (props, sub_refs)

<!-- Decorator (otx_writer.py Z.74-92) -->
def register_writer(*klass_names: str) -> Callable[[type], type]

<!-- Helper (otx_writer.py — am Anfang oder mittig im File definiert): -->
def _serialize_scalars(py, attrs: tuple[str, ...]) -> dict[str, Any]
    # liest jedes attr vom py-Objekt via getattr, packt in dict; None-Felder
    # werden trotzdem geschrieben (siehe existierender Code)

<!-- writer.get_oid (Methode auf OtxWriter): -->
writer.get_oid(py_obj_or_None) -> int | None
    # liefert die OID einer registrierten Python-Instanz; None wenn Input None ist
    # oder Instanz nicht im writer-Mapping

<!-- Pattern-Schablone PAssozBeleg-Factory (otx_writer.py Z.917-944): -->
def _make_assoz_writer():
    class _H(WriterHandler):
        def serialize(self, writer, py, oid):
            props = _serialize_scalars(py, ("m_sName",))
            src = (writer._original_otx.by_oid.get(oid)
                   if writer._original_otx is not None else None)
            if src is not None and "m_LinkStatusList" in src.attrs:
                props["m_LinkStatusList"] = src.attrs["m_LinkStatusList"]
            return props, []
    return _H

register_writer("PAssozBeleg")(_make_assoz_writer())
register_writer("PAssozRessEnt")(_make_assoz_writer())
register_writer("PAssozELogikEnt")(_make_assoz_writer())

<!-- Pattern-Schablone Scalar-Pointer (otx_writer.py Z.892-901 _PTagRessWriter): -->
@register_writer("PTagRess")
class _PTagRessWriter(WriterHandler):
    SCALARS = ("m_iTag",)
    def serialize(self, writer, py, oid):
        props = _serialize_scalars(py, self.SCALARS)
        ress = writer.get_oid(getattr(py, "m_oRessBeleg", None))
        if ress is not None:
            props["m_oRessBeleg"] = ress
        return props, []
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 5 register_writer-Klassen in otx_writer.py einfügen</name>
  <files>engine/src/osim_engine/io/otx_writer.py</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_writer.py
      (komplett — minimum die Sektion "Assoziationen" Z.912-946 und die Scalar-Pointer-Pattern Z.892-901 (PTagRess), plus die Definition von _serialize_scalars (am Anfang/Mittig des Files, exakte Zeile via grep finden))
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_loader.py
      (die 5 neu hinzugefügten Handler aus Plan 02 — als 1:1-Spiegel-Vorlage)
    - .planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md (Sektion 4 LinkStatusList-Analogie verbindlich)
  </read_first>
  <behavior>
    - Test 1: Nach Plan-Execute enthält otx_writer._WRITERS die 5 Schlüssel "PAssozMenge", "PAssozMengeErzgt", "PAssozMengeVerbr", "PAssozMengeVerbrZwischen", "PAssozMengeAbfr".
    - Test 2: Writer für PAssozMengeErzgt liefert bei serialize(writer_stub, py=PAssozMengeErzgt(sim, m_sName="E1", m_iMengeAus=5), oid=42) ein props-Dict mit m_sName="E1" und m_iMengeAus=5.
    - Test 3: Writer für PAssozMengeVerbr liefert m_iMengeEin im props-Dict.
    - Test 4: Writer für PAssozMengeVerbrZwischen liefert m_iMengeEin im props-Dict.
    - Test 5: Writer für PAssozMengeAbfr liefert m_iMengeAbfr im props-Dict.
    - Test 6: Wenn py.m_lMengRess ein registriertes Sim-Objekt ist, schreibt der Writer props["m_lMengRess"] = <oid des Lagers>.
    - Test 7: Wenn py.m_lMengRess None ist, fehlt der Key m_lMengRess im props-Dict (analog Pattern bei m_oRessBeleg im PTagRess-Writer).
  </behavior>
  <action>
    In `engine/src/osim_engine/io/otx_writer.py` im Abschnitt "Assoziationen"
    (nach den drei `register_writer("PAssozBeleg"/"PAssozRessEnt"/"PAssozELogikEnt")`-
    Aufrufen, aktuell Z.944-946) FÜNF neue Writer einfügen.

    Stil-Vorlage: Factory-Pattern `_make_X_writer()` (wie `_make_assoz_writer`),
    weil 5 Klassen mit derselben m_lMengRess-Logik aber unterschiedlichen
    Mengen-SCALARS. Empfohlene Struktur:

    ```python
    def _make_passozmenge_writer(extra_scalars: tuple[str, ...] = ()):
        """Factory für PAssozMenge-Subklassen.

        Schreibt m_sName + ggf. zusätzliche Skalare (m_iMengeAus / m_iMengeEin /
        m_iMengeAbfr) + Scalar-Pointer auf m_lMengRess.
        """
        scalars = ("m_sName",) + extra_scalars

        class _H(WriterHandler):
            def serialize(self, writer, py, oid):
                props = _serialize_scalars(py, scalars)
                ref = writer.get_oid(getattr(py, "m_lMengRess", None))
                if ref is not None:
                    props["m_lMengRess"] = ref
                return props, []
        return _H

    register_writer("PAssozMenge")(_make_passozmenge_writer(()))
    register_writer("PAssozMengeErzgt")(_make_passozmenge_writer(("m_iMengeAus",)))
    register_writer("PAssozMengeVerbr")(_make_passozmenge_writer(("m_iMengeEin",)))
    register_writer("PAssozMengeVerbrZwischen")(_make_passozmenge_writer(("m_iMengeEin",)))
    register_writer("PAssozMengeAbfr")(_make_passozmenge_writer(("m_iMengeAbfr",)))
    ```

    WICHTIG zur LinkStatusList-Frage (per AUDIT.md Sektion 4 verbindlich):
    - Wenn AUDIT.md Sektion 4 verbindlich sagt "kein m_L*-Spezialfall nötig"
      → den Code wie oben verwenden (kein writer._original_otx-Adoption-Block).
    - Wenn AUDIT.md doch einen Spezialfall identifiziert hat → den Block aus
      _make_assoz_writer Z.932-938 anpassen und einbauen.

    KEINE Modul-Top-Importe einführen. Die Factory liegt im selben File und
    nutzt nur bereits-importierte Symbole (WriterHandler, _serialize_scalars,
    register_writer).

    Reihenfolge: PAssozMenge zuerst, dann Erzgt / Verbr / VerbrZwischen / Abfr.
  </action>
  <verify>
    <automated>cd engine && uv run python -c "from osim_engine.io import otx_writer; assert all(k in otx_writer._WRITERS for k in ('PAssozMenge','PAssozMengeErzgt','PAssozMengeVerbr','PAssozMengeVerbrZwischen','PAssozMengeAbfr')), sorted(otx_writer._WRITERS.keys())"</automated>
    <automated>cd engine && uv run pytest tests/integration/io/test_otx_roundtrip.py::test_writer_handles_all_known_loader_classes -v</automated>
    <automated>cd engine && uv run pytest -x -q</automated>
  </verify>
  <done>
    `_WRITERS` enthält die 5 neuen Klassen. Set-Equality-Check (test_writer_handles_all_known_loader_classes) grün.
    Gesamtes Test-Set grün (0 Regressionen ggü. Stand vor Plan 02 + 03).
  </done>
  <acceptance_criteria>
    - Source: otx_writer.py enthält 5 Zeilen mit dem Pattern `register_writer("PAssozMenge..")(`.
    - Source: otx_writer.py enthält die Factory-Funktion (oder 5 einzelne Klassen-Definitionen) mit dem Pattern, das m_lMengRess als Scalar-Pointer schreibt.
    - Source: Die 4 konkreten Writer enthalten unterschiedliche Mengen-Attrs im _serialize_scalars-Tupel oder in der Factory-Parameter-Übergabe.
    - Test: `uv run python -c "from osim_engine.io import otx_writer; assert all(k in otx_writer._WRITERS for k in ('PAssozMenge','PAssozMengeErzgt','PAssozMengeVerbr','PAssozMengeVerbrZwischen','PAssozMengeAbfr'))"` exit-code 0.
    - Test: `cd engine && uv run pytest tests/integration/io/test_otx_roundtrip.py::test_writer_handles_all_known_loader_classes -v` exit-code 0.
    - Test: `cd engine && uv run pytest -x -q` exit-code 0.
    - Behavior: Wenn py.m_lMengRess None ist, fehlt der Key im resulting props-Dict (not None → present; None → absent).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-Tests für die 5 Writer in test_otx_writer.py</name>
  <files>engine/tests/unit/io/test_otx_writer.py</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\unit\io\test_otx_writer.py (existierende Stil-Vorlage, bestehender Test-Aufbau)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_writer.py (die geänderte Sektion)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\resources\assoziation\menge.py
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\resources\menge.py (für PRessMenge-Konstruktion in Tests)
  </read_first>
  <behavior>
    - Test test_passozmenge_writers_registered: 5 Klassen-Keys vorhanden.
    - Test test_passozmenge_erzgt_writer_serializes_m_iMengeAus: Konstruiert sim + PAssozMengeErzgt mit m_sName="E1", m_iMengeAus=7. writer-stub mit get_oid → None. Ruft writer.serialize. Prüft props["m_sName"]=="E1", props["m_iMengeAus"]==7, "m_lMengRess" not in props.
    - Test test_passozmenge_verbr_writer_serializes_m_iMengeEin: analog mit m_iMengeEin=3.
    - Test test_passozmenge_verbr_zwischen_writer_serializes_m_iMengeEin: analog.
    - Test test_passozmenge_abfr_writer_serializes_m_iMengeAbfr: analog mit m_iMengeAbfr=2.
    - Test test_passozmenge_erzgt_writer_serializes_m_lMengRess_when_set: writer-stub mit get_oid(lager) → 99; py.m_lMengRess = lager; props["m_lMengRess"] == 99.
  </behavior>
  <action>
    Datei `engine/tests/unit/io/test_otx_writer.py` erweitern (oder anlegen).

    Mindestens 6 neue Test-Funktionen schreiben.

    Stub-Konstruktion (writer ohne kompletten OtxWriter — Mock-Objekt mit
    `get_oid`-Method und `_original_otx = None`):
    ```python
    from types import SimpleNamespace
    from osim_engine.io import otx_writer
    from osim_engine.pps.simulator import PSimulator
    from osim_engine.resources.menge import PRessMenge
    from osim_engine.resources.assoziation.menge import (
        PAssozMengeErzgt, PAssozMengeVerbr, PAssozMengeVerbrZwischen, PAssozMengeAbfr,
    )

    def _writer_stub(oid_for=None):
        return SimpleNamespace(
            get_oid=lambda obj: oid_for.get(id(obj)) if obj is not None and oid_for else None,
            _original_otx=None,
        )

    def test_passozmenge_writers_registered():
        for k in ("PAssozMenge","PAssozMengeErzgt","PAssozMengeVerbr",
                  "PAssozMengeVerbrZwischen","PAssozMengeAbfr"):
            assert k in otx_writer._WRITERS

    def test_passozmenge_erzgt_writer_serializes_m_iMengeAus():
        sim = PSimulator()
        py = PAssozMengeErzgt(sim)
        py.m_sName = "E1"
        py.m_iMengeAus = 7
        w = otx_writer._WRITERS["PAssozMengeErzgt"]
        props, sub_refs = w.serialize(_writer_stub(), py, 42)
        assert props["m_sName"] == "E1"
        assert props["m_iMengeAus"] == 7
        assert "m_lMengRess" not in props
        assert sub_refs == []
    ```

    Beim m_lMengRess-Test: ein PRessMenge erzeugen, ID-basiert ins
    oid_for-Mapping einsetzen, py.m_lMengRess = lager setzen. Erwartung:
    props["m_lMengRess"] == 99.

    KEIN integration-Test (load → dump → load) — der gehört in Plan 04.
    Hier nur die Writer-Methoden ISOLIERT testen.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/unit/io/test_otx_writer.py -v</automated>
    <automated>cd engine && uv run pytest -x -q</automated>
  </verify>
  <done>
    Mindestens 6 neue Tests grün. Gesamtsuite grün.
  </done>
  <acceptance_criteria>
    - Source: Datei `engine/tests/unit/io/test_otx_writer.py` existiert.
    - Source: Datei enthält mindestens 6 Funktionen, die mit `def test_passozmenge` beginnen.
    - Test: `cd engine && uv run pytest tests/unit/io/test_otx_writer.py -v` exit-code 0.
    - Test: `cd engine && uv run pytest -x -q` exit-code 0.
    - Behavior: test_passozmenge_writers_registered prüft alle 5 Klassen-Schlüssel.
    - Behavior: test_passozmenge_erzgt_writer_serializes_m_lMengRess_when_set verifiziert dass props["m_lMengRess"] == <OID> (Integer).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python-Sim ↔ OTX-File | Writer schreibt Python-Sim-State zurück nach OTX; Datenverlust beim Roundtrip = Tampering-Risiko für UI-Save-Pfad. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3.03-01 | Tampering | Writer schreibt falsche Attr-Namen | mitigate | AUDIT.md (Plan 01) ist verbindliche Source-of-Truth; Acceptance-Criteria zwingen die genauen Identifier m_iMengeAus / m_iMengeEin / m_iMengeAbfr / m_lMengRess. |
| T-01.3.03-02 | Tampering | LinkStatusList-Analogie übersehen, Roundtrip verliert Pointer | mitigate | AUDIT.md Sektion 4 entscheidet JA/NEIN verbindlich; Plan-Action befolgt das. Plan 04 verifiziert Roundtrip-Verlustfreiheit. |
| T-01.3.03-03 | Tampering | Writer ohne Loader-Pendant (oder umgekehrt) | mitigate | test_writer_handles_all_known_loader_classes Set-Equality erzwingt die Symmetrie. |
| T-01.3.03-SC | Tampering | Package-Installs | accept | Plan modifiziert nur bestehende Source-Files, kein Package-Install. |
</threat_model>

<verification>
- Gesamtsuite `cd engine && uv run pytest` grün (0 Regressionen).
- test_writer_handles_all_known_loader_classes ist grün (Loader-Writer-Symmetrie).
- 5 neue Writer in `_WRITERS`.
</verification>

<success_criteria>
- Set-Equality-Check (Loader-Writer-Symmetrie) grün.
- Round-Trip-Test (Plan 04) kann darauf aufbauen.
</success_criteria>

<output>
Create `.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-03-SUMMARY.md` when done.
</output>
</content>
</invoke>