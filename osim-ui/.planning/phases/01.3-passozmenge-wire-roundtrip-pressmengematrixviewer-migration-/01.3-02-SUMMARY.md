---
phase: 01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-
plan: 02
subsystem: engine-io-loader
tags: [otx-loader, passozmenge, material-fluss, scalar-pointer]
one_liner: 5 OTX-Loader-Handler für die PAssozMenge-Familie (abstract + 4 konkrete Subklassen) registriert, mit copy_scalars + resolve_ref nach AUDIT-Tupeln aus Plan 01.
requires:
  - 01.3-01-AUDIT.md (verbindliche SCALAR + Pointer-Tupel)
provides:
  - otx_loader._HANDLERS["PAssozMenge"]
  - otx_loader._HANDLERS["PAssozMengeErzgt"]
  - otx_loader._HANDLERS["PAssozMengeVerbr"]
  - otx_loader._HANDLERS["PAssozMengeVerbrZwischen"]
  - otx_loader._HANDLERS["PAssozMengeAbfr"]
affects:
  - engine/src/osim_engine/io/otx_loader.py
  - engine/tests/unit/io/test_otx_loader.py
tech_stack:
  added: []
  patterns: [class-registry-handler, two-pass-load, scalar-pointer-resolve]
key_files:
  created:
    - engine/tests/unit/io/test_otx_loader.py
  modified:
    - engine/src/osim_engine/io/otx_loader.py
decisions:
  - "Pattern 1:1 zu _PAssozBelegHandler (otx_loader.py:782-795) — copy_scalars im instantiate, resolve_ref im wire."
  - "m_lMengRess wird per resolve_ref aufgelöst (Scalar-Pointer auf einzelne PRessMenge), NICHT per resolve_list — AUDIT.md Sektion 4.4."
  - "Eigener Handler für PAssozMengeVerbrZwischen trotz identischer Scalars zu Verbr — wegen Klassennamen-Dispatch im _HANDLERS-Dict."
  - "Imports lazy im instantiate (Convention im File, analog PAssozBeleg) statt Modul-Ebene."
  - "Optionale Refs m_lKnoten/m_lOberAssoz/m_lUnterAssoz NICHT in dieser Welle gewired — Plan-Direktive, analog Bestand PAssozBeleg."
metrics:
  duration_minutes: 25
  tasks_completed: 2
  files_created: 1
  files_modified: 1
  tests_added: 10
  commits: 2
completed: 2026-05-28
---

# Phase 01.3 Plan 02: Engine otx_loader.py Handler — Summary

## Was

Fünf neue `register_handler`-Klassen für die PAssozMenge-Familie in
`engine/src/osim_engine/io/otx_loader.py` ergänzt, plus 10 begleitende
Unit-Tests in `engine/tests/unit/io/test_otx_loader.py`.

Damit kann der Loader OTX-Dateien einlesen, die Material-Fluss-Assoziationen
(`PAssozMengeErzgt` / `Verbr` / `VerbrZwischen` / `Abfr`) enthalten, und sie
korrekt mit ihren PRessMenge-Lagern verdrahten. Die Sim-Logik selbst ist
unverändert (1:1-Portierung lebt seit Phase 1 in `menge.py`).

## Warum

Phase 1.3 fügt die OTX-Persistenz für die Material-Fluss-Klassen hinzu, die
in `osim_engine.resources.assoziation.menge` Sim-seitig bereits vollständig
sind, aber bisher nicht aus OTX geladen werden konnten. Plan 02 ist die
Loader-Seite; Plan 03 liefert die korrespondierenden Writer-Handler, Plan
04 die End-to-End-Roundtrip-Tests.

## Wie

Die Implementierung folgt dem etablierten Class-Registry-Handler-Pattern
des OTX-Loaders (otx_loader.py § "Class-Registry + Two-Pass-Loading"). Jeder
der fünf Handler:

1. **`instantiate(loader, obj)`** instanziiert die Sim-Klasse aus
   `osim_engine.resources.assoziation.menge` (lazy import im instantiate
   analog `_PAssozBelegHandler`) und kopiert ihre SCALAR-Attribute per
   `copy_scalars` aus `obj.attrs`. Die Tupel sind mechanisch aus
   AUDIT.md Sektion 2.7 übernommen:
   - `PAssozMenge` (abstract): `("m_sName",)`
   - `PAssozMengeErzgt`: `("m_sName", "m_iMengeAus")`
   - `PAssozMengeVerbr`: `("m_sName", "m_iMengeEin")`
   - `PAssozMengeVerbrZwischen`: `("m_sName", "m_iMengeEin")`
   - `PAssozMengeAbfr`: `("m_sName", "m_iMengeAbfr")`

2. **`wire(loader, py, obj)`** löst den Scalar-Pointer auf das zugehörige
   Lager auf: `py.m_lMengRess = resolve_ref(loader, obj, "m_lMengRess")`.
   Das Pattern ist analog zu `_PTagRessHandler.m_oRessBeleg` — eine
   einzelne PRessMenge-Referenz, KEINE LList-Container-Auflösung (siehe
   AUDIT-Sektion 4.4 für die Begründung der case-sensitiven m_l*-Falle).

Container-Sub-Refs (`m_lKnoten`, `m_lOberAssoz`, `m_lUnterAssoz`) werden in
dieser Welle bewusst nicht gewired — analog zum Bestand der PAssozBeleg-Familie
und wie im Plan vorgegeben.

## Geänderte Files

### engine/src/osim_engine/io/otx_loader.py

89 Zeilen ergänzt, eingefügt nach `register_skip("PAssozBelegLinkInfo")`
(Z.799) und vor der Phase-5-A-Sektion. Der Block ist mit einem eigenen
Kommentar-Header ("Phase-1.3 W2: PAssozMenge-Familie") markiert und
verweist auf AUDIT.md.

### engine/tests/unit/io/test_otx_loader.py

Datei neu angelegt (257 Zeilen). 10 Tests in drei Sektionen:

| Sektion         | Tests | Verifiziert |
|-----------------|-------|-------------|
| Registry-Check  | 1     | Alle 5 Klassen-Schlüssel in `_HANDLERS`. |
| instantiate-SCALARS | 5 | Erzgt/Verbr/VerbrZwischen/Abfr lesen ihr jeweiliges `m_iMenge*`; abstract liest `m_sName`. |
| wire-Resolution | 4     | `m_lMengRess` wird auf eine vor-instanziierte PRessMenge in `loader.instances` aufgelöst; bleibt None bei fehlendem Attribut (DoS-Hardening). |

Test-Helper `_stub_loader(instances)` baut einen minimal-befüllten
OtxLoader (eigener PSimulator, leeres OtxFile als `loader.otx`, optional
vor-belegte `instances`).

## Commits

| Hash       | Type | Beschreibung                                          | Files |
|------------|------|-------------------------------------------------------|-------|
| `398d83d`  | feat | 5 PAssozMenge-Loader-Handler in otx_loader.py         | 1     |
| `97a3a22`  | test | 10 Unit-Tests in tests/unit/io/test_otx_loader.py     | 1     |

(Zwischen den beiden Plan-02-Commits wurde aus einer parallelen Agent-Session
ein nicht-zugehöriger Commit `728d3a7 chore: install graphify skill` aufgenommen.
Dieser Commit gehört NICHT zu Plan 02 und ist hier nur als Reflog-Beobachtung
notiert.)

## Verifikation

| Check                                                                               | Ergebnis |
|-------------------------------------------------------------------------------------|----------|
| `_HANDLERS` enthält alle 5 Klassen-Schlüssel                                        | OK       |
| `pytest tests/unit/io/test_otx_loader.py -v`                                        | 10 passed |
| `pytest tests/integration/test_otx_loader.py tests/integration/io/`                 | 24 passed + 1 deselected (plan-erwartet) |
| `pytest` (volle Engine-Suite, ohne plan-erwarteten Writer-Coverage-Test)            | 511 passed / 1 xfailed |
| Pre-existing Failures (test_azeitsim_runner, test_python_vs_cpp) durch git stash verifiziert | NICHT durch diese Welle verursacht |

### Plan-erwartete Failure

`tests/integration/io/test_otx_roundtrip.py::test_writer_handles_all_known_loader_classes`
schlägt nach Plan 02 RED an — die 4 konkreten PAssozMenge-Subklassen werden
in der Set-Equality-Check als "Writer fehlt"-Diff gemeldet:

```
WriterHandler fehlt für 5 Loader-Klassen:
['PAssozMenge', 'PAssozMengeAbfr', 'PAssozMengeErzgt',
 'PAssozMengeVerbr', 'PAssozMengeVerbrZwischen']
```

Das ist **per Design** (siehe Plan-Section `<verification>`). Plan 03
fügt die korrespondierenden Writer-Handler hinzu und macht den Test wieder
grün.

### Pre-existing Failures (NICHT durch diese Welle verursacht)

3 Tests sind in der vollen Suite rot, alle drei betreffen den Python-vs-C++-
Bit-Vergleich der Einsatzzeit-Sim (`m_dPtkEinsatzzeit`-Drift):

- `test_azeitsim_runner.py::test_roundtrip_runs_and_modifies_otx` (wall_clock-Performance)
- `test_azeitsim_runner.py::test_roundtrip_produces_counter_changes`
- `test_python_vs_cpp.py::test_python_run_matches_cpp_run_on_embb`

Verifiziert via `git stash` → ohne unsere Plan-02-Änderungen sind die
gleichen 3 Tests rot. Aufgabe für eine separate Welle (Engine-Sim-Drift-Diagnose),
NICHT Scope von Phase 1.3.

## Deviations from Plan

Keine. Plan wurde mechanisch wie spezifiziert ausgeführt, SCALAR-Tupel
1:1 aus AUDIT.md Sektion 2.7 übernommen.

### Anmerkung zur Commit-Sequenz

Während der Task-2-Commit-Phase trat eine Race-Condition mit einer
parallelen Agent-Session auf: `.git/COMMIT_EDITMSG` enthielt jeweils
fremde Commit-Vorlagen, und das Staging-Area-State wurde zwischen
`git add` und `git commit -m` durch den Parallel-Agent verändert. Lösung:
ein `git reset --soft HEAD~1` plus `git reset HEAD` zum Aufräumen,
dann erneutes `git add engine/tests/unit/io/test_otx_loader.py` und
`git commit -F /tmp/plan02-task2-commit.msg` (atomar in einem Aufruf,
COMMIT_EDITMSG-Hijacking umgangen). Endgültiger korrekter Commit:
`97a3a22` mit den richtigen 257 Zeilen `test_otx_loader.py`.

## Self-Check: PASSED

- `engine/src/osim_engine/io/otx_loader.py` — FOUND
- `engine/tests/unit/io/test_otx_loader.py` — FOUND
- `.planning/phases/.../01.3-02-SUMMARY.md` — FOUND
- Commit `398d83d` (Task 1) — FOUND in git log
- Commit `97a3a22` (Task 2) — FOUND in git log
- Tests `pytest tests/unit/io/test_otx_loader.py` — 10/10 grün
- Acceptance Criteria Task 1 (Source-Pattern, Behavior, Tests) — alle erfüllt
- Acceptance Criteria Task 2 (≥6 Tests, alle grün, Wire-Resolution) — alle erfüllt
