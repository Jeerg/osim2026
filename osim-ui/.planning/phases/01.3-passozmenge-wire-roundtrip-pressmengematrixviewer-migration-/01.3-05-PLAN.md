---
phase: 01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-
plan: 05
type: execute
wave: 3
depends_on: ["01.3-01"]
files_modified:
  - osim-ui/app/static/schemas/v1/schemas.json
autonomous: false
requirements: [SC-6]
user_setup: []
must_haves:
  truths:
    - "schemas.json enthält Klassen-Einträge für PAssozMenge, PAssozMengeErzgt, PAssozMengeVerbr, PAssozMengeVerbrZwischen, PAssozMengeAbfr."
    - "Jede konkrete Subklasse hat ihre per AUDIT.md verifizierten Attrs: Erzgt → m_iMengeAus; Verbr/VerbrZwischen → m_iMengeEin; Abfr → m_iMengeAbfr."
    - "Jede Klasse hat m_sName (Variable/string) und m_lMengRess (Link auf PRessMenge)."
    - "PRessMenge-Eintrag in schemas.json hat einen Hinweis im description_de dass die existierenden Phase-1-Phantasie-Attrs (m_iMenge / m_iNachschubMenge / m_iMaxMenge) NICHT engine-treu sind und in Phase 3 ersetzt werden — sie bleiben aber unangetastet, weil der existierende PRessMengeMatrixViewer-Stub sie liest."
    - "Schemas.json ist syntaktisch valides JSON."
    - "API-Container hat das gepatchte Schema (nach docker-compose cp + restart)."
  artifacts:
    - path: "osim-ui/app/static/schemas/v1/schemas.json"
      provides: "5 neue Klassen-Einträge für PAssozMenge-Familie"
      contains: "\"klass\": \"PAssozMengeErzgt\""
  key_links:
    - from: "osim-ui/app/static/schemas/v1/schemas.json"
      to: "osim-ui-api-1 Container /app/app/static/schemas/v1/schemas.json"
      via: "docker compose cp + restart"
      pattern: "docker compose cp"
    - from: "Plan 06 (UI-Migration)"
      to: "schemas.json PAssozMenge-Einträge"
      via: "PRessMengeMatrixViewer liest die Subklassen-Liste aus dem Schema"
      pattern: "PAssozMenge"
---

<objective>
UI-Schema (osim-ui/app/static/schemas/v1/schemas.json) um die 5 PAssozMenge-
Klassen erweitern, damit der zu migrierende PRessMengeMatrixViewer (Plan 06)
die Subklassen-Liste + Mengen-Attrs sauber aus dem Schema lesen kann.

Diese Datei wird HANDGEPFLEGT (kein Engine-Reflection-Schema-Generator —
das kommt erst in Phase 3 laut ROADMAP "Phase 3: JSON Editor"). Bis dahin
ist schemas.json die einzige UI-Schema-Quelle.

Sekundär: PRessMenge-Eintrag (aktuell mit Phase-1-Phantasie-Attrs wie
`m_iMenge`/`m_iNachschubMenge`/`m_iMaxMenge`, die NICHT der osim-engine-
Klasse entsprechen — die echte Klasse hat `m_iBestandAnfang`/`m_iBestandMax`
etc.) bekommt einen Hinweis im description_de. Die existierenden Attrs
bleiben unangetastet, weil der existierende PRessMengeMatrixViewer (vor
Plan 06) sie noch liest.

Container-Sync danach: `docker compose cp` ins osim-ui-api-1 + restart api.

Output: 1 modifiziertes JSON-File + 1 Container-Sync. Dieser Plan
benötigt einen menschlichen Verifikations-Checkpoint nach dem Sync, weil
der API-Container-Restart visuell nachvollziehbar sein soll und der nächste
Plan (06 UI-Migration) auf das gepatchte Schema angewiesen ist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md
@osim-ui/CLAUDE.md
</context>

<interfaces>
<!-- Bestehende Klassen-Eintrag-Struktur in schemas.json (Beispiel PRessMenge Z.184-199): -->
{
  "klass": "PRessMenge",
  "label_de": "Mengenressource",
  "viewer_hints": ["std", "matrix"],
  "description_de": "Plan-Begriff. Real-OTX-Klassen: PSpeicher/PSpeicherProz. Schema-Match in Phase 3.",
  "properties": [
    {"name": "m_sName", "label_de": "Name", "octrl_type": "Variable", "value_type": "string"},
    {"name": "m_iMenge", ..., "value_type": "int"},
    ...
  ]
}

<!-- Bestehende Klassen-Eintrag-Struktur in schemas.json (PAssozBeleg, Z.200-214): -->
{
  "klass": "PAssozBeleg",
  "label_de": "Ressourcen-Verknuepfung (Belegung)",
  "viewer_hints": ["std"],
  "description_de": "Plan-Begriff: 'PRessVerknuepfung'. Real-OTX-Klasse: PAssozBeleg.",
  "properties": [
    {"name": "m_sName", ...},
    {"name": "m_lKnoten", "octrl_type": "Link", "link_target_klass": "PDpKnBase"},
    {"name": "m_lOberAssoz", "octrl_type": "Link", "link_target_klass": "PAssozBeleg", "nullable": true},
    {"name": "m_lUnterAssoz", "octrl_type": "Link", "link_target_klass": "PAssozBeleg", "nullable": true},
    {"name": "m_lRessourcen", "octrl_type": "List", "list_item_klass": "PBetriebsmittel"},
    ...
  ]
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 5 Klassen-Einträge in schemas.json einfügen + PRessMenge-Hinweis</name>
  <files>osim-ui/app/static/schemas/v1/schemas.json</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\app\static\schemas\v1\schemas.json (komplett, mindestens Z.180-240 als Stil-Vorlage)
    - .planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md (Sektionen 2 + 6 — verbindliche Attr-Listen)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\resources\assoziation\menge.py (Soll-Spalte für Attr-Vergleich)
  </read_first>
  <action>
    In `osim-ui/app/static/schemas/v1/schemas.json` 5 neue Klassen-Objekte
    in das `classes`-Array einfügen (direkt nach dem PAssozBeleg-Eintrag,
    aktuell Z.214).

    Jeder Klassen-Eintrag folgt dem PAssozBeleg-Stil mit:
    - `klass`: exakter C++-Klassenname
    - `label_de`: deutsches Label aus AUDIT.md/CLAUDE.md (akademisch, kein Marketing)
    - `viewer_hints`: ["matrix"] für die 4 konkreten Subklassen, ["std"] für die abstract PAssozMenge
    - `description_de`: 1 Satz Plan-Pendant + Real-OTX-Klasse
    - `properties`: m_sName + m_lMengRess + (Subklasse-spezifisch) das Mengen-Attr

    Konkrete Einträge (Reihenfolge wie in der odh-Hierarchie):

    ```json
    {
      "klass": "PAssozMenge",
      "label_de": "Mengen-Assoziation (Basis)",
      "viewer_hints": ["std"],
      "description_de": "Abstrakte Basis-Klasse fuer die 4 konkreten Mengen-Assoziations-Typen (Erzgt/Verbr/VerbrZwischen/Abfr). Verbindet einen Durchlaufplan-Knoten mit einer PRessMenge (Lager/Bestand).",
      "properties": [
        {"name": "m_sName", "label_de": "Name", "octrl_type": "Variable", "value_type": "string"},
        {"name": "m_lMengRess", "label_de": "Mengen-Ressource", "octrl_type": "Link", "link_target_klass": "PRessMenge"}
      ]
    },
    {
      "klass": "PAssozMengeErzgt",
      "label_de": "Mengen-Assoziation: Erzeuger",
      "viewer_hints": ["matrix"],
      "description_de": "Knoten produziert m_iMengeAus Einheiten am Prozess-Ende.",
      "properties": [
        {"name": "m_sName", "label_de": "Name", "octrl_type": "Variable", "value_type": "string"},
        {"name": "m_lMengRess", "label_de": "Mengen-Ressource", "octrl_type": "Link", "link_target_klass": "PRessMenge"},
        {"name": "m_iMengeAus", "label_de": "Ausgehende Menge", "octrl_type": "Variable", "value_type": "int"}
      ]
    },
    {
      "klass": "PAssozMengeVerbr",
      "label_de": "Mengen-Assoziation: Verbraucher",
      "viewer_hints": ["matrix"],
      "description_de": "Knoten verbraucht m_iMengeEin Einheiten am Prozess-Beginn.",
      "properties": [
        {"name": "m_sName", "label_de": "Name", "octrl_type": "Variable", "value_type": "string"},
        {"name": "m_lMengRess", "label_de": "Mengen-Ressource", "octrl_type": "Link", "link_target_klass": "PRessMenge"},
        {"name": "m_iMengeEin", "label_de": "Eingehende Menge", "octrl_type": "Variable", "value_type": "int"}
      ]
    },
    {
      "klass": "PAssozMengeVerbrZwischen",
      "label_de": "Mengen-Assoziation: Zwischen-Verbrauch",
      "viewer_hints": ["matrix"],
      "description_de": "Verbrauch mit getrennter Kosten-Aggregation; Sim-Semantik identisch zu PAssozMengeVerbr.",
      "properties": [
        {"name": "m_sName", "label_de": "Name", "octrl_type": "Variable", "value_type": "string"},
        {"name": "m_lMengRess", "label_de": "Mengen-Ressource", "octrl_type": "Link", "link_target_klass": "PRessMenge"},
        {"name": "m_iMengeEin", "label_de": "Eingehende Menge", "octrl_type": "Variable", "value_type": "int"}
      ]
    },
    {
      "klass": "PAssozMengeAbfr",
      "label_de": "Mengen-Assoziation: Abfrage",
      "viewer_hints": ["matrix"],
      "description_de": "Knoten prueft, ob m_iMengeAbfr Einheiten verfuegbar sind, ohne sie zu entnehmen.",
      "properties": [
        {"name": "m_sName", "label_de": "Name", "octrl_type": "Variable", "value_type": "string"},
        {"name": "m_lMengRess", "label_de": "Mengen-Ressource", "octrl_type": "Link", "link_target_klass": "PRessMenge"},
        {"name": "m_iMengeAbfr", "label_de": "Abzufragende Menge", "octrl_type": "Variable", "value_type": "int"}
      ]
    }
    ```

    ZUSÄTZLICH: PRessMenge-Eintrag (Z.184-199) bekommt einen erweiterten
    description_de-Text:
    ```
    "description_de": "Mengen-Ressource (Bestand/Lager). HINWEIS 2026-05-28: Die Phase-1-Attrs (m_iMenge, m_iNachschubMenge, m_iMaxMenge, m_iNachschubIntervall) entsprechen NICHT der osim-engine-Klasse (echte Attrs: m_iBestandAnfang, m_iBestandMax, m_iBestandAktuell + Counter). Schema-Re-Sync in Phase 3 (JSON Editor mit Engine-Reflection). Bis dahin werden die Phase-1-Attrs vom existierenden Viewer-Stub gelesen — kein Bruch."
    ```
    Die Properties-Liste der PRessMenge bleibt unangetastet (damit der
    existierende Viewer in PRess/PRessMengeMatrixViewer.tsx vor Plan-06-
    Migration nicht bricht).

    Nach dem Edit: JSON-Syntax mit einem Validator prüfen (z.B. `python -m json.tool`).
  </action>
  <verify>
    <automated>powershell -Command "Get-Content 'osim-ui/app/static/schemas/v1/schemas.json' -Raw | python -c \"import sys,json; json.loads(sys.stdin.read()); print('valid')\""</automated>
    <automated>powershell -Command "Select-String -Path 'osim-ui/app/static/schemas/v1/schemas.json' -Pattern 'PAssozMengeErzgt','PAssozMengeVerbr','PAssozMengeVerbrZwischen','PAssozMengeAbfr','m_iMengeAus','m_iMengeEin','m_iMengeAbfr','m_lMengRess'"</automated>
  </verify>
  <done>
    schemas.json ist valides JSON, enthält alle 5 neuen Klassen + die 4 Mengen-Attr-Identifier + m_lMengRess.
    PRessMenge-description_de erwähnt den Phase-3-Re-Sync-Hinweis.
  </done>
  <acceptance_criteria>
    - Source: `osim-ui/app/static/schemas/v1/schemas.json` enthält 5 Substrings: `"klass": "PAssozMenge"`, `"klass": "PAssozMengeErzgt"`, `"klass": "PAssozMengeVerbr"`, `"klass": "PAssozMengeVerbrZwischen"`, `"klass": "PAssozMengeAbfr"`.
    - Source: Datei enthält 4 Mengen-Attr-Substrings: `"name": "m_iMengeAus"`, `"name": "m_iMengeEin"`, `"name": "m_iMengeAbfr"`, `"name": "m_lMengRess"`.
    - Source: PRessMenge-Eintrag enthält im description_de das Wort "Phase 3" oder "Schema-Re-Sync".
    - Test: `python -c "import json; json.load(open('osim-ui/app/static/schemas/v1/schemas.json'))"` exit-code 0.
    - Behavior: Die Properties-Liste von PRessMenge ist unverändert (nur description_de gepatcht).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: API-Container-Sync (cp + restart)</name>
  <files>
    (kein File-Edit — nur Container-Operationen)
  </files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\osim-ui\CLAUDE.md (Container-Operations-Konventionen)
    - .planning/STATE.md (zur Bestätigung dass osim-ui-api-1 der Container-Name ist)
  </read_first>
  <action>
    Schema-Datei in den API-Container kopieren und API neu starten,
    damit der nachfolgende UI-Plan (06) auf das aktualisierte Schema
    zugreifen kann.

    Befehlssequenz (PowerShell):
    ```powershell
    docker compose cp osim-ui/app/static/schemas/v1/schemas.json osim-ui-api-1:/app/app/static/schemas/v1/schemas.json
    docker compose restart api
    ```

    Verifikation:
    ```powershell
    docker compose exec api cat /app/app/static/schemas/v1/schemas.json | Select-String 'PAssozMengeErzgt'
    ```

    Wenn der Container-Befehl fehlschlägt, ist das vermutlich ein osim-ui-
    Docker-Compose-Problem — in diesem Fall einen Auth-Gate-Checkpoint
    erzeugen (User wird informiert, der Plan pausiert).
  </action>
  <verify>
    <automated>docker compose ps api 2>&1 | findstr "running" || docker compose ps api</automated>
    <automated>docker compose exec api sh -c "grep -c PAssozMengeErzgt /app/app/static/schemas/v1/schemas.json"</automated>
  </verify>
  <done>
    API-Container läuft, Schema-Datei darin enthält PAssozMengeErzgt-Substring.
  </done>
  <acceptance_criteria>
    - CLI-Output: `docker compose exec api sh -c "grep -c PAssozMengeErzgt /app/app/static/schemas/v1/schemas.json"` liefert eine Zahl ≥ 1.
    - CLI-Output: `docker compose ps api` zeigt den api-Container als "Up" oder "running".
    - Behavior: Nach Container-Restart liefert ein HTTP-Aufruf an `/api/v1/schemas` (oder das per Schema-Reflection genutzte API) das neue PAssozMenge-Schema. (Nicht zwingend in Acceptance — kann in Plan 06 Task 0 als Sanity-Check geprüft werden.)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: User verifiziert API-Container-Schema-Update visuell</name>
  <what-built>
    schemas.json um 5 PAssozMenge-Klassen-Einträge erweitert. API-Container
    via `docker compose cp + restart` synchronisiert. Schema-Endpoint liefert
    jetzt die neuen Klassen.
  </what-built>
  <how-to-verify>
    1. Browser: http://localhost:8000/api/v1/schemas öffnen (oder den
       project-spezifischen Schema-Endpoint).
    2. Suchen nach "PAssozMengeErzgt" — sollte im JSON-Output erscheinen.
    3. Optional: http://localhost:3002 öffnen, eingeloggt mit
       jwfischer69@gmail.com / 123456, im Workspace ein Modell mit
       PAssozMenge laden (falls vorhanden). KEIN Render-Test —
       das ist Plan 06.
    4. Wenn das Schema OK ist: "approved" zurückgeben.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-Time-Schema ↔ Runtime-API | schemas.json wird vom API-Container statisch ausgeliefert; Container-Image-Cache kann veraltete Version enthalten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3.05-01 | Tampering | Schema-Drift zwischen UI und Engine | accept | Schemas.json ist handgepflegt; Phase 3 (JSON Editor mit Engine-Reflection) wird das Drift-Risiko strukturell auflösen. Plan 03 description_de macht das transparent. |
| T-01.3.05-02 | Information Disclosure | Schema-Endpoint enthält Modell-Klassennamen | accept | Modell-Klassennamen sind durch das Frontend-Build sowieso öffentlich; keine Geheimnisse im Schema. |
| T-01.3.05-03 | Tampering | API-Container hat veraltetes Schema im Image-Layer | mitigate | `docker compose cp` schreibt direkt in den Container-Filesystem-Overlay; restart liest die neue Datei. Verify-Task prüft das via exec+grep. |
</threat_model>

<verification>
- schemas.json ist valides JSON, enthält alle 5 neuen Klassen.
- API-Container hat das gepatchte Schema im Filesystem.
- User hat den Schema-Endpoint visuell verifiziert.
</verification>

<success_criteria>
- SC-6 (Schema-Export für UI) der ROADMAP erfüllt.
- Plan 06 (UI-Migration) kann die neuen Klassen aus dem Schema lesen.
</success_criteria>

<output>
Create `.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-05-SUMMARY.md` when done.
</output>
</content>
</invoke>