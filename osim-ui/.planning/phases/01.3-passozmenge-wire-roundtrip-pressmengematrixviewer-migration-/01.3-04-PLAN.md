---
phase: 01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-
plan: 04
type: execute
wave: 3
depends_on: ["01.3-02", "01.3-03"]
files_modified:
  - engine/tests/integration/io/test_otx_passozmenge_roundtrip.py
  - engine/tests/fixtures/otx/passozmenge_minimal.otx
autonomous: true
requirements: [SC-4, SC-5]
must_haves:
  truths:
    - "Mindestens 8 neue Roundtrip-Tests grün für die PAssozMenge-Klassenfamilie."
    - "Pro konkrete Subklasse (Erzgt, Verbr, VerbrZwischen, Abfr) gibt es ≥ 1 Roundtrip-Test mit Single-Instance UND ≥ 1 Multi-Instance-Test (oder Multi-Knoten / Multi-Lager)."
    - "Demo-OTX-Fixture für die Tests ist im Repo committed (engine/tests/fixtures/otx/passozmenge_minimal.otx ODER programmatisch via dump_simulator_to_otx erzeugt)."
    - "Alle existierenden Engine-Tests bleiben grün (Stand: 70+); 0 Regressionen."
    - "Roundtrip-Vertrag wird konkret verifiziert: load_to_wire → wire_to_otx → load_to_wire erhält für jedes PAssozMenge*-Objekt: Klasse-Identität, m_sName, das jeweilige Mengen-Attr-Wert, m_lMengRess-OID-Bindung."
  artifacts:
    - path: "engine/tests/integration/io/test_otx_passozmenge_roundtrip.py"
      provides: "Mindestens 8 Test-Funktionen für PAssozMenge-Roundtrip"
      contains: "def test_roundtrip_passozmenge"
    - path: "engine/tests/fixtures/otx/passozmenge_minimal.otx"
      provides: "Demo-OTX mit Instanzen aller 4 konkreten PAssozMenge-Subklassen"
      contains: "PAssozMengeErzgt"
  key_links:
    - from: "test_otx_passozmenge_roundtrip.py"
      to: "engine/tests/fixtures/otx/passozmenge_minimal.otx"
      via: "load_otx_file(passozmenge_minimal_otx_path)"
      pattern: "passozmenge_minimal"
    - from: "test_otx_passozmenge_roundtrip.py"
      to: "PAssozMenge*-Subklassen aus assoziation.menge"
      via: "isinstance-Check"
      pattern: "isinstance\\(.*, PAssozMenge"
---

<objective>
Engine-Round-Trip-Tests für die PAssozMenge-Klassenfamilie, die den
verbindlichen SC-4 / SC-5 aus der ROADMAP erfüllen:

  load_to_wire(otx) → wire_to_otx → load_to_wire(otx2)
  ⇒ strukturell identische PAssozMenge*-Anzahl + identische m_iMenge*-Werte
    + identische m_lMengRess-Bindung.

Test-Fixture-Wahl: per AUDIT.md (Plan 01) Sektion 5 verbindlich entschieden.
Erwartung (default, falls AUDIT.md das so empfiehlt): konstruiertes OTX-File
`engine/tests/fixtures/otx/passozmenge_minimal.otx`, programmatisch via
`dump_simulator_to_otx` aus einem ad-hoc PSimulator-Setup erzeugt (analog
embb_pre_run.otx, aber klein und auf PAssozMenge-Coverage zugeschnitten).
Wenn AUDIT.md eine andere Option (existierende Fixture wiederverwenden ODER
ad-hoc-PSimulator pro Test ohne File-Persistenz) verbindlich nennt, wird
die umgesetzt.

Output: 1 neue Test-Datei mit ≥ 8 Tests + 1 Fixture-File (oder Builder-
Funktion, je nach AUDIT.md-Entscheidung).

KEINE Sim-Logik-Änderung. PAssozMenge-Sim-Methoden (RessVerfuegbar etc.)
sind bereits in engine/src/osim_engine/resources/assoziation/menge.py und
durch test_v5_material.py + test_v8_relation.py grün geprüft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md
@osim-ui/CLAUDE.md
</context>

<interfaces>
<!-- Roundtrip-Pattern aus tests/integration/io/test_otx_roundtrip.py Z.51-77 -->
def test_roundtrip_xxx(fixture_path: Path) -> None:
    original = load_otx_file(fixture_path)
    dumped_text = dump_simulator_to_otx(
        original.simulator,
        original_otx=original.otx,
        instances=original.instances,
    )
    parsed_back = parse_otx(dumped_text)
    from osim_engine.io.otx_loader import OtxLoader
    reloaded = OtxLoader().load(parsed_back)
    # assertions...

<!-- Existierende Sim-Builder aus tests/integration/test_v5_material.py — können wiederverwendet werden für Fixture-Generation -->
def _make_lager(sim, *, name, anfang=0, maximum=-1) -> PRessMenge
def _make_knoten(sim, *, name, dauer) -> PDpKnKonstant
def _attach_erzgt(knoten, lager, menge_aus=1) -> PAssozMengeErzgt
def _attach_verbr(knoten, lager, menge_ein=1) -> PAssozMengeVerbr
def _attach_ausloeser(sim, *, knoten, name, t) -> PAslEinzel

<!-- OtxLoader-API (otx_loader.py): -->
load_otx_file(path: Path) -> LoadResult  # .simulator, .otx, .instances, .loaded, .unsupported, .coverage_ratio

<!-- OtxWriter-API (otx_writer.py): -->
dump_simulator_to_otx(sim, *, original_otx=None, instances=None,
                       include_unsupported_passthrough=True) -> str

<!-- Sim-Klassen-Pfade: -->
from osim_engine.pps.simulator import PSimulator
from osim_engine.pps.ausloeser.einzel import PAslEinzel
from osim_engine.pps.knoten.zeitvorgabe import PDpKnKonstant
from osim_engine.resources.menge import PRessMenge
from osim_engine.resources.assoziation.menge import (
    PAssozMengeErzgt, PAssozMengeVerbr, PAssozMengeVerbrZwischen, PAssozMengeAbfr,
)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Test-Fixture-Datei (oder Builder-Funktion) erzeugen</name>
  <files>
    engine/tests/fixtures/otx/passozmenge_minimal.otx
    engine/tests/integration/io/conftest.py
  </files>
  <read_first>
    - .planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md (Sektion 5 verbindlich)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\integration\io\conftest.py (bestehende Fixture-Konventionen, z.B. embb_pre_run_otx_path)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\integration\test_v5_material.py (Builder-Pattern)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\fixtures\otx\embb_pre_run.otx (Format-Referenz)
  </read_first>
  <action>
    Implementierungsstrategie hängt von AUDIT.md Sektion 5 ab. Die default
    Vorgehensweise (wenn AUDIT.md nicht anders entscheidet):

    1. Schreibe ein Skript-/Builder-Pattern in einer NEUEN conftest-Fixture
       in `engine/tests/integration/io/conftest.py`:
       ```python
       @pytest.fixture
       def passozmenge_minimal_otx_path(tmp_path):
           """Generiert ein minimales OTX-File mit allen 4 PAssozMenge-Subklassen.

           Inhalt:
           - 1 Lager (PRessMenge) für Erzgt+Verbr
           - 1 weiteres Lager für VerbrZwischen+Abfr
           - 4 Knoten (jeder mit 1 PAssozMenge-Subklasse)
           - 1 Auslöser pro Knoten
           Total: 4 PAssozMenge-Instanzen, 2 PRessMenge.
           """
           sim = _build_passozmenge_demo()
           text = dump_simulator_to_otx(sim)
           p = tmp_path / "passozmenge_minimal.otx"
           p.write_text(text, encoding="latin-1")
           return p

       def _build_passozmenge_demo() -> PSimulator:
           sim = PSimulator()
           lager1 = PRessMenge(sim); lager1.m_sName = "Lager1"; lager1.m_iBestandAnfang = 0
           sim.register_ress_menge(lager1)
           lager2 = PRessMenge(sim); lager2.m_sName = "Lager2"; lager2.m_iBestandAnfang = 10
           sim.register_ress_menge(lager2)

           # K_Erz mit PAssozMengeErzgt(menge_aus=5) → Lager1
           # K_Vbr mit PAssozMengeVerbr(menge_ein=2) → Lager1
           # K_Zwi mit PAssozMengeVerbrZwischen(menge_ein=3) → Lager2
           # K_Abf mit PAssozMengeAbfr(menge_abfr=1) → Lager2
           ...
           return sim
       ```

    2. ZUSÄTZLICH: persistiere das File auf Repo-Ebene unter
       `engine/tests/fixtures/otx/passozmenge_minimal.otx`. Pre-build-Skript:
       ```python
       # engine/scripts/build_passozmenge_fixture.py (optional)
       from pathlib import Path
       from <module> import _build_passozmenge_demo, dump_simulator_to_otx
       sim = _build_passozmenge_demo()
       text = dump_simulator_to_otx(sim)
       Path("engine/tests/fixtures/otx/passozmenge_minimal.otx").write_text(text, encoding="latin-1")
       ```
       Ausgeführt via:
       `cd engine && uv run python -c "<inline-code>"`

    Falls AUDIT.md eine Wiederverwendung einer existierenden Fixture
    (embb_pre_run.otx, falls PAssozMenge-haltig) als Empfehlung markiert,
    DANN nur die conftest-Fixture ohne File-Persistenz schreiben (per Lookup
    auf das existierende File).

    Falls AUDIT.md ad-hoc-Sim ohne File-Persistenz empfiehlt: dann nur die
    Builder-Funktion in der Test-Datei (Plan 04 Task 2) — kein conftest,
    kein File. Dann Task 1 leerer Hülle: Builder-Funktion in Task 2 inkludiert.

    Die Default-Variante (File-basiert) ist robuster, weil das File später
    als E2E-Fixture für UI-Tests (Plan 07) wiederverwendbar ist.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/integration/io/conftest.py --collect-only -q 2>&1 || true</automated>
    <automated>powershell -Command "if (Test-Path 'engine/tests/fixtures/otx/passozmenge_minimal.otx') { Write-Host 'fixture file present'; (Get-Content 'engine/tests/fixtures/otx/passozmenge_minimal.otx' -Raw) -match 'PAssozMengeErzgt' } else { Write-Host 'fixture file absent (Builder-Variante)' }"</automated>
  </verify>
  <done>
    Fixture-Path verfügbar — entweder als File im Repo + conftest-Fixture
    oder als Builder-Funktion in der Test-Datei (je nach AUDIT.md-Entscheidung).
  </done>
  <acceptance_criteria>
    - Source (Default-File-Variante): Datei `engine/tests/fixtures/otx/passozmenge_minimal.otx` existiert.
    - Source (Default-File-Variante): Inhalt der Datei enthält "PAssozMengeErzgt", "PAssozMengeVerbr", "PAssozMengeAbfr" (mindestens diese 3 Substrings).
    - Source: conftest.py (oder die Test-Datei) enthält eine Builder-Funktion `_build_passozmenge_demo` ODER eine pytest-Fixture `passozmenge_minimal_otx_path`.
    - Test: `cd engine && uv run pytest tests/integration/io/conftest.py --collect-only -q` exit-code 0.
    - Behavior: Die Fixture/Builder erzeugt mindestens 4 PAssozMenge-Instanzen (1 pro Subklasse).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ≥ 8 Round-Trip-Tests in test_otx_passozmenge_roundtrip.py</name>
  <files>engine/tests/integration/io/test_otx_passozmenge_roundtrip.py</files>
  <read_first>
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\integration\io\test_otx_roundtrip.py (Pattern-Vorlage)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\tests\integration\test_v5_material.py (Sim-Setup-Pattern)
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\resources\assoziation\menge.py
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_loader.py
    - C:\Users\JörgWFischer\PycharmProjects\osim-engine\engine\src\osim_engine\io\otx_writer.py
    - .planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-01-AUDIT.md
  </read_first>
  <action>
    Datei `engine/tests/integration/io/test_otx_passozmenge_roundtrip.py` neu
    anlegen mit MINDESTENS 8 Test-Funktionen. Jeder Test folgt dem Pattern aus
    test_otx_roundtrip.py:

    ```python
    from pathlib import Path
    import pytest
    from osim_engine.io.otx_loader import load_otx_file, OtxLoader
    from osim_engine.io.otx_reader import parse_otx
    from osim_engine.io.otx_writer import dump_simulator_to_otx
    from osim_engine.resources.assoziation.menge import (
        PAssozMengeErzgt, PAssozMengeVerbr, PAssozMengeVerbrZwischen, PAssozMengeAbfr,
    )
    from osim_engine.resources.menge import PRessMenge

    def _roundtrip(sim_or_path):
        """Helfer: load → dump → reload, gibt (original, reloaded) zurück."""
        if isinstance(sim_or_path, Path):
            original = load_otx_file(sim_or_path)
        else:
            # ad-hoc sim path
            text = dump_simulator_to_otx(sim_or_path)
            original = OtxLoader().load(parse_otx(text))
        dumped = dump_simulator_to_otx(
            original.simulator,
            original_otx=original.otx,
            instances=original.instances,
        )
        reloaded = OtxLoader().load(parse_otx(dumped))
        return original, reloaded
    ```

    Pflicht-Test-Set (≥ 8):

    1. **test_roundtrip_passozmenge_erzgt_count_and_mengeaus**:
       Fixture lädt → Roundtrip → `reloaded.loaded["PAssozMengeErzgt"] == original.loaded["PAssozMengeErzgt"]`. Mengen-Werte (`m_iMengeAus`) auf allen Instanzen unverändert.

    2. **test_roundtrip_passozmenge_verbr_count_and_mengein**: analog für Verbr + m_iMengeEin.

    3. **test_roundtrip_passozmenge_verbr_zwischen_count_and_mengein**: analog für VerbrZwischen + m_iMengeEin.

    4. **test_roundtrip_passozmenge_abfr_count_and_mengeabfr**: analog für Abfr + m_iMengeAbfr.

    5. **test_roundtrip_passozmenge_m_l_meng_ress_bindung_erhalten**:
       Für jede PAssozMenge-Instanz im reloaded-Sim: `py.m_lMengRess is not None` AND `isinstance(py.m_lMengRess, PRessMenge)` AND `py.m_lMengRess.m_sName == <expected>`.

    6. **test_roundtrip_multi_erzgt_an_einem_lager**:
       Sim mit 3 Erzgt-Knoten → alle 3 → Lager1. Nach Roundtrip: 3 Erzgt-Instanzen, alle verweisen auf dieselbe (per OID-Identität rekonstruierte) PRessMenge.

    7. **test_roundtrip_multi_lager_und_subklassen_misch**:
       Sim mit 2 Lager und 4 Subklassen (Erzgt → L1, Verbr → L1, VerbrZwischen → L2, Abfr → L2). Nach Roundtrip: alle 4 Subklassen-Counts identisch, Lager-Bindung jeweils korrekt.

    8. **test_roundtrip_passozmenge_sim_runnable_after_reload**:
       Nach Roundtrip darf der reloaded-PSimulator gestartet werden
       (`reloaded.simulator.start()`) ohne Crash, und die Sim-Counter
       (z.B. lager.m_iBestandAktuell) ändern sich plausibel — mindestens
       1 Erzgt-Cycle muss durchlaufen.

    OPTIONAL (Bonus-Tests wenn Zeit):
    9. test_roundtrip_idempotent_zweimal: roundtrip → roundtrip nochmal → identisches Ergebnis.
    10. test_roundtrip_coverage_ratio_not_decreased: reloaded.coverage_ratio ≥ original.coverage_ratio (analog test_roundtrip_embb_coverage_ratio).

    Verwende für die Roundtrip-Fixtures (1-7) entweder:
    - die in Task 1 erzeugte passozmenge_minimal_otx_path-Fixture, ODER
    - eine pro-Test inline aufgebaute PSimulator-Instanz (für Tests 6+8, wo
      spezielle Setups nötig sind).

    KEINE Test darf das C++-Original benötigen oder eine externe Datei
    außerhalb von engine/tests/fixtures/otx/.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/integration/io/test_otx_passozmenge_roundtrip.py -v</automated>
    <automated>cd engine && uv run pytest -x -q</automated>
  </verify>
  <done>
    ≥ 8 neue Tests grün. Gesamtsuite grün. Set-Equality-Check unverändert grün.
  </done>
  <acceptance_criteria>
    - Source: Datei `engine/tests/integration/io/test_otx_passozmenge_roundtrip.py` existiert.
    - Source: Datei enthält mindestens 8 Funktionen, die mit `def test_roundtrip_passozmenge` beginnen (mit/ohne weiteren Suffix).
    - Test: `cd engine && uv run pytest tests/integration/io/test_otx_passozmenge_roundtrip.py -v` exit-code 0 mit ≥ 8 passed Tests.
    - Test: `cd engine && uv run pytest -x -q` exit-code 0 (kein Regression).
    - Behavior: Mindestens 1 Test prüft m_lMengRess-Bindung nach Reload (key_link).
    - Behavior: Mindestens 1 Test prüft, dass reloaded.simulator.start() ohne Crash läuft.
    - Behavior: Pro Subklasse (Erzgt, Verbr, VerbrZwischen, Abfr) gibt es mindestens 1 Test, der Count+Mengen-Attr prüft.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Round-Trip-Pipeline | Pipeline ist Datenverlust-kritisch — Tests sind die Wahrheits-Gatekeeper für Plan 02 + 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.3.04-01 | Tampering | Loader liest Attr, Writer schreibt aber falsch → Test grün trotz Bug | mitigate | Tests vergleichen ZWEI vollständige Roundtrips (load→dump→load), nicht nur Loader oder Writer isoliert. m_lMengRess wird über OID-Identität verifiziert (Test 5). |
| T-01.3.04-02 | Information Disclosure | Test-Fixture enthält Echte-Modell-Daten | accept | Fixture wird programmatisch aus dummy-Daten gebaut, keine Bosch-/Echte-Modelle. |
| T-01.3.04-03 | Denial of Service | Roundtrip-Sim hängt | mitigate | Test 8 hat impliziten pytest-Timeout (default); Sim ist klein (1-4 Knoten, 1-4 Auslöser). |
</threat_model>

<verification>
- ≥ 8 Tests neu, alle grün, 0 Regressionen.
- Set-Equality-Check (test_writer_handles_all_known_loader_classes) bleibt grün.
- m_lMengRess-Bindung nach Reload verifiziert.
</verification>

<success_criteria>
- SC-4 (Round-Trip-Tests) und SC-5 (alle Tests grün) der ROADMAP erfüllt.
- Plan 05 + 06 (UI) können auf einen stabilen Engine-Vertrag aufbauen.
</success_criteria>

<output>
Create `.planning/phases/01.3-passozmenge-wire-roundtrip-pressmengematrixviewer-migration-/01.3-04-SUMMARY.md` when done.
</output>
</content>
</invoke>