---
phase: 05-reports
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - app/db/models/report_template.py
  - alembic/versions/05xx_report_templates_table.py
  - app/services/template_service.py
  - app/services/template_security.py
  - app/api/v1/templates.py
  - app/templates/reports/report-beratung-steinbeis.html
  - app/templates/reports/report-klausur-hka.html
  - app/templates/reports/seeds/__init__.py
  - app/templates/reports/seeds/seed_default_templates.py
  - app/static/css/branding-steinbeis.css
  - app/static/css/branding-hka.css
  - portal/src/pages/admin/TemplatesPage.tsx
  - portal/src/components/templates/TemplateEditor.tsx
  - portal/src/api/templates.ts
  - tests/services/test_template_service.py
  - tests/services/test_template_security.py
  - tests/api/test_templates_endpoint.py
must_haves:
  truths:
    - "In Postgres existiert eine Tabelle `report_templates` mit Spalten (id, tenant_id NULL=global, name, jinja_template, css, is_default)"
    - "Drei Default-Templates sind seedbar: `report-standard` (aus Plan 02), `report-beratung-steinbeis`, optional `report-klausur-hka`"
    - "Tenant-Admins können in der UI eigene Templates anlegen, editieren, löschen und als Default setzen"
    - "User wählt beim Report-Trigger ein Template; Default-Template wird verwendet, wenn keins angegeben"
    - "Jinja2-Sandbox verhindert Code-Execution (kein Filesystem-Access, kein `eval`, kein Subprocess)"
    - "HKA-Klausur-Template ist EIN Beispiel-Template unter mehreren — NICHT die zentrale Motivation der Phase. Beratungs-Template (Steinbeis) ist gleichrangig"
  artifacts:
    - path: "app/db/models/report_template.py"
      provides: "Template-Tabelle mit Tenant-Isolation"
      contains: "class ReportTemplate"
    - path: "app/services/template_service.py"
      provides: "CRUD + Resolve (Template-ID + Tenant-Kontext → Jinja2-Quelle)"
      min_lines: 100
    - path: "app/services/template_security.py"
      provides: "Jinja2-Sandbox-Setup + erlaubte Filter/Tests"
      min_lines: 40
    - path: "app/templates/reports/report-beratung-steinbeis.html"
      provides: "Steinbeis-Beratungs-Brief-Template (sachlich, mit Kunde/Projekt-Header)"
      min_lines: 60
    - path: "portal/src/pages/admin/TemplatesPage.tsx"
      provides: "Tenant-Admin-UI für Template-Verwaltung"
      min_lines: 100
  key_links:
    - from: "app/services/report_service.py"
      to: "app/services/template_service.py"
      via: "Bei `render_pdf_from_run(template_id)` löst TemplateService die Quelle auf (Tenant-Custom > Default)"
      pattern: "template_service\\.resolve"
    - from: "app/services/template_service.py"
      to: "app/services/template_security.py"
      via: "Sandboxed Jinja2-Environment für User-Templates"
      pattern: "SandboxedEnvironment"
    - from: "portal/src/pages/admin/TemplatesPage.tsx"
      to: "/api/v1/templates"
      via: "TanStack-Query-Hook lädt Tenant-Templates"
      pattern: "useQuery.*templates"
requirements:
  - PHASE5-AC-5   # Tenant-spezifische Templates austauschbar
  - PHASE5-AC-6   # Steinbeis-Beratungs- + (optional) HKA-Klausur-Template
autonomous: true
---

<objective>
Template-Verwaltung in Postgres + Tenant-Admin-UI + zwei zusätzliche Default-Templates: ein industrielles Beratungs-Brief-Template (Steinbeis als konkretes Beispiel) und ein optionales akademisches Klausur-Datenblatt (HKA als konkretes Beispiel). User-Templates werden in einer Jinja2-Sandbox gerendert.

**Zweck:** Reports müssen Tenant-spezifisch brandbar sein, ohne dass dafür Code-Deployments nötig sind. Beratungskunden und Hochschulen brauchen unterschiedliche Headerlayouts, Schriftarten, Logos.

**Industrie-Fokus (Memo `feedback-target-audience-framing`):**
- Das **Beratungs-Template (Steinbeis-RIM-Brief)** ist ein voll ausgearbeitetes Beispiel — sachlich, mit Kunde/Projekt-Header, Management-Summary-Sektion oben, KPIs als Anhang. Es ist gleichrangig oder primär gegenüber dem HKA-Template.
- Das **HKA-Klausur-Template** ist ein OPTIONALES Beispiel — schlankes Datenblatt für Lehrkontexte. Es wird als zweites Beispiel mitgeliefert, um zu zeigen, dass Templates flexibel anpassbar sind.
- Die Tenant-Admin-UI ist generisch: jeder Tenant kann beliebig viele eigene Templates anlegen. Steinbeis und HKA sind nur die mitgelieferten Beispiele.

**Output:** Tabelle + CRUD + Sandbox + zwei zusätzliche Default-Templates + Tenant-Admin-UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-reports/05-02-PLAN.md
@.planning/phases/05-reports/05-02-SUMMARY.md
</context>

<interfaces>
class ReportTemplate (SQLAlchemy + Pydantic):
  id: UUID
  tenant_id: str | None      # NULL = global Default
  template_id: str           # slug: "standard", "beratung-steinbeis", "klausur-hka", "<custom-slug>"
  name: str                  # human readable
  description: str | None
  jinja_template: str        # full Jinja2 source
  css: str                   # custom CSS (additive zu print.css)
  is_default: bool           # nur eines pro Tenant true
  created_at, updated_at
  created_by_uid: str

class TemplateService:
  def resolve(template_id: str, tenant_id: str) -> tuple[str, str]:  # (jinja_src, css)
  def list_for_tenant(tenant_id: str) -> list[ReportTemplate]:
  def create(tenant_id: str, ...) -> ReportTemplate:
  def update(template_uuid: UUID, ...) -> ReportTemplate:
  def delete(template_uuid: UUID) -> None:
  def set_default(template_uuid: UUID) -> None:

# Resolve-Reihenfolge:
# 1. Tenant-Custom mit template_id (exact match in tenant scope)
# 2. Tenant-Default (is_default=true im Tenant)
# 3. Global Default (tenant_id IS NULL, template_id="standard")
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tabelle + Service + Sandbox + Endpoints</name>
  <files>app/db/models/report_template.py, alembic/versions/05xx_report_templates_table.py, app/services/template_service.py, app/services/template_security.py, app/api/v1/templates.py, tests/services/test_template_service.py, tests/services/test_template_security.py, tests/api/test_templates_endpoint.py</files>
  <behavior>
    - Service-Test: `resolve("standard", tenant="tenant_a")` ohne Tenant-Custom → liefert globales `report-standard`-Template
    - Service-Test: `resolve("standard", tenant="tenant_a")` mit Tenant-Custom `template_id="standard"` → liefert das Custom-Template (Tenant überschreibt Global)
    - Service-Test: `resolve("nonexistent", tenant="tenant_a")` → fällt auf Tenant-Default → wenn keiner gesetzt → fällt auf Global "standard"
    - Service-Test: `create` mit `is_default=True` setzt vorherige Defaults im Tenant auf `is_default=False`
    - Sandbox-Test: Template-Source `{{ ''.__class__.__mro__ }}` wirft `SecurityError` (klassischer Python-Sandbox-Escape verhindert)
    - Sandbox-Test: Template-Source `{% import os %}` wirft `SecurityError`
    - Sandbox-Test: Erlaubte Filter funktionieren: `{{ value | round(2) }}`, `{{ value | de_currency }}` (custom Filter für €-Format)
    - Endpoint-Test: `GET /api/v1/templates` listet Tenant-Templates + Global-Defaults (UNION)
    - Endpoint-Test: `POST /api/v1/templates` erfordert `tenant.role in (admin, owner)` (Firebase Custom Claim) — sonst 403
    - Endpoint-Test: `PUT /api/v1/templates/{id}` validiert Jinja2-Syntax vor dem Speichern (compile-time, kein Render)
    - Tenant-Test: User aus Tenant A kann Templates aus Tenant B nicht editieren → 404
  </behavior>
  <action>
    SQLAlchemy-Modell `app/db/models/report_template.py`:
    - Tabelle `report_templates` mit Constraints:
      - `UNIQUE (tenant_id, template_id)` (case-sensitive slug)
      - Partial Index: `CREATE UNIQUE INDEX one_default_per_tenant ON report_templates(tenant_id) WHERE is_default = true`
      - `jinja_template`, `css` als TEXT (kein Limit, aber API validiert max ~100 KB)

    Alembic-Migration mit Up/Down + Seed-Hook für Globals.

    Sandbox in `app/services/template_security.py`:
    - Verwende `jinja2.sandbox.SandboxedEnvironment` (nicht das normale `Environment`)
    - `autoescape=True` für HTML-Output
    - Whitelist Filter: `round, length, default, upper, lower, capitalize, replace, join, format`
    - Custom Filter: `de_currency` (`{:.2f}`.format → `,` statt `.` als Dezimal-Trenner), `de_percent` (Wert mit `%`-Suffix), `de_datetime` (Format `dd.mm.YYYY HH:MM`)
    - Globals: stelle `now()`, `engine_version`, `osim_ui_version` zur Verfügung — KEIN Filesystem, KEIN `os`, KEIN `import`
    - `attribute_access` callback: verhindere Access auf `__class__`, `__bases__`, `__mro__`, `__init__`, `__globals__` (analog gängiger Jinja-Sandbox-Escape-CVEs)

    Service `app/services/template_service.py`:
    - Methoden gemäß `<interfaces>`-Block
    - `resolve`: Reihenfolge implementieren wie spezifiziert
    - `validate_jinja(source: str) -> None`: erzeugt `env.from_string(source)` und wirft bei Syntaxfehler `TemplateValidationError` mit Zeile/Spalte

    Endpoints `app/api/v1/templates.py`:
    - `GET /api/v1/templates` — listet Tenant + Globals
    - `GET /api/v1/templates/{template_uuid}` — einzelnes Template
    - `POST /api/v1/templates` — erstellt (nur admin/owner)
    - `PUT /api/v1/templates/{template_uuid}` — updated (nur admin/owner, nur eigene Tenant-Templates)
    - `DELETE /api/v1/templates/{template_uuid}` — löscht (nur admin/owner)
    - `POST /api/v1/templates/{template_uuid}/set-default` — setzt als Tenant-Default
    - `POST /api/v1/templates/validate` — validiert Jinja-Source ohne zu speichern (für Live-Editor in Plan 03 Task 3)

    Berechtigung via Firebase-Custom-Claim `role`. Helper `require_admin_or_owner(request)` in `app/auth/permissions.py`.

    Update `app/services/report_service.py` (aus Plan 02):
    - `_load_template(template_id)` ruft jetzt `TemplateService.resolve(template_id, tenant_id)` auf
    - Wenn Tenant-Custom: rendere in Sandbox
    - Wenn Global-Default (built-in): rendere in normalem Env (vertrauenswürdig)
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_template_service.py tests/services/test_template_security.py tests/api/test_templates_endpoint.py -x</automated>
  </verify>
  <done>Template-CRUD funktioniert mit Tenant-Isolation; Sandbox verhindert Code-Execution; Resolve-Hierarchie korrekt; Endpoints rollenbasiert geschützt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Beratungs- + Klausur-Default-Template + Branding-CSS + Seed</name>
  <files>app/templates/reports/report-beratung-steinbeis.html, app/templates/reports/report-klausur-hka.html, app/static/css/branding-steinbeis.css, app/static/css/branding-hka.css, app/templates/reports/seeds/__init__.py, app/templates/reports/seeds/seed_default_templates.py, tests/services/test_default_templates_render.py</files>
  <behavior>
    - Render-Test: `report-beratung-steinbeis` mit Dummy-Run-Daten + Optionen `{"customer": "ACME GmbH", "project": "Werk-1-Optimierung"}` → PDF enthält Kunden-/Projekt-Header oben, "Management-Summary"-Sektion, anschließend KPIs als Anhang
    - Render-Test: `report-klausur-hka` mit Dummy-Run-Daten + Optionen `{"course": "ERP-Systeme", "exam_date": "2026-07-15", "student_id_redacted": True}` → PDF mit HKA-Kopfzeile, Aufgabenstellungs-Slot leer (vom Lehrenden auszufüllen), KPI-Tabellen
    - Branding-Test: Beratungs-Template lädt `branding-steinbeis.css` (Sans-Serif klassisch, dezenter Steinbeis-Akzent in Trennlinien), Logo via Konfig-Option-Slot
    - Branding-Test: HKA-Template lädt `branding-hka.css`, hat Slot für HKA-Logo im Header (Pfad aus Tenant-Konfig, kein hartkodiertes Asset im Repo)
    - Seed-Test: `seed_default_templates()` legt 3 Templates mit `tenant_id IS NULL` in DB an; zweiter Aufruf ist idempotent (UPSERT)
    - DSGVO-Test: Beratungs-Template hat KEINE Felder für Personendaten ohne expliziten Opt-in; Klausur-Template-Option `student_id_redacted=True` ersetzt User-IDs durch `[redigiert]`
  </behavior>
  <action>
    Template `app/templates/reports/report-beratung-steinbeis.html` (sachlich, akademisch, industrie-orientiert):
    - Header: `{% block header %}` mit Kunden-Logo-Slot (`options.customer_logo_url`), "Steinbeis-Transferzentrum RIM" als Default-Absender, Datum
    - Sektionen:
      1. **Projekt-Header**: Kunde, Projekt, Auftragsnummer (alles aus `options`)
      2. **Management-Summary**: 1 Absatz Freitext aus `options.summary` (vom Berater ausgefüllt), automatisch generierte Top-3-KPIs darunter (z.B. höchste Auslastung, mittlere Durchlaufzeit, gesamte Prozesskosten)
      3. **Annahmen**: Sim-Parameter (Seed, Perioden, Repeats) — wichtig für Reproduzierbarkeit der Analyse
      4. **Detail-Auswertung**: alle KPIs als Tabellen (wie Standard, aber mit Steinbeis-Akzent in Trennlinien)
      5. **Charts**: Auslastung + Durchlaufzeit-Histogramm
      6. **Anhang**: Modell-Topologie-Übersicht (Liste von Durchlaufplänen + Knoten-Count)
    - Footer: "Erstellt mit osim-ui — Steinbeis-Transferzentrum RIM" + Generierungs-Timestamp

    Template `app/templates/reports/report-klausur-hka.html` (schlankes Datenblatt):
    - Header: HKA-Logo-Slot (`options.institution_logo_url`), "Hochschule Karlsruhe — Fakultät Wirtschaftsinformatik" als Default, Veranstaltung + Klausurdatum aus `options`
    - Sektionen:
      1. **Aufgaben-Header**: Veranstaltung, Datum, Aufgabe (Freitext-Slot)
      2. **Modell-Spezifikation**: Sim-Parameter + Modell-Topologie
      3. **Geforderte Auswertung**: KPI-Tabellen (Lehrende können per Option entscheiden, welche KPIs anzuzeigen sind — z.B. `options.kpi_filter=["mittlere_durchlaufzeit", "auslastung"]`)
      4. **Optional Charts** je nach Option
    - Footer: "Klausur-Datenblatt — HKA / osim-ui"
    - DSGVO-Bewusstsein: Wenn `options.student_id_redacted=True`, ersetze alle User-/Auftragsnamen, die einer Person zuordenbar sind, durch `[redigiert]`. Standardmäßig OFF — Lehrende entscheiden bewusst.

    CSS `app/static/css/branding-steinbeis.css`:
    - Akzent-Farbe `#003063` (dezentes Dunkelblau, kein Steinbeis-Logo-Rot reproduzieren ohne Markenrechtsabsicherung)
    - Tabellen-Header mit dünner unterer Trennlinie in Akzent-Farbe
    - Footer mit dezenter Trennlinie

    CSS `app/static/css/branding-hka.css`:
    - Akzent-Farbe `#7A003C` (HKA-Burgund, dezent verwendet)
    - Klausur-typisches Punktegitter im Aufgaben-Header für handschriftliche Notizen (`background-image: linear-gradient(...)`)
    - Schmalere Margins als Standard (mehr Platz für Antworten/Berechnungen)

    Seed `app/templates/reports/seeds/seed_default_templates.py`:
    - Funktion `seed_default_templates(session)` mit 3 Einträgen:
      1. `template_id="standard"`, lade `report-standard.html` von Filesystem
      2. `template_id="beratung-steinbeis"`, lade `report-beratung-steinbeis.html`
      3. `template_id="klausur-hka"`, lade `report-klausur-hka.html`
    - Alle `tenant_id=NULL`, `is_default=False`
    - Idempotent via `INSERT ... ON CONFLICT (tenant_id, template_id) DO UPDATE`
    - Aufgerufen von App-Startup (`app/main.py`) ODER expliziter CLI: `uv run python -m app.cli seed-templates`. Entscheidung: CLI, weil Startup-Hook in Tests/Migrations nervig wird.

    **Wichtig — Markenrecht / Logos:**
    - Repo enthält KEINE Logo-Bitmaps. Slots `{{ options.customer_logo_url }}`, `{{ options.institution_logo_url }}` erwarten Storage-URLs, die Tenant-Admins hochladen.
    - Default-CSS verwendet dezente Akzentfarben, NICHT die geschützten Marken-Rot/Blau-Töne von Steinbeis/HKA in voller Sättigung.
    - Im SUMMARY notieren: Tenant-Admin muss vor produktiver Nutzung freigeben, dass Logo-URL korrekt ist und Marke verwendet werden darf.
  </action>
  <verify>
    <automated>uv run pytest tests/services/test_default_templates_render.py -x</automated>
  </verify>
  <done>Beide Default-Templates rendern PDFs mit Branding-CSS; Seed-Skript ist idempotent; DSGVO-Optionen für Klausur-Template funktionieren; keine geschützten Markenwerte im Repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tenant-Admin-UI für Template-Verwaltung</name>
  <files>portal/src/pages/admin/TemplatesPage.tsx, portal/src/components/templates/TemplateEditor.tsx, portal/src/api/templates.ts, portal/src/components/templates/TemplatesList.tsx, portal/tests/pages/TemplatesPage.test.tsx</files>
  <behavior>
    - List-Test: `/admin/templates` zeigt Tabelle: Name, Template-ID, "Standard?"-Badge, "Bearbeiten"/"Löschen"/"Als Default setzen"-Buttons
    - List-Test: Globale Default-Templates erscheinen mit Label "Global" und nur Read-Only ("Kopie erstellen"-Button statt "Bearbeiten")
    - Editor-Test: TemplateEditor zeigt zwei Tabs: "Jinja2-Quelle" und "Custom CSS". Beide Monaco-Editor (Syntax-Highlight Jinja2 / CSS)
    - Editor-Test: "Validieren"-Button ruft `POST /api/v1/templates/validate` → zeigt Syntax-Fehler inline mit Zeile/Spalte
    - Editor-Test: "Vorschau"-Button → POST `/api/v1/runs/{demo_run_id}/reports` mit `template_id=<draft>` → PDF-Preview-Dialog
    - Permission-Test: Nicht-Admin-User sieht "/admin/templates" nicht in Navigation und bekommt 403 bei direktem Aufruf
    - Tenant-Isolation: Templates aus anderem Tenant werden nicht in der Liste angezeigt
  </behavior>
  <action>
    Frontend-API-Client `portal/src/api/templates.ts`:
    - `listTemplates() -> ReportTemplate[]`
    - `getTemplate(id) -> ReportTemplate`
    - `createTemplate(data) -> ReportTemplate`
    - `updateTemplate(id, data) -> ReportTemplate`
    - `deleteTemplate(id) -> void`
    - `setDefault(id) -> void`
    - `validateJinja(source) -> { valid: bool, error?: { line, column, message } }`
    - `cloneTemplate(globalTemplateId) -> ReportTemplate` (kopiert ein globales Template in den eigenen Tenant zum Editieren)
    - TanStack-Query-Hooks: `useTemplates()`, `useTemplate(id)`, `useCreateTemplate()`, etc.

    Page `portal/src/pages/admin/TemplatesPage.tsx`:
    - Route: `/admin/templates` (geschützt durch `<RequireRole role="admin" />`-Wrapper aus 3fls-Pattern)
    - Layout: Sidebar-Tree (aus Phase 1 Viewer-Foundation) links mit Templates, Detail rechts (TemplateEditor)
    - Header: "Neu erstellen"-Button (öffnet leeres Template-Form), Filter "Eigene / Global"

    Component `portal/src/components/templates/TemplatesList.tsx`:
    - Tabelle (shadcn `<Table>`) mit Spalten: Name, Template-ID, Default-Badge, Aktionen
    - Bei Klick auf Zeile: rechter Detail-Bereich zeigt Editor

    Component `portal/src/components/templates/TemplateEditor.tsx`:
    - Form-State mit `react-hook-form` + Zod-Validierung
    - Tab 1: Jinja2-Quelle (Monaco-Editor, `language="html"` mit Jinja-Snippets)
    - Tab 2: Custom CSS (Monaco-Editor, `language="css"`)
    - Toolbar: "Speichern", "Validieren", "Vorschau", "Als Default", "Löschen"
    - "Vorschau"-Button: nutzt einen vom Backend definierten Demo-Run (z.B. ein vorgenerierter Run mit Fixture-KPIs); öffnet PDF im neuen Tab via Signed URL
    - Live-Save: Auto-Save 30 s wie in Phase 1 (D-11), Dirty-Indicator wie Modell-Editor

    Backend-Erweiterung (klein, gehört aber logisch hier rein):
    - `GET /api/v1/templates/demo-run` — liefert die Run-ID eines vorgenerierten Demo-Runs für Preview. Wenn nicht vorhanden: 404 mit Hinweis "Lege per Sim-Lauf einen Demo-Run an und markiere ihn via `PATCH /api/v1/runs/{id} {is_template_demo: true}`"
    - Migration ergänzen: `runs.is_template_demo BOOLEAN NOT NULL DEFAULT false`

    Navigation: ergänze in `portal/src/components/Navigation.tsx` einen Admin-Bereich mit Link auf `/admin/templates`, sichtbar nur bei `role in (admin, owner)`.
  </action>
  <verify>
    <automated>cd portal && npm run test -- TemplatesPage</automated>
  </verify>
  <done>Tenant-Admins können Templates browser-basiert erstellen, editieren, validieren, in der Vorschau testen, als Default setzen und löschen; Sandbox verhindert gefährliche Templates serverseitig; UI rollenbasiert geschützt.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/services/test_template_service.py tests/services/test_template_security.py tests/services/test_default_templates_render.py tests/api/test_templates_endpoint.py -v` → grün
- `cd portal && npm run test` → grün
- `uv run python -m app.cli seed-templates` → seeded 3 Globals
- Integration: Admin-User loggt sich ein, navigiert `/admin/templates`, sieht 3 Globals + Tenant-Customs, kann ein Global "klonen", editieren, validieren, Vorschau anzeigen, speichern, als Default setzen
- Sicherheits-Smoke-Test: versuche Template mit `{{ ''.__class__.__mro__ }}` zu speichern → Validate-Endpoint lehnt ab
- Markenrechts-Check: `git grep "steinbeis-logo\|hka-logo"` findet KEINE Bitmap-Dateien im Repo
</verification>

<success_criteria>
- Template-Verwaltung über UI funktional; Tenant-Isolation und Rollen-Gating durchgesetzt
- Jinja2-Sandbox verhindert gängige Escape-Patterns
- Drei Default-Templates: Standard (industrieneutral), Beratung-Steinbeis (Beispiel B2B), Klausur-HKA (Beispiel Lehre) — alle gleichrangig als Beispiele
- Branding-CSS ohne geschützte Markenwerte im Repo; Logos kommen via Tenant-Upload
- Preview-Funktion erlaubt Live-Test eines Templates gegen Demo-Run
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/05-reports/05-03-SUMMARY.md` mit:
- Sandbox-Konfiguration und welche Filter/Globals zugelassen sind
- Wie Tenants eigene Logos einspielen (Upload-Pfad, Storage-Konvention)
- Markenrechts-Hinweis: Logos sind Verantwortung des Tenants
- Wie der Demo-Run für Preview konfiguriert wird
- Welche Optionen für jedes Template existieren (für Entwickler-Doku in Plan 07)
</output>
</content>
</invoke>