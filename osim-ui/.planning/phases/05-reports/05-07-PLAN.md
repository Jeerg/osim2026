---
phase: 05-reports
plan: 07
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06]
files_modified:
  - portal/tests/e2e/reports/standard-report-flow.spec.ts
  - portal/tests/e2e/reports/template-management-flow.spec.ts
  - portal/tests/e2e/reports/export-csv-xlsx-flow.spec.ts
  - portal/tests/e2e/reports/bundle-roundtrip-flow.spec.ts
  - portal/tests/e2e/reports/chart-viewer-flow.spec.ts
  - portal/tests/visual/pdf-snapshots.spec.ts
  - portal/tests/visual/chart-snapshots.spec.ts
  - tests/integration/test_phase5_stress.py
  - tests/integration/test_phase5_end_to_end.py
  - tests/fixtures/phase5_demo_run.py
  - portal/playwright.config.ts
  - docs/PHASE-5-REPORTS.md
  - docs/TEMPLATE-AUTHORING-GUIDE.md
  - .planning/phases/05-reports/05-07-SUMMARY.md
must_haves:
  truths:
    - "Playwright-E2E-Suite deckt alle 5 User-Hauptpfade ab: Standard-Report, Template-Verwaltung, CSV/XLSX-Export, Bundle-Round-Trip, Chart-Viewer-Interaktion"
    - "Visual-Regression-Tests vergleichen PDF-Snapshots und Chart-SVG-Snapshots gegen versionierte Baselines; Fail bei Pixel-Diff > Schwellwert"
    - "Backend-Integration-Tests decken kompletten Phase-5-Datenfluss ab: Run → KPI-Berechnung → PDF-Generation + Export + Bundle"
    - "Stress-Test: 10 parallele Report-Generation-Jobs laufen ohne API-Ausfall in <60 s Gesamtzeit"
    - "Doku in `docs/PHASE-5-REPORTS.md` (System-Übersicht + API-Referenz) und `docs/TEMPLATE-AUTHORING-GUIDE.md` (für Tenant-Admins, die eigene Templates schreiben)"
    - "Phase-5-SUMMARY mit Status aller Akzeptanzkriterien, bekannten Limitationen, Empfehlungen für Phase 6"
  artifacts:
    - path: "portal/tests/e2e/reports/standard-report-flow.spec.ts"
      provides: "Playwright-Test: Run → Trigger Standard-Report → PDF-Download → Inhalts-Validierung"
      min_lines: 60
    - path: "portal/tests/visual/pdf-snapshots.spec.ts"
      provides: "Visual-Regression für 3 Default-Templates"
      min_lines: 40
    - path: "tests/integration/test_phase5_stress.py"
      provides: "10x parallele Report-Generation via asyncio.gather"
      min_lines: 50
    - path: "docs/PHASE-5-REPORTS.md"
      provides: "Architektur-Übersicht + API-Referenz + Operating-Notes"
      min_lines: 150
    - path: "docs/TEMPLATE-AUTHORING-GUIDE.md"
      provides: "Tenant-Admin-Anleitung zum Schreiben eigener Templates"
      min_lines: 100
  key_links:
    - from: "portal/tests/e2e/"
      to: "Backend-Endpoints aus Plan 01-06"
      via: "Playwright nutzt echten Browser gegen lokalen docker-compose-Stack"
      pattern: "page\\.goto\\|page\\.click"
    - from: "tests/integration/test_phase5_end_to_end.py"
      to: "Services aus Plan 01-05"
      via: "httpx.AsyncClient direkt gegen FastAPI-App"
      pattern: "await client\\.(get|post)"
requirements:
  - PHASE5-AC-15  # E2E-Verifikation
  - PHASE5-AC-16  # Visual-Regression
  - PHASE5-AC-17  # Stress-Test 10 parallele Reports
  - PHASE5-AC-18  # Doku komplett
autonomous: false   # enthält checkpoint:human-verify am Ende (Phase-Abnahme)
---

<objective>
Phase-5-Verifikation: E2E-Tests, Visual-Regression, Stress-Tests und Doku. Sichert ab, dass alle Plan-01-06-Komponenten zusammen funktionieren, keine UI- oder PDF-Regressionen bei zukünftigen Änderungen entstehen, und das System produktiv betreibbar ist.

**Zweck:** Phase 5 erzeugt sechs neue Subsysteme (KPI-Pipeline, PDF, Templates, Export, Bundle, Chart-Viewer). Die Integrationsfläche ist groß. Ohne dedizierte E2E + Visual + Stress + Doku ist Phase 5 nicht abnehmbar.

**Output:** Test-Suite vollständig grün, Visual-Baselines committed, Doku publiziert, Phase-Abnahme-Checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-reports/05-PRELIMINARY-PLAN.md
@.planning/phases/05-reports/05-01-PLAN.md
@.planning/phases/05-reports/05-02-PLAN.md
@.planning/phases/05-reports/05-03-PLAN.md
@.planning/phases/05-reports/05-04-PLAN.md
@.planning/phases/05-reports/05-05-PLAN.md
@.planning/phases/05-reports/05-06-PLAN.md

# Alle SUMMARYs aus den vorherigen Plans (entstehen während Phase 5):
@.planning/phases/05-reports/05-01-SUMMARY.md
@.planning/phases/05-reports/05-02-SUMMARY.md
@.planning/phases/05-reports/05-03-SUMMARY.md
@.planning/phases/05-reports/05-04-SUMMARY.md
@.planning/phases/05-reports/05-05-SUMMARY.md
@.planning/phases/05-reports/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-Fixtures + Demo-Run-Seed + Playwright-E2E-Suite</name>
  <files>tests/fixtures/phase5_demo_run.py, portal/tests/e2e/reports/standard-report-flow.spec.ts, portal/tests/e2e/reports/template-management-flow.spec.ts, portal/tests/e2e/reports/export-csv-xlsx-flow.spec.ts, portal/tests/e2e/reports/bundle-roundtrip-flow.spec.ts, portal/tests/e2e/reports/chart-viewer-flow.spec.ts, portal/playwright.config.ts</files>
  <action>
    Erstelle `tests/fixtures/phase5_demo_run.py`:
    - Pytest-Fixture + CLI-Skript `seed_phase5_demo_data`:
      - Legt einen Demo-Tenant `tenant_phase5_test` an
      - Importiert `Vorstellung04/Dummy.otx` als Modell
      - Erzeugt einen Mock-Run mit synthetischer Trace-JSONL (5000 Events, deterministisch via fixed Seed)
      - Erzeugt einen Multi-Run-Eintrag mit 5 Sub-Runs (für Aggregations-Tests)
    - Wird in CI vor E2E-Lauf via `uv run python -m tests.fixtures.phase5_demo_run` aufgerufen

    Playwright-Konfig `portal/playwright.config.ts`:
    - Base-URL `http://localhost:5173` (Vite-Dev) oder `http://localhost:8080` (Production-Build)
    - Browser: Chromium + Firefox (Safari opt-in)
    - Auth-State-Setup: Login einmal pro Suite, JWT in `playwright/.auth/user.json` cachen
    - Globaler Setup-Hook: ruft `seed_phase5_demo_data` falls Demo-Run noch nicht existiert

    E2E-Suite (5 Files):

    **1. `standard-report-flow.spec.ts`:**
    - Login → Navigate zu Demo-Run → Klick "Report erzeugen" → Template "Standard" wählen → Wait for Status "succeeded" → Click "Download" → Assert PDF Magic-Header in heruntergeladener Datei

    **2. `template-management-flow.spec.ts`:**
    - Login als Admin → Navigate `/admin/templates` → "Standard" klonen → Editor öffnet sich → Jinja-Source modifizieren (z.B. Header-Text ändern) → "Validieren" → "Speichern" → Run-Detail-Page → Neuen Report mit Custom-Template → PDF enthält geänderten Header-Text

    **3. `export-csv-xlsx-flow.spec.ts`:**
    - Login → Demo-Run-Page → Klick "CSV exportieren" → ZIP wird heruntergeladen → entpacken (Playwright-Filehandling) → assert 5 CSV-Files + metadata.json vorhanden, deutsche Umlaute im Knoten-CSV erhalten
    - Klick "Excel exportieren" → XLSX wird heruntergeladen → mit `xlsx`-Node-Lib in Test parsen → assert 6 Tabs vorhanden, Header bold

    **4. `bundle-roundtrip-flow.spec.ts`:**
    - Login → Demo-Run-Page → "Bundle exportieren" → ZIP heruntergeladen
    - Logout, einloggen als anderer User (anderer Tenant)
    - Navigate `/bundles/import` → ZIP per Drag-and-Drop hochladen → Checkbox "Replay-Lauf" → Submit
    - Wait for Replay-Run-Erfolg → KPIs des Replay-Runs holen → mit Original-KPIs vergleichen → assert deep-equal (bei gleicher Engine-Version)

    **5. `chart-viewer-flow.spec.ts`:**
    - Login → Demo-Run → Tab "Analyse" → Tab "Auslastung" → assert 4 Balken sichtbar (eine pro Belegungsressource), Hover-Tooltip funktioniert, Klick auf Balken triggert Selection-Event
    - Tab "Durchsatz" → Line-Chart sichtbar
    - Tab "Durchlaufzeit" → Histogramm sichtbar
    - Tab "Ressourcen-Status" → Gantt sichtbar

    CI-Integration: Playwright läuft gegen `docker compose up` Stack (Postgres + Redis + Storage + Headless-Renderer). GitHub-Actions-Workflow (separater Plan außerhalb von 05 — als Notiz im SUMMARY).
  </action>
  <verify>
    <automated>cd portal && npx playwright test reports/</automated>
  </verify>
  <done>Alle 5 E2E-Specs grün; Demo-Fixture deterministisch reproduzierbar.</done>
</task>

<task type="auto">
  <name>Task 2: Visual-Regression + Stress-Test + Backend-Integration</name>
  <files>portal/tests/visual/pdf-snapshots.spec.ts, portal/tests/visual/chart-snapshots.spec.ts, tests/integration/test_phase5_stress.py, tests/integration/test_phase5_end_to_end.py</files>
  <action>
    Visual-Regression `portal/tests/visual/pdf-snapshots.spec.ts`:
    - Für jedes Default-Template (`standard`, `beratung-steinbeis`, `klausur-hka`):
      - Generiere PDF gegen Demo-Run
      - Konvertiere PDF → PNG pro Seite (via `pdf2pic` oder `pdftoppm` CLI im CI)
      - Vergleiche mit Baseline in `portal/tests/visual/baselines/pdf/` via Pixel-Diff (z.B. `pixelmatch`)
      - Schwellwert: 0.1% Pixel-Diff erlaubt (für Anti-Aliasing-Toleranz)
      - Bei Fail: schreibt Diff-Bild ins Artifact-Verzeichnis
    - Baselines werden bei erster Ausführung committed (manuell-genehmigt)
    - Update-Workflow: `npx playwright test visual/pdf-snapshots.spec.ts --update-snapshots` nach genehmigter UI-Änderung

    Visual-Regression `portal/tests/visual/chart-snapshots.spec.ts`:
    - Pro Chart-Viewer-Klasse (4 Stück): Headless-Render mit fixiertem Input → SVG mit Baseline vergleichen
    - SVGs sind text-deterministisch → Diff via `diff` Tool, kein Pixel-Diff nötig
    - Bei Diff: Test fails mit Inline-Diff der SVG-Strings

    Stress-Test `tests/integration/test_phase5_stress.py`:
    - Pytest-Test `test_10_parallel_report_jobs`:
      - Setup: 10 unterschiedliche Demo-Runs in DB
      - `asyncio.gather` für 10 parallele `POST /api/v1/runs/{id}/reports` Calls
      - Warten auf alle Job-Completion (Polling jeden 500ms, Timeout 60s)
      - Assert: alle 10 succeeded, kein Job >30s
      - Assert: Server-Memory am Ende < 1 GB (via `psutil`)
    - Optional: `test_100_serial_csv_exports` als Throughput-Baseline

    Backend-Integration `tests/integration/test_phase5_end_to_end.py`:
    - `test_full_phase5_data_flow`:
      1. Erstelle Run (skipped wenn aus Phase 1+ Sim-Flow ohnehin existiert)
      2. GET /runs/{id}/kpis → Cache leer, Berechnung läuft, Persistierung erfolgt
      3. POST /runs/{id}/reports → Job erzeugt, Worker rendert PDF, GET liefert Signed URL
      4. GET /runs/{id}/export?format=xlsx → XLSX heruntergeladen
      5. POST /runs/{id}/bundle → Bundle heruntergeladen
      6. POST /bundles/import mit Bundle → in anderem Tenant, mit replay=True
      7. Replay-Run abwarten, KPIs vergleichen
    - `test_template_resolve_hierarchy`: deckt Plan-03-Resolve-Logik gegen real-life Daten ab
    - `test_aggregator_with_failed_subruns`: Multi-Run mit 1 failed Sub-Run → Aggregation überspringt
  </action>
  <verify>
    <automated>cd portal && npx playwright test visual/ && uv run pytest tests/integration/test_phase5_stress.py tests/integration/test_phase5_end_to_end.py -v</automated>
  </verify>
  <done>Visual-Baselines committed; Stress-Test bestätigt 10 parallele Jobs ohne Ausfall; Integration-Tests decken kompletten Datenfluss ab.</done>
</task>

<task type="auto">
  <name>Task 3: Doku — PHASE-5-REPORTS.md + TEMPLATE-AUTHORING-GUIDE.md</name>
  <files>docs/PHASE-5-REPORTS.md, docs/TEMPLATE-AUTHORING-GUIDE.md</files>
  <action>
    Schreibe `docs/PHASE-5-REPORTS.md`:
    - Architektur-Übersicht: Datenfluss von Trace → KPI-Service → (PDF | XLSX | CSV | Bundle), Diagramm als Mermaid
    - API-Referenz: alle Endpoints aus Plan 01-05 mit Request/Response-Schemas (Auto-Generierung aus OpenAPI ist Plus, manuelle Kuration für die wichtigsten Endpoints)
    - Operating-Notes:
      - Welche Services müssen laufen (Postgres, Redis, Storage, Headless-Renderer-Sidecar)
      - Wie skaliert man Report-Worker (in Phase 4 Cloud Run Jobs, in Phase 5 lokal via arq-Worker-Replicas)
      - Cache-Invalidation: `kpi_schema_version` und `engine_version` triggern Recompute
      - Storage-Layout: `reports/{tenant}/{job_id}.pdf`, `traces/{tenant}/{run_id}.jsonl.gz`
    - Performance-Tabelle: Render-Zeit pro Template + Modellgröße (Messungen aus Plan 02/06 SUMMARYs)
    - Bekannte Limitationen
    - Migrations-Pfad Phase 5 → Phase 6 (3fls-Integration nutzt diese Pipeline iframe-eingebettet)

    Schreibe `docs/TEMPLATE-AUTHORING-GUIDE.md`:
    - Zielgruppe: Tenant-Admins, die eigene Branding-/Inhalts-Templates schreiben
    - Sektionen:
      1. **Warum eigene Templates?** Beispiele Beratungs-Brief, Klausur-Datenblatt, internes Audit-Format
      2. **Voraussetzungen:** Admin-Rolle, Grundkenntnisse HTML + CSS + Jinja2
      3. **Template-Struktur:** verfügbare Variablen (KPIs, Run-Metadaten, Options), erlaubte Jinja-Filter (aus Sandbox), Beispiele
      4. **Custom-CSS:** wie man Print-Layout anpasst (`@page`, Page-Breaks, eigene Schriftarten via Tenant-Asset-URLs)
      5. **Charts einbetten:** wie man `{{ chart('auslastung', width=600, height=300) }}` verwendet
      6. **Logo-Upload:** Storage-Pfad für Tenant-Assets, Referenzierung im Template
      7. **Vorschau-Workflow:** Demo-Run nutzen, Live-Editor in Admin-UI
      8. **Sicherheits-Grenzen:** was die Sandbox erlaubt/verbietet, warum
      9. **Beispiele:** vollständiges Mini-Template als Copy-Paste-Startpunkt
      10. **Häufige Probleme** (FAQ): "Tabelle zu breit für A4", "Schriftart wird im PDF nicht angezeigt", "Sandbox lehnt mein Template ab"
    - Markenrechts-Hinweis: Tenant verantwortet Logo-Lizenzen
  </action>
  <verify>
    <automated>test -f docs/PHASE-5-REPORTS.md && test -f docs/TEMPLATE-AUTHORING-GUIDE.md && wc -l docs/PHASE-5-REPORTS.md docs/TEMPLATE-AUTHORING-GUIDE.md</automated>
  </verify>
  <done>Beide Doku-Files vorhanden, mindestens 150 bzw. 100 Zeilen, inhaltlich vollständig.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase-5-Abnahme-Checkpoint</name>
  <what-built>
    Sechs Plan-Outputs aus Phase 5 wurden integriert:
    - KPI-Pipeline (Plan 01) liefert deterministisch alle Original-OSim2004-KPIs
    - PDF-Generation (Plan 02) mit Standard-Template, async via Worker
    - Template-Verwaltung (Plan 03) mit drei Default-Templates und Tenant-Admin-UI
    - CSV/XLSX-Export (Plan 04) mit korrekter Locale-Formatierung
    - JSON-Bundle (Plan 05) mit Export, Import, Replay-Run, Audit-Tabelle
    - Chart-Viewer-Subklassen (Plan 06) mit Headless-Render-Bridge für Konsistenz Live ↔ PDF
    - E2E + Visual + Stress + Doku (Plan 07, dieser Plan)
  </what-built>
  <how-to-verify>
    1. `docker compose up` startet alle Phase-5-Services (Postgres, Redis, Storage, Headless-Renderer)
    2. `uv run python -m tests.fixtures.phase5_demo_run` legt Demo-Daten an
    3. Browser: einloggen, Demo-Run öffnen, alle Hauptpfade durchgehen:
       - Standard-Report generieren → PDF ansehen (Inhalt + Layout prüfen)
       - Beratungs-Template wählen → PDF (Steinbeis-Branding sichtbar)
       - Klausur-Template wählen → PDF (HKA-Branding sichtbar)
       - CSV exportieren → in Excel öffnen, Umlaute prüfen
       - XLSX exportieren → in Excel öffnen, Tabs prüfen
       - Bundle exportieren → in zweitem Browser/Tenant importieren
       - Run-Analysis-Page → alle 4 Charts ansehen, Interaktivität testen
       - `/admin/templates` als Admin → eigenes Template klonen, modifizieren, Preview, speichern
    4. Doku review: `docs/PHASE-5-REPORTS.md` und `docs/TEMPLATE-AUTHORING-GUIDE.md` lesen — sind die Anleitungen verständlich für jemanden, der das System nicht selbst gebaut hat?
    5. Stress-Test-Output prüfen: `uv run pytest tests/integration/test_phase5_stress.py -v` → alle 10 Jobs in <60s
    6. Visual-Baselines prüfen: PR-Diff zeigt erste Baselines committed, keine unerwarteten Bilder

    Erwartete Ergebnisse:
    - Alle PDFs sehen druck-tauglich aus (keine abgeschnittenen Tabellen, vernünftige Page-Breaks)
    - Charts in PDF entsprechen Charts im Browser (Pixel-Diff < 0.1%)
    - Excel-Datei öffnet ohne Encoding-Probleme
    - Bundle-Round-Trip ergibt identischen KPI-Output (bei gleicher Engine-Version)
    - Doku ist konsistent, deckt Beratungs- und Lehr-Anwendungsfälle gleichermaßen ab
  </how-to-verify>
  <resume-signal>Type "approved" wenn alles passt, oder liste konkrete Issues mit Erwartungsabweichung.</resume-signal>
</task>

</tasks>

<verification>
- `cd portal && npx playwright test` → alle E2E + Visual grün
- `uv run pytest tests/integration/test_phase5_*.py -v` → grün
- `uv run pytest tests/ -k phase5` → alle Phase-5-spezifischen Tests grün
- Doku-Files vorhanden und sinnvoll befüllt
- Human-Approval erhalten
</verification>

<success_criteria>
- E2E-Suite deckt 5 User-Hauptpfade ab, läuft in CI grün
- Visual-Regression-Baselines committed, Pixel-Diff-Schwellwert hält
- Stress-Test bestätigt 10 parallele Jobs in <60s
- Backend-Integration-Tests decken kompletten Datenfluss ab
- Doku ist vollständig, anwendungsfall-neutral (Beratung + Lehre gleichberechtigt)
- Human-Abnahme erfolgreich
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/05-reports/05-07-SUMMARY.md` UND eine konsolidierte Phase-Zusammenfassung `.planning/phases/05-reports/PHASE-5-COMPLETE.md` mit:
- Status aller 18 (PHASE5-AC-1 bis PHASE5-AC-18) Akzeptanzkriterien (✓ / ✗ / partial mit Begründung)
- Aggregierte Performance-Messungen (PDF-Gen-Zeit, Export-Zeit, Bundle-Größe, Render-Zeit Charts)
- Bekannte Limitationen und Workarounds
- Empfehlungen für Phase 6 (3fls-Integration): welche Phase-5-Komponenten iframe-tauglich sind, welche Anpassungen nötig
- Liste der Backlog-Items, die in Phase 5 explizit nicht adressiert wurden (z.B. E-Mail-Versand, Scheduled-Reports, WYSIWYG-Template-Editor)
- Memory-Eintrag-Empfehlungen für AI-Agents (z.B. "phase-5-template-sandbox-rules", "phase-5-headless-renderer-architecture")
</output>
</content>
</invoke>