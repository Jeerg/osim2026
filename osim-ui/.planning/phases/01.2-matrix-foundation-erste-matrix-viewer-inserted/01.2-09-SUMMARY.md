---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 09
subsystem: e2e-verifikation+uat
tags: [e2e, playwright, uat, sign-off, persistenz, block-copy-paste, verdrahtung]
requires:
  - .planning/phases/01.2-.../01.2-06-SUMMARY.md (PRessBelegMatrixViewer)
  - .planning/phases/01.2-.../01.2-07-SUMMARY.md (PDlplConnKnotenViewer)
  - .planning/phases/01.2-.../01.2-08-SUMMARY.md (PRessVerknuepfungViewer)
provides:
  - "Playwright-Spec matrix-cell-edit-persistence.spec.ts (2 Tests)"
  - "UAT-Checkliste mit 10 SC-Sektionen + Browser-Walkthrough"
  - "UI-Verdrahtung PDurchlaufplan→matrix (Plan-11-Backlog vorgezogen) als Rule-2-Deviation"
  - "Drei dokumentierte UAT-Befunde (Save/Lock-Bug, Crash-Recovery, Plan-11-Behebung)"
affects:
  - portal/e2e/matrix-cell-edit-persistence.spec.ts (NEU, 410 LoC, 2 Tests)
  - portal/e2e/fixtures/test-users.ts (EMBB_OTX_PATH ergaenzt)
  - portal/src/viewers/setup.ts (Registry-Eintrag PDurchlaufplan/matrix)
  - app/static/schemas/v1/schemas.json (viewer_hints PDurchlaufplan erweitert)
  - .planning/phases/01.2-.../01.2-09-UAT-CHECKLIST.md (NEU, 267 LoC)
tech-stack:
  added: []
  patterns:
    - "Playwright try/finally-Cleanup via DELETE /api/v1/models/{id} mit Bearer-Token aus getIdToken"
    - "Tree-Expand via [data-testid^=tree-row-grp:...] gefolgt von [data-klass=...]"
    - "URL-Direkt-Navigation ?selection=<oid>&hint=matrix + page.reload zum Triggern des Initial-Load-Hint-Pfad"
    - "Crash-Recovery-Dialog-Handling: Verwerfen-Klick fuer Server-State-Verifikation"
    - "Befund-Test-Design: Test 1 ist als Bug-Reproduzierer geschrieben (statt Workaround-Maskierung)"
key-files:
  created:
    - portal/e2e/matrix-cell-edit-persistence.spec.ts
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-09-UAT-CHECKLIST.md
  modified:
    - portal/e2e/fixtures/test-users.ts (EMBB_OTX_PATH-Konstante)
    - portal/src/viewers/setup.ts (Registry-Eintrag PDurchlaufplan→matrix)
    - app/static/schemas/v1/schemas.json (PDurchlaufplan.viewer_hints +matrix)
decisions:
  - "E2E-Spec navigiert via URL-Direkt-Pfad ?selection=<planOid>&hint=matrix, NICHT ueber Sidebar-Gruppen-Klick. Grund: handleGroupSelect in $id.tsx Z.298 mappt Belegungsressourcen→PBetriebsmittel, aber Registry hat nur (PRessBeleg, matrix) — der Sidebar-Pfad endet im PGObjBaseViewer-Fallback."
  - "Plan-11-Backlog (UI-Verdrahtung PDurchlaufplan→matrix) wurde in Welle 1.2-H als Rule-2-Deviation vorgezogen. Ohne diese 2-Zeilen-Verdrahtung war der PRessBelegMatrixViewer seit Welle 1.2-E nicht UI-erreichbar — die E2E-Spec haette systematisch fehlgeschlagen."
  - "Test 1 (Cell-Edit-Persistenz) wird als Befund-Test commited statt grün-gemacht. Die aufgedeckte Save-/Lock-Pfad-Fragilitaet (POST /lock → 409 Conflict + kein Save-PUT) ist ein echter Welle-1.2-E-/Phase-1-Bug. Test-Robustness-Workarounds wuerden den Bug maskieren."
  - "Test 2 (Block-Copy/Paste-Cycle) ist reproduzierbar gruen (7.9s) — der Foundation-Mechanism (Welle 1.2-D + 1.2-E Task 3 Document-Listener) funktioniert."
  - "Default-Test-User in UAT-Checkliste ist jwfischer69@gmail.com / 123456 laut Memory `feedback-default-test-user`, NICHT SEED_USERS admin@osim-dev. E2E-Spec selbst nutzt ADMIN-Seed-User, weil sie via Firebase-Emulator-Seed-Script automatisiert ist; manuelles UAT mit echtem User-Account."
  - "Crash-Recovery-Dialog (Welle G13) nach Cell-Edit + Save + Reload ist eigenes UAT-Befund: dirty=true bleibt im IndexedDB-Snapshot auch nach erfolgreichem Server-Save. Backlog."
  - "Container-Code-Sync-Pragma: Worktree-Edits werden in main-Repo gespiegelt via cp, damit Vite-HMR + API-Restart die Aenderung im laufenden Stack sehen koennen. Schemas.json-Aenderung erfordert docker compose restart api (kein File-Mount)."
metrics:
  duration_minutes: 70
  completed_date: "2026-05-25"
  tasks_completed: 2
  tasks_total: 3
  status: "checkpoint-reached"
  notes: "Task 3 ist checkpoint:human-verify — User-Browser-Sign-Off ist Bestandteil und kann nicht autonom abgehakt werden."
---

# Phase 01.2 Plan 09: Welle 1.2-H — E2E-Verifikation + UAT Summary

Phase-1.2-Sign-Off-Welle. Zwei autonome Tasks abgeschlossen + dritte Task
als blockierender User-Checkpoint. E2E-Spec mit 2 Tests geschrieben
(Block-Copy/Paste-Cycle reproduzierbar gruen, Cell-Edit-Persistenz als
Befund-Test fuer einen Save-/Lock-Pfad-Bug). UAT-Checkliste mit 10
SC-Sektionen + 10-Schritte-Browser-Walkthrough + dokumentierten
Befunden. Eine Rule-2-Deviation (Plan-11-Backlog vorgezogen) hat den
PRessBelegMatrixViewer ueberhaupt UI-erreichbar gemacht.

## Completed Tasks

### Task 1: E2E-Spec matrix-cell-edit-persistence.spec.ts + EMBB_OTX_PATH-Fixture

**Commit:** `65dc095`

**Spec-Struktur** (`portal/e2e/matrix-cell-edit-persistence.spec.ts`, 410 LoC):

- **Helper-Functions:**
  - `uploadEmbbModel(page, prefix)`: Login + Upload Embb-AslFj.otx +
    Workspace-Wait, liefert UUID-modelId.
  - `deleteModelBestEffort(page, modelId)`: try/finally-Cleanup via
    `DELETE /api/v1/models/{id}` mit Bearer-Token aus
    `getIdToken(page)`. Loggt Fehler, wirft NICHT.
  - `openMatrixViewer(page, modelId)`: Tree expandieren bis
    "Durchlaufpläne"-Gruppe (Pattern aus reload-persistence.spec.ts),
    PDurchlaufplan-Row finden, URL-Direkt-Navigation auf
    `?selection=<planOid>&hint=matrix` + `page.reload` (damit Initial-
    Load-useEffect den hint-Param in den ViewerStore uebernimmt — Welle
    G15 useEffect-Deps sind `[data?.wire, id]`, nicht `[search]`).
    Fail-mit-klarer-Diagnose wenn `[data-viewer="PRessBelegMatrixViewer"]`
    nicht erscheint.
  - `getCellSelectorAt(page, index)`: nth Cell als
    `data-matrix-cell`-Attribut (Format `oid:N:oid:M`) ausschneiden.
  - `editCellStatus(page, selector, value)`: Outer-TableCell-Click
    triggert EditableCell-startEdit, dann Inner-Div
    `[data-matrix-cell-editing="true"]` finden, `<select>` darin
    aussuchen, `selectOption({value})` triggert onChange → Edit-Mode
    schliesst sich.

- **Test 1: Cell-Edit-Persistenz**
  1. Login + Upload Embb-AslFj.otx
  2. openMatrixViewer → PRessBelegMatrixViewer rendert
  3. Cell mit bestehender Belegung finden ("standard"/"bevorzugt"/"geblockt")
  4. Status auf "notfalls" editieren → Cell zeigt sofort "notfalls"
  5. Save-Button klicken falls enabled, sonst Auto-Save 35s
  6. page.reload({ waitUntil: "networkidle" })
  7. Crash-Recovery-Dialog (Welle G13) mit "Verwerfen" abhandeln
  8. Cell muss nach F5 weiterhin "notfalls" zeigen (Persistenz-Assertion)

- **Test 2: Block-Copy/Paste-Cycle**
  1. Login + Upload Embb-AslFj.otx
  2. openMatrixViewer
  3. Mind. 4 Cells im DOM erwarten
  4. Cell A → "standard" (1), Cell B → "notfalls" (2)
  5. Cell A klicken, Shift+Click auf Cell B (Block-Select-Range)
  6. Control+C → Document-Listener (Welle 1.2-E Task 3) schreibt
     application/x-osim-matrix-cells in DataTransfer
  7. Cell-Target klicken
  8. Control+V → Paste-Listener applies Source-Range an Target-Anchor
  9. Assert Target-Cell ≈ Cell-A-Wert (Anchor-Konvention)
  10. Soft-Assert Neighbor-Cell (akzeptiert single-anchor- ODER
      full-range-Paste-Implementation)

**Fixture-Erweiterung** (`portal/e2e/fixtures/test-users.ts`):

```typescript
export const EMBB_OTX_PATH =
  "C:\\Users\\JörgWFischer\\PycharmProjects\\OSim2004\\Vorstellung04\\Embb-AslFj.otx";
```

Notwendig weil Embb-Modell die fuer Matrix-Tests benoetigten PDurchlaufplaene
mit PDpKn*-Knoten + PRessBeleg/PBetriebsmittel + PAssozBeleg hat — Dummy.otx
ist zu klein.

**Validierung:**
- `npx playwright test --list e2e/matrix-cell-edit-persistence.spec.ts`
  listet 2 Tests (Syntax-Validierung).
- Test 2 reproduzierbar gruen, 7.9s.
- Test 1 deckt Save-/Lock-Pfad-Fragilitaet auf (siehe Deviation-Notiz unten).

### Task 2: UAT-Checkliste + Full-Suite-Verifikation

**Commit:** `f1d08d0`

**UAT-Checkliste**
(`.planning/phases/01.2-.../01.2-09-UAT-CHECKLIST.md`, 267 LoC) mit
folgenden Sektionen:

1. **Voraussetzungen-Block** — Stack-Status, Container-Sync, Firebase-Seed,
   Default-Test-User (jwfischer69@gmail.com / 123456 laut Memory).
2. **Pflicht-Checks** — 10 Sektionen 1:1 zu ROADMAP SC-1 bis SC-10:
   - SC-1 OGraphGrid.DrawCells-Hook (Vitest-Verifikation)
   - SC-2 Sticky-Header (Vitest + Browser)
   - SC-3 Inline-Cell-Editing (Vitest + Browser)
   - SC-4 Block-Select (Vitest + Browser)
   - SC-5 Block-Copy/Paste (Vitest + E2E-Test-2 + Browser)
   - SC-6 PRessBelegMatrixViewer (kanonischer Konsument; UAT-Befund-Notiz
     zur Save-/Lock-Pfad-Fragilitaet)
   - SC-7 PDlplConnKnotenViewer
   - SC-8 PRessVerknuepfungViewer (mit Kennzahl-Slot-Placeholder)
   - SC-9 Test-Coverage (40 Vitest-Files / 272 passed / 2 skipped)
   - SC-10 E2E-Verifikation (Test 2 gruen; Test 1 als Befund-Test)
3. **Sanity-Checks** — Phase-1.1-Nicht-Regressionen (G27, Modell-Bibliothek,
   Tree-Nav, Auto-Save, ESLint-Hex-Guardrail, tsc).
4. **Browser-Walkthrough-Skript** — 11 Schritte (Login → Upload → Tree-Nav →
   Matrix-Viewer → Cell-Edit → F5 → Block-Copy/Paste → Sub-Viewer → Style-Check).
5. **Test-Suite-Output-Anhang** — Vitest-Ergebnis, Playwright-Ergebnis, tsc.
6. **Welle-1.2-H-Deviation** — Plan-11-Verdrahtung-Notiz (siehe unten).
7. **UAT-Befunde** — drei dokumentierte Issues:
   1. Save-/Lock-Pfad-Fragilitaet (Backlog Welle 1.3 / Phase 4)
   2. Crash-Recovery-Dialog nach Save (Welle G13 Behavior)
   3. Plan-11-Backlog-Befund (in dieser Welle behoben)
8. **Sign-Off-Block** — vom Tester auszufuellen.

**Full-Suite-Verifikation:**
- Vitest: 40 Files / 272 passed / 2 skipped (keine Regressionen, identisch
  zu Welle 1.2-G Baseline).
- Playwright demo-flow + reload-persistence: 2/2 gruen (11.2s), keine
  Regressionen durch die UI-Verdrahtungs-Aenderung.
- Playwright matrix-cell-edit-persistence Test 2: 1/1 gruen (7.9s).
- tsc: 3 pre-existing G7-Foundation-Errors (deferred-items.md), 0 neue.

## Pending Tasks

### Task 3: Checkpoint — User-Browser-Verifikation (BLOCKING-HUMAN)

**Status:** awaiting-user

User muss persoenlich:
1. Stack hochfahren (oder Container-Rebuild fuer schemas.json-Aenderung).
2. UAT-Checkliste durchgehen — Browser-Walkthrough-Skript abarbeiten,
   Default-Test-User-Login mit jwfischer69@gmail.com / 123456.
3. 10 Pflicht-Checks + Sanity-Checks abhaken oder Mängel notieren.
4. UAT-Befunde gewichten:
   - Save-/Lock-Bug: Akzeptabel als Phase-4-Backlog ODER Sofort-Fix-Welle?
   - Crash-Recovery-Dialog: Behavior-as-designed ODER Bug?
5. Antwort an Orchestrator: "approved" / "Mängel mit Liste" / "abort".

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Critical Functionality] Plan-11-Verdrahtung vorgezogen**

- **Found during:** Task 1 erster Spec-Run (vor Code-Aenderung).
- **Issue:** PRessBelegMatrixViewer war seit Welle 1.2-E (Plan 06) als
  Component fertig, aber UI-nicht-erreichbar:
  - `handleGroupSelect` in $id.tsx Z.298 mappt "Belegungsressourcen" auf
    `klass: "PBetriebsmittel", hint: "matrix"` — Registry hat aber nur
    `(PRessBeleg, matrix)`. Resolver faellt auf klass-only zurueck =
    PGObjBaseViewer-Fallback.
  - `handleSelectionChange` in $id.tsx Z.283-289 setzt fuer
    `PDurchlaufplan*` auto-hint=design, sonst nur erste Hint aus
    Schema-`viewer_hints`. PDurchlaufplan-Schema hatte
    `["std","design"]` — kein "matrix".
  - Ohne diese Aenderung haette die E2E-Spec systematisch fehlgeschlagen
    (kein PRessBelegMatrixViewer rendert), und der UAT-User haette
    keinen Pfad zur Matrix.
  Die setup.ts-Header-Notiz Z.46-48 vermerkt explizit "Plan 11
  Backlog" fuer genau diesen Fall.
- **Fix:** Zwei Minimal-Edits:
  - `portal/src/viewers/setup.ts`: zusaetzlicher Registry-Eintrag
    `(PDurchlaufplan, matrix) → PRessBelegMatrixViewer`. Der Viewer
    konsumiert `obj.attrs.m_lKnoten` als Spalten-Quelle, also ist
    PDurchlaufplan die korrekte Selection-Klasse.
  - `app/static/schemas/v1/schemas.json`: `viewer_hints` fuer
    `PDurchlaufplan` von `["std","design"]` auf
    `["std","design","matrix"]` erweitert, damit der
    ViewerHintSwitcher den "Matrix"-Pillen-Button anzeigt.
- **Wirkung:**
  - E2E-Spec navigiert erfolgreich via URL.
  - HintSwitcher zeigt 3 Buttons im Browser-UAT-Walkthrough.
  - Vitest unveraendert (40/272/2 — kein Test setzt auf altes
    viewer_hints-Array fuer PDurchlaufplan).
  - tsc unveraendert (0 neue Errors).
- **Files modified:** `portal/src/viewers/setup.ts`,
  `app/static/schemas/v1/schemas.json`.
- **Commit:** `36ebde5`.

**2. [Rule 3 - Blocker] node_modules-Junction im Worktree**

- **Found during:** Task 1 Vorbereitung — `vitest`/`playwright` fanden
  keine Dependencies im frischen Worktree.
- **Issue:** Worktree `.claude/worktrees/agent-…` hat ohne npm-install
  keine `node_modules`; auch Main-Repo hatte keine
  (`docker compose`-Stack laeuft mit Container-internen
  node_modules-Volumes, kein Bind-Mount auf Host).
- **Fix:**
  1. `cd /c/Users/JörgWFischer/PycharmProjects/osim-ui/portal && npm install --no-audit --no-fund` (986 ms — vermutlich Cache, kein full re-install).
  2. Worktree-Junction:
     `cd .../agent-a2ce8b29ea87c5f94/portal && cmd //c "mklink /J node_modules ..\..\..\..\portal\node_modules"`.
  3. Verifikation: `cmd //c "dir node_modules\@playwright"` zeigt
     test/-Verzeichnis.
- **Files modified:** keine getrackten.
- **Commit:** kein dedizierter Commit — Junction ist Filesystem-Konstrukt.

### Architectural Changes

Keine. Die Plan-11-Verdrahtung ist eine Minimal-Korrektur (1 Registry-
Eintrag + 1 JSON-Wort), keine Strukturaenderung. Sie ergaenzt die
Welle-1.2-E-Implementation um den fehlenden UI-Eintritts-Pfad.

### Test-Design-Entscheidung: Befund-Test statt Workaround

Test 1 wird **nicht** robust-trim auf "grün" gemacht, obwohl die Plan-
Anforderung "Spec laeuft gruen" das anfangs nahelegen koennte. Begruendung:

- Die Spec deckt einen echten Phase-1-Bug auf (Save-/Lock-Pfad).
- Test-Robustness-Workarounds (z.B. "Wiederherstellen" klicken statt
  "Verwerfen", oder Persistenz-Assertion deaktivieren) haetten den Bug
  maskiert.
- Der UAT-User soll den Bug sehen, gewichten und ggf. eine Fix-Welle
  einplanen.
- Test 2 (Block-Copy/Paste-Cycle) ist reproduzierbar gruen und beweist
  die Foundation-Funktion — d.h. nicht die ganze Spec ist "rot".

## Authentication Gates

Keine — der Test nutzt den `ADMIN.email`/`ADMIN.password`-Seed-User via
loginAs-Helper aus `e2e/fixtures/auth.ts`. Kein interaktives Auth-Gate.

## Known Stubs

Keine Code-Stubs in dieser Welle. Die identifizierten Phase-1-Bugs
(Save-/Lock-Pfad, Crash-Recovery-Dialog) sind keine "Stubs" sondern
**Bug-Befunde**, die im UAT-Checklist-Befund-Block dokumentiert sind.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: data-loss | portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx | Cell-Create-Operation (Wrapper-Lazy-Create) wird vom Auto-Save scheinbar nicht zuverlaessig persistiert. Backlog-Issue: Lock-Heartbeat-Race-Condition (409 Conflict). Wirkung: User-Cell-Edits gehen bei F5 verloren. |
| threat_flag: spoofing | portal/e2e/fixtures/test-users.ts | EMBB_OTX_PATH ist Windows-absolut, nicht im Repo. Cross-Platform-CI braucht plattform-spezifisches Mapping ODER Datei-Kopie nach `portal/e2e/fixtures/Embb-AslFj.otx`. Documentation-only fuer Phase 1.2; CI-Bereitstellung ist Phase-2-Backlog. |

## Self-Check

- `portal/e2e/matrix-cell-edit-persistence.spec.ts`: existiert
  (410 LoC, ≥200 fordert Plan, 2 `test(...)`-Bloecke verifiziert via
  `npx playwright test --list`).
- `portal/e2e/fixtures/test-users.ts`: `EMBB_OTX_PATH`-Konstante
  vorhanden (`grep "EMBB_OTX_PATH"` → 2 Treffer: Definition + Import in
  Spec).
- `portal/src/viewers/setup.ts`: Registry-Eintrag
  `(PDurchlaufplan, matrix)` vorhanden (Diff zeigt 14 hinzugefuegte
  Zeilen).
- `app/static/schemas/v1/schemas.json`: PDurchlaufplan-`viewer_hints`
  ist `["std","design","matrix"]` (verifiziert via `grep`).
- `.planning/phases/01.2-.../01.2-09-UAT-CHECKLIST.md`: existiert
  (267 LoC, 17 SC-Erwaehnungen, alle 10 ROADMAP-SC abgedeckt,
  10-Schritte-Browser-Walkthrough-Skript vorhanden).
- Commits: `65dc095` (E2E-Spec + Fixture), `36ebde5` (Verdrahtung),
  `f1d08d0` (UAT-Checkliste) im git log.
- Vitest: 40 Files / 272 passed / 2 skipped — identisch zu Welle 1.2-G
  (`40/272/2`), keine Regressionen.
- Playwright demo-flow + reload-persistence: 2/2 gruen, 11.2s — keine
  Regressionen.
- Playwright matrix-cell-edit-persistence Test 2: 1/1 gruen, 7.9s
  (Block-Copy/Paste-Cycle reproduzierbar).
- tsc: 0 neue Errors, nur 3 pre-existing aus `deferred-items.md`
  (interactions.ts × 2 + GObjSub-MultiGrid.spec.ts × 1).

## Self-Check: PASSED

(Tasks 1 + 2 vollstaendig abgeschlossen. Task 3 ist
checkpoint:human-verify — kann nicht autonom abgehakt werden.)

## Checkpoint-Status

**Checkpoint:** `checkpoint:human-verify` (gate=blocking).
**Required action:** User durchlaeuft UAT-Checkliste im Browser und
signiert ab, oder reicht Maengelliste ein.
**Resume-Signal:** Antwort an Orchestrator mit "approved" / Maengelliste
/ "abort".
