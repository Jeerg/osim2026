---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 06
type: execute
wave: 4
depends_on: [01.2-03, 01.2-04, 01.2-05]
files_modified:
  - portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx
  - portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx
  - portal/src/viewers/PRessBelegMatrix/index.ts
  - portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx
  - portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx
  - portal/src/viewers/setup.ts
  - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md
autonomous: true
requirements: [SC-6, SC-9]
must_haves:
  truths:
    - "C++-Audit aus PRessBelegMatrixViewer.cpp ist in 01.2-06-CPP-AUDIT.md dokumentiert (Methoden-Liste, Pipeline, Wire-Klassen, Mutation-Pfade)"
    - "Wire-Slot-Verifikation für PAssozBeleg.m_lRessourcen + Knoten.m_lAssozRess aus SCHEMA-MAP.md (Plan 01 Task 3) in 01.2-06-CPP-AUDIT.md übernommen"
    - "Viewer rendert 2D-Matrix Zeilen=PRessBeleg (+PBetriebsmittel+PPerson) × Spalten=PDlplKnoten × Cells=PAssozBeleg-Status"
    - "Cell-Edit (Status-Change auf existierende Belegung) dispatcht patchObject(assoz.oid, {m_eStatus: newStatus})"
    - "Cell-Create (leere Cell + Status) dispatcht createObject('PAssozBeleg', ...) + appendSubRef(knoten.oid, slot, newOid)"
    - "Cell-Delete (Cell geleert) dispatcht deleteObject(assoz.oid) wenn m_lRessourcen leer wird, sonst patchObject mit gefilterter Liste"
    - "Toolbar mit 3 Comboboxes (ALL/PERS/RESS, AND/OR, PREFER/STD/IF_NEEDED/BLOCKED) — alle 1:1 zu C++ PRessBelegMatrixViewer.cpp"
    - "ViewerRegistry-Eintrag ('PRessBeleg', hint='matrix') zeigt auf neuen Viewer"
    - "Vitest-Spec für Viewer rendert ohne Crash + verifiziert mind. 3 Cell-Lifecycle-Pfade (Create/Edit/Delete)"
    - "Document-Level-Keyboard-Listener für Ctrl+C/Ctrl+V ist mounted wenn der Viewer aktiv ist und ruft writeToClipboardData (Block-Select-Range) bzw. readFromClipboardData (Paste-Ziel-Cell) — SC-5 ist im UI verdrahtet, nicht nur als Pure-Function vorhanden"
    - "Cleanup-Pfad im useEffect-Return removed beide Listener wenn der Viewer unmountet"
  artifacts:
    - path: portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx
      provides: "Kanonischer Matrix-Konsument der Foundation"
      min_lines: 180
    - path: portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx
      provides: "Toolbar mit 3 Combobox-Modi (View/Verknüpfung/Status)"
      min_lines: 80
    - path: portal/src/viewers/PRessBelegMatrix/index.ts
      provides: "Barrel-Export"
    - path: portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx
      provides: "Spec für den Document-Level-Keyboard-Listener (Ctrl+C/Ctrl+V)"
      min_lines: 60
    - path: .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md
      provides: "Multi-Source-Audit aus C++-PRessBelegMatrixViewer.cpp + TS-Bestands-Analyse"
  key_links:
    - from: portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx
      to: portal/src/graph/foundation/matrix
      via: "MatrixGrid + useInlineCellEdit + useBlockSelection + writeToDataTransfer/readFromDataTransfer"
      pattern: "from \"@/graph/foundation/matrix\""
    - from: portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx
      to: portal/src/stores/model-store.ts
      via: "createObject/patchObject/deleteObject/appendSubRef"
      pattern: "useModelStore.getState\\(\\)"
    - from: portal/src/viewers/setup.ts
      to: PRessBelegMatrixViewer
      via: "Registry-Eintrag mit hint='matrix'"
      pattern: "PRessBelegMatrixViewer"
    - from: portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx
      to: portal/src/graph/foundation/matrix/matrix-clipboard.ts
      via: "document.addEventListener('copy') + document.addEventListener('paste') mit writeToDataTransfer/readFromDataTransfer"
      pattern: "addEventListener\\([\\'\\"](copy|paste)"
---

<objective>
Welle 1.2-E: Erster Konsument der Matrix-Foundation — `PRessBelegMatrixViewer` als echte 2D-Matrix Ressource × Knoten × PAssozBeleg-Belegung.

**Purpose:**
Die Foundation (Welle 02-05) wird durch den kanonischen Use-Case verifiziert. Vor der Implementation steht ein zweipassiger Audit (C++ + Backend-Wire-Sondierung), damit der Wire-Slot für `PAssozBeleg.m_lRessourcen` und `Knoten.m_lAssozRess` korrekt ist (Open-Question OQ5 aus RESEARCH §8.3). Der Viewer ersetzt KEIN bestehendes Pattern — die alte (falsche) Property-Liste wurde in Welle 01 entfernt; dieser Viewer ist die korrekte 2D-Matrix gemäß OSim2004-Original.

**Output:**
- `01.2-06-CPP-AUDIT.md` mit C++-Pipeline + Wire-Slot-Verifikation
- `portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx` (echte 2D-Matrix)
- `portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx` (3 Combobox-Modi)
- Registry-Update in `setup.ts`
- Component-Spec (≥6 Tests, deckt SC-6 + 1 Smoke aus SC-9 ab)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-CONTEXT.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-SUMMARY.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-02-SUMMARY.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-03-SUMMARY.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-04-SUMMARY.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-05-SUMMARY.md
@portal/src/viewers/PDurchlaufplan/PDurchlaufplanViewerDesign.tsx
@portal/src/viewers/PDurchlaufplan/PlanToolbar.tsx
@portal/src/viewers/core/ChildDialog.tsx
@portal/src/viewers/core/types.ts
@portal/src/stores/model-store.ts
@portal/src/viewers/setup.ts

<interfaces>
<!-- Foundation-API (aus Welle 02-05): -->

```typescript
// portal/src/graph/foundation/matrix/index.ts
import {
  MatrixGrid, MatrixCell,
  useInlineCellEdit, parseRaw,
  useBlockSelection, computeCellRange, isInRange,
  writeToDataTransfer, readFromDataTransfer, checkOriginCompatible,
  MATRIX_CLIPBOARD_MIME,
} from "@/graph/foundation/matrix";
import type {
  MatrixGridProps, MatrixCellProps,
  UseInlineCellEditOptions, OctrlType, ValueType, EnumOption,
  CellRange, CellRef, BlockSelection,
  MatrixClipboardPayload,
} from "@/graph/foundation/matrix";
```

```typescript
// ViewerProps-Convention (aus portal/src/viewers/core/types.ts)
export interface ViewerProps<T extends OBaseObj = OBaseObj> {
  obj: T;                                    // hier: PDurchlaufplan
  schema: ClassSchema;
  allObjects: Record<number, OBaseObj>;
  onChange: (patch: Partial<T["attrs"]>) => void;
  onCommand: (cmd: ViewerCommand) => void;
  disabled?: boolean;
}
```

```typescript
// model-store API (aus portal/src/stores/model-store.ts):
useModelStore.getState().patchObject(oid, patch);
useModelStore.getState().createObject(klass, attrs): number;  // returnt neue OID
useModelStore.getState().deleteObject(oid);
useModelStore.getState().appendSubRef(parentOid, slot, childOid);
useModelStore.getState().removeSubRef(parentOid, slot, childOid);
```

Wire-Slot-Konvention (Source-of-Truth: 01.2-SCHEMA-MAP.md aus Plan 01 Task 3; in Task 1 nur übernommen, nicht neu verifiziert):
- PDurchlaufplan.sub_refs[0] = m_lKnoten (Welle G20-C bestätigt)
- PDlplKnoten.sub_refs[N] = m_lAssozRess  ← N aus SCHEMA-MAP.md (Default 0 wenn Backend-Status=fallback)
- PAssozBeleg.sub_refs[N] = m_lRessourcen  ← N aus SCHEMA-MAP.md (Default 0 wenn Backend-Status=fallback)
- PAssozBeleg.attrs.m_eStatus: number (0=PREFER, 1=STD, 2=IF_NEEDED, 3=BLOCKED — analog C++ PRessBelegMatrixGObj::DrawSymbol Z.60)

Combobox-Konstanten (aus PATTERNS §7 + RESEARCH §1.1):
- VIEW_MODI: ALL/PERS/RESS (filtert Zeilen)
- VERK_STATUS: 0=bevorzugt, 1=standard, 2=notfalls, 3=geblockt
- VERK_MODI: AND (immer neuer PAssozBeleg pro Cell) / OR (Best-Match-Suche)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: C++-Audit + Wire-Slot-Übernahme aus SCHEMA-MAP.md</name>
  <files>.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md</files>
  <read_first>
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md (PRIMARY — aus Plan 01 Task 3; enthält Wire-Slot-Indizes für PAssozBeleg.m_lRessourcen + PDpKn*.m_lAssozRess + den `_slotToAttrName`-Patch-Vorschlag)
    - C:/Users/JörgWFischer/PycharmProjects/OSim2004/OSimV01(Fj)/OSimPro/PRessBelegMatrixViewer.cpp (komplett, ~1500 Zeilen — Methoden dframeFill, OnLButtonDown, OnRButtonDown, OnLButtonDblClk, CopyCell2Buffer, PasteCellFromBuffer, OnDrawFrame)
    - C:/Users/JörgWFischer/PycharmProjects/OSim2004/OSimV01(Fj)/OSimPro/PRessBelegMatrixViewer.h
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md (§1.1 + §4)
    - portal/src/stores/model-store.ts (Z.195-300 — createObject/appendSubRef/deleteObject/_slotToAttrName)
  </read_first>
  <action>
    Multi-Source-Audit-Pattern aus Phase 1.1 (Welle G1-G27).

    1. **C++-Pipeline-Extraktion** in `01.2-06-CPP-AUDIT.md` § "C++-Audit":
       - Tabelle aller relevanten Methoden mit Zeilennummer, Eingangs-Signatur, Mutation-Pfad, beteiligte Wire-Klassen.
       - Sequenz-Diagramm (text-basiert) für: (a) dframeFill (Modell-Load), (b) OnLButtonDown (leere Cell + Status wählen → Cell-Create), (c) OnRButtonDown (besetzte Cell → Cell-Delete), (d) CopyCell2Buffer + PasteCellFromBuffer (Block-Copy/Paste).
       - Cell-Status-Mapping: m_eStatus-Enum (PREFER/STD/IF_NEEDED/BLOCKED) → numerische Werte aus C++.

    2. **Wire-Slot-Übernahme aus SCHEMA-MAP.md** in § "Wire-Slot-Verifikation":
       - `01.2-SCHEMA-MAP.md` (aus Plan 01 Task 3) ist die Quelle der Wahrheit. KEINE eigenen curl-Sondierungen mehr in dieser Welle.
       - Aus SCHEMA-MAP.md folgendes nach `01.2-06-CPP-AUDIT.md` § "Wire-Slot-Verifikation" kopieren:
         - PAssozBeleg.m_lRessourcen-Slot-Index (`N` aus der dortigen Tabelle)
         - PDpKn*.m_lAssozRess-Slot-Index
         - PDlplKnoten.m_lAssozRess-Slot-Index
         - attrs-vs-sub_refs-Mirror-Status (ob beide gepflegt sind — relevant für `obj.attrs.m_lAssozRess ?? obj.sub_refs[N]` Pattern im Viewer)
         - `**Backend-Status:** ok | fallback` Marker aus SCHEMA-MAP übernehmen (entscheidet ob die Slot-Indizes verifiziert oder `[ASSUMED]` sind)
       - Wenn SCHEMA-MAP.md `[ASSUMED]`-Marker hat: in CPP-AUDIT.md ebenfalls als `[ASSUMED]` markieren — Plan 06 Task 2 nutzt die Werte trotzdem (Default sub_refs[0] gemäß RESEARCH §4-Beispiel-JSON Z.426-430). Finale Verifikation passiert via PRessBelegMatrixViewer.spec.tsx Test 4 (assertet konkreten appendSubRef-Aufruf-Slot 0).

    3. **_slotToAttrName-Erweiterung dokumentieren** in § "model-store-Erweiterung":
       - In `portal/src/stores/model-store.ts` ist `_slotToAttrName(klass, slot)` definiert (siehe Z.255-280 in der appendSubRef-Logik). Aktuell nur PDurchlaufplan mapping.
       - Den `_slotToAttrName`-Patch-Vorschlag aus SCHEMA-MAP.md 1:1 hierher übernehmen (Slot-Indizes für PDpKn* und PAssozBeleg). Plan 06 Task 2 implementiert diese Erweiterung dann.

    4. **TS-Bestands-Quick-Check** in § "TS-Bestand":
       - `grep -rn "PAssozBeleg" portal/src/` — wo bereits referenziert? (Erwartung: nur in Plan-Doku, kein Code).
       - `grep -rn "m_lAssozRess" portal/src/` — analog.
       - `grep -rn "m_eStatus" portal/src/` — analog.
       - Wenn unerwartete Treffer: in Audit-Doc notieren.
  </action>
  <verify>
    <automated>test -f .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md && grep -q "C++-Audit" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md && grep -q "Wire-Slot-Verifikation" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md && grep -q "model-store-Erweiterung" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md</automated>
  </verify>
  <done>Audit-Datei existiert mit 4 Sektionen (C++-Audit, Wire-Slot-Verifikation, model-store-Erweiterung, TS-Bestand); konkrete Slot-Indizes für PAssozBeleg.m_lRessourcen und PDpKn*.m_lAssozRess sind aus SCHEMA-MAP.md übernommen (mit Source-Marker: "SCHEMA-MAP.md ok" oder "SCHEMA-MAP.md [ASSUMED]").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PRessBelegMatrixToolbar + PRessBelegMatrixViewer + Spec + Registry</name>
  <files>
    portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx,
    portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx,
    portal/src/viewers/PRessBelegMatrix/index.ts,
    portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx,
    portal/src/viewers/setup.ts,
    portal/src/stores/model-store.ts
  </files>
  <read_first>
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md (aus Task 1 — Wire-Slot-Befunde sind die Grundlage)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md (§6 + §7 — Viewer + Toolbar Pattern; PATTERNS §11 für Spec-Setup)
    - portal/src/viewers/PDurchlaufplan/PDurchlaufplanViewerDesign.tsx (Z.22-95 für Imports/State/Revision-Pattern; Z.116-225 für Wire-Daten-Lookup-useMemo + bumpRevision)
    - portal/src/viewers/PDurchlaufplan/PlanToolbar.tsx (Z.33-105 + Z.239-280 — Combobox + Container-Pattern)
    - portal/src/viewers/core/ChildDialog.tsx
    - portal/src/viewers/core/types.ts (ViewerProps, OBaseObj)
    - portal/src/stores/model-store.ts (Z.180-305 — _slotToAttrName + appendSubRef-Logik)
    - portal/src/graph/foundation/matrix/ (alle Files aus Welle 02-05)
    - portal/src/viewers/setup.ts (Z.131-160 — wo der neue Eintrag hin muss)
    - C:/Users/JörgWFischer/PycharmProjects/OSim2004/OSimV01(Fj)/OSimPro/PRessBelegMatrixViewer.cpp (für Status-Label-Strings + Combobox-Modi)
  </read_first>
  <behavior>
    Spec (`PRessBelegMatrixViewer.spec.tsx`):
    - Test 1: Empty model (allObjects nur Plan) → emptyMessage gerendert, kein Crash.
    - Test 2: Render mit 2 Ress + 2 Knoten + 1 PAssozBeleg-Cell → 2 Row-Header + 2 Col-Header + 4 Cells; eine Cell zeigt Status-Text ("standard").
    - Test 3: Click auf existierende Belegungs-Cell → Status-Select erscheint (editing=true); Select-Change auf "geblockt" (value=3) → patchObjectMock wurde mit (assoz.oid, {m_eStatus: 3}) aufgerufen.
    - Test 4: Click auf leere Cell → Edit-Mode startet; Select "standard" → createObjectMock wurde mit ("PAssozBeleg", {m_lRessourcen: [ress.oid], m_eStatus: 1}) aufgerufen UND appendSubRefMock wurde mit (knoten.oid, 0, 999) aufgerufen (999 = mock-return).
    - Test 5: Toolbar zeigt 3 Comboboxes (data-testid combo-view-select, combo-verk-select, combo-status-select).
    - Test 6: View-Combo-Change auf "PERS" → nur PPerson-Zeilen sind sichtbar (Filter angewendet — PRessBeleg + PBetriebsmittel rauf).
    - Test 7: data-viewer="PRessBelegMatrixViewer" + data-matrix-grid="PRessBeleg" auf Root vorhanden.

    Viewer-Behavior:
    - Zeilen: alle `OBaseObj` mit `klass ∈ {"PRessBeleg", "PBetriebsmittel", "PPerson"}`, gefiltert nach View-Mode (ALL/PERS/RESS), sortiert nach oid.
    - Spalten: knotenOids = obj.attrs.m_lKnoten ?? obj.sub_refs[0]; Map auf allObjects.
    - cellMap: Iteriere alle Spalten-Knoten, lese ihre m_lAssozRess-Liste, finde PAssozBeleg-Objekte, mappe rowOid:colOid → PAssozBeleg.
    - Cell-Edit-Pfade (Status-Change / Create / Delete) exakt wie in RESEARCH §Code Examples Z.687-721.
    - bumpRevision() nach jeder Mutation (Pattern S2).
  </behavior>
  <action>
    1. **model-store-Erweiterung**: `portal/src/stores/model-store.ts` öffnen, Funktion `_slotToAttrName` (suche per `function _slotToAttrName` oder analog) erweitern:
       - WENN `klass.startsWith("PDpKn") && slot === <SLOT_AUS_AUDIT>`: return "m_lAssozRess"
       - WENN `klass === "PAssozBeleg" && slot === <SLOT_AUS_AUDIT>`: return "m_lRessourcen"
       Konkreten Slot-Index aus `01.2-06-CPP-AUDIT.md` § "Wire-Slot-Verifikation" einsetzen (Default 0 wenn Audit Slot-Index 0 ergab — was die wahrscheinlichste Konvention ist).

    2. **PRessBelegMatrixToolbar.tsx** anlegen (analog PATTERNS §7):
       - Konstanten `VIEW_MODI = [{value:"ALL",label:"Alle Ressourcen"}, {value:"PERS",label:"Nur Personen"}, {value:"RESS",label:"Nur Betriebsmittel"}]`
       - `VERK_STATUS = [{value:0,label:"bevorzugt"}, {value:1,label:"standard"}, {value:2,label:"notfalls"}, {value:3,label:"geblockt"}]`
       - `VERK_MODI = [{value:"OR",label:"OR (Best-Match)"}, {value:"AND",label:"AND (neu pro Cell)"}]`
       - Innere `ViewerCombobox`-Component (1:1 aus PlanToolbar Z.103-225, vereinfacht zu nativem `<select>` mit `data-testid={'combo-' + comboKind + '-select'}`).
       - Toolbar-Container `<div data-testid="matrix-toolbar" className="flex flex-wrap items-center gap-4 border-b border-border bg-card px-4 py-2">` mit 3 Comboboxes + Trennstrich `<div className="h-5 w-px bg-border-light" />` dazwischen.
       - Props: `{ viewMode, onViewModeChange, verkMode, onVerkModeChange, defaultStatus, onDefaultStatusChange, disabled }`.

    3. **PRessBelegMatrixViewer.tsx** anlegen (alle Imports aus PATTERNS §6 Block):
       - State: `[revision, setRevision]`, `[viewMode, setViewMode] = "ALL"`, `[verkMode, setVerkMode] = "OR"`, `[defaultStatus, setDefaultStatus] = 1` (standard).
       - `bumpRevision`-Callback.
       - `useMemo` für `{ rows, cols, cellMap }` (analog PATTERNS §6 Code-Block) mit Deps `[obj.oid, allObjects, revision, viewMode]`:
         - Zeilen filtern nach View-Mode: ALL=alle PRessBeleg+PBetriebsmittel+PPerson, PERS=PPerson, RESS=PBetriebsmittel.
         - Spalten = obj.attrs.m_lKnoten oder obj.sub_refs[0].
         - cellMap = `Map<string, OBaseObj | null>` aus Iteration über alle (col, assoz, ress).
       - `onCellEdit` (PATTERNS §6 Code-Block): Branches für (a) existing+newStatus!=null → patchObject mit m_eStatus; (b) leer+newStatus!=null → createObject PAssozBeleg + appendSubRef; (c) existing+newStatus===null → deleteObject oder patchObject mit gefilterter m_lRessourcen. ggf. verkMode-Branch (AND/OR — bei AND immer neuer PAssozBeleg, bei OR Best-Match-Suche; in Phase 1.2 reicht für OR die Default-Logik "wenn die existing-Cell hier am Knoten passt, nutzen wir sie"; AND ist die Default-Implementation Schritt b).
       - renderCell-Function: liefert `MatrixCell` mit:
         - `cellId = ${row.oid}:${col.oid}`
         - `selected = blockSelection.isSelected({...})` (optional in Phase 1.2 ohne Visual-Feedback wenn nicht im UI gewünscht — useBlockSelection-Hook trotzdem mounten für SC-4)
         - `editing = inlineCell?.cellId === cellId` (mit per-Cell-Hook-Variante: ein Wrapper `<EditableCell {...}/>` der `useInlineCellEdit({ cellId, value: assoz?.attrs.m_eStatus ?? null, onCommit: (newStatus) => onCellEdit(row, col, newStatus), octrlType: "Enum", enumValues: VERK_STATUS, readOnly: disabled })` aufruft).
         - Content: wenn `editing` → `EditorElement`; sonst Status-Label aus VERK_STATUS[assoz.attrs.m_eStatus] ODER "—" wenn null.
         - onClick: WENN nicht disabled, `startEdit()`.
       - Layout: `<ChildDialog title={'Ressourcen-Belegung — ${rows.length} Ressourcen × ${cols.length} Knoten'} description={...} footer={<PRessBelegMatrixToolbar ... />}><div data-viewer="PRessBelegMatrixViewer" data-matrix-grid="PRessBeleg"><MatrixGrid<OBaseObj, OBaseObj, OBaseObj> rows={rows} cols={cols} rowKey={r => 'oid:'+r.oid} colKey={c => 'oid:'+c.oid} cellLookup={(r,c) => cellMap.get(r.oid+':'+c.oid) ?? null} renderCell={...} revision={revision} disabled={disabled} cornerLabel="Knoten / Ressource" /></div></ChildDialog>`
       - Block-Copy via DataTransfer ist Phase-1.2-Scope, aber UI-Verdrahtung (Ctrl+C / Ctrl+V Listener auf document) kann minimal sein: per Vitest-Spec NICHT getestet, hand-test in Welle 1.2-09 E2E. Hook ist mounted; Konsument kann später erweitern.

    4. **Barrel-Export** `portal/src/viewers/PRessBelegMatrix/index.ts`:
       ```typescript
       export { PRessBelegMatrixViewer } from "./PRessBelegMatrixViewer";
       export { PRessBelegMatrixToolbar } from "./PRessBelegMatrixToolbar";
       ```

    5. **setup.ts** anpassen:
       - Import `import { PRessBelegMatrixViewer } from "@/viewers/PRessBelegMatrix/PRessBelegMatrixViewer";` ergänzen.
       - Registry-Eintrag (falls bei Welle 01 Entscheidung A=LÖSCHEN: neu anlegen; falls Entscheidung A=UMBENENNEN: zusätzlich zum bestehenden PRessBelegPropertyListViewer-Eintrag mit hint="property-list" registrieren).
         ```typescript
         viewerRegistry.register({
           klass: "PRessBeleg",
           hint: "matrix",
           Component: PRessBelegMatrixViewer,
         });
         ```
       - Doku-Block am Datei-Anfang aktualisieren.

    6. **Spec** `portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx` (REWRITE — alter Spec wurde in Welle 01 entfernt):
       - useModelStore-Mock-Pattern (PATTERNS S10).
       - Wire-Daten-Fixture aus PATTERNS §11 Code-Block (plan, knotenA, assoz, ress mit konkreten attrs).
       - Mindestens 7 Tests aus `<behavior>`.

    7. KEINE Änderungen an `matrix-common.tsx` oder `PRessMengeMatrixViewer.tsx` (CONTEXT deferred).
  </action>
  <verify>
    <automated>cd portal && npm test -- --run src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx 2>&1 | tail -25 | grep -E "(✓|passed|Tests)" && npx tsc --noEmit 2>&1 | grep -c "error TS" | head -1</automated>
  </verify>
  <done>
    1. Audit-Datei aus Task 1 vorhanden.
    2. 7 Specs grün in PRessBelegMatrixViewer.spec.tsx.
    3. Viewer + Toolbar + Index existieren, ≥180 / ≥80 / ≥3 LoC.
    4. setup.ts registriert PRessBelegMatrixViewer auf hint="matrix".
    5. `cd portal && npx tsc --noEmit` 0 neue Errors.
    6. `cd portal && npm test -- --run` gesamte Suite grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document-Level-Keyboard-Listener für Ctrl+C/Ctrl+V (SC-5 UI-Verdrahtung)</name>
  <files>
    portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx,
    portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx
  </files>
  <read_first>
    - portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx (aus Task 2 — useBlockSelection ist bereits mounted, Selection-State liegt vor)
    - portal/src/graph/foundation/matrix/matrix-clipboard.ts (writeToDataTransfer, readFromDataTransfer, MATRIX_CLIPBOARD_MIME, checkOriginCompatible — alles aus Plan 05)
    - portal/src/graph/foundation/matrix/useBlockSelection.ts (Selection-State + computeCellRange — aus Plan 04)
    - portal/src/graph/foundation/matrix/useInlineCellEdit.ts (Edit-Mode-State — aus Plan 03; relevant für paste-target-Erkennung)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md (§11 Vitest-Spec-Pattern + §10 useModelStore-Mock)
  </read_first>
  <behavior>
    Spec (`PRessBelegMatrixViewerClipboard.spec.tsx`):
    - Test 1: Mount Viewer mit 2 Ress + 2 Knoten + 2 PAssozBeleg-Cells (Wert 1=standard + Wert 2=notfalls). Block-Select-Range über beide Cells (via manuelles Setzen via useBlockSelection-API ODER per Cell-Click+Shift-Click). Dispatch `copy`-Event auf `document`: `document.dispatchEvent(new ClipboardEvent('copy', {clipboardData: new DataTransfer()}))`. Assert: `event.preventDefault()` wurde gerufen (via spy) UND DataTransfer enthält MIME `application/x-osim-matrix-cells` mit JSON-Payload der 2 Werte.
    - Test 2: Mount Viewer mit kopierter Payload (DataTransfer mit MATRIX_CLIPBOARD_MIME). Eine Ziel-Cell (z.B. row1:col1) in Edit-Mode setzen (via Click). Dispatch `paste`-Event mit dem DataTransfer. Assert: `event.preventDefault()` wurde gerufen; für jede Cell der Range wurde `onCellEdit` mit dem kopierten Wert aufgerufen (patchObject-Mock-Counter erhöht).
    - Test 3: Wenn der Viewer KEINE aktive Selection hat: Dispatch `copy`-Event darf KEINEN preventDefault rufen (Pass-Through für andere Page-Elements).
    - Test 4: Wenn `disabled=true` als ViewerProp: copy/paste-Listener tun nichts (kein preventDefault, kein onCellEdit).
    - Test 5: Unmount der Viewer-Komponente → die Listener sind removed (kein Re-Trigger nach Unmount via document.dispatchEvent).
  </behavior>
  <action>
    1. **PRessBelegMatrixViewer.tsx** erweitern um zweiten `useEffect`-Hook (nach dem bestehenden Wire-Daten-useMemo, vor dem return):
       - Hook-Body (Pseudocode):
         ```typescript
         useEffect(() => {
           if (disabled) return undefined;

           const isActive = () => {
             // Viewer ist "aktiv" wenn der Viewer-Root im DOM ist UND einer seiner Nachfahren focused/aktiv ist.
             // Heuristik: document.activeElement liegt INNERHALB des [data-viewer="PRessBelegMatrixViewer"]-Knotens
             // ODER es gibt eine aktive Block-Selection ODER eine Cell ist im Edit-Mode.
             // Fallback: ein per useRef gehaltenes rootRef.current?.contains(document.activeElement).
             return rootRef.current?.contains(document.activeElement) || hasActiveSelection || inlineCell !== null;
           };

           const onCopy = (e: ClipboardEvent) => {
             if (!isActive()) return;
             if (!blockSelection.hasSelection()) return;
             const range = blockSelection.getRange();
             const payload = serializeMatrixCellsFromRange(range, rows, cols, cellMap);
             if (!payload) return;
             e.preventDefault();
             e.clipboardData?.setData(MATRIX_CLIPBOARD_MIME, JSON.stringify(payload));
           };

           const onPaste = (e: ClipboardEvent) => {
             if (!isActive()) return;
             if (inlineCell === null && !blockSelection.hasSelection()) return; // Paste braucht Ziel-Cell ODER aktive Selection
             const data = e.clipboardData;
             const payload = data ? readFromDataTransfer(data) : null;
             if (!payload) return;
             e.preventDefault();
             // Target ermitteln: bevorzugt die im Edit-Mode liegende Cell, sonst Anchor der Selection.
             const target = inlineCell?.cellId ?? blockSelection.getAnchor();
             if (!target) return;
             // Per-Cell onCellEdit für die Range relativ zum Target.
             applyClipboardPayloadAt(payload, target, rows, cols, onCellEdit);
           };

           document.addEventListener('copy', onCopy);
           document.addEventListener('paste', onPaste);
           return () => {
             document.removeEventListener('copy', onCopy);
             document.removeEventListener('paste', onPaste);
           };
         }, [disabled, blockSelection, inlineCell, rows, cols, cellMap, onCellEdit]);
         ```
       - `rootRef = useRef<HTMLDivElement>(null)` einführen + an den `<div data-viewer="PRessBelegMatrixViewer">`-Wrapper hängen.
       - Helfer-Funktionen `serializeMatrixCellsFromRange` und `applyClipboardPayloadAt` als kleine Module-Local-Functions (oder inline) implementieren — sie nutzen `writeToDataTransfer` / `readFromDataTransfer` aus matrix-clipboard.ts als Bausteine.
       - `inlineCell` ist der State des EditableCell-Wrappers oder global im Viewer als `[inlineCellId, setInlineCellId]` gehalten — Pattern aus dem bestehenden Viewer-Code übernehmen.

    2. **Spec** `portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx` schreiben (5 Tests aus `<behavior>`).
       Setup-Skelett:
       ```typescript
       import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
       import { render, cleanup } from '@testing-library/react';
       import userEvent from '@testing-library/user-event';
       import { PRessBelegMatrixViewer } from '@/viewers/PRessBelegMatrix/PRessBelegMatrixViewer';
       import { MATRIX_CLIPBOARD_MIME } from '@/graph/foundation/matrix';
       import { mockUseModelStore } from '../setup-store-mock'; // analog PATTERNS S10

       const fixture = { /* aus PATTERNS §11 Code-Block: plan + 2 Knoten + 2 Ress + 2 PAssozBeleg */ };

       describe('PRessBelegMatrixViewer — Clipboard-Listener', () => {
         beforeEach(() => mockUseModelStore.reset());
         afterEach(() => cleanup());
         // Test 1-5 aus <behavior>
       });
       ```
       Für `document.dispatchEvent(new ClipboardEvent(...))`: JSDOM hat eingeschränkten ClipboardEvent-Support — als Workaround `new Event('copy')` + manuelles Setzen von `clipboardData` via `Object.defineProperty(event, 'clipboardData', { value: new DataTransfer() })`. Pattern bei Bedarf aus tbx_stzrim oder Web-Search übernehmen.

    3. **Vorhandene PRessBelegMatrixViewer.spec.tsx** (aus Task 2) bleibt unverändert — die neuen Clipboard-Tests gehören in die separate Spec-Datei, damit die Setup-Komplexität (DataTransfer-Mock) die anderen 7 Tests nicht aufbläht.

    4. **Validierung**:
       - `cd portal && npm test -- --run src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx` — alle 5 Tests grün.
       - `cd portal && npx tsc --noEmit` — 0 neue Errors.
       - Manuelle Browser-Sanity (optional, formal in Welle 09 E2E Test 6 verifiziert): Modell laden, 2x2-Range markieren, Ctrl+C, andere Cell als Edit-Target, Ctrl+V — Werte landen.

    KEIN Touch an Plan 05 matrix-clipboard.ts (das ist die Pure-Function-Schicht).
  </action>
  <verify>
    <automated>cd portal && npm test -- --run src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx 2>&1 | tail -15 | grep -E "(passed|Tests)"</automated>
  </verify>
  <done>
    1. PRessBelegMatrixViewer.tsx hat einen zweiten useEffect-Hook der document-copy/paste-Listener registriert + cleanup-Funktion.
    2. PRessBelegMatrixViewerClipboard.spec.tsx existiert ≥60 LoC mit 5 Tests grün.
    3. `cd portal && npx tsc --noEmit` 0 neue Errors.
    4. Listener honoriert die `disabled`-ViewerProp (Test 4) und cleanupt sich beim Unmount (Test 5).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cell-Status-Edit → model-store | User wählt Status aus Combobox → patchObject schreibt m_eStatus. Validierung: Status-Werte sind durch Combobox auf {0,1,2,3} beschränkt. |
| Cell-Create → osim-engine | createObject("PAssozBeleg",...) erzeugt neue Wire-Klass. PAssozBeleg ist eine echte OSim-Engine-Klass (im Gegensatz zu PRessVerknuepfung) — der OTX-Writer kennt sie (Welle G16-Pass-Through-Pfad gehärtet). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-14 | Tampering | Cell-Edit ohne Lock-Check | mitigate | `disabled`-Prop wird an MatrixGrid + EditableCell-Wrapper durchgereicht; readOnly-Branch im useInlineCellEdit-Hook (verifiziert Welle 1.2-03 Test 3). |
| T-01.2-15 | Tampering | Wire-Slot-Index falsch → m_lAssozRess landet im falschen Slot | mitigate | Plan 01 Task 3 verifiziert Slot-Index via Backend-curl in SCHEMA-MAP.md; Plan 06 Task 1 übernimmt den Index 1:1 in 01.2-06-CPP-AUDIT.md; Plan 06 Task 2 setzt _slotToAttrName-Mapping. Finale Test-Verifikation via PRessBelegMatrixViewer.spec.tsx Test 4 (assertet konkreten appendSubRef-Slot). |
| T-01.2-16 | Input Validation | m_eStatus außerhalb {0,1,2,3} | mitigate | Combobox liefert nur die 4 erlaubten Werte; Backend hat zusätzlich PropertySchema-Validation. |
| T-01.2-17 | Data Corruption | createObject schreibt nicht-existierende Klass | mitigate | PAssozBeleg ist echte OSim-Engine-Klass (RESEARCH §4.1 + §4.5 bestätigt — Unterschied zur erfundenen PRessVerknuepfung-Klass die in Welle 01 entfernt wurde). |
</threat_model>

<verification>
- `cd portal && npm test -- --run` zeigt alle Specs grün (Foundation aus Welle 02-05 + neue Viewer-Spec).
- `cd portal && npx tsc --noEmit` 0 neue Errors.
- Audit-Datei `01.2-06-CPP-AUDIT.md` enthält konkrete Slot-Indizes (nicht nur "[ASSUMED]").
- Registry hat Eintrag für `(klass="PRessBeleg", hint="matrix")`.
</verification>

<success_criteria>
- SC-6 (PRessBelegMatrixViewer): erfüllt — echte 2D-Matrix mit Combobox-Toolbar + Cell-Edit-Lifecycle.
- SC-9 (Viewer-Smoke-Test): 1/3 Viewer-Specs erfüllt (Rest in Welle 07/08).
</success_criteria>

<output>
Create `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-SUMMARY.md` when done.
</output>
