---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portal/src/viewers/PRess/PRessBelegMatrixViewer.tsx
  - portal/src/viewers/PRess/PRessVerknuepfungViewer.tsx
  - portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx
  - portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx
  - portal/src/viewers/setup.ts
  - portal/src/viewers/__tests__/__snapshots__/
  - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md
autonomous: false
requirements: [SC-6, SC-8]
must_haves:
  truths:
    - "Stub PRessVerknuepfungViewer.tsx (erfundenes PRessVerknuepfung-Schema) ist entfernt"
    - "Stub PRessBelegMatrixViewer.tsx ist entweder entfernt oder als PRessBelegPropertyListViewer.tsx umbenannt (User-Entscheidung)"
    - "Keine Wire-Klass PRessVerknuepfung existiert mehr im Code (Grep liefert null Treffer)"
    - "viewerRegistry hat keinen Eintrag mehr für klass='PRessVerknuepfung'"
    - "SQL-Check auf Modell-DB ist gelaufen, korrumpierte Modelle dokumentiert (oder Befund: keine vorhanden)"
    - "192 Vitest-Specs grün (alte 2 entfernte Specs subtrahiert, keine neuen)"
    - "01.2-SCHEMA-MAP.md dokumentiert die sub_refs[N]-Slot-Indizes für PAssozBeleg.m_lRessourcen, PDpKn*.m_lAssozRess und PDlplKnoten.attrs.m_lAssozRess (Source: curl gegen lokales Backend oder RESEARCH-Fallback)"
  artifacts:
    - path: portal/src/viewers/setup.ts
      provides: "Bereinigtes Viewer-Registry — kein PRessVerknuepfung mehr"
    - path: .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-CLEANUP-LOG.md
      provides: "User-Entscheidung + SQL-Check-Befund + gelöschte Files dokumentiert"
    - path: .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md
      provides: "Wire-Slot-Index-Tabelle für PAssozBeleg.m_lRessourcen + PDpKn*.m_lAssozRess (Voraussetzung für Plan 06 Task 2 _slotToAttrName-Erweiterung)"
  key_links:
    - from: "Grep -r PRessVerknuepfung portal/src/"
      to: "0 Treffer"
      via: "Codebase-Sweep nach Cleanup"
      pattern: "PRessVerknuepfung"
    - from: portal/src/viewers/setup.ts
      to: "Registry ohne PRessVerknuepfung-Eintrag"
      via: "Code-Anpassung"
      pattern: "klass:\\s*\"PRessVerknuepfung\""
---

<objective>
**Pre-Cleanup** der Phase-1-Stubs, die für die Phase-1.2-Implementation konzeptionell falsch sind und Welle E/G blockieren würden.

**Purpose:**
Phase 1 hat zwei Stubs angelegt (`PRessBelegMatrixViewer.tsx`, `PRessVerknuepfungViewer.tsx`), die nicht der OSim2004-Treue entsprechen:
1. `PRessBelegMatrixViewer.tsx` zeigt eine **flache Property-Liste** statt einer 2D-Matrix (Ress × Knoten × PAssozBeleg).
2. `PRessVerknuepfungViewer.tsx` schreibt die **erfundene** Wire-Klass `PRessVerknuepfung` mit `m_iAnteil` — diese Klass existiert NICHT in der osim-engine. Bei jedem Save würden Modelle mit `PRessVerknuepfung`-Objekten korrumpieren (analog Welle G16 Engine-Bug).

Welle 1.2-0 muss die Stubs entfernen (oder den 1. umbenennen), den Registry-State bereinigen, und prüfen ob bereits korrumpierte Modelle in der DB liegen. Erst dann darf Welle 1.2-A loslegen.

**Output:**
- Entfernte/umbenannte Stubs
- Bereinigte `portal/src/viewers/setup.ts`
- `01.2-01-CLEANUP-LOG.md` mit User-Entscheidung + SQL-Check-Befund
- 0 Treffer für `PRessVerknuepfung` im Codebase-Grep
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-CONTEXT.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md

<interfaces>
<!-- Aktuelle Registry-Einträge die zu bereinigen sind. Aus portal/src/viewers/setup.ts Z.131-152. -->

```typescript
// MUSS ENTFERNT WERDEN (Welle 0):
viewerRegistry.register({
  klass: "PRessVerknuepfung",
  Component: PRessVerknuepfungViewer,
});
viewerRegistry.register({
  klass: "PAssozBeleg",
  Component: PRessVerknuepfungViewer,  // Reflection-Alias auf das falsche Schema
});

// MUSS angepasst werden je nach User-Entscheidung in <task-3>:
viewerRegistry.register({
  klass: "PRessBeleg",
  hint: "matrix",
  Component: PRessBelegMatrixViewer,
});
```

Bestehende Stubs (zu löschen):
- portal/src/viewers/PRess/PRessVerknuepfungViewer.tsx (171 Zeilen, schreibt erfundenes Schema)
- portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx (testet erfundenes Schema)
- portal/src/viewers/PRess/PRessBelegMatrixViewer.tsx (145 Zeilen, flache Property-Liste — User-Entscheidung)
- portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx (testet flache Property-Liste — User-Entscheidung)

NICHT TOUCHEN (CONTEXT deferred-Liste):
- portal/src/viewers/PRess/PRessMengeMatrixViewer.tsx
- portal/src/viewers/PRess/matrix-common.tsx  (Logik wird in Welle 1.2-B als Hook gelift, Datei bleibt für PRessMenge-Stub)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL-Check auf Modell-DB nach PRessVerknuepfung-Korruption</name>
  <files>.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-CLEANUP-LOG.md</files>
  <read_first>
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md (§Runtime State Inventory, §4.5, §8.1 R2)
    - .planning/STATE.md (Welle G16 SQL-Repair-Pattern als Referenz: UPDATE models SET storage_key=original_storage_key)
  </read_first>
  <action>
    1. Postgres-Container hochfahren falls nicht läuft: `docker compose ps postgres` — wenn nicht running: `docker compose up -d postgres && bash scripts/wait-healthy.sh 60`.
    2. SQL-Check ausführen (alle 4 Tenant-Schemas durchsuchen). Erste Sondierung:
       ```sql
       SELECT schemaname FROM pg_tables WHERE tablename = 'models' AND schemaname LIKE 'tenant_%';
       ```
       Dann pro Tenant-Schema:
       ```sql
       SET search_path TO <tenant>;
       SELECT m.id, m.original_storage_key, m.storage_key
       FROM models m;
       ```
       Für jedes Modell den storage_key (oder original_storage_key wenn != current) via MinIO-Client (`docker compose exec minio mc ls local/osim-models/<key>`) inspizieren und den JSON-Body greppen:
       ```bash
       docker compose exec minio mc cat local/osim-models/<key> | grep -c PRessVerknuepfung
       ```
    3. Befund in `01.2-01-CLEANUP-LOG.md` dokumentieren — Format:
       ```markdown
       # 01.2-01-CLEANUP-LOG.md
       ## SQL-Check 2026-05-25
       - Tenants gescannt: <liste>
       - Modelle gesamt: <n>
       - Modelle mit PRessVerknuepfung-Objekten: <n>
         - <tenant>/<model_id>: <count> PRessVerknuepfung-Objekte
       - Aktion: <keine | reset auf original_storage_key | manueller Cleanup>
       ```
    4. WENN korrumpierte Modelle gefunden: KEINE automatische Mutation — Treffer im Log dokumentieren und in Task 2 (AskUserQuestion) als Punkt aufnehmen.
  </action>
  <verify>
    <automated>test -f .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-CLEANUP-LOG.md && grep -q "SQL-Check 2026-05-25" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-CLEANUP-LOG.md</automated>
  </verify>
  <done>01.2-01-CLEANUP-LOG.md existiert mit SQL-Check-Sektion, Tenant-Liste, Modell-Befund (auch wenn 0 Treffer).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-Entscheidung zur Stub-Behandlung (AskUserQuestion)</name>
  <what-built>
    SQL-Check aus Task 1 liegt vor — Befund über PRessVerknuepfung-Korruption ist dokumentiert.
    Zwei offene Entscheidungen für den User:
    1. `PRessBelegMatrixViewer.tsx` (flache Property-Liste, 145 LoC): LÖSCHEN oder UMBENENNEN als sekundäre `PRessBelegPropertyListViewer.tsx` (Hint `property-list`)?
    2. Wenn korrumpierte Modelle gefunden wurden (SQL-Check): RESET auf `original_storage_key` (analog Welle G16) ODER manueller User-Cleanup im UI ODER ignorieren (Lösch-Endpoint stellt sicher dass User es nicht erneut speichert)?
  </what-built>
  <how-to-verify>
    1. Öffne `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-CLEANUP-LOG.md` und lies den SQL-Check-Befund.
    2. Lies den bestehenden Code:
       - `portal/src/viewers/PRess/PRessBelegMatrixViewer.tsx` (flache Property-Liste mit Spalten Name/Kapazität/Einheit/Fixkosten/Var.Kosten/Anwesenheit/Bemerkung)
       - `portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx`
    3. Antworte mit 2 Entscheidungen:
       - **Entscheidung A (Property-Liste):** `LÖSCHEN` (empfohlen) ODER `UMBENENNEN` (= behalten als sekundäre View)
       - **Entscheidung B (DB-Cleanup, nur falls Korruption gefunden):** `RESET` ODER `MANUELL` ODER `IGNORIEREN`
    4. Entscheidungen kurz begründen — wird in `01.2-01-CLEANUP-LOG.md` § "User-Entscheidung" eingetragen.
  </how-to-verify>
  <resume-signal>Antworte mit "Entscheidung A: LÖSCHEN|UMBENENNEN, Entscheidung B: RESET|MANUELL|IGNORIEREN|N/A" plus Begründung.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Schema-Sondierung — Wire-Slot-Indizes für PAssozBeleg + PDpKn* + PDlplKnoten</name>
  <files>.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md</files>
  <read_first>
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md (§4 + §4.5 + §8.3 OQ5 — Wire-Schema-Konventionen + Open-Question zur Slot-Index-Verifikation)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md (Z.382-430 — Klass-Übersicht + Beispiel-Wire-JSON für PDpKnKonstant und PAssozBeleg)
    - portal/src/stores/model-store.ts (Z.180-305 — _slotToAttrName + appendSubRef-Logik; aktueller PDurchlaufplan-Mapping-Stand)
    - portal/e2e/fixtures/test-users.ts (ADMIN-Credentials + API_BASE_URL für curl-Pattern)
  </read_first>
  <action>
    Ziel: Vor Plan 06 muss feststehen, in welchem sub_refs[N]-Slot
    (a) `PAssozBeleg.m_lRessourcen` (die Liste der enthaltenen PRessBeleg-OIDs) und
    (b) `PDpKnKonstant`/`PDpKnAlternativ`/`PDpKnRuecksprung`/etc. ihre `m_lAssozRess`-Liste halten,
    (c) `PDlplKnoten.attrs.m_lAssozRess` (gegen Doppel-Schreibung via attrs vs sub_refs)
    damit Plan 06 Task 2 die `_slotToAttrName`-Mapping-Funktion korrekt erweitern kann.
    Ohne diese Sondierung würde Plan 06 raten müssen → Cell-Create-Pfad würde im falschen Slot landen → korrumpierte Modelle (analog R2-Risiko aus RESEARCH §8).

    1. **Stack-Bootstrap** (falls nicht läuft):
       ```bash
       docker compose ps api postgres firebase-emulator minio
       # falls nicht running:
       docker compose up -d && bash scripts/wait-healthy.sh 90 && uv run python scripts/seed_firebase_emulator.py
       ```

    2. **Auth-Token holen** (Pattern aus Plan 09 Task 1 + portal/e2e/fixtures/auth.ts):
       ```bash
       TOKEN=$(curl -s -X POST "http://localhost:9099/identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key=fake-api-key"          -d '{"email":"admin@osim.local","password":"admin123","returnSecureToken":true}' | jq -r .idToken)
       ```

    3. **Modell mit PAssozBeleg-Objekten finden oder hochladen**:
       - Erste Sondierung — sind bereits Modelle vorhanden?
         ```bash
         curl -s http://localhost:8000/api/v1/models -H "Authorization: Bearer $TOKEN" | jq '.[0:10] | map({id, name})'
         ```
       - Wenn ein Modell mit Namen `Embb-AslFj` oder `Fertigungsstruktur` existiert: dessen `id` verwenden.
       - Sonst: Embb-AslFj.otx hochladen (Multipart-POST analog `portal/e2e/modeling-flow.spec.ts`):
         ```bash
         curl -s -X POST http://localhost:8000/api/v1/models            -H "Authorization: Bearer $TOKEN"            -F "file=@C:/Users/JörgWFischer/PycharmProjects/OSim2004/Vorstellung04/Embb-AslFj.otx"            -F "name=schema-sondierung-$(date +%s)" | jq '{id}'
         ```
         Resultierende `id` als `$MODEL_ID` festhalten.

    4. **Wire-Daten abrufen + Slots inspizieren**:
       ```bash
       curl -s http://localhost:8000/api/v1/models/$MODEL_ID          -H "Authorization: Bearer $TOKEN" > /tmp/model.json

       # PAssozBeleg-Sample
       jq '.objects | to_entries | map(select(.value.klass == "PAssozBeleg")) | .[0]' /tmp/model.json
       # → notiere: attrs.m_lRessourcen vs sub_refs[N] (welcher Slot)

       # PDpKn*-Sample (Knoten-Subklassen)
       jq '.objects | to_entries | map(select(.value.klass | test("^PDpKn"))) | .[0:3]' /tmp/model.json
       # → notiere: attrs.m_lAssozRess vs sub_refs[N]

       # PDlplKnoten direkter Treffer (nicht Subklasse)
       jq '.objects | to_entries | map(select(.value.klass == "PDlplKnoten")) | .[0]' /tmp/model.json
       ```

    5. **Schema-Map-Dokument schreiben**: `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md` mit folgendem Format:
       ```markdown
       # 01.2-SCHEMA-MAP.md — Wire-Slot-Indizes

       **Sondierungs-Datum:** 2026-05-25
       **Quell-Modell:** <name>/<id> (oder "fallback: RESEARCH §4")
       **Backend-Status:** <ok | fallback>

       ## PAssozBeleg
       | Feld          | Source         | Slot |
       |---------------|----------------|------|
       | m_lRessourcen | sub_refs[N]    | N=?  |
       | m_lRessourcen | attrs (mirror) | ja/nein |
       | m_eStatus     | attrs          | n/a  |

       ## PDpKnKonstant / PDpKnAlternativ / PDpKnRuecksprung / PDpKnAELogikSteuerung
       | Feld         | Source      | Slot |
       |--------------|-------------|------|
       | m_lAssozRess | sub_refs[N] | N=?  |
       | m_lAssozRess | attrs       | ja/nein |
       | m_lAssozSpeich | sub_refs[N] | N=?  |

       ## PDlplKnoten (Basisklasse, falls direkt instanziiert)
       | Feld         | Source      | Slot |
       |--------------|-------------|------|
       | m_lAssozRess | sub_refs[N] | N=?  |

       ## Konsequenzen für Plan 06 Task 2 (_slotToAttrName)
       ```typescript
       // erweitere _slotToAttrName(klass, slot) um:
       if (klass.startsWith("PDpKn") && slot === <N_aus_Tabelle>) return "m_lAssozRess";
       if (klass === "PAssozBeleg" && slot === <N_aus_Tabelle>) return "m_lRessourcen";
       ```

       ## Fallback (wenn Backend nicht erreichbar war)
       Marker: `[ASSUMED]` — Werte aus RESEARCH §4 (Z.382-430):
       - PAssozBeleg.m_lRessourcen: sub_refs[0] [ASSUMED]
       - PDpKn*.m_lAssozRess: sub_refs[0] [ASSUMED]
       Plan 06 Task 2 muss in diesem Fall den Slot-Index zur Laufzeit nochmal verifizieren
       (Test 4 in PRessBelegMatrixViewer.spec.tsx prüft appendSubRef-Call mit erwartetem Slot).
       ```

    6. **Falls Backend-Sondierung scheitert** (Docker startet nicht, Embb-AslFj nicht verfügbar, Auth-Token-Generierung fehlschlägt): den Fallback-Block aus 5. verwenden und im SCHEMA-MAP.md klar markieren `**Backend-Status:** fallback`. KEIN Blocker — Plan 06 ist robust, weil sein Test 4 den appendSubRef-Aufruf mit konkretem Slot-Wert assertet.

    KEIN Touch an Code-Files in dieser Task — nur Doku.
  </action>
  <verify>
    <automated>test -f .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md && grep -q "PAssozBeleg" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md && grep -q "_slotToAttrName" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-SCHEMA-MAP.md</automated>
  </verify>
  <done>
    1. `01.2-SCHEMA-MAP.md` existiert.
    2. Enthält Slot-Indizes (konkret aus curl ODER `[ASSUMED]`-Fallback) für PAssozBeleg.m_lRessourcen, PDpKn*.m_lAssozRess, PDlplKnoten.m_lAssozRess.
    3. Enthält den `_slotToAttrName`-Patch-Vorschlag, den Plan 06 Task 2 1:1 übernehmen kann.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Stub-Cleanup ausführen (gemäß User-Entscheidung)</name>
  <files>
    portal/src/viewers/PRess/PRessVerknuepfungViewer.tsx,
    portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx,
    portal/src/viewers/PRess/PRessBelegMatrixViewer.tsx,
    portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx,
    portal/src/viewers/setup.ts,
    .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-CLEANUP-LOG.md
  </files>
  <read_first>
    - portal/src/viewers/setup.ts (komplett — Registry-Einträge die zu bereinigen sind, Z.131-152)
    - portal/src/viewers/PRess/PRessVerknuepfungViewer.tsx (Stub der zu löschen ist)
    - portal/src/viewers/PRess/PRessBelegMatrixViewer.tsx (Stub — User-Entscheidung)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-CLEANUP-LOG.md (Entscheidungs-Befund aus Task 2)
  </read_first>
  <action>
    Schritte (in dieser Reihenfolge):

    1. **PRessVerknuepfungViewer löschen (immer):**
       - `rm portal/src/viewers/PRess/PRessVerknuepfungViewer.tsx`
       - `rm portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx`
       - Bestehende Snapshot-Files unter `portal/src/viewers/__tests__/__snapshots__/PRessVerknuepfungViewer.spec.tsx.snap` (falls existiert) ebenfalls löschen.

    2. **setup.ts bereinigen:**
       - Import-Statement `import { PRessVerknuepfungViewer } from "@/viewers/PRess/PRessVerknuepfungViewer";` (Z.57) entfernen.
       - Registry-Eintrag `klass: "PRessVerknuepfung"` (Z.141-144) entfernen.
       - Registry-Eintrag `klass: "PAssozBeleg" → PRessVerknuepfungViewer` (Z.149-152) entfernen — der Reflection-Alias zeigt aufs falsche Schema.
       - Kommentar-Block "Reflection-Alias: die echte OTX-Klasse fuer Belegungs-Verknuepfung ist PAssozBeleg…" (Z.145-148) entfernen.
       - Doku-Comment am Datei-Anfang (Z.24-25 `PRessVerknuepfung → PRessVerknuepfungViewer` und Z.25 `PAssozBeleg → PRessVerknuepfungViewer`) entfernen.

    3. **PRessBelegMatrixViewer behandeln (gemäß Entscheidung A aus Task 2):**
       - WENN `LÖSCHEN`:
         - `rm portal/src/viewers/PRess/PRessBelegMatrixViewer.tsx`
         - `rm portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx`
         - Snapshot-Files (falls existieren) ebenfalls.
         - In `setup.ts`: Import (Z.55) entfernen + Registry-Eintrag (Z.131-135) entfernen + Doku-Kommentar (Z.22 + Z.113-126) entsprechend kürzen.
       - WENN `UMBENENNEN`:
         - `git mv portal/src/viewers/PRess/PRessBelegMatrixViewer.tsx portal/src/viewers/PRess/PRessBelegPropertyListViewer.tsx`
         - Komponente in der Datei umbenennen: `export function PRessBelegMatrixViewer` → `export function PRessBelegPropertyListViewer`
         - `data-viewer="PRessBelegMatrixViewer"` → `data-viewer="PRessBelegPropertyListViewer"`
         - In `setup.ts`: Import-Pfad + Klassen-Name aktualisieren, Hint von `"matrix"` auf `"property-list"` umstellen.
         - Spec analog umbenennen: `git mv portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx portal/src/viewers/__tests__/PRessBelegPropertyListViewer.spec.tsx` + Innen-Refs anpassen.

    4. **DB-Cleanup (gemäß Entscheidung B aus Task 2):**
       - WENN `RESET`: für jedes korrumpierte Modell aus dem Log: `psql` Befehl ausführen
         ```sql
         UPDATE <tenant>.models SET storage_key = original_storage_key WHERE id = '<model_id>';
         ```
         (analog Welle G16 SQL-Repair-Pattern, siehe STATE.md). Befehle + Output in `01.2-01-CLEANUP-LOG.md` § "DB-Repair" dokumentieren.
       - WENN `MANUELL`/`IGNORIEREN`/`N/A`: keine SQL-Aktion, Befund im Log notieren.

    5. **Final-Validierung:**
       - `grep -rn "PRessVerknuepfung" portal/src/` muss 0 Treffer haben (Klassname + Stub-Name beide weg).
       - Wenn Entscheidung A `LÖSCHEN`: `grep -rn "PRessBelegMatrixViewer" portal/src/` muss 0 Treffer haben.
       - `cd portal && npx tsc --noEmit` muss 0 neue Errors zeigen (Pre-existing Foundation-Errors aus G7-Altlast bleiben).
       - `cd portal && npm test -- --run` muss grün laufen — alte 2 Specs sind weg, kein Test darf importieren was es nicht mehr gibt.

    6. **CLEANUP-LOG.md finalisieren** mit Sektionen `## User-Entscheidung`, `## Gelöschte Files`, `## setup.ts-Diff`, `## DB-Repair`, `## Validierung` (tsc + npm test Output).

    KEINE neuen Files in `portal/src/graph/foundation/matrix/` anlegen — das ist Welle 1.2-A.
    KEIN Touch von `matrix-common.tsx` oder `PRessMengeMatrixViewer.tsx` (CONTEXT deferred).
  </action>
  <verify>
    <automated>cd portal && grep -rn "PRessVerknuepfung" src/ | grep -v '^#' | wc -l | grep -q '^0$' && npx tsc --noEmit 2>&1 | grep -c "error TS" | head -1 && npm test -- --run 2>&1 | tail -20 | grep -E "(passed|✓|Tests)"</automated>
  </verify>
  <done>
    1. `grep -rn "PRessVerknuepfung" portal/src/` = 0 Treffer.
    2. `portal/src/viewers/setup.ts` enthält keinen Eintrag mehr für `klass: "PRessVerknuepfung"` oder den falschen `klass: "PAssozBeleg" → PRessVerknuepfungViewer`-Alias.
    3. `cd portal && npx tsc --noEmit` zeigt keine neuen Errors (Pre-existing G7-Errors dürfen bleiben).
    4. `cd portal && npm test -- --run` läuft grün.
    5. `01.2-01-CLEANUP-LOG.md` enthält alle 5 Sektionen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → osim-engine (Save-Pfad) | Wire-Daten mit erfundenen Klassen-Namen werden via OTX-Writer in MinIO geschrieben. Pass-Through-Pfad nimmt unbekannte Klassen mit attrs={} (vor Welle G16) oder mit props=dict(otx_obj.attrs) (nach G16) — beide Pfade KÖNNEN das Modell korrumpieren, wenn die Klass nie aus einer echten OTX gelesen wurde. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Tampering | PRessVerknuepfungViewer Stub (createObject("PRessVerknuepfung", ...)) | mitigate | Stub vollständig löschen (Task 4 Schritt 1). Klass kommt damit nicht mehr in den Wire-Store. |
| T-01.2-02 | Data Corruption | Bereits existierende Modelle mit PRessVerknuepfung-Objekten in MinIO | mitigate | SQL-Check (Task 1) + RESET auf original_storage_key bei User-Entscheidung B=RESET (Task 4 Schritt 4) |
| T-01.2-03 | Tampering | viewerRegistry-Alias `klass:"PAssozBeleg" → PRessVerknuepfungViewer` | mitigate | Eintrag entfernen — sonst landen echte OTX-PAssozBeleg-Objekte auf einem nicht-existierenden Viewer (Crash bei Render) |
</threat_model>

<verification>
**Phase Gate (vor Übergang zu Welle 1.2-A):**
- `grep -rn "PRessVerknuepfung" portal/src/` = 0 Treffer
- `cd portal && npx tsc --noEmit` = 0 neue Errors
- `cd portal && npm test -- --run` = alle Specs grün (Anzahl = 192 minus 2 entfernte Specs, ggf. minus 1 weitere wenn Entscheidung A=LÖSCHEN)
- `01.2-01-CLEANUP-LOG.md` vollständig (5 Sektionen)
- Codebase ist sauber für Welle 1.2-A
</verification>

<success_criteria>
- Codebase enthält keine Referenz mehr auf das erfundene `PRessVerknuepfung`-Schema.
- User-Entscheidung über `PRessBelegMatrixViewer`-Behandlung ist dokumentiert und umgesetzt.
- Modell-DB-Status ist bekannt (Cleanup ausgeführt oder bewusst übersprungen).
- Vitest-Suite grün, tsc grün — Foundation kann auf sauberem Stand starten.
</success_criteria>

<output>
Create `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-SUMMARY.md` when done.
</output>
