---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 02
subsystem: matrix-foundation
tags: [foundation, matrix, sticky-headers, react-component, vitest, 3fls-tokens]
requires:
  - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-SUMMARY.md (Pre-Cleanup, Stub-Removal abgeschlossen)
  - portal/src/graph/foundation/ (Phase 1.1 GraphObject-Foundation A–G27 — Pattern-Quelle)
  - portal/src/components/ui/table.tsx (shadcn-Table-Komposition)
  - portal/src/viewers/PRess/matrix-common.tsx (Sticky-Header- + Empty-State-Pattern)
provides:
  - "<MatrixGrid>-Component mit renderCell-Slot, sticky Col/Row/Corner-Headers (CSS-Grid via position:sticky)"
  - "<MatrixCell>-Component memoized mit Selection/Edit/Disabled-State (3FLS-EAM-Tokens)"
  - "Barrel-Export @/graph/foundation/matrix (MatrixGrid + MatrixGridProps + MatrixCell + MatrixCellProps)"
  - "15 Foundation-Specs grün (9 MatrixGrid + 6 MatrixCell — deckt SC-9 in Welle A vollständig ab)"
affects:
  - portal/src/graph/foundation/matrix/MatrixGrid.tsx (NEU)
  - portal/src/graph/foundation/matrix/MatrixCell.tsx (NEU)
  - portal/src/graph/foundation/matrix/index.ts (NEU)
  - portal/src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx (NEU)
  - portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx (NEU)
  - .planning/phases/01.2-…/deferred-items.md (NEU — pre-existing tsc-Errors in interactions.ts)
tech-stack:
  added: []
  patterns:
    - "Renderer-agnostisches React-Component via Component-Slot-Prop (renderCell statt Canvas-Callback) — CONTEXT D-2"
    - "Sticky-Headers via CSS position:sticky + z-Index-Stacking (col=z-10, row=z-10, corner=z-20)"
    - "React.useMemo mit revision-getriggertem Re-Build (1:1 GraphFlowCanvas Z.92-96)"
    - "React.memo-Wrap für Cell-Wrapper (1:1 OsimNode Z.135) — Pflicht bei großen Matrizen"
    - "data-Attribute für E2E-Selektoren (data-matrix-grid/-header/-cell)"
    - "3FLS-EAM-Token-Disziplin: 0 ad-hoc Hex-Strings (validiert via grep '#[0-9A-F]{6}' = 0 Treffer)"
key-files:
  created:
    - portal/src/graph/foundation/matrix/MatrixGrid.tsx
    - portal/src/graph/foundation/matrix/MatrixCell.tsx
    - portal/src/graph/foundation/matrix/index.ts
    - portal/src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx
    - portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/deferred-items.md
  modified: []
decisions:
  - "MatrixGrid und MatrixCell als zwei separate Tasks mit zwei eigenen Commits implementiert (Plan-Vorgabe). Task 1 hat den Barrel-Export zunächst nur mit MatrixGrid bestückt; Task 2 ergänzt MatrixCell. Damit ist jede Wave eigenständig grün baubar (TDD-Disziplin)."
  - "ESLint hat _disabled / _tick als unused gemeldet (no-unused-vars greift trotz Underscore-Prefix). Beide Inline-Fixes: disabled aus der Destrukturierung gestrichen (es wird in dieser Welle nicht ausgewertet — Welle 1.2-B reicht es an useInlineCellEdit durch), _tick als data-tick auf einem Wrapper-Div konsumiert (sichtbarer Test-Force-Rerender-Trigger)."
  - "Cell-Lookup-Cache: Map mit `${rowKey}:${colKey}`-Schlüssel statt geschachtelter Map (1-Level-O(1)-Lookup, simpler Debug, identische Performance bis ~10k Cells)."
  - "Empty-State liefert `data-matrix-grid='empty'`, gefüllter Zustand `data-matrix-grid='filled'` — E2E-Tests können beide Zustände unterscheiden ohne CSS-Class-Sniffing."
  - "Corner-Cell-Default-Label `DlplKnoten / Bel.ress.` ist 1:1 aus OSim2004 OnDrawFrame übernommen — bei Bedarf via cornerLabel-Prop überschreibbar (Welle 1.2-E PRessBelegMatrixViewer wird das setzen, andere Matrix-Viewer ggf. nicht)."
metrics:
  duration_minutes: 18
  completed_date: "2026-05-25"
  tasks_completed: 2
  tasks_total: 2
  status: "complete"
  specs_added: 15
  loc_production: 375
  loc_tests: 364
---

# Phase 01.2 Plan 02: Matrix-Foundation Welle A Summary

Foundation-Welle 1.2-A der Matrix-Foundation: renderer-agnostisches
`<MatrixGrid>`-Component mit Sticky-Col/Row/Corner-Headers (Excel-Pattern via
CSS `position: sticky`, CONTEXT D-3) und memoized `<MatrixCell>`-Wrapper für
einzelne Zellen. Pendant zu `GraphFlowCanvas`/`OsimNode` aus der Graph-
Foundation (Phase 1.1 Welle A–G27), aber für 2D-Matrix-Layouts. Keine Wire-
Knowledge in der Foundation — beide Components sind generisch über `TRow`,
`TCol`, `TVal` und kennen weder `PAssozBeleg` noch `PDpKn*` (CONTEXT D-1).
Konsumenten (Welle 1.2-E `PRessBelegMatrixViewer`, später `PRessMengeMatrixViewer`
etc.) liefern `cellLookup` + `renderCell` als Component-Slot (CONTEXT D-2)
statt Canvas-Callback. 15 Foundation-Specs (9 MatrixGrid + 6 MatrixCell)
laufen via Vitest jsdom grün, tsc ist für `src/graph/foundation/matrix/` 0
Errors, ESLint-Output ist sauber, `grep '#[0-9A-F]{6}'` über matrix/ liefert
0 Treffer (3FLS-EAM-Token-Disziplin gewahrt).

## Completed Tasks

### Task 1: MatrixGrid-Component + 9 Specs (Commit `aba4c19`)

**Was gebaut:**

- `portal/src/graph/foundation/matrix/MatrixGrid.tsx` (258 Zeilen) — generisches
  `<MatrixGrid<TRow, TCol, TVal>>`-Component. Props: `rows`, `cols`,
  `cellLookup`, `renderCell`, `renderRowHeader?`, `renderColHeader?`,
  `rowKey`, `colKey`, `revision?`, `disabled?`, `emptyMessage?`,
  `cornerLabel?`, `className?`.
- `cellMap` via `React.useMemo`, Deps `[rows, cols, revision]` + ESLint-
  disable mit Begründung (1:1 GraphFlowCanvas Z.92-96). Konsument macht
  `revision++` nach Wire-Mutation.
- Empty-State (rows.length===0 ODER cols.length===0) → zentrierter Hint
  mit `data-slot="matrix-empty"` + `data-matrix-grid="empty"`. Tabelle wird
  gar nicht gerendert.
- Sticky-Architektur:
  - `<TableHeader>` mit `sticky top-0 z-10 bg-background`
  - Corner-Cell: `sticky left-0 top-0 z-20 bg-muted` + Default-Label
    "DlplKnoten / Bel.ress." (1:1 OSim2004 OnDrawFrame), via `cornerLabel`-
    Prop überschreibbar.
  - Col-Header-Cells: `border-b border-r border-border bg-muted font-mono text-xs`
  - Row-Header-Cells (erste Cell jeder TableRow): `sticky left-0 z-10`
- Body-Cells: `<TableCell data-matrix-cell="${rowKey}:${colKey}" className="border-b border-r border-border p-0">{renderCell(r, c, val)}</TableCell>`
- `portal/src/graph/foundation/matrix/index.ts` (Task-1-Variante) re-
  exportiert `MatrixGrid` + `MatrixGridProps`.

**Specs (9/9 grün):**

| # | Beschreibung |
|---|--------------|
| 1 | rows.length===0 → Empty-State, keine Tabelle |
| 2 | cols.length===0 → Empty-State (auch bei non-empty rows) |
| 3 | renderCell wird genau 2×2=4× pro Render aufgerufen, Row-Major-Reihenfolge |
| 4 | cellLookup wird pro (row, col) konsultiert, Value an renderCell durchgereicht |
| 5 | Sticky-CSS-Klassen-Presence (`sticky` + `top-0` auf TableHeader, `sticky` + `left-0` auf row-Header) |
| 6 | Corner-Cell mit Default-Label + Override via cornerLabel-Prop |
| 7 | revision-Wechsel triggert Re-Build (4 → 8 cellLookup-Aufrufe nach rerender mit revision++) |
| 8 | Keine "missing key"-Warning bei React (rowKey/colKey werden konsistent verwendet) |
| 9 | data-Attribute komplett: data-matrix-grid + 1 corner + 2 col + 1 row + 2 cell-Attribute |

**Verifikation:**

- Vitest: `15 passed (15)`
- tsc für `src/graph/foundation/matrix/`: 0 Errors
- `grep '#[0-9A-F]{6}' portal/src/graph/foundation/matrix/MatrixGrid.tsx`: 0 Treffer

### Task 2: MatrixCell-Component + 6 Specs + Barrel-Update (Commit `2b89008`)

**Was gebaut:**

- `portal/src/graph/foundation/matrix/MatrixCell.tsx` (117 Zeilen) — memoized
  Renderer-Wrapper. Props: `cellId`, `oid?`, `selected?`, `editing?`,
  `disabled?`, `onClick?`, `onPointerDown?`, `className?`, `children?`.
- `React.memo`-Wrap (1:1 OsimNode Z.135) — Pflicht für große Matrizen
  (Bosch-Größe 30k Knoten).
- Visual-State-Mapping:
  - `selected` → `border-primary` + `bg-[var(--color-surface-soft-cyan)]` +
    `data-matrix-cell-selected="true"`
  - `editing` → `ring-2 ring-primary ring-offset-0` +
    `data-matrix-cell-editing="true"`
  - `disabled` → `cursor-not-allowed opacity-60` + onClick wird im Handler
    abgefangen
  - klickbar (nicht disabled + onClick gesetzt) → `cursor-pointer hover:bg-accent`
- Click-/PointerDown-Handler respektieren `disabled` (no-op).
- KEIN Wire-Knowledge, KEIN Inline-Edit-Hook (kommt in Welle 1.2-B).
- `portal/src/graph/foundation/matrix/index.ts` (Task-2-Variante) erweitert
  um `MatrixCell` + `MatrixCellProps`.

**Specs (6/6 grün):**

| # | Beschreibung |
|---|--------------|
| 1 | Default-Render — children erscheinen, keine State-Marker im DOM |
| 2 | selected=true → data-Attribut + border-primary + bg-[var(--color-surface-soft-cyan)] |
| 3 | editing=true → data-Attribut + ring-2 ring-primary |
| 4 | disabled=true → onClick wird NICHT gefeuert + cursor-not-allowed-Klasse, KEIN cursor-pointer |
| 5 | cellId → data-cell-id; oid → data-oid (nur wenn gesetzt) |
| 6 | React.memo: Re-Render mit identischen Props ruft ChildComp.render nicht erneut auf (Render-Counter via stable-child-element-Pattern) |

**Verifikation:**

- Vitest: 6/6 für MatrixCell + 9/9 für MatrixGrid + full suite 33 Files /
  198 passed / 2 skipped (vorher 183 passed — exakt +15 neu)
- tsc: 0 Errors für matrix/
- ESLint: 0 Errors für matrix/
- `grep '#[0-9A-F]{6}' portal/src/graph/foundation/matrix/`: 0 Treffer

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] ESLint no-unused-vars für `_disabled` und `_tick`**

- **Found during:** Erste Validierung nach Task-1- + Task-2-Implementation
- **Issue:** ESLint hat trotz Underscore-Prefix zwei Warnings gemeldet:
  - `MatrixGrid.tsx:143:15 — '_disabled' is assigned a value but never used`
  - `MatrixCell.spec.tsx:96:58 — '_tick' is defined but never used`
- **Fix:** Beide Inline-Refactorings:
  - In `MatrixGrid.tsx` die `disabled`-Prop aus der Destrukturierung
    entfernt — sie wird in dieser Welle nicht direkt ausgewertet (Welle
    1.2-B reicht sie an `useInlineCellEdit` durch). Die Type-Signatur
    behält `disabled?: boolean`, damit Konsumenten die Prop trotzdem
    setzen können; ein erläuternder Kommentar dokumentiert, warum die
    Variable nicht destrukturiert wird.
  - In `MatrixCell.spec.tsx` den Test-6-Wrapper umgebaut: `tick` wird jetzt
    als `data-tick={tick}` auf einem umschließenden `<div>` konsumiert.
    Damit greift die Prop tatsächlich auf das DOM und ESLint sieht sie als
    benutzt.
- **Files modified:** `portal/src/graph/foundation/matrix/MatrixGrid.tsx`,
  `portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx`
- **Commit:** Beide Korrekturen sind innerhalb der jeweiligen Task-Commits
  (aba4c19 / 2b89008) — keine separaten Fix-Commits, weil die Issues vor
  dem ersten Commit gefangen wurden.

### Architectural Changes

Keine — beide Tasks bewegen sich vollständig im Plan-Scope.

## Authentication Gates

Keine — Welle A ist reine Foundation-Implementation ohne externe Abhängigkeit
(weder Backend-API noch Firebase noch MinIO).

## Known Stubs

Keine — `<MatrixGrid>` und `<MatrixCell>` sind voll funktionsfähige
Foundation-Components. Was in dieser Welle bewusst NICHT enthalten ist
(siehe Plan-Boundary):

- **Inline-Cell-Edit** — kommt in Welle 1.2-B als `useInlineCellEdit`-Hook.
- **Block-Select** — kommt in Welle 1.2-C als `useBlockSelection`-Hook + Pure-Functions.
- **Copy/Paste** — kommt in Welle 1.2-D als `matrix-clipboard.ts`.
- **Virtualisierung** — deferred bis User-Use-Case mit >500 Cells
  (RESEARCH §5.1).

Das sind keine Stubs im engeren Sinne, sondern geplante Folge-Wellen.

## Threat Flags

Keine neuen Threat-Surfaces. Der Plan-Threat-Model-Eintrag T-01.2-04
("renderCell akzeptiert beliebige React-Nodes vom Konsumenten") ist mit
Disposition `accept` belegt — Foundation rendert was Konsument liefert,
XSS-Guard ist Sache der Cell-Renderer (z.B. PRessBelegMatrixViewer in
Welle E nutzt nur Status-Enum + Ressource-Name, keine HTML-Injection).

T-01.2-05 (Performance bei >500 Cells) ist ebenfalls `accept` — Virtualisierung
ist deferred und wird via Konsumenten-Wrapper (TanStack-Virtual) nachgezogen,
wenn ein User-Use-Case auftaucht. Die Foundation legt sich nicht auf eine
Lösung fest.

## Deferred Issues

Die globale `tsc -p tsconfig.app.json --noEmit`-Validierung zeigt 3
pre-existing Errors in nicht-matrix-Dateien — siehe
`.planning/phases/01.2-…/deferred-items.md`. Diese stammen aus Phase 1.1
(Welle G24/G26/G27) und sind nicht durch Welle 1.2-A verursacht. Sie sind
nicht blockierend (Vitest läuft komplett grün) und werden in einer
separaten Cleanup-Welle behandelt.

## Self-Check

- ✅ `portal/src/graph/foundation/matrix/MatrixGrid.tsx` existiert (258 Zeilen, >= 120)
- ✅ `portal/src/graph/foundation/matrix/MatrixCell.tsx` existiert (117 Zeilen, >= 50)
- ✅ `portal/src/graph/foundation/matrix/index.ts` exportiert MatrixGrid, MatrixGridProps, MatrixCell, MatrixCellProps
- ✅ `portal/src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx` mit 9 Specs grün
- ✅ `portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx` mit 6 Specs grün
- ✅ `.planning/phases/01.2-…/deferred-items.md` existiert (out-of-scope pre-existing tsc-Errors dokumentiert)
- ✅ Commit `aba4c19` (Task 1) in `git log` vorhanden
- ✅ Commit `2b89008` (Task 2) in `git log` vorhanden
- ✅ Vitest gesamt: 33 Files / 198 passed / 2 skipped — keine Regression (+15 neu)
- ✅ tsc-clean für matrix/: 0 Errors im neuen Code
- ✅ ESLint-clean für matrix/: 0 Errors
- ✅ `grep -E '#[0-9A-F]{6}' portal/src/graph/foundation/matrix`: 0 Treffer (Token-Disziplin)
- ✅ Sticky-Header-Klassen via CSS-Class-Assertion verifiziert (jsdom kann sticky nicht layouten — RESEARCH §5.3)
- ✅ Branch `worktree-agent-abedfa0adf531dfc7` (kein Drift auf protected branch)
- ✅ STATE.md / ROADMAP.md NICHT modifiziert (Orchestrator-Domäne)

## Self-Check: PASSED
