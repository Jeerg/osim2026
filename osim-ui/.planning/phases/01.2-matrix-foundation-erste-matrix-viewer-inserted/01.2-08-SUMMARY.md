---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 08
subsystem: viewers/ress-verknuepfung-graph
tags: [graph, viewer, ressource-detail, foundation-consumer, reverse-index, wrapper-indirektion, phase-4-prep, kennzahl-slot-placeholder]
requires:
  - .planning/phases/01.2-.../01.2-01-SUMMARY.md (Stub-Cleanup + SCHEMA-MAP für Wrapper-Indirektion)
  - .planning/phases/01.2-.../01.2-06-SUMMARY.md (PRessBeleg-Konsument-Pattern)
  - .planning/phases/01.2-.../01.2-07-SUMMARY.md (PDlplConnKnotenViewer + useSimulationListener-Hook + Wrapper-Helper)
  - portal/src/graph/foundation (OGraphGrid + GObjLink + GLink + GraphFlowCanvas — Phase 1.1)
provides:
  - "PRessVerknuepfungViewer als ressource-zentrierter Detail-Graph-Viewer (1:1 zu C++ PRessVerknuepfungViewer)"
  - "KennzahlSlotPlaceholder als visueller Phase-4-Vorgriff (m_drawKennzahl-Slot ohne Backend-Call)"
  - "Reverse-Index-Builder buildConnectedKnoten für Ressource → PDpKn*-Lookup über Wrapper-Pfad"
  - "Registry-Einträge für PRessBeleg + PBetriebsmittel + PPerson mit hint='verknuepfung'"
  - "01.2-08-CPP-AUDIT.md: dframeFill + dFillKnoten + dFillAssoz + Reverse-Index + Kennzahl-Slot-Felder"
affects:
  - portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx (NEU, 378 LoC)
  - portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx (NEU, 47 LoC)
  - portal/src/viewers/PRessVerknuepfung/index.ts (NEU, 13 LoC)
  - portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx (NEU, 329 LoC, 9 Tests)
  - portal/src/viewers/setup.ts (Registry erweitert um 3 Einträge)
  - .planning/phases/01.2-.../01.2-08-CPP-AUDIT.md (NEU, 258 LoC)
tech-stack:
  added: []
  patterns:
    - "Reverse-Index über allObjects: O(K × A × R) Iteration zur Knoten-Liste pro Ressource"
    - "Wrapper-Indirektion 2-stufig: knoten.m_lAssozRess → PAssozRessourceLList → PAssozBeleg → m_lRessourcen → PRessBelegLList → ressOid"
    - "Zweispaltiges Flex-Layout: TTY-Pane links (w-48 font-mono) + Graph-Pane rechts (relative flex-1)"
    - "Kennzahl-Slot-Placeholder als visueller Phase-4-Vorgriff (opacity-60 + Cyan-Soft-Tokens + ARIA-Disabled-Status)"
    - "Cross-Modul-Import von useSimulationListener aus PDlplConnKnoten (single source of truth, akzeptiert per Threat T-01.2-22)"
    - "ReactFlowProvider-Lift im Outer-Component (1:1 aus Welle G21 / 1.2-F)"
    - "Ressource zentral bei (knotenCount/2, 1) mit SetMoveForbidden + SetDeleteForbidden — Anker des Detail-Graphs"
    - "Pro Verknüpfung 2 Links: Knoten→Assoz + Assoz→Ressource (1:1 C++ dFillAssoz Z.207-215)"
key-files:
  created:
    - portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx
    - portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx
    - portal/src/viewers/PRessVerknuepfung/index.ts
    - portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md
  modified:
    - portal/src/viewers/setup.ts (Registry-Eintrag PRessBeleg + PBetriebsmittel + PPerson mit hint='verknuepfung')
decisions:
  - "KEINE erfundene Wire-Klass PRessVerknuepfung mehr — der Viewer liest existierende OSim2004-Strukturen rückwärts (Reverse-Index über allObjects). Damit erfüllt diese Welle die Korrektur des Phase-1-Fehlers, der in Plan 01.2-01 dokumentiert wurde."
  - "Wrapper-Indirektion 1:1 aus SCHEMA-MAP.md übernommen — der Reverse-Index iteriert in 3 Stufen (Knoten → PAssozRessourceLList → PAssozBeleg → PRessBelegLList → ressOid)."
  - "KennzahlSlotPlaceholder ist visueller Phase-4-Vorgriff ohne Backend-Call. Phase 4 ersetzt den Placeholder durch eine aktive Live-Visualisierung (m_drawKennzahl + WebSocket-Tick), ohne Component-Restruktur."
  - "useSimulationListener-Hook wird aus Welle 1.2-F (PDlplConnKnoten) re-used — kein zweiter Hook-Pfad. Cross-Modul-Import akzeptiert per Threat T-01.2-22 (refactor in Phase 4 falls Listener-Foundation eigenen Modul-Pfad bekommt)."
  - "PAssozBetrPers + Speicher-Assoz-Pfad NICHT in Welle 1.2-G — Backlog Welle 1.3/Phase 4. Der C++-Code (Z.159-173, Z.177-193) behandelt diese, hier reduzieren wir auf reinen PAssozBeleg-Pfad — die Architektur (Reverse-Index + Grid-Builder) ist erweiterbar."
  - "PRessMenge-Branch mit Lagertyp-Sortierung (Verbrauch / Abfrage / Erzeugung) NICHT in Welle 1.2-G — Mengen-Verknüpfungen sind Backlog Welle 1.3 (Mengen-Matrix-Viewer-Familie)."
  - "Layout (col, row) als logische Hints; finalizeLayout berechnet die echten Pixel-Positionen — Reihen-Konvention: Reihe 0 Knoten / Reihe 1 Ressource / Reihe 2 Assoz."
  - "Ressource bekommt SetMoveForbidden + SetDeleteForbidden (Anker des Detail-Graphs); Knoten und Assoz bleiben grundsätzlich mutierbar — aber Phase 1.2 ist Read-Only (disabled default true), daher kein Pfad zu echter Mutation."
metrics:
  duration_minutes: 15
  completed_date: "2026-05-25"
  tasks_completed: 2
  tasks_total: 2
  status: "complete"
---

# Phase 01.2 Plan 08: PRessVerknuepfungViewer (Welle 1.2-G) Summary

Ressource-zentrierter Detail-Graph-Viewer mit Reverse-Index +
Kennzahl-Slot-Placeholder (Phase-4-Vorgriff) auf der GraphObject-
Foundation aus Phase 1.1. Drei Commits: C++-Audit, RED-Spec,
GREEN-Implementation/Registry. 9 Vitest-Specs grün im **ersten
Durchlauf** — keine GREEN-Iteration nötig. Gesamte Suite 40 Files / 272
passed / 2 skipped (gegen 39/263 vor der Welle).

Diese Welle korrigiert den **Phase-1-Fehler** der erfundenen Wire-Klass
`PRessVerknuepfung` mit `m_iAnteil` (gelöscht in Plan 01.2-01) durch
einen **echten Reverse-Index** über existierende OSim2004-Strukturen.

## Completed Tasks

### Task 1: C++-Audit `PRessVerknuepfungViewer`

**Commit:** `41d3c37`

`.planning/phases/01.2-.../01.2-08-CPP-AUDIT.md` (258 LoC) mit vier
Sektionen:

1. **C++-Audit `dframeFill` + `dFillKnoten` (Z.337-362, Z.69-111):**
   10-Schritt-Pipeline-Tabelle für den äußeren Build-Loop +
   7-Schritt-Tabelle für die `dFillKnoten`-Iteration. Kernerkenntnis
   aus `dFillKnoten` Z.106: flaches lineares Layout mit `laufx++`,
   kein dynamisches Re-Positioning. Zentrale **Ressource** bei
   `(knotenCount/2, 0)` (siehe `dFillResourcenGitter` Z.582) — anders
   als der knoten-zentrierte 1.2-F-Viewer bei `(3, 0)`. Pro Assoz
   2 Links Knoten→Assoz + Assoz→Ress (C++ Z.207-215).
2. **Reverse-Index `GetListeKonnektierterKnoten`:** Algorithmus-
   Pseudocode + Komplexitäts-Analyse (O(K × A × R)). Engine-Reflection-
   Pfad ist nicht in Phase-1.2-Scope; daher Frontend-Iteration über
   `allObjects`. Drei-stufige Wrapper-Indirektion sauber dokumentiert
   (PAssozRessourceLList → PAssozBeleg → PRessBelegLList).
3. **Kennzahl-Slot-Felder (Phase 4):** Aus `PGObjBaseViewer.h` Z.157-160
   inventarisiert: `m_drawKennzahl`, `m_iKnz`, `m_dScaleKnz`, `m_dMax`,
   plus `DrawKennzahl(pDC)`-Methode. Phase-4-Aktivierung über
   `OListenerSimulator::XM_GFX_EVENT`; Phase-1.2-Umsetzung als
   visueller Placeholder mit Cyan-Soft-Surface + Phase-4-Label + ARIA-
   Disabled-Status.
4. **TS-Foundation-Reuse:** Vergleichstabelle 1.2-F vs. 1.2-G —
   gleiche Foundation (OGraphGrid + GObjLink + GLink + GraphFlowCanvas),
   anderes Layout + neue Reverse-Index-Logik + TTY-Pane + Kennzahl-Slot.
   Helper-Re-use (readListViaWrapper, labelFor, useSimulationListener)
   einzeln aufgelistet.

### Task 2: KennzahlSlotPlaceholder + PRessVerknuepfungViewer + Spec + Registry

**Commits:** `394340d` (RED), `bd43550` (GREEN)

**RED-Phase** (`portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx`,
329 LoC, 9 Tests):

KennzahlSlotPlaceholder-Tests (isoliert getestet):

- **T1** Slot rendert `div` mit `data-slot="kennzahl-placeholder"`.
- **T2** Text enthält "Phase 4".
- **T3** className enthält `opacity-60` UND
  `bg-[var(--color-surface-soft-cyan)]` (Token-basiert, keine Hex-Strings).
- **T4** `aria-label` beschreibt explizit "Kennzahl … Phase 4" für
  Screen-Reader-Disambiguation.

Viewer-Tests (mit Wrapper-Indirektions-Fixtures 1:1 aus SCHEMA-MAP.md):

- **T5** Lone-Ressource (kein verknüpfter Knoten) → ReactFlow-Mock zeigt
  1 Node (nur Ressource selbst), TTY-Pane hat 0 `<li data-oid>`-Einträge.
- **T6** Ressource + 2 verbundene Knoten → TTY-Pane zeigt 2 `<li>` mit
  korrekten `data-oid`-Werten (200, 201, sortiert).
- **T7** Graph-Mock zeigt > 1 Node (Ressource + Knoten + Assoz) und
  Edges > 0 (Verknüpfungs-Links existieren).
- **T8** Kennzahl-Placeholder im Viewer-DOM auffindbar via
  `[data-slot="kennzahl-placeholder"]`.
- **T9** `data-viewer="PRessVerknuepfungViewer"` auf Root.

xyflow-Mock 1:1 aus `PDlplConnKnotenViewer.spec.tsx` (Welle 1.2-F).
RED-Trigger sauber: `vitest` meldet `Failed to resolve import
"@/viewers/PRessVerknuepfung/PRessVerknuepfungViewer"`.

**GREEN-Phase** (4 neue Files + setup.ts-Erweiterung):

- **`PRessVerknuepfungViewer.tsx`** (378 LoC):

  - **Public Component:** `PRessVerknuepfungViewer` (Wraps in
    `ReactFlowProvider`, 1:1 Welle-G21-Lift-Pattern).
  - **Inner Component:** `PRessVerknuepfungViewerInner` mit
    `useMemo`-build-Grid + `useSimulationListener({})`-Mount (no-op
    Phase-4-Vorbereitung).
  - **`buildConnectedKnoten(ressOid, allObjects)`:** Reverse-Index
    über alle `PDpKn*`-Objekte. Drei-stufige Wrapper-Iteration
    (knoten.m_lAssozRess → PAssozRessourceLList → PAssozBeleg →
    PRessBelegLList → ressOid). Result deterministisch sortiert nach
    oid. Komplexität O(K × A × R).
  - **`findAssozBelegBetween(knoten, ressOid, allObjects)`:** liefert
    pro Knoten die passenden PAssozBeleg-Zwischenobjekte für den
    Grid-Build.
  - **`buildRessVerknuepfungGrid(ress, knoten, allObjects)`:**
    Pipeline-Builder 1:1 zu C++ `dframeFill`:
    1. Ressource zentral bei `(knotenCount/2, 1)` mit
       SetMoveForbidden + SetDeleteForbidden.
    2. Pro Knoten in Reihe 0 (`laufx++`).
    3. Pro PAssozBeleg in Reihe 2 + 2 Links (Knoten→Assoz, Assoz→Ress).
    4. `grid.finalizeLayout({x:0, y:0})` für Pixel-Positionen.
  - **`readListViaWrapper` + `labelFor`:** Helper aus Welle 1.2-F
    re-used (kopiert; Lift in shared utils ist Backlog Welle 1.3
    siehe Threat T-01.2-22).
  - **Layout (1:1 C++ Z.337-362):**
    ```
    <ChildDialog title="Ressourcen-Verknüpfungen — <name>" description="Verknüpft mit N Knoten">
      <div data-viewer="PRessVerknuepfungViewer" class="flex h-full">
        <aside data-testid="tty-pane" class="w-48 ...">
          <ul>
            {connectedKnoten.map((k) => <li key={k.oid} data-oid={k.oid}>{label}</li>)}
          </ul>
        </aside>
        <div class="relative flex-1">
          <GraphFlowCanvas grid={grid} revision={revision} readOnly={disabled ?? true} />
          <KennzahlSlotPlaceholder />  ← absolute right-2 top-2
        </div>
      </div>
    </ChildDialog>
    ```

- **`KennzahlSlotPlaceholder.tsx`** (47 LoC): Reiner visueller
  Phase-4-Vorgriff. Token-basierte Styling (kein Hex), opacity-60 +
  Cyan-Soft-Background + ARIA-Disabled-Label. KEIN Backend-Call,
  KEIN useEffect, KEINE Logik — Phase 4 ersetzt die Component durch
  eine aktive Live-Visualisierung.

- **`index.ts`** (13 LoC): Barrel-Export von `PRessVerknuepfungViewer`
  + `KennzahlSlotPlaceholder` mit Querverweisen auf das CPP-AUDIT
  und SCHEMA-MAP.

- **`setup.ts`** Erweiterung: Import + 3 Registry-Einträge mit
  `hint: "verknuepfung"`:
  - `PRessBeleg` (Belegungs-Ressource)
  - `PBetriebsmittel` (Maschine)
  - `PPerson` (Personalkraft)

  Plus Doku-Block am Eintrag, der die Wrapper-Indirektion + den
  Phase-4-Kennzahl-Plan beschreibt und auf das CPP-AUDIT verweist.

**Validierung:**

| Gate                       | Wert                                            |
| -------------------------- | ----------------------------------------------- |
| Spec PRessVerknuepfung     | 9/9 grün (1.95 s) — kein GREEN-Iterate          |
| `tsc --noEmit` Exit-Code   | `0` (keine neuen Errors)                        |
| Hex-Check in PRessVerknuepfung/ | `0` Treffer (ESLint-Guardrail)             |
| Registry-Treffer           | `7` (3× Component-Binding + 4× Doku-Verweise)   |
| Gesamte Vitest-Suite       | 40 Files / 272 passed / 2 skipped               |
| Delta zur Vor-Welle (07)   | +1 File, +9 Tests, 0 Regressionen               |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] `node_modules` im Worktree per Junction**

- **Found during:** Task 2 RED-Phase, vor erstem `vitest run`.
- **Issue:** Frischer Worktree (`.claude/worktrees/agent-…`) hat ohne
  `npm install` keine `node_modules` — `vitest.config.ts` schlägt fehl
  beim Laden von `@vitejs/plugin-react`.
- **Fix:** Junction angelegt via
  `cmd //c "mklink /J node_modules ..\..\..\..\portal\node_modules"`
  — keine Admin-Rechte nötig (Win11-Default für Verzeichnisse).
  `portal/node_modules` ist via `.gitignore` ausgeschlossen.
- **Files modified:** keine getrackten.
- **Commit:** kein dedizierter Commit — Junction ist ein Filesystem-
  Konstrukt außerhalb des Repos.

**2. [Rule 2 - Critical Functionality] Hex-Strings aus Doku-Kommentaren entfernt**

- **Found during:** GREEN-Validierung (`grep -rE "#[0-9A-F]{6}" portal/src/viewers/PRessVerknuepfung/`).
- **Issue:** Initial enthielt der JSDoc-Block von
  `KennzahlSlotPlaceholder.tsx` zwei Hex-Strings als Doku-Hinweise
  (`#ECFEFF`, `#155E75`). Der Plan-Done-Gate (`grep ... = 0 Treffer`)
  schließt auch Kommentare ein — das ist die korrekte 3FLS-EAM-Lesart
  (ESLint-Guardrail durchsetzbar, keine Ausnahmen).
- **Fix:** Doku-Kommentare auf semantische Token-Namen umgestellt
  ("Cyan-Soft-Surface" / "Cyan-Dark") ohne Hex-Werte.
- **Files modified:** `portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx`.
- **Commit:** im GREEN-Commit `bd43550` enthalten.

### Architectural Changes

Keine. Der Viewer hält sich strikt an die Foundation-Konsumenten-
Architektur aus Welle G + Welle 1.2-E/F.

### Plan-Text-Korrekturen

**Plan-Action-Block Z.219 schlug eine eigene Helper-Function in einem
gemeinsamen `viewers/PDlpl/utils.ts`-Modul vor.** Antwort: NICHT
implementiert. Stattdessen folgen wir der Plan-Konvention aus Welle
1.2-F (Threat T-01.2-22) und kopieren `readListViaWrapper` + `labelFor`
1:1 in den neuen Viewer (Duplikation in 2 Konsumenten ist akzeptiert,
ein Lift in shared utils ist Backlog Welle 1.3 falls dritter Konsument
kommt). Das hält die Wellen klein und reduziert Cross-Modul-Imports.

## Authentication Gates

Keine — kein Backend-Touch. Reine UI-/Spec-Welle auf der Foundation.

## Known Stubs

- **`KennzahlSlotPlaceholder` ist intentional visueller Phase-4-
  Vorgriff** — kein Backend-Call, kein useEffect, keine Logik. Die
  Component wird in Phase 4 durch eine aktive Live-Visualisierung
  ersetzt (m_drawKennzahl + m_iKnz aus WebSocket-Tick). Tests T1-T4
  verifizieren das visuelle Verhalten + ARIA-Label; Test T8 verifiziert
  die Einbettung im Viewer. Dokumentiert im JSDoc-Block und im
  CPP-Audit §3.

- **`useSimulationListener` ist intentional no-op** — aus Welle 1.2-F
  re-used. Phase 4 hängt hier den WebSocket-Subscriber an. Dokumentiert
  in Welle 1.2-F-Audit §3.

- **PAssozBetrPers-Pfad NICHT portiert** — der C++-Code (Z.159-173)
  prüft pro `betrper`-Eintrag ob Person/Betriebsmittel-ID passt. In
  Phase 1.2 reduzieren wir auf reinen PAssozBeleg-Pfad. Wenn ein
  PBetrPers-Modell verwendet wird, sind die Verknüpfungen über
  PAssozBetrPers temporär unsichtbar — Backlog Phase 4.

- **Speicher-Assoz-Pfad (m_lAssozSpeich) NICHT portiert** — der C++-
  Code (Z.177-193) zeichnet Ressourcen, die in einem SpeicherProz
  aktiv sind. In Phase 1.2 deferred — Backlog Welle 1.3.

- **PRessMenge-Branch mit Lagertyp-Sortierung NICHT portiert** —
  C++ Z.227-302 behandelt PRessMenge-Verknüpfungen mit
  Verbrauch/Abfrage/Erzeugung-Sortierung. Phase-1.2-Scope ist
  PRessBeleg/PAssozBeleg only. Backlog Welle 1.3 (Mengen-Matrix-
  Viewer-Familie).

- **m_pntRaster-Persistenz** beim Auto-Placement NICHT zurückgeschrieben
  (analog Welle 1.2-F). Phase 1.2 ist Read-Only.

Diese Stubs sind Phase-1.2-Scope-Cuts laut CONTEXT.md und im Plan
explizit ausgeschlossen.

## Threat Flags

Keine neuen Threat-Surfaces. Die im Plan dokumentierten Threats sind
mitigiert:

- **T-01.2-20 (DoS via O(K × A × R) Reverse-Index bei Bosch-Modell mit
  30k+ Objekten):** accept — Real-Modelle haben < 500 Verknüpfungen
  (verifiziert in Welle 1.2-01 Sondierung). Performance-Stress-Test
  als Phase-4-Backlog dokumentiert.

- **T-01.2-21 (UX-Bug, Kennzahl-Placeholder verwirrt User):** mitigiert
  via opacity-60 + "Phase 4"-Label + `aria-label="Kennzahl-Anzeige
  (Phase 4 — derzeit deaktiviert)"` + `title`-Tooltip mit gleichem
  Inhalt. Tests T2-T4 verifizieren alle drei Disambiguations-Quellen
  (visuell, screen-reader, tooltip).

- **T-01.2-22 (Cross-Modul-Import von useSimulationListener):** accept
  — single source of truth statt Duplikation. Refactor in eigenes
  `viewers/PDlpl/utils.ts`-Modul ist Backlog falls dritter Konsument
  auftaucht.

## Self-Check

- `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md`: existiert (258 LoC, 4 Sektionen).
- `portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx`: existiert (378 LoC, ≥140 fordert Plan).
- `portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx`: existiert (47 LoC, ≥20 fordert Plan).
- `portal/src/viewers/PRessVerknuepfung/index.ts`: existiert (13 LoC, Barrel-Export).
- `portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx`: existiert (329 LoC, 9/9 Tests grün).
- `portal/src/viewers/setup.ts`: 3 `Component: PRessVerknuepfungViewer`-Bindings (PRessBeleg + PBetriebsmittel + PPerson mit hint='verknuepfung').
- Commits: `41d3c37` (Audit), `394340d` (RED), `bd43550` (GREEN) im git log.
- `tsc --noEmit`: Exit-Code 0 (Baseline-Image aus Welle 1.2-07 identisch).
- Vitest: 40 Files / 272 passed / 2 skipped — keine Regressionen, +9 Tests gegenüber 39/263 vor der Welle.
- Hex-Check `grep -rE "#[0-9A-F]{6}" portal/src/viewers/PRessVerknuepfung/`: 0 Treffer.

## Self-Check: PASSED
