---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 02
type: execute
wave: 2
depends_on: [01.2-01]
files_modified:
  - portal/src/graph/foundation/matrix/MatrixGrid.tsx
  - portal/src/graph/foundation/matrix/MatrixCell.tsx
  - portal/src/graph/foundation/matrix/index.ts
  - portal/src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx
  - portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-9]
must_haves:
  truths:
    - "MatrixGrid rendert eine 2D-Tabelle mit Sticky-Col-Header (oben) + Sticky-Row-Header (links) + Corner-Cell"
    - "renderCell-Prop wird pro (row, col) genau einmal aufgerufen pro Render-Pass"
    - "Re-Build triggert nur bei rows/cols/revision-Wechsel (useMemo-Pattern wie GraphFlowCanvas)"
    - "Empty-State zeigt zentrierten Hint bei rows.length===0 oder cols.length===0"
    - "data-matrix-grid + data-matrix-header + data-matrix-cell-Attribute auf E2E-tauglichen DOM-Elementen"
    - "MatrixCell ist memoized (React.memo) und respektiert selected/editing/disabled-Props via data-Attributes + 3FLS-EAM-Tokens"
    - "Mindestens 6 Vitest-Specs grün: 3 für MatrixGrid (empty, sticky-CSS, renderCell-Aufrufe) + 3 für MatrixCell (memo, selected-Class, disabled-Skip)"
  artifacts:
    - path: portal/src/graph/foundation/matrix/MatrixGrid.tsx
      provides: "Generisches Matrix-Component mit renderCell-Slot + Sticky-Headers"
      min_lines: 120
    - path: portal/src/graph/foundation/matrix/MatrixCell.tsx
      provides: "Memoized Cell-Renderer-Wrapper mit Selection/Edit/Disabled-State"
      min_lines: 50
    - path: portal/src/graph/foundation/matrix/index.ts
      provides: "Barrel-Export für Foundation-Konsumenten"
      exports: ["MatrixGrid", "MatrixCell", "MatrixGridProps", "MatrixCellProps"]
    - path: portal/src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx
      provides: "Foundation-Specs MatrixGrid"
      contains: "describe(\"MatrixGrid"
    - path: portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx
      provides: "Foundation-Specs MatrixCell"
      contains: "describe(\"MatrixCell"
  key_links:
    - from: portal/src/graph/foundation/matrix/MatrixGrid.tsx
      to: portal/src/components/ui/table.tsx
      via: "shadcn Table-Komponente"
      pattern: "from \"@/components/ui/table\""
    - from: portal/src/graph/foundation/matrix/MatrixCell.tsx
      to: portal/src/styles/tokens.css
      via: "3FLS-EAM-Tokens (--color-primary, --color-surface-soft-cyan)"
      pattern: "border-primary|bg-\\[var\\(--color-surface-soft-cyan\\)\\]|bg-card"
---

<objective>
Foundation-Welle 1.2-A: Matrix-Cell-Rendering inkl. Sticky-Headers (Excel-Pattern) als renderer-agnostisches React-Component.

**Purpose:**
Die bestehende GraphObject-Foundation ist für freie 2D-Graphen ausgelegt; Matrix-Viewer brauchen eine eigene Foundation, die parallel läuft. Diese Welle liefert die kernige Render-Mechanik: `<MatrixGrid>` mit `renderCell`-Prop (CONTEXT D-2: Component-Slot statt Canvas-Callback), Sticky-Headers via CSS `position: sticky` (CONTEXT D-3), und `<MatrixCell>` als memoized Wrapper für Cells. Konsumenten (Welle E) liefern Custom-Renderer pro Cell.

**Output:**
- `portal/src/graph/foundation/matrix/MatrixGrid.tsx` — Hauptkomponente
- `portal/src/graph/foundation/matrix/MatrixCell.tsx` — Memoized Cell-Wrapper
- `portal/src/graph/foundation/matrix/index.ts` — Barrel-Export
- ≥6 Vitest-Specs in `__tests__/`
- 3FLS-EAM-Tokens für alle visuellen Elemente
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-CONTEXT.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-01-SUMMARY.md
@docs/3FLS-EAM-STYLE-GUIDE.md
@portal/src/components/ui/table.tsx
@portal/src/viewers/PRess/matrix-common.tsx
@portal/src/graph/foundation/GraphFlowCanvas.tsx
@portal/src/graph/foundation/OsimNode.tsx
@portal/src/styles/tokens.css

<interfaces>
<!-- Aus PATTERNS.md §1+§2 abgeleitet. Executor implementiert gegen diese Signaturen. -->

```typescript
// portal/src/graph/foundation/matrix/MatrixGrid.tsx
export interface MatrixGridProps<TRow, TCol, TVal> {
  rows: TRow[];
  cols: TCol[];
  cellLookup: (row: TRow, col: TCol) => TVal | null;
  renderCell: (row: TRow, col: TCol, value: TVal | null) => React.ReactNode;
  renderRowHeader?: (row: TRow) => React.ReactNode;
  renderColHeader?: (col: TCol) => React.ReactNode;
  rowKey: (row: TRow) => string;
  colKey: (col: TCol) => string;
  /** Inkrementiert bei Wire-Mutation — triggert Re-Compute des cellLookup. */
  revision?: number;
  disabled?: boolean;
  emptyMessage?: string;
  cornerLabel?: string;  // Default "DlplKnoten / Bel.ress." (1:1 OSim2004 OnDrawFrame)
  className?: string;
}

export function MatrixGrid<TRow, TCol, TVal>(props: MatrixGridProps<TRow, TCol, TVal>): React.ReactElement;
```

```typescript
// portal/src/graph/foundation/matrix/MatrixCell.tsx
export interface MatrixCellProps {
  cellId: string;            // ${rowKey}:${colKey}
  oid?: number;              // Wire-OID falls vorhanden (für data-oid)
  selected?: boolean;
  editing?: boolean;
  disabled?: boolean;
  onClick?: (e: React.MouseEvent) => void;
  onPointerDown?: (e: React.PointerEvent) => void;
  className?: string;
  children?: React.ReactNode;
}

export const MatrixCell: React.MemoExoticComponent<React.FC<MatrixCellProps>>;
```

```typescript
// portal/src/graph/foundation/matrix/index.ts
export { MatrixGrid } from "./MatrixGrid";
export type { MatrixGridProps } from "./MatrixGrid";
export { MatrixCell } from "./MatrixCell";
export type { MatrixCellProps } from "./MatrixCell";
```

Tokens (aus PATTERNS S6):
- `--color-primary` (#0EA5C7) Cyan-Primary
- `--color-surface-soft-cyan` (#ECFEFF)
- `--color-border` (#DDE2EA)
- `--color-border-light` (#EAECF0)
- Tailwind-Aliase: `bg-card`, `bg-muted`, `border-border`, `text-primary`, `font-mono`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MatrixGrid-Component + Specs</name>
  <files>portal/src/graph/foundation/matrix/MatrixGrid.tsx, portal/src/graph/foundation/matrix/index.ts, portal/src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx</files>
  <read_first>
    - portal/src/viewers/PRess/matrix-common.tsx (Sticky-Header-Pattern Z.251-263, Empty-State Z.232-244, MatrixTable-Body Z.265-317)
    - portal/src/graph/foundation/GraphFlowCanvas.tsx (Z.38-96: GraphFlowCanvasProps + useMemo-Revision-Pattern, exhaustive-deps-disable-Pattern)
    - portal/src/components/ui/table.tsx (shadcn Table-Komponente — Table/TableHeader/TableHead/TableBody/TableRow/TableCell)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md (§1 MatrixGrid)
    - C:/Users/JörgWFischer/PycharmProjects/OSim2004/OSimV01(Fj)/OSimPro/PRessBelegMatrixViewer.cpp (OnDrawFrame Z.406-520 — Header-Layout: Col-Header oben + Row-Header links + Corner-Cell mit "DlplKnoten / Bel.ress."-Label)
    - docs/3FLS-EAM-STYLE-GUIDE.md (Token-Verwendung)
  </read_first>
  <behavior>
    - Test 1: Empty rows → rendert zentrierten emptyMessage statt Tabelle, `data-slot="matrix-empty"` Selektor vorhanden.
    - Test 2: Empty cols (rows non-empty) → rendert ebenfalls emptyMessage.
    - Test 3: rows=[r1,r2], cols=[c1,c2] → renderCell wird genau 4× aufgerufen mit allen 4 (row, col)-Kombinationen.
    - Test 4: cellLookup wird genau 4× aufgerufen mit allen 4 (row, col)-Kombinationen + Wert wird an renderCell durchgereicht.
    - Test 5: Sticky-CSS-Class — Col-Header-Row hat Klasse `sticky` UND `top-0`; Row-Header-Cell hat Klasse `sticky` UND `left-0`.
    - Test 6: Corner-Cell trägt `data-matrix-header="corner"` und Default-Label "DlplKnoten / Bel.ress." (überschreibbar via cornerLabel-Prop).
    - Test 7: revision-Wechsel triggert cellLookup-Re-Aufruf (initial 4 Calls + nach revision++ wieder 4 Calls = total 8).
    - Test 8: rowKey/colKey werden für React-Keys verwendet — keine "missing key"-Warning bei Vitest-Console.
    - Test 9: data-matrix-grid + data-matrix-header="col"/"row"/"corner" + data-matrix-cell="${rowKey}:${colKey}" sind im DOM vorhanden.
  </behavior>
  <action>
    1. Datei `portal/src/graph/foundation/matrix/MatrixGrid.tsx` anlegen. Imports analog `matrix-common.tsx` Z.31-41 (`Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow` aus `@/components/ui/table`) + `cn` aus `@/lib/utils`.
    2. Component-Signatur `MatrixGrid<TRow, TCol, TVal>(props: MatrixGridProps<TRow, TCol, TVal>)` mit allen Props aus dem `<interfaces>`-Block. Generisch über drei Type-Params.
    3. Empty-State: WENN `rows.length === 0` ODER `cols.length === 0`: rendere `<div data-slot="matrix-empty" data-matrix-grid="..." className="p-8 text-center text-sm text-muted-foreground">{emptyMessage ?? "Keine Daten"}</div>`.
    4. `cellMap` via `React.useMemo`: für jede (row, col)-Kombi `cellLookup(row, col)` aufrufen und in `Map<string, TVal | null>` mit Key `${rowKey(row)}:${colKey(col)}` cachen. Deps: `[rows, cols, revision]` + `eslint-disable-next-line react-hooks/exhaustive-deps`-Kommentar (Begründung: explizite Re-Build-Quelle wie in GraphFlowCanvas Z.92-96).
    5. Body-Render: outer `<Table className="border-separate border-spacing-0" data-matrix-grid="...">` (className optional aus Props). Header-Row mit `sticky top-0 z-10 bg-background`. Corner-Cell als erstes `<TableHead>` mit `sticky left-0 z-20 border-b border-r bg-muted` + `<span className="font-mono text-[10px] text-muted-foreground">{cornerLabel ?? "DlplKnoten / Bel.ress."}</span>` + `data-matrix-header="corner"`. Danach pro Spalte ein `<TableHead data-matrix-header="col" className="border-b bg-muted font-mono text-xs">{renderColHeader?.(c) ?? (c as any).attrs?.m_sName ?? colKey(c)}</TableHead>`.
    6. Body: pro Zeile `<TableRow>` + erste Cell als `<TableCell data-matrix-header="row" className="sticky left-0 z-10 border-r bg-muted font-mono text-xs">{renderRowHeader?.(r) ?? (r as any).attrs?.m_sName ?? rowKey(r)}</TableCell>` + danach pro Spalte `<TableCell data-matrix-cell="${rk}:${ck}" className="border-b border-r p-0">{renderCell(r, c, cellMap.get(...))}</TableCell>`.
    7. KEINE Inline-Hex-Strings — ausschließlich Token-Klassen.
    8. Barrel-Export `portal/src/graph/foundation/matrix/index.ts` mit `export { MatrixGrid }` + `export type { MatrixGridProps }` erstellen.
    9. Spec-Datei `portal/src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx` mit allen 9 Tests aus `<behavior>`. Test-Setup analog `portal/src/graph/foundation/__tests__/GridBackground.spec.tsx`: `import { render } from "@testing-library/react"; import { describe, it, expect, vi } from "vitest";`. Für Sticky-Test (Test 5): Layout NICHT messen, sondern `expect(header.className).toMatch(/sticky/)` (PATTERNS S5 + RESEARCH §5.3: jsdom kann sticky nicht layouten).
    10. KEINE TanStack-Virtual-Integration (RESEARCH §5.1: deferred bis User-Use-Case mit >500 Cells).
  </action>
  <verify>
    <automated>cd portal && npm test -- --run src/graph/foundation/matrix/__tests__/MatrixGrid.spec.tsx 2>&1 | tail -25 | grep -E "(✓|passed|Tests)"</automated>
  </verify>
  <done>9 Specs grün; MatrixGrid.tsx ≥120 Zeilen; index.ts exportiert MatrixGrid + MatrixGridProps; `grep -E "#[0-9A-F]{6}" portal/src/graph/foundation/matrix/MatrixGrid.tsx` = 0 Treffer (keine ad-hoc Hex-Strings).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MatrixCell-Component + Specs + Barrel-Export-Update</name>
  <files>portal/src/graph/foundation/matrix/MatrixCell.tsx, portal/src/graph/foundation/matrix/index.ts, portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx</files>
  <read_first>
    - portal/src/graph/foundation/OsimNode.tsx (Memoization-Pattern + 3FLS-Tokens — `React.memo`-Wrap, `data-oid`-Attribut, pointer-events-Disziplin Welle G9)
    - portal/src/graph/foundation/matrix/MatrixGrid.tsx (Task 1 — Cell-Rendering-Wrapping-Kontext)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md (§2 MatrixCell)
    - portal/src/styles/tokens.css (Token-Definitionen für ESLint-Token-Check)
  </read_first>
  <behavior>
    - Test 1: Default-Render — children werden gerendert, kein selected/editing-Marker im DOM.
    - Test 2: selected=true → `data-matrix-cell-selected="true"` im DOM + `border-primary` UND `bg-[var(--color-surface-soft-cyan)]` Tailwind-Klassen vorhanden.
    - Test 3: editing=true → `data-matrix-cell-editing="true"` im DOM + Ring-Klasse `ring-2 ring-primary` vorhanden.
    - Test 4: disabled=true → onClick wird bei click NICHT aufgerufen, cursor-Klasse zeigt nicht "cursor-pointer".
    - Test 5: cellId-Prop wird als `data-testid="matrix-cell"` und Kontext-Identifikation verwendet; oid (optional) als `data-oid`.
    - Test 6: React.memo-Verhalten — Re-Render mit identischen Props ruft Render-Funktion nicht erneut auf (verifiziert via Render-Counter-Mock).
  </behavior>
  <action>
    1. Datei `portal/src/graph/foundation/matrix/MatrixCell.tsx` anlegen. Imports: `React` und `cn` aus `@/lib/utils` (1:1 OsimNode.tsx Z.16-21).
    2. Innere Function-Component `MatrixCellImpl: React.FC<MatrixCellProps>` definieren, die ein `<div>` rendert mit:
       - `data-testid="matrix-cell"`
       - `data-oid={oid}` (optional, nur wenn gesetzt)
       - `data-matrix-cell-selected={selected ? "true" : undefined}`
       - `data-matrix-cell-editing={editing ? "true" : undefined}`
       - className zusammengesetzt via `cn(...)`:
         - Base: `"flex h-full w-full items-center justify-center px-2 py-1 text-xs border border-transparent transition-colors"`
         - Wenn `!disabled && onClick`: `"cursor-pointer hover:bg-accent"`
         - Wenn `selected`: `"border-primary bg-[var(--color-surface-soft-cyan)]"`
         - Wenn `editing`: `"ring-2 ring-primary ring-offset-0"`
         - Custom className aus Props anhängen.
       - onClick-Handler: wenn `disabled` → no-op, sonst `props.onClick?.(e)`.
       - onPointerDown analog.
    3. Export: `export const MatrixCell = React.memo(MatrixCellImpl);` (1:1 OsimNode.tsx Z.135).
    4. index.ts erweitern: `export { MatrixCell } from "./MatrixCell";` + `export type { MatrixCellProps } from "./MatrixCell";`.
    5. Spec `portal/src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx` mit 6 Tests aus `<behavior>`. Für Test 6 (Memo-Verhalten): `const renderSpy = vi.fn(); <MatrixCell cellId="..." onClick={() => {}}>{(() => { renderSpy(); return null; })()}</MatrixCell>` — und nach re-render assert `renderSpy.mock.calls.length` unverändert (Hinweis: React.memo vergleicht shallow, also stable children-Reference + onClick reference verwenden via useCallback in der Test-Komponente).
    6. KEINE Wire-Klass-Abhängigkeit — MatrixCell ist generisch (CONTEXT D-1: Cells sind Wire-Daten-getrieben, aber MatrixCell selbst ist ein Renderer-Wrapper ohne Wire-Knowledge).
    7. KEIN onChange-Hook — Inline-Edit kommt in Welle 1.2-03 via useInlineCellEdit.
  </action>
  <verify>
    <automated>cd portal && npm test -- --run src/graph/foundation/matrix/__tests__/MatrixCell.spec.tsx 2>&1 | tail -25 | grep -E "(✓|passed|Tests)"</automated>
  </verify>
  <done>6 Specs grün; MatrixCell.tsx ≥50 Zeilen; React.memo-Wrap verifiziert; index.ts exportiert MatrixCell + MatrixCellProps; `grep -E "#[0-9A-F]{6}" portal/src/graph/foundation/matrix/MatrixCell.tsx` = 0 Treffer (keine ad-hoc Hex-Strings).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foundation → Konsument | renderCell-Prop akzeptiert beliebige React-Nodes vom Konsumenten. XSS-Vector wenn Konsument User-Input ungekapselt rendert — aber Verantwortung liegt beim Konsumenten, nicht der Foundation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-04 | Tampering | renderCell-Prop in MatrixGrid | accept | Foundation rendert was Konsument liefert; XSS-Guard ist Sache der Cell-Renderer (z.B. PRessBelegMatrixViewer in Welle E nutzt nur Status-Enum + Ressource-Name, keine HTML-Injection). |
| T-01.2-05 | Information Disclosure | Sticky-Header-Render bei großen Modellen (Bosch 30k+) | accept | Performance-Grenze 500 Cells `[ASSUMED]` (RESEARCH §5.2). Virtualisierung deferred bis User-Use-Case. |
</threat_model>

<verification>
- `cd portal && npm test -- --run src/graph/foundation/matrix/__tests__/` zeigt alle Specs grün (≥6, Ziel: 9 in MatrixGrid + 6 in MatrixCell = 15 — deckt SC-9 zu 100% in dieser Welle ab).
- `cd portal && npx tsc --noEmit src/graph/foundation/matrix/*.tsx` 0 neue Errors.
- `grep -E "#[0-9A-F]{6}" portal/src/graph/foundation/matrix/` 0 Treffer (3FLS-EAM-Token-Disziplin).
- Barrel-Export verifizierbar: `cd portal && node -e "console.log(Object.keys(require('./src/graph/foundation/matrix')))"` zeigt `["MatrixGrid", "MatrixCell"]`.
</verification>

<success_criteria>
- SC-1 (DrawCells-Hook): erfüllt via `renderCell`-Prop.
- SC-2 (Sticky-Headers): erfüllt via `position: sticky`-Tailwind-Klassen.
- SC-9 (Foundation-Tests): 15 von ≥15 Specs in dieser Welle erreicht (Rest verteilt auf Welle 03/04/05).
</success_criteria>

<output>
Create `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-02-SUMMARY.md` when done.
</output>
