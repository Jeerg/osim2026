---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 08
type: execute
wave: 6
depends_on: [01.2-06, 01.2-07]
files_modified:
  - portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx
  - portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx
  - portal/src/viewers/PRessVerknuepfung/index.ts
  - portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx
  - portal/src/viewers/setup.ts
  - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md
autonomous: true
requirements: [SC-8, SC-9]
must_haves:
  truths:
    - "C++-Audit aus PRessVerknuepfungViewer.cpp ist in 01.2-08-CPP-AUDIT.md dokumentiert (dframeFill + dFillKnoten + dFillAssoz + GetListeKonnektierterKnoten)"
    - "Viewer rendert eine Ressource zentral mit allen verknüpften Durchlaufplan-Knoten als Graph"
    - "Reverse-Index buildConnectedKnoten iteriert allObjects, findet PDpKn*-Knoten deren m_lAssozRess-Assoz-Items diese Ressource referenzieren"
    - "Layout: links eine TTY-Pane mit Knoten-Namen als Text-Liste; rechts der Graph via GraphFlowCanvas"
    - "Kennzahl-Slot-Placeholder mit data-slot='kennzahl-placeholder', 'Phase 4'-Label, --color-surface-soft-cyan-Background, opacity-60, primary/30-Border"
    - "Listener-Placeholder useSimulationListener (re-use aus PDlplConnKnoten) wird gemountet aber no-op"
    - "ViewerRegistry-Eintrag (klass='PRessBeleg', hint='verknuepfung') zeigt auf neuen Viewer"
    - "Vitest-Smoke-Spec rendert ohne Crash, prüft Kennzahl-Placeholder-Existenz + Phase-4-Label, verifiziert verbundene Knoten-Anzahl"
  artifacts:
    - path: portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx
      provides: "Ressource-zu-Knoten-Graph-Viewer mit Kennzahl-Slot-Vorgriff"
      min_lines: 140
    - path: portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx
      provides: "Visueller Phase-4-Placeholder mit korrekten Tokens"
      min_lines: 20
    - path: portal/src/viewers/PRessVerknuepfung/index.ts
      provides: "Barrel-Export"
    - path: .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md
      provides: "C++-Audit zu PRessVerknuepfungViewer.cpp"
  key_links:
    - from: portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx
      to: portal/src/graph/foundation/GraphFlowCanvas
      via: "1:1-Reuse Phase-1.1-Foundation für Graph-Pane"
      pattern: "GraphFlowCanvas|ReactFlowProvider"
    - from: portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx
      to: portal/src/viewers/PDlplConnKnoten/PDlplConnKnotenViewer
      via: "useSimulationListener-Reuse"
      pattern: "useSimulationListener"
---

<objective>
Welle 1.2-G: `PRessVerknuepfungViewer` — Graph-Viewer für eine Ressource mit allen verknüpften Durchlaufplan-Knoten + Phase-4-Vorgriff für Kennzahl-Anzeige.

**Purpose:**
Im OSim2004-Original ist `PRessVerknuepfungViewer` KEIN Matrix-Viewer (das war der erfundene Stub aus Welle 01), sondern ein Graph-Viewer: links TTY-Pane mit Knoten-Liste, rechts Graph mit Ressource zentral und Knoten + Assoz-Zwischenobjekten drumherum. Während Sim-Lauf kann `m_drawKennzahl` aktiviert werden — Phase 1.2 baut nur den UI-Slot dafür (Cyan-Disabled-Box), die Verdrahtung kommt in Phase 4.

**Output:**
- `01.2-08-CPP-AUDIT.md` mit dframeFill + dFillAssoz + Reverse-Index-Pipeline
- `portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx`
- `portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx`
- Registry-Update in `setup.ts`
- Smoke-Spec (≥4 Tests inkl. Kennzahl-Placeholder-Existenz)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-CONTEXT.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-SUMMARY.md
@.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-07-SUMMARY.md
@portal/src/viewers/PDlplConnKnoten/PDlplConnKnotenViewer.tsx
@portal/src/graph/foundation/GraphFlowCanvas.tsx
@portal/src/graph/foundation/OGraphGrid.ts
@portal/src/viewers/PDurchlaufplan/PlanToolbar.tsx
@portal/src/viewers/core/ChildDialog.tsx
@portal/src/styles/tokens.css

<interfaces>
```typescript
// portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx
export function PRessVerknuepfungViewer(props: ViewerProps): React.ReactElement;
// props.obj = PRessBeleg (oder PBetriebsmittel/PPerson — alle Ressourcen-Klassen)

// portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx
export function KennzahlSlotPlaceholder(): React.ReactElement;
```

Layout-Konvention (aus C++ PRessVerknuepfungViewer.cpp):
- Top-Level: 2-Spalten Flex-Layout
- Links: `<aside>` mit width 192px (w-48), bg-muted, font-mono text-xs
- Rechts: `<div className="flex-1">` mit GraphFlowCanvas
- Overlay: KennzahlSlotPlaceholder oben rechts (absolute positioning)

Reverse-Index-Logik (RESEARCH §1.3 + PATTERNS §9):
```typescript
function buildConnectedKnoten(ressOid: number, allObjects: Record<number, OBaseObj>): OBaseObj[] {
  const result: OBaseObj[] = [];
  for (const o of Object.values(allObjects)) {
    if (!o.klass.startsWith("PDpKn")) continue;
    const assozOids = (o.attrs.m_lAssozRess as number[] | undefined) ?? o.sub_refs[<SLOT>] ?? [];
    const hasRef = assozOids.some((aOid) => {
      const assoz = allObjects[aOid];
      if (!assoz) return false;
      const ressOids = (assoz.attrs.m_lRessourcen as number[] | undefined) ?? assoz.sub_refs[<SLOT>] ?? [];
      return ressOids.includes(ressOid);
    });
    if (hasRef) result.push(o);
  }
  return result;
}
```

Tokens für KennzahlSlotPlaceholder (PATTERNS §6):
- Border: `border-primary/30`
- Background: `bg-[var(--color-surface-soft-cyan)]`
- Text: `text-primary-dark`
- Opacity: `opacity-60`
- Position: `absolute right-2 top-2` (innerhalb relative-Parent)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: C++-Audit PRessVerknuepfungViewer</name>
  <files>.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md</files>
  <read_first>
    - C:/Users/JörgWFischer/PycharmProjects/OSim2004/OSimV01(Fj)/OSimPro/PRessVerknuepfungViewer.cpp (dframeFill Z.337, dFillKnoten Z.69, dFillAssoz Z.116, GetListeKonnektierterKnoten — Engine-Side)
    - C:/Users/JörgWFischer/PycharmProjects/OSim2004/OSimV01(Fj)/OSimPro/PRessVerknuepfungViewer.h
    - C:/Users/JörgWFischer/PycharmProjects/OSim2004/OSimV01(Fj)/inc/PGObjBaseViewer.h (m_drawKennzahl + m_iKnz + m_dScaleKnz + m_dMax Z.157 — Phase-4-Felder)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-RESEARCH.md (§1.3)
  </read_first>
  <action>
    1. Sektion `## C++-Audit dframeFill + dFillKnoten`:
       - Pipeline-Schritte (analog Welle 07) mit C++-Zeilennummer + Wire-Klassen + Foundation-Methode.
       - Wichtige Erkenntnis aus C++ dFillKnoten Z.69: pro Knoten ein `PDlplKnotenGObj` an Position `(laufx, 0)` mit `laufx++` — flaches Layout, kein dynamisches Re-Positioning.
       - dFillAssoz Z.116: erzeugt zu jedem AssozObj zwei Links: Knoten→Assoz + Assoz→Ress. Für PRessMenge zusätzlich Gruppierung nach Lagertyp (Verbrauch zuerst, dann Abfrage, dann Erzeugung) — in Phase 1.2 nur für PRessBeleg/PAssozBeleg, kein Lagertyp-Branch.
    2. Sektion `## Reverse-Index GetListeKonnektierterKnoten`:
       - C++-Code ruft Engine-Methode auf der Ressource auf. In osim-ui (kein Engine-Reflection in Phase 1.2) bauen wir den Index Frontend-side via Iteration über allObjects.
       - Algorithmus: für jeden PDpKn*-Knoten, iteriere seine m_lAssozRess; für jede Assoz, prüfe ob m_lRessourcen die aktuelle Ressource enthält. Wenn ja → Knoten ist verknüpft.
       - Performance: O(K × A × R) — Knoten × Assoz/Knoten × Ress/Assoz. Bei realistischen Modellen <500.
    3. Sektion `## Kennzahl-Slot-Felder (Phase 4)`:
       - Aus PGObjBaseViewer.h Z.157 listen welche Felder Phase 4 aktiviert: `m_drawKennzahl`, `m_iKnz`, `m_dScaleKnz`, `m_dMax`, `m_drawAchse`.
       - Während Sim-Lauf zeichnet C++ `DrawKennzahl(pDC)` einen Bar oder Numeric-Overlay. In Phase 1.2 zeigen wir nur den Slot-Container.
    4. Sektion `## TS-Foundation-Reuse`:
       - GraphFlowCanvas + OGraphGrid (Phase 1.1)
       - useSimulationListener-Hook aus Welle 07 (re-use, kein zweiter Import-Pfad-Definition)
  </action>
  <verify>
    <automated>test -f .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md && grep -q "dframeFill" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md && grep -q "Reverse-Index" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md && grep -q "Kennzahl-Slot" .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md</automated>
  </verify>
  <done>Audit-Datei existiert mit 4 Sektionen; Reverse-Index-Algorithmus + Phase-4-Felder dokumentiert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KennzahlSlotPlaceholder + PRessVerknuepfungViewer + Spec + Registry</name>
  <files>
    portal/src/viewers/PRessVerknuepfung/KennzahlSlotPlaceholder.tsx,
    portal/src/viewers/PRessVerknuepfung/PRessVerknuepfungViewer.tsx,
    portal/src/viewers/PRessVerknuepfung/index.ts,
    portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx,
    portal/src/viewers/setup.ts
  </files>
  <read_first>
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-CPP-AUDIT.md (aus Task 1)
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-PATTERNS.md (§9 PRessVerknuepfungViewer + KennzahlSlotPlaceholder Code-Block)
    - portal/src/viewers/PDlplConnKnoten/PDlplConnKnotenViewer.tsx (für useSimulationListener-Re-Use + ReactFlowProvider-Lift)
    - portal/src/viewers/PDurchlaufplan/PDurchlaufplanViewerDesign.tsx (Z.62-95)
    - portal/src/graph/foundation/GraphFlowCanvas.tsx
    - portal/src/graph/foundation/OGraphGrid.ts (für buildRessVerknuepfungGrid-Helper)
    - portal/src/viewers/core/ChildDialog.tsx
    - portal/src/viewers/__tests__/PDurchlaufplanViewerDesign.spec.tsx (xyflow-Mock-Pattern PATTERNS S9)
    - portal/src/styles/tokens.css (Token-Verifikation)
  </read_first>
  <behavior>
    KennzahlSlotPlaceholder-Spec (Bestandteil der gleichen Spec-Datei):
    - Test 1: rendert div mit `data-slot="kennzahl-placeholder"`.
    - Test 2: enthält "Phase 4" Text (oder Phase-4-Label-Variante).
    - Test 3: className enthält `opacity-60` UND einen Token-basierten Background (z.B. `bg-[var(--color-surface-soft-cyan)]`).
    - Test 4: ARIA-title-Attribut beschreibt Phase-4-Status für Accessibility.

    Viewer-Spec:
    - Test 5: Render mit Ressource ohne verbundene Knoten → leere Knoten-Liste-Pane + Graph-Mock zeigt nur 1 Node (die Ressource selbst).
    - Test 6: Render mit Ressource + 2 verbundenen Knoten → Knoten-Liste-Pane zeigt 2 Einträge mit data-oid pro Knoten.
    - Test 7: Graph-Mock zeigt mehr als 1 Node (Ressource + min 1 Assoz oder Knoten).
    - Test 8: Kennzahl-Placeholder ist im DOM vorhanden (Selektor `[data-slot="kennzahl-placeholder"]`).
    - Test 9: data-viewer="PRessVerknuepfungViewer" auf Root vorhanden.

    Viewer-Behavior:
    - obj = Ressource (PRessBeleg/PBetriebsmittel/PPerson).
    - buildConnectedKnoten via Reverse-Index.
    - buildRessVerknuepfungGrid baut: Ressource zentral, dann pro Knoten ein Knoten-Node + Assoz-Zwischen-Node + 2 Links.
    - Listener-Hook gemountet (no-op).
  </behavior>
  <action>
    1. **KennzahlSlotPlaceholder.tsx** (1:1 aus PATTERNS §9):
       ```tsx
       export function KennzahlSlotPlaceholder(): React.ReactElement {
         return (
           <div
             className="absolute right-2 top-2 rounded-[var(--radius-sm)] border border-primary/30 bg-[var(--color-surface-soft-cyan)] px-3 py-2 text-xs text-primary-dark opacity-60"
             title="Live-Kennzahl-Anzeige — Phase 4 (Sim-Visualisierung) wird hier aktiviert"
             data-slot="kennzahl-placeholder"
             aria-label="Kennzahl-Anzeige (Phase 4 — derzeit deaktiviert)"
           >
             <span className="font-medium">Kennzahl</span>
             <span className="ml-2 text-[10px] uppercase tracking-wider">Phase 4</span>
           </div>
         );
       }
       ```
       KEINE Hex-Strings — alle Farben über Tokens + Tailwind-Aliase.

    2. **PRessVerknuepfungViewer.tsx**:
       - Outer-Component mit `ReactFlowProvider`-Lift (1:1 aus PDlplConnKnoten Task).
       - Inner-Component `PRessVerknuepfungViewerInner`:
         - Imports: `useSimulationListener` aus `@/viewers/PDlplConnKnoten/PDlplConnKnotenViewer` (re-use, kein zweiter Hook).
         - State: `[revision, setRevision]`, `bumpRevision`.
         - `const { connectedKnoten, grid } = React.useMemo(() => { ... }, [obj.oid, allObjects, revision]);`
           - `buildConnectedKnoten(obj.oid, allObjects)` aus `<interfaces>` Block (Reverse-Index).
           - `buildRessVerknuepfungGrid(ressObj, connectedKnoten, allObjects)`: erzeugt OGraphGrid mit:
             - Ressource zentral (z.B. Position (0, 0) oder (3, 0) — analog C++ Layout).
             - Pro Knoten: ein PDlplKnotenGObj an Position `(laufx, 0)` mit `laufx++`.
             - Für die jeweilige Assoz (PAssozBeleg) zwischen Ress und Knoten: ein Assoz-Node + 2 Links (Ress→Assoz + Assoz→Knoten).
             - Slot-Indizes aus `01.2-06-CPP-AUDIT.md` übernehmen.
         - `useSimulationListener({})` no-op-Mount.
         - Layout (aus PATTERNS §9):
           ```tsx
           <ChildDialog
             title={`Ressourcen-Verknüpfungen — ${ressObj.attrs.m_sName ?? `oid ${obj.oid}`}`}
             description={`Verknüpft mit ${connectedKnoten.length} Knoten`}
           >
             <div data-viewer="PRessVerknuepfungViewer" className="relative flex h-full">
               <aside data-testid="tty-pane" className="w-48 border-r border-border bg-muted p-2 font-mono text-xs overflow-auto">
                 <h3 className="mb-2 text-[10px] uppercase tracking-wider text-muted-foreground">Verknüpfte Knoten</h3>
                 <ul className="space-y-1">
                   {connectedKnoten.map((k) => (
                     <li key={k.oid} data-oid={k.oid} className="truncate">
                       {(k.attrs.m_sName as string | undefined) ?? `oid ${k.oid}`}
                     </li>
                   ))}
                   {connectedKnoten.length === 0 && (
                     <li className="text-muted-foreground">Keine verknüpften Knoten</li>
                   )}
                 </ul>
               </aside>
               <div className="relative flex-1">
                 <GraphFlowCanvas grid={grid} revision={revision} readOnly={disabled ?? true} />
                 <KennzahlSlotPlaceholder />
               </div>
             </div>
           </ChildDialog>
           ```

    3. **Barrel-Export** `portal/src/viewers/PRessVerknuepfung/index.ts`:
       ```typescript
       export { PRessVerknuepfungViewer } from "./PRessVerknuepfungViewer";
       export { KennzahlSlotPlaceholder } from "./KennzahlSlotPlaceholder";
       ```

    4. **setup.ts** erweitern:
       - Import + Registry-Einträge (Hint "verknuepfung" für alle Ressource-Klassen):
         ```typescript
         import { PRessVerknuepfungViewer } from "@/viewers/PRessVerknuepfung/PRessVerknuepfungViewer";
         viewerRegistry.register({ klass: "PRessBeleg", hint: "verknuepfung", Component: PRessVerknuepfungViewer });
         viewerRegistry.register({ klass: "PBetriebsmittel", hint: "verknuepfung", Component: PRessVerknuepfungViewer });
         viewerRegistry.register({ klass: "PPerson", hint: "verknuepfung", Component: PRessVerknuepfungViewer });
         ```
       Doku-Block aktualisieren.

    5. **Spec** `portal/src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx`:
       - xyflow-Mock (PATTERNS S9 — 1:1 aus PDurchlaufplanViewerDesign.spec.tsx).
       - useModelStore-Mock (PATTERNS S10).
       - Wire-Fixture: Ressource (oid 400) + 2 Knoten (oid 200, 201) jeweils mit m_lAssozRess → [PAssozBeleg 300, PAssozBeleg 301] → m_lRessourcen=[400].
       - 9 Tests aus `<behavior>` (4 für KennzahlSlotPlaceholder + 5 für Viewer).
       - KennzahlSlotPlaceholder kann direkt importiert + isoliert getestet werden (`render(<KennzahlSlotPlaceholder />)`).

    6. KEINE Edit-Logik — Viewer ist Read-only (analog Welle 07).
    7. KEIN Hex-String im JSX — alle Farben via Tokens (`grep -E "#[0-9A-F]{6}"` ergibt 0).
  </action>
  <verify>
    <automated>cd portal && npm test -- --run src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx 2>&1 | tail -25 | grep -E "(✓|passed|Tests)" && npx tsc --noEmit 2>&1 | grep -c "error TS" | head -1 && grep -E "#[0-9A-F]{6}" src/viewers/PRessVerknuepfung/ | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    1. Audit aus Task 1 vorhanden.
    2. 9 Specs grün.
    3. KennzahlSlotPlaceholder.tsx + PRessVerknuepfungViewer.tsx + index.ts existieren mit Mindest-LoC.
    4. setup.ts hat Registry-Einträge für PRessBeleg/PBetriebsmittel/PPerson mit hint="verknuepfung".
    5. tsc 0 neue Errors; gesamte Vitest-Suite grün.
    6. `grep -E "#[0-9A-F]{6}" portal/src/viewers/PRessVerknuepfung/` = 0 Treffer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reverse-Index → DOM-Render | O(K × A × R) Iteration über alle Objekte; Sortierung deterministisch (nach oid). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-20 | Denial of Service | Reverse-Index O(K×A×R) bei Bosch-Modell (30k+ Objekte) | accept | Performance-Stress-Test deferred (RESEARCH §8.1 R5); realistische Phase-1.2-Modelle <500 Verknüpfungen |
| T-01.2-21 | UX-Bug | Kennzahl-Placeholder verwirrt User mit echter Anzeige | mitigate | Opacity 60% + "Phase 4"-Label + ARIA-Label macht Disabled-Status visuell + screen-reader-erkennbar |
| T-01.2-22 | Tampering | useSimulationListener-Import kreuzt Modul-Grenze (PRessVerknuepfung importiert aus PDlplConnKnoten) | accept | Single-Source-of-Truth statt Duplikation; refactor in Phase 4 falls Listener-Foundation eigenen Modul-Pfad bekommt |
</threat_model>

<verification>
- `cd portal && npm test -- --run src/viewers/__tests__/PRessVerknuepfungViewer.spec.tsx` 9 Specs grün.
- `cd portal && npx tsc --noEmit` 0 neue Errors.
- Registry: `cd portal && grep -c "PRessVerknuepfungViewer" src/viewers/setup.ts` ≥3 (3 Klassen × hint="verknuepfung").
- `grep -rE "#[0-9A-F]{6}" portal/src/viewers/PRessVerknuepfung/` = 0 (ESLint-Guardrail compliance).
</verification>

<success_criteria>
- SC-8 (PRessVerknuepfungViewer): erfüllt — Graph mit Kennzahl-Slot-Placeholder.
- SC-9 (Viewer-Smoke-Test): 3/3 erreicht (Welle 06 + 07 + 08).
</success_criteria>

<output>
Create `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-08-SUMMARY.md` when done.
</output>
