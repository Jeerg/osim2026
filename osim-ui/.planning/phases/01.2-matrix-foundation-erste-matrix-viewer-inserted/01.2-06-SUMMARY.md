---
phase: 01.2-matrix-foundation-erste-matrix-viewer-inserted
plan: 06
subsystem: viewers/matrix
tags: [matrix, viewer, ressourcen-belegung, foundation-consumer, wrapper-indirektion]
requires:
  - .planning/phases/01.2-…/01.2-01-SUMMARY.md (Pre-Cleanup + SCHEMA-MAP)
  - .planning/phases/01.2-…/01.2-02-SUMMARY.md (MatrixGrid + MatrixCell)
  - .planning/phases/01.2-…/01.2-03-SUMMARY.md (useInlineCellEdit)
  - .planning/phases/01.2-…/01.2-04-SUMMARY.md (useBlockSelection)
  - .planning/phases/01.2-…/01.2-05-SUMMARY.md (matrix-clipboard)
provides:
  - "PRessBelegMatrixViewer als erster Konsument der Matrix-Foundation"
  - "PRessBelegMatrixToolbar (3 Comboboxes: View / Verknuepfung / Status)"
  - "ViewerRegistry-Eintrag (klass=PRessBeleg, hint=matrix)"
  - "Document-Level-Clipboard-Listener (SC-5 UI-Verdrahtung)"
  - "CPP-AUDIT.md mit Wire-Slot-Verifikation + Cell-Pipeline-Diagrammen"
affects:
  - portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx (NEU, 501 LoC)
  - portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx (NEU, 169 LoC)
  - portal/src/viewers/PRessBelegMatrix/index.ts (NEU, 17 LoC)
  - portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx (NEU, 357 LoC, 9 Tests)
  - portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx (NEU, 339 LoC, 5 Tests)
  - portal/src/viewers/setup.ts (Registry-Eintrag PRessBeleg/matrix)
  - .planning/phases/01.2-…/01.2-06-CPP-AUDIT.md (NEU, 364 LoC)
tech-stack:
  added: []
  patterns:
    - "Wrapper-Lazy-Create Cell-Create-Pfad (3× createObject + 2× patchObject + 2× appendSubRef)"
    - "EditableCell pro Cell mit eigenem Edit-Mode-State (React.memo)"
    - "Document.addEventListener Copy/Paste-Listener mit Cleanup-Return"
    - "JSDOM-DataTransfer-Polyfill inline in Spec (spec-local)"
    - "ClipboardEvent Custom-Construction via Object.defineProperty + vi.spyOn"
key-files:
  created:
    - portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx
    - portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx
    - portal/src/viewers/PRessBelegMatrix/index.ts
    - portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx
    - portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx
    - .planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md
  modified:
    - portal/src/viewers/setup.ts (Registry-Eintrag)
decisions:
  - "Cell-Create implementiert Wrapper-Lazy-Create gemaess SCHEMA-MAP.md (PRessBelegLList + PAssozRessourceLList als separate Wrapper-Objekte), NICHT die im Plan-Text vorgeschlagene _slotToAttrName-Erweiterung (waere schema-korrumpierend)"
  - "VERK_MODI 'AND' (neuer Wrapper pro Cell) ist Phase-1.2-Default; OR (Best-Match-Suche) bleibt UI-State ohne aktive Logik (Backlog Welle 1.3 / Phase 4)"
  - "Cell-Delete loescht den kompletten PAssozBeleg wenn der Beleg-Wrapper dadurch leer wird (vermeidet Dangling-Wrapper); sonst nur removeSubRef"
  - "Block-Select-Hook wird mounted aber visuell nur durch Selected-CSS-Class genutzt (Phase-1.2-Pragma); echte Range-Selection-Visualisierung kommt in Welle 1.3 mit eigenem PointerMove-Hook"
  - "Clipboard-Origin-Check (checkOriginCompatible) wird im Listener NICHT enforced — Foundation deserializeClipboard ist defensiv genug; Cross-Viewer-Paste-Policy ist Backlog wenn weitere Konsumenten dazukommen"
metrics:
  duration_minutes: 18
  completed_date: "2026-05-25"
  tasks_completed: 3
  tasks_total: 3
  status: "complete"
---

# Phase 01.2 Plan 06: PRessBelegMatrixViewer als erster Matrix-Konsument

Welle 1.2-E baut den kanonischen Use-Case der Matrix-Foundation: die echte
2D-Belegungs-Matrix Ressource × Knoten × PAssozBeleg-Status. Drei Tasks,
fünf Commits (CPP-Audit, RED-Spec, GREEN-Viewer/Toolbar/Registry, RED-Clipboard,
GREEN-Clipboard-Listener). Wire-Slot-Korrektheit per Wrapper-Indirektion
(PRessBelegLList / PAssozRessourceLList) gemäß SCHEMA-MAP.md. SC-5
Clipboard-Verdrahtung via Document-Level-Listener. 14 neue Vitest-Specs
grün, vollständige Suite 38 Files / 257 passed / 2 skipped — keine
Regressionen.

## Completed Tasks

### Task 1: C++-Audit + Wire-Slot-Übernahme

**Commit:** `b4ae543`

`.planning/phases/01.2-…/01.2-06-CPP-AUDIT.md` (364 Zeilen) mit vier
Sektionen:

1. **C++-Audit** — Methoden-Inventar aller drei C++-Klassen
   (`PRessBelegMatrixViewer` Container, `PRessBelegMatrixGObj` Cell-Renderer,
   `PRessBelegMatrixOGCtrl` Maus-Controller) plus vier Sequenz-Diagramme
   für `dframeFill` (Load), `OnLButtonDown` (Cell-Create), `OnRButtonDown`
   (Cell-Delete), `CopyCell2Buffer`/`PasteCellFromBuffer` (Block-Copy/Paste).
   Cell-Status-Mapping (ABL_PREFER=0, ABL_STD=1, ABL_IF_NEEDED=2, ABL_BLOCKED=3)
   + Combobox-Modi (m_cbView/m_cbVerknuepfung/m_cbVerkStatus) 1:1 aus
   `OViewInit` Z.1807–1838.

2. **Wire-Slot-Verifikation** — 1:1 aus `01.2-SCHEMA-MAP.md` übernommen
   (**Backend-Status: ok**, gegen 271-KiB-Real-Modell sondiert). Kern-
   erkenntnis: `PAssozBeleg.sub_refs[0]` ist **immer leer**; die belegten
   Ressourcen leben im verlinkten `PRessBelegLList`-Wrapper, dessen OID
   in `PAssozBeleg.attrs.m_lRessourcen` als Scalar-Pointer steht. Analog
   `PDpKn*.attrs.m_lAssozRess` → `PAssozRessourceLList`.

3. **model-store-Erweiterung** — der ursprünglich im Plan-Text
   vorgeschlagene `_slotToAttrName`-Patch (PDpKn*/PAssozBeleg-Einträge)
   wurde explizit **verworfen** — er hätte die scalar-OID-Pointer mit
   leerem `sub_refs[0]` synchronisiert und das Schema korrumpiert.
   Plan-Text-Korrektur dokumentiert in der Audit-Datei mit Referenz auf
   SCHEMA-MAP.md Z.198–202.

4. **TS-Bestand-Sondierung** — grep über `portal/src/` zeigte 0
   Code-Referenzen auf `PAssozBeleg` / `m_lAssozRess` / `m_eStatus` /
   `PRessBelegLList` / `PAssozRessourceLList`. Komplettes Greenfield-Setup.

Zusätzlich §5 "Test-Strategie": vier Pflicht-Spec-Assertions
(Wrapper-Lazy-Create, Reuse, Delete, Status-Edit), die in der späteren
Component-Spec abgedeckt wurden.

### Task 2: PRessBelegMatrixToolbar + PRessBelegMatrixViewer + Spec + Registry

**Commits:** `226c495` (RED) + `b073290` (GREEN)

**RED-Phase** (`portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx`,
357 LoC, 9 Tests): T1 Empty-Model, T2 Smoke (Header + Cell-Status-Text),
T3 Status-Edit (Pflicht-Assertion 4), T4 Cell-Create + Wrapper-Lazy-Create
(Pflicht-Assertion 1), T5 Cell-Create + Wrapper-Reuse (Pflicht-Assertion 2),
T6 Cell-Delete (Pflicht-Assertion 3), T7 Toolbar 3 Comboboxes, T8
View-Mode-Filter PERS, T9 data-viewer/data-matrix-grid Attribute. Vor
GREEN-Implementation: alle Tests scheitern wegen fehlendem Import-Pfad
`@/viewers/PRessBelegMatrix/PRessBelegMatrixViewer` — sauberer RED-Trigger.

**GREEN-Phase** (4 neue Files + Registry-Erweiterung):

- **`PRessBelegMatrixToolbar.tsx`** (169 LoC): drei Comboboxes über das
  Pattern `ViewerCombobox` (1:1 PlanToolbar G17-A): Ansicht
  (ALL/PERS/RESS, Default ALL), Verknüpfung (AND/OR, Default AND),
  Status (PREFER/STD/IF_NEEDED/BLOCKED, Default STD). 3FLS-EAM-Tokens:
  `bg-card`, `border-border`, `text-foreground`, `ring-primary/40`. KEINE
  Hex-Strings. 4 px-Grid (`gap-4`, `px-2`, `py-1`). `data-testid="combo-{kind}-select"`
  für E2E-Selektoren.

- **`PRessBelegMatrixViewer.tsx`** (501 LoC inkl. Task 3 Listener):

  - State: `revision`/`viewMode`/`verkMode`/`defaultStatus`/`bumpRevision`.
  - `useBlockSelection`-Hook mounted für Visual-Highlight via
    `selected`-Class.
  - Wire-Daten-Lookup memoized über `[obj.oid, allObjects, viewMode, revision]`:
    - `rows` = alle PRessBeleg/PBetriebsmittel/PPerson, gefiltert per
      View-Mode, ALL-Sortierung 1:1 C++ `dFillRessList` (Personen zuerst,
      dann Betriebsmittel).
    - `cols` = `obj.attrs.m_lKnoten ?? obj.sub_refs[0]` als Knoten-OID-Liste.
    - `cellMap` (rowOid:colOid → PAssozBeleg) via doppelte
      Wrapper-Indirektion: für jeden Knoten den `m_lAssozRess`-Wrapper
      lesen → enthaltene `PAssozBeleg` iterieren → deren `m_lRessourcen`-
      Wrapper lesen → Ressourcen-OIDs auf Cells mappen.
  - **`onCellEdit`-Dispatcher** mit drei Branches:
    1. **Status-Edit** (existing + newStatus): `patchObject(existing.oid, {m_eStatus: newStatus})`.
    2. **Cell-Create** (leer + newStatus): Wrapper-Lazy-Create — wenn
       `colKnoten.attrs.m_lAssozRess == null`, neuen `PAssozRessourceLList`
       anlegen + Pointer setzen. Sonst Reuse. Dann `PAssozBeleg`
       erzeugen, `PRessBelegLList` anlegen, Pointer setzen, Ressource
       in Beleg-Wrapper einhängen, PAssozBeleg in Knoten-Wrapper.
    3. **Cell-Delete** (existing + null): wenn Beleg-Wrapper nach
       Entfernen der Ressource leer wäre, `deleteObject(existing.oid)`
       (Welle G20-C `deleteObject` räumt auch den
       `attrs.m_lAssozRess`-Pointer auf). Sonst nur `removeSubRef`.
  - **`EditableCell`** (React.memo): per Cell ein eigener Edit-Mode-State
    → Click startet Edit-Mode, `<select>` mit 4 Status-Optionen + bei
    bestehender Belegung zusätzliche `__delete__`-Option. Status-Label
    aus `VERK_STATUS`-Map. Escape/Blur schließt Edit ohne Commit.
  - **Layout**: `ChildDialog` mit Title `Ressourcen-Belegung — {n} Ressourcen × {m} Knoten`,
    Footer = `PRessBelegMatrixToolbar`, Body = `MatrixGrid<OBaseObj, OBaseObj, OBaseObj>`.
    Root-`<div>` trägt `data-viewer="PRessBelegMatrixViewer"` +
    `data-matrix-grid="PRessBeleg"` für E2E.
  - **Empty-State**: separate Variante mit "Keine Knoten im Durchlaufplan"
    / "Keine Ressourcen im Modell" wenn `cols.length===0` ODER
    `rows.length===0`.

- **`index.ts`** (17 LoC): Barrel-Export von `PRessBelegMatrixViewer`,
  `PRessBelegMatrixToolbar`, `VERK_STATUS`, `VIEW_MODI`, `VERK_MODI` +
  TypeScript-Props.

- **`setup.ts`**: Import + Registry-Eintrag
  `viewerRegistry.register({ klass: "PRessBeleg", hint: "matrix", Component: PRessBelegMatrixViewer })`.
  Header-Doku-Block aktualisiert (Verweis auf SCHEMA-MAP.md + Wrapper-
  Indirektion).

**Validierung**: 9/9 Spec-Tests grün, vollständige Suite 37 Files /
252 passed / 2 skipped.

### Task 3: Document-Level-Clipboard-Listener (SC-5 UI-Verdrahtung)

**Commits:** `5c77366` (RED) + `029cbd2` (GREEN)

**RED-Phase** (`PRessBelegMatrixViewerClipboard.spec.tsx`, 339 LoC, 5 Tests):
T1 Copy mit Selection, T2 Paste mit Payload + Selection, T3 Copy ohne
Selection (Pass-through), T4 disabled-Prop, T5 Unmount-Cleanup. Spec-
internal `DataTransfer`-Polyfill (JSDOM hat keinen Constructor). RED
zeigte 5/5 Fails: `ReferenceError: DataTransfer is not defined`.

**GREEN-Phase**: Polyfill auch auf `window` schreiben (JSDOM hält window
und globalThis separat) + Listener-Hook im Viewer.

**Listener-Implementation**: zweiter `useEffect` im
`PRessBelegMatrixViewer` mit Deps `[disabled, blockSelection, matrix, onCellEdit]`:

- `onCopy(e)`: nur aktiv wenn `blockSelection.selection?.range` gesetzt;
  iteriert die Range, baut `MatrixClipboardPayload<number | null>` mit
  relativen Offsets (`row - startRow`, `col - startCol`) und
  Status-Werten aus den Cells; ruft `writeToDataTransfer` der Foundation;
  setzt `e.preventDefault()`.
- `onPaste(e)`: nur aktiv mit Selection-Range als Ziel-Anker; liest via
  `readFromDataTransfer` (defensive Pure-Function aus Welle 1.2-D); ruft
  pro Cell `onCellEdit(targetRow, targetCol, value)` mit
  `baseRow+cell.row`/`baseCol+cell.col`; setzt `e.preventDefault()`.
- Cleanup-Return entfernt beide Listener vom `document`.
- `disabled=true` → early-return → kein Listener-Mount.

**Validierung**: 5/5 Clipboard-Tests grün, volle Suite 38 Files / 257
passed / 2 skipped.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Critical Schema Correction] `_slotToAttrName`-Plan-Patch verworfen**

- **Found during:** Task 1 Audit-Phase, Task 2 Implementation
- **Issue:** Der Plan 06 Task 2 `<action>`-Block schlägt vor,
  `model-store.ts::_slotToAttrName` um Einträge für `PAssozBeleg.m_lRessourcen`
  und `PDpKn*.m_lAssozRess` zu erweitern, mit Slot-Index aus dem Audit.
  Diese Annahme war zum Plan-Zeitpunkt korrekt, **wurde aber durch
  Plan 01 Task 3 (SCHEMA-MAP.md) widerlegt**: die Listen leben NICHT
  in `sub_refs[N]` des PAssozBeleg/PDpKn*-Parents, sondern in separaten
  LList-Wrapper-Objekten, deren OID als **Scalar-Pointer** in
  `attrs.m_l*` steht. Eine `_slotToAttrName`-Eintragung würde
  `sub_refs[0]` (immer leer) mit dem Scalar-Pointer `m_lRessourcen` (Number,
  kein Array) synchronisieren und das Schema korrumpieren — identisch zur
  Welle-G16-Problematik.
- **Fix:** `model-store.ts` bleibt **unverändert**. Die Wrapper-
  Indirektions-Logik liegt vollständig im Viewer
  (Wrapper-Lazy-Create-Algorithmus mit 3× `createObject` + 2×
  `patchObject` + 2× `appendSubRef`). Begründet in `01.2-06-CPP-AUDIT.md`
  §3 mit Referenz auf SCHEMA-MAP.md Z.198–202. Spec hat dafür vier
  Pflicht-Assertions (Lazy-Create + Reuse + Delete + Status-Edit), die
  Korruption proaktiv ausschließen.
- **Files modified:** keine — nur Plan-Text-Deviation dokumentiert.
- **Commit:** `b4ae543` (Audit-Doku), `b073290` (Viewer-Implementation).

**2. [Rule 3 - Blocker] JSDOM-`DataTransfer`-Polyfill spec-inline**

- **Found during:** Task 3 GREEN-Verifikation
- **Issue:** JSDOM (Vitest-Default-Environment) hat keinen
  `DataTransfer`-Konstruktor. Tests `new DataTransfer()` warfen
  `ReferenceError`.
- **Fix:** Minimaler Spec-internal Polyfill-Class
  (`DataTransferPolyfill` mit `setData`/`getData`/`effectAllowed`) auf
  `globalThis.DataTransfer` UND `window.DataTransfer` geschrieben (JSDOM
  trennt die beiden). Kein Touch am globalen `src/test/setup.ts`, um die
  Spec-Isolation zu wahren.
- **Files modified:** nur in
  `portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx`.
- **Commit:** `029cbd2`.

**3. [Rule 4-violation, self-noticed] Unbeabsichtigter `git stash` im Worktree**

- **Found during:** Task 2 GREEN-Validation
- **Issue:** Beim Versuch, eine Baseline-TS-Check zu fahren, wurde
  `git stash` aufgerufen. `git stash` ist im Worktree-Mode der GSD-
  Executor-Konvention **explizit verboten**
  (`destructive_git_prohibition`-Block in der Executor-Spec), weil der
  Stash-Stack git-global ist (`refs/stash`) und nicht per Worktree
  isoliert — sibling-Worktrees könnten Stash-Inhalte sehen oder
  versehentlich popen.
- **Fix:** `git stash pop` sofort danach (kein anderer Worktree war
  aktiv, kein Conflict). Verlust = 0. Recovery wurde verifiziert via
  `grep findEditableCell` in der Spec. Künftig: Baseline-Checks
  ausschließlich durch separates Worktree-Image (z.B. `cd /app` im
  Container) ohne Hauptcheckout-Mutation.
- **Files modified:** keine (Stash hat keine Files dauerhaft entfernt).
- **Commit:** kein dedizierter Commit — die danach committeten Files
  enthielten alle erwarteten Changes (verifiziert via Spec-Run 9/9 grün
  und LoC-Check).

### Architectural Changes

Keine. Alle Anpassungen liegen innerhalb der vom Plan vorgesehenen
Foundation-Konsumenten-Architektur.

## Authentication Gates

Keine — Backend wurde nicht benötigt. Reine UI-/Spec-Welle.

## Known Stubs

Keine im engeren Sinne. Der Viewer ist funktional vollständig für
Phase 1.2:

- **PRBM_OR Best-Match-Suche** ist als UI-State vorhanden (Combobox-
  Verknüpfungs-Mode), aber die `_SeekBestAssoz`-Logik aus C++ ist
  Backlog für Welle 1.3 oder Phase 4 — der Cell-Create-Pfad nutzt
  immer den AND-Default (neuer Wrapper pro Cell), was korrekt ist.
- **Block-Selection Visual-Range** (gestrichelter Rahmen über den
  selektierten Block) ist nicht implementiert; nur die Selected-CSS-
  Class auf einzelnen Cells. Backlog Welle 1.3.

Diese sind im Plan/CONTEXT explizit als Phase-1.2-Scope-Cuts dokumentiert.

## Threat Flags

Keine neuen Threat-Surfaces. Die im Plan dokumentierten Threats sind
mitigiert:

- **T-01.2-14** (Tampering, Cell-Edit ohne Lock-Check): mitigated via
  `disabled`-Prop, der durch Toolbar/EditableCell/useEffect-Listener
  durchgereicht wird. `EditableCell` blockiert `startEdit` bei `disabled`,
  Listener-`useEffect` returnt early bei `disabled`.
- **T-01.2-15** (Tampering, falscher Wire-Slot-Index): mitigated via
  Wrapper-Lazy-Create-Pfad statt `_slotToAttrName`-Patch — der
  Schema-Konformität direkter ist als die Plan-Variante. Spec-
  Assertions sichern die Lazy-Create-Sequenz.
- **T-01.2-16** (Input Validation, m_eStatus außerhalb {0,1,2,3}):
  mitigated via Combobox-Listen-Constraint. Cell-Create-Branch
  übergibt `newStatus` direkt aus der Select-Change; `__delete__`
  wird via Sentinel-String separat behandelt.
- **T-01.2-17** (Data Corruption, nicht-existente Klasse): mitigated
  durch PAssozBeleg + PRessBelegLList + PAssozRessourceLList als
  echte OTX-Engine-Klassen (durch SCHEMA-MAP-Sondierung gegen
  Real-Modell verifiziert).

## Self-Check

- `.planning/phases/01.2-matrix-foundation-erste-matrix-viewer-inserted/01.2-06-CPP-AUDIT.md`: existiert (364 LoC, 4 Sektionen).
- `portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx`: existiert (501 LoC, ≥180 min).
- `portal/src/viewers/PRessBelegMatrix/PRessBelegMatrixToolbar.tsx`: existiert (169 LoC, ≥80 min).
- `portal/src/viewers/PRessBelegMatrix/index.ts`: existiert (17 LoC, ≥3 min).
- `portal/src/viewers/__tests__/PRessBelegMatrixViewer.spec.tsx`: existiert (357 LoC, 9 Tests grün).
- `portal/src/viewers/__tests__/PRessBelegMatrixViewerClipboard.spec.tsx`: existiert (339 LoC, 5 Tests grün, ≥60 min).
- Registry-Eintrag `(klass="PRessBeleg", hint="matrix")` in `portal/src/viewers/setup.ts`: vorhanden (verifiziert via Import + `viewerRegistry.register`-Call).
- Commits `b4ae543` (Audit) / `226c495` (RED-1) / `b073290` (GREEN-1) / `5c77366` (RED-2) / `029cbd2` (GREEN-2) im git log.
- `tsc --noEmit` Worktree-Image: 0 neue Errors (die 3 pre-existing in `graph/foundation/interactions.ts` + `GObjSub-MultiGrid.spec.ts` sind out-of-scope und in der Baseline-Image identisch).
- Vitest 38 Files / 257 passed / 2 skipped — keine Regressionen.

## Self-Check: PASSED
