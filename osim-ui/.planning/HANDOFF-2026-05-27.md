# HANDOFF — 2026-05-27 (Session voll, Resume via /gsd-resume-work)

Phase **01.2** (Matrix-Foundation). Viel uneingecheckte Arbeit + offene Punkte.
Nach `/clear` → `/gsd-resume-work` zuerst DIESES Dokument lesen.

## 0. Stack-Status
- Docker-Stack läuft (api/portal/postgres/firebase/minio healthy). Falls unten:
  `docker compose up -d && bash scripts/wait-healthy.sh 90 && uv run python scripts/seed_firebase_emulator.py`.
- **Container-Sync-Pflicht** (siehe Memory `dev-stack-container-sync`): `api` hat
  KEIN Source-Mount. Nach Backend-Änderung:
  `docker compose cp app/services/lock_service.py api:/app/app/services/lock_service.py`
  bzw. `... app/static/schemas/v1/schemas.json api:/app/...` + `docker compose restart api`.
  Aktuell sind lock_service.py + schemas.json bereits in den Container kopiert.
- Test-User Browser: `jwfischer69@gmail.com` / `123456` (Memory `feedback-default-test-user`).
  E2E nutzt ADMIN `admin@osim-dev` / `admin123`.
- Test-Modell mit Matrix-Daten: `C:\Users\JörgWFischer\PycharmProjects\OSim2004\Vorstellung04\Embb-AslFj.otx`.

## 1. NICHTS ist committed (15 Dateien M, branch master, HEAD a7c0a1b)
Bündeln vorschlagen, wenn User OK gibt. Logische Commits:
- `fix(lock)`: `app/services/lock_service.py` + `tests/backend/test_lock_service.py`
  — Re-Entrant-Lock (gleicher User sperrt sich nach F5 nicht mehr aus). 12 Tests grün.
- `style(3fls-blau)`: `portal/src/styles/tokens.css`, `globals.css`, `main.tsx`,
  `components/AuthenticatedLayout.tsx`, `components/BrandLogo.tsx`, `sidebar/ModelTree.tsx`
  — Cyan→Blau Re-Key, Segoe UI, Topbar-Gradient + 3fls-Logo, Sidebar soft-blau,
  `--color-primary-foreground` (weiße Button-Schrift), **OSim**-Casing.
- `fix(01.2 matrix)`: `graph/foundation/matrix/MatrixGrid.tsx`,
  `viewers/PRessBelegMatrix/PRessBelegMatrixViewer.tsx`, beide `__tests__/*`,
  `graph/foundation/OsimNode.tsx`, `app/static/schemas/v1/schemas.json`.
- `.planning/STATE.md` (Frontmatter) separat/mit Doc-Commit.

## 2. Was diese Session gemacht + verifiziert wurde
1. **Lock-Fix** — Re-Entrant-Acquire in `LockService.acquire` (IntegrityError →
   wenn `owner_user_uid == user_uid` → refresh + neuer Token statt 409). 12 Vitest… nein, 12 *pytest* grün.
2. **3FLS-EAM Blau-Restyle** — komplett, per Screenshot (Login/Library/Workspace) verifiziert.
   `--color-primary-foreground` war das fehlende Token (weiße Schrift auf blauen Buttons).
3. **Matrix-Viewer Rebuild** (`PRessBelegMatrixViewer`):
   - Resize-Bug weg: kein In-Cell-`<select>` mehr (gab's im Original nie), feste
     Spaltenbreiten in `MatrixGrid` (`table-layout:fixed`, +`colWidth`/`rowHeaderWidth`).
   - Interaktion 1:1 C++: Linksklick leer = anlegen mit Toolbar-Status, Linksklick
     belegt = nix, Rechtsklick belegt = löschen, Ctrl/Shift = Block-Select.
   - **Datenmodell-Fix:** Status liegt NICHT in `m_eStatus` (existiert nicht!),
     sondern pro Ressource in `m_LinkStatusList` → `PAssozBelegLinkInfo`
     (`{m_oRessBeleg, m_eStatus, m_eBaseStatus}`), Default ABL_STD=1. Helpers
     `getLinkStatus`/`findLinkInfo`/`setLinkStatus` neu. Vorher: belegte Zellen
     leer angezeigt. Jetzt: E2E filledBefore=64, +1 pro Klick, Spalte stabil.
   - **Farbige Pills** (§6.3): standard=blau, bevorzugt=grün, notfalls=amber,
     geblockt=rot.
   - `MatrixGrid`: redundanten internen cellMap-Memo entfernt (war Stale-Quelle).
4. **Drag-Ghost** — über React-Flow `dragging`-Prop in `OsimNode`/`OsimEdgeBox`/
   `OsimGroupNode` (nicht mehr CSS-Klasse): angehoben + halbtransparent +
   gestrichelte Blau-Kontur. Verifiziert: `data-dragging=1` mid-drag.
5. **OSim-Casing** — Topbar + BrandLogo „osim" → „OSim".
6. **Andere Viewer erreichbar gemacht** — `schemas.json` viewer_hints:
   `conn` an PDpKnKonstant/PDpKnAlternativ, `verknuepfung` an
   PBetriebsmittel/PPerson/PRessBeleg. In api-Container kopiert (2 conn + 3 verknuepfung verifiziert).
- Gesamt-Status: tsc 0 neue Fehler (nur 3 pre-existing aus deferred-items.md),
  ESLint 0 Errors, **Vitest 273 passed / 2 skipped**.

## 3. OFFENE PUNKTE — hier weitermachen (Priorität ↓)

### P1 — „+"-Knopf legt keine Belegressource an (BUG)
`portal/src/viewers/core/ViewerFrame.tsx:142-147` — der Plus-Button sendet
`onCommand({ type: "create", objKlass: "" })` mit **leerem objKlass**. Der
Handler in `portal/src/routes/_authenticated/models/$id.tsx` (onCommand,
case "create") kann mit "" nichts anlegen → no-op.
**Fix-Vorschlag:** objKlass aus der aktuellen Selektion/Gruppe ableiten
(beim Betrachten/Selektieren einer PBetriebsmittel → `create PBetriebsmittel`;
in der Gruppe „Belegungsressourcen" → PBetriebsmittel). $id.tsx onCommand prüfen
+ ViewerFrame `objKlass` füllen. Danach: neue Ressource im Tree sichtbar?

### P2 — Arbeits-/Einsatzzeiten an Ressourcen (fehlt im Modell)
User ist sicher dass es die gab — stimmt: **OSimAZeit-Subsystem** (separate
Personal-/Einsatzzeit-Planung), NICHT das PPS-Modell. Klassen:
`AEinsatzWunsch`/`AEinsatzzeitWunsch` (Wochen-Kalender: m_iGesamtWoche/
m_iAktWoche/m_itage), `AKapBed`, `AGruppe`, `APerson`, AZeit-`ASimulator`.
Refs: `OSim2004/OSimV01(Fj)/inc/OSimAZeit.h`, `AEinsatzWunschViewer.h`.
Heutige PPS-Ressourcen exponieren nur `m_iAnwWahrsch` + `m_dPtkEinsatzzeit`(Ergebnis).
**→ Braucht ENGINE-Arbeit** (Wire-Exposure): Notiz liegt in
`../osim-engine/engine/docs/CONTEXT-P1-osim-ui-integration-TODO.md` (vom User
gewünscht). osim-ui hat Viewer-Stubs `AEinsatzWunsch`/`AKapBed`/`AGruppe`
registriert — warten auf Wire-Daten.
**UI-Vorschlag (3 Optionen, mit User klären):**
- (A) Voller AZeit-Wochenkalender-Viewer pro Ressource (wie Original
  AEinsatzWunschViewer: Wochen × Tage Grid auf der Matrix-Foundation).
- (B) Schlankes „Arbeitszeit"-Property-Panel an PBetriebsmittel/PPerson
  (Wochen-Std/Schicht-Felder) — wenn Engine vereinfacht exponiert.
- (C) Read-only-Anzeige der vorhandenen Zeit-Metriken (m_dPtkEinsatzzeit) +
  Verfügbarkeits-% jetzt, voller Kalender als Phase-4-Backlog.
Empfehlung: erst Engine klären (was ist überhaupt aus .otx ladbar), dann (A) auf
MatrixGrid-Foundation, weil die Wochen×Tage-Struktur exakt dazu passt.

### P3 — Matrix-Status-Persistenz (Engine-abhängig)
Cell-Edits schreiben jetzt `PAssozBelegLinkInfo` in `m_LinkStatusList`. Ob der
OTX-Writer das round-trippt, ist UNGEPRÜFT (Engine-Risiko wie G16). Auto-Save ist
weiterhin AUS (G19-A). Engine-Notiz Punkt 1 in
`../osim-engine/engine/docs/CONTEXT-P1-osim-ui-integration-TODO.md`.
Verifizieren mit `scripts/otx_roundtrip_audit.py` gegen ein Modell mit Belegung.

### P4 — Veraltete E2E + Phase-Abschluss
- `portal/e2e/matrix-cell-edit-persistence.spec.ts` nutzt noch das alte
  `<select>`/`data-matrix-cell-editing` → an Klick-Paint-Modell anpassen.
- Phase 01.2 UAT (`01.2-09-UAT-CHECKLIST.md`) noch nicht signiert; Save/Lock-Befund
  von dort ist durch den Lock-Fix teil-adressiert.

## 4. Erste Schritte beim Resume
1. Stack-Status prüfen; Browser http://localhost:3002 (jwfischer69/123456).
2. P1 angehen (ViewerFrame `objKlass` + $id.tsx onCommand "create").
3. P2/P3: Engine-Notiz mit User durchgehen — „da müssen wir weitermachen" (User-Wunsch).
4. Wenn P1 + Pills/Drag/Restyle vom User abgenommen → Commits bündeln (siehe §1).
